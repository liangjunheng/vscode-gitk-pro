# 2026-08-13

- 将 Gitk 可见且单仓库选中时的轻量工作区变更存在性串行轮询间隔由查询完成后 5 秒调整为 3 秒；首次查询仍立即执行。`npm run compile` 通过，相关文件诊断为零。
- `CustomDiffPanel` 的 diff 已向 VS Code `DefaultLinesDiffComputer` 对齐：行级比较改用 `trim()` 弱匹配，完整文本不同的弱匹配行转为 `modified-*`；修改行 word diff 改为字符级 LCS（总字符数小于 500）并扩展到完整 Unicode 词边界；纯新增/删除仍只整行着色。Myers hunk 在乘积超过 2,500,000 时降级为整块增删，避免 Webview 主线程和内存风险。已补充 VS Code `computeMovedLines` 的保守子集：仅跨 hunk、至少 3 行、总有效内容至少 15 字符、至少 2 行有效字符的规范化完全相同纯删/纯增块标记为 `moved-*`，并在并排/内联按 moved-text 主题色渲染。尚未实现相似度大于 0.90 的移动匹配、移动后内容细化、hunk 后处理、子词扩展与 Worker/超时。`npm run compile`、文件诊断和 `git diff --check` 通过。
- 查阅 VS Code `DefaultLinesDiffComputer` 后确认：`optimizeSequenceDiffs` 是行级和字符级原始差异的边界优化；字符级 `removeShortMatches` 与 `removeVeryShortMatchingTextBetweenLongDiffs` 用于合并被极短公共片段切碎的高亮，行级 `removeVeryShortMatchingLinesBetweenDiffs` 用于合并短公共行夹着的相邻 hunk。当前 CustomDiffPanel 尚未接入这些后处理；其价值是减少碎片化和不自然 hunk，而非修正文本正确性。
- 已向用户说明移动代码块检测：它把跨位置完全相同的删除块与新增块关联为同一段移动，而非独立增删；当前项目实现是 `collectPureBlocks` 的保守完全匹配子集，并用 `moved-*` 主题色显示。
- 持续按 VS Code `DefaultLinesDiffComputer` 改造 `CustomDiffPanel`：`wordDiff` 新增 ASCII camelCase/PascalCase/数字切换子词边界；`emitRegion` 以 VS Code `removeShortMatches` 的 <=2 行条件，将内部短相同行以中性行形式归入前后同一 hunk，内联同步显示；移动块保留严格匹配优先，并为未匹配纯块加入 >0.90 的 token Jaccard×长度比相似度候选标色。未实现 `optimizeSequenceDiffs` 的边界平移、字符级短匹配清理、三行锚点移动扩展和 Worker 超时。`npm run compile`、`git diff --check` 及文件诊断通过。
- 修复 `CustomDiffPanel` 无法显示：HTML 模板内新加 Unicode 属性正则和 `join('\\n')` 未双重转义，生成的 Webview 脚本发生解析错误；已改为模板安全转义。直接提取并以 `new Function` 验证生成脚本语法，且 `npm run compile`、`git diff --check`、文件诊断均通过。
- 最终审查 `CustomDiffPanel` 时修正两项不会阻断显示的逻辑缺口：普通上下文区间扫描仅继续吸收 `change === undefined` 的行，避免短匹配 hunk 的中性行被误折叠；`moved-*` 行纳入文件头增删统计。重新执行模板脚本解析、`npm run compile`、`git diff --check` 和文件诊断均通过。
- 只读审查发现尚未改动的风险：移动行会通过 hunk 级 `moved-block` 容器样式覆盖同 hunk 非移动行；`wordDiff` 对每一对短修改行独立构建 LCS 矩阵但无每文件总预算，超大 hunk 可能阻塞 Webview；词级范围按 Unicode code point 而非扩展字素簇处理，组合音标/ZWJ 序列高亮可能不完整。构建、补丁检查和实际 Webview 脚本语法均通过。
- 完整只读审计还确认：Myers 的 `height * width` 阈值无法约束 `trace` 的 O((N+M)D) 内存，且超阈值分支会把全部行伪装成增删；末尾换行变化被 `splitLines` 丢失；相似移动块可掩盖真实编辑；行统计从自定义渲染算法而非 Git numstat 反推；complete 消息与 `renderAllDiffs` 全量同步渲染和多份 payload 保留会造成大提交 UI/内存风险；进度轨道和填充条同色导致比例不可见。未修改代码；`npm run compile`、`git diff --check`、模板脚本语法和文件诊断通过。
- 根据审计实施修复：大 hunk 降级改为 64 行前瞻线性重同步，并在总行数超过 5000 时避开 Myers trace；word diff 移除 500 字符拒绝阈值，保留公共前后缀后仅在差异矩阵不超过 250000 单元时精细 LCS，否则整段高亮；使用 `Intl.Segmenter` 字素簇；优先显示 Git `addedLines`/`removedLines`；严格完全匹配移动块；显示 EOF newline 差异；全量渲染改为每帧约 8ms 批处理；消息路径加白名单；进度轨道可见。`npm run compile`、`git diff --check`、实际 Webview 脚本语法和文件诊断通过。
- `CustomDiffPanel` 未修改区域折叠改造：每个文件用 `expandedRangesByDiff` 持久化区间状态；折叠区域保留顶部/底部局部展开与全部展开，完全展开后显示区域收起控件；工具栏新增全局“展开全部/收起全部”，新 loading/reset generation 清空旧状态；并排、内联共用相同区间状态。`npm run compile`、`git diff --check`、生成 Webview 脚本语法和文件诊断通过。
- 未修改区域控件图标进一步对齐 VS Code 风格：上方展开、下方展开、全部展开、区域收起均改为统一线框 SVG chevron/fold 图标，不再使用文本 `⌃`；提示文本与 aria-label 明确对应各动作。新 `reset` 清除 `expandedRangesByDiff`，避免同索引新快照沿用旧展开区间。`npm run compile`、`git diff --check`、生成脚本 `new Function` 校验与文件诊断通过。
- 同步工具栏已完成：Pull 前以 `showInformationMessage` 询问是否同步子模块；Fetch、Pull、Push 均通过 `withProgress` 显示通知进度，成功/失败分别提示，失败包含 Git 原始错误原因。`runGitSync` 仅在确认后传 `--recurse-submodules` 并更新子模块；三项按钮的 title/aria-label 改为 Fetch、Pull、Push。`npm run compile` 与相关文件诊断通过。
- Pull 选择同步子模块后，`git submodule update` 已补充 `--remote`，完整命令为 `git submodule update --init --recursive --remote`：可初始化新发现子模块，并递归更新所有层级的嵌套子模块远程跟踪分支。`npm run compile`、文件诊断和 `git diff --check` 通过。
- 对同步补丁进行只读审查：`--remote` 会把子模块切到远端 tip 而非父仓库 gitlink，可能使工作树与 Pull 后父提交不一致；新初始化子模块未失效仓库发现缓存；同步操作可并发触发，可能出现 Git 锁冲突；无子模块仓库仍会显示 Pull 子模块确认。未修改代码。
- 按审查修复同步逻辑（保留无子模块仓库的 Pull 确认）：子模块更新改为 `git submodule update --init --recursive`，确保检出父仓库 gitlink 指定版本；随后以 `git submodule status --recursive` 对每个已完成子模块上报通知进度。确认同步子模块时失效仓库发现缓存，保证新初始化子模块立即显示；`GitActionRunner` 增加同步互斥，运行中请求提示警告而不并发执行。`npm run compile`、`git diff --check`、文件诊断通过。
- Fetch 已扩展为主仓库 fetch 完成后通过 `collectSubmoduleRepositories` 递归发现所有已初始化子模块，依次运行 `git fetch --all --prune`，通知显示当前序号、总数、路径和完成状态。`npm run compile`、`git diff --check`、文件诊断通过。
- Fetch、Pull、Push 现明确绑定仓库下拉框选中的单一仓库路径，不再因当前提交归属而误操作其他仓库。Webview 加载 `@vscode/codicons` 资源，三个按钮分别采用 `codicon-sync`、`codicon-repo-pull`、`codicon-repo-push` 标准 VS Code 图标。`npm run compile`、`git diff --check`、相关文件诊断通过。
- Pull 流程重构为：先执行主仓库 `git pull`，再用 `git submodule status --recursive` 检查不一致或未初始化的子模块；仅有待更新项时弹出确认。确认后调用 `updateGitSubmodules` 递归初始化/更新并逐项报告完成进度；仅初始状态检测到未初始化新子模块（`-` 标记）且用户确认更新后，才失效仓库发现缓存。Fetch/Push 同步结果返回无子模块更新标记。`npm run compile`、`git diff --check`、文件诊断通过。
- Fetch 工具栏图标从通用 `codicon-sync` 改为 VS Code 专用 `codicon-repo-fetch`，避免与刷新图标混淆。`npm run compile`、`git diff --check`、文件诊断通过。
- 用户询问纯增删识别后确认当前 `CustomDiffPanel.appendChanged` 已处于无条件位置配对状态：`modifiedLineCount = min(removed.length, added.length)`，同一 hunk 中前 N 对删/增行一律为 `modified-*` 并调用 `wordDiff`，无内容相似度验证；纯插入/纯删除正确，但混合删增 hunk 会把无关行误判为修改。未改代码；后续需设计兼顾短文本真实替换的行配对规则。
