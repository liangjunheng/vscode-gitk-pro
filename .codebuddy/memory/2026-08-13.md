# 工作记录

- 根据截图修复空白/单标点 word 方框：根因是 `removeShortMatches` 将夹在差异间的空格、括号等非词字符强制并入 `innerChanges`，统一 `inline-block` 方框使其显形。现为短匹配清理传入 grapheme 内容，仅包含词字符的短相等范围才并入；实测 `if (active.get()) { → if (enabled.get()) {` 仅输出 `active`/`enabled` 的范围。`npm run compile`、`git diff --check` 和文件诊断通过。
- 对 `companion object` 与 `private object CleanerHolder` 截图做只读核对：当前完整词方框是预期的 word diff，实际变化 token 为 `companion`、`private`、`CleanerHolder`，共同 `object` 应不进入 `innerChanges`；不建议移除 `extendToWord`，否则会退化为难读的字符级框选。未改代码。
- 根据结构性替换截图调整并排投影：多行且不等长的 replacement mapping 现在标记 `isStructuralReplacement`，先将左范围完整投影为纯 `removed` + 右 placeholder，再将右范围完整投影为纯 `added` + 左 placeholder；不再对该范围按 offset 产生伪 `modified-*`/word diff。单行和等长 replacement 保持逐行修改投影。样例确认左 3 行/右 2 行 mapping 的虚拟行类型正确；`npm run compile`、`git diff --check`、Webview 脚本语法和文件诊断通过。
- 两张现状/期望截图确认等长但语义无关的多行 replacement 也不应逐行修改。`appendChanged` 现对多行范围计算整体 token Jaccard 相似度：行数不等或相似度 <0.50 时均标记 `isStructuralReplacement`，完整投影为左纯 `removed` 后右纯 `added`；只有等长且整体相似的多行范围才生成 `modified-*`/word diff。截图同类 4 行→4 行样例已验证为纯删除/纯新增。`npm run compile`、`git diff --check` 和文件诊断通过。
- 修复 Custom Diff 的块/行/词级高亮错位：`lineEquals` 改为精确原始文本比较，避免行算法忽略首尾空白而渲染层又标为 `modified-*`；移除 `.change-block` 容器背景，颜色仅由行和词级元素承担；`modified-removed`/`modified-added` 的 gutter 与 code 分别复用删除/插入主题色，消除灰色未修改区域覆盖。`npm run compile`、`git diff --check` 与文件诊断通过。
- 根据复现截图继续修复 Custom Diff：移除不等长替换先完整删除、后完整新增的结构性拆分，统一按同一并排槽位逐行配对，仅用尾部 placeholder 补齐；删除 `.change-block .modified-*` 与 `.modified-block .line-code` 的后置灰色覆盖；成对修改的 word diff 框选改为左删红、右增绿。`npm run compile`、`git diff --check` 与文件诊断通过。
- 修复 Custom Diff placeholder 斜纹的行间断层：`.placeholder` 的 `background-size` 从固定 `8px 8px` 改为 `var(--line-height)`，让每个固定高度行的斜纹相位在边界连续。`npm run compile`、`git diff --check` 与文件诊断通过。
- 按最新目标截图调整 Custom Diff 样式：placeholder 恢复 `8px` 细斜纹并使用 `background-attachment: fixed` 连续对齐；`modified-*` 在 change-block 内恢复行、gutter 与 code 的低饱和红绿背景；word diff 改为无 `box-shadow`、无圆角的纯填充，消除白色方框。`npm run compile`、`git diff --check` 与文件诊断通过。
- 修复 Custom Diff 水平滚动：行和 change-block 改为 `overflow: visible`，代码列使用 `max-content`，避免长行在 pane 横滚后被二次裁剪；并排模式 `.line-number` 设为 `position: sticky; left: 0`，内联模式为 marker 和双行号配置 0/18/70px 粘性偏移。`npm run compile`、`git diff --check` 与文件诊断通过。
- 修复 Custom Diff word diff 范围过度扩张：移除单侧插删时在另一侧伪造完整单词差异的逻辑，并停止把变化字符扩展至完整 `snake_case`/camelCase 标识符；继续保留 LCS/Myers 的实际字符级结果与仅包含词字符的短匹配合并。因此 `m_active → active` 仅高亮左侧 `m_`，共同的 `active` 保持普通文本。`npm run compile`、`git diff --check` 与文件诊断通过。
- 定位 Changes/Staged Changes 点击后不保持高亮：工作区虚拟行仅带 `data-hash`，点击时以空仓库路径乐观选中；Store 保留真实 `currentRepositoryPath`，stateUpdate 回传后 `applyCommitSelection` 要求 hash 与仓库路径同时匹配，故清除该虚拟行的 `.selected` 且无法恢复。最小修复是在 `GitkViewProvider` 序列化 `selectedCommit` 时，对 `changes`/`staged` 强制发送空 `repositoryPath`；不影响内部读取工作区变更所需的真实仓库路径。未改代码。
- 已修复工作区虚拟提交的选中高亮：`pushStateToWebview` 对 `changes`、`staged` 的 `selectedCommit.repositoryPath` 发送空字符串，匹配无路径的虚拟行；普通提交继续携带真实仓库路径。`npm run compile`、`git diff --check` 和 `gitkViewProvider.ts` 诊断通过。
- 修复最新 Custom Diff 截图问题：移除 removed/added/modified code 的 `1px` border，消除红绿下划线；多行范围继续以不等长或 token 相似度低于 .50 判为结构性替换，单行 word diff 仅在公共前后缀占最长行 ≥35% 时细化，且不再合并短公共片段，避免无关行出现零散词级框选；每个 pane 渲染后以 `scrollWidth` 统一设定 `.line`/`.change-block` 的最小宽度，使背景延伸至横向滚动最右端。`npm run compile`、`git diff --check` 与文件诊断通过。
- 按截图修复 Custom Diff 修改行与结构块：`modified-* .line-code`（含 change-block）保持透明，浅色行背景承载共同文本，只有 word diff span 使用深红/深绿，故 `private val active` 的共同 `active` 不再被深绿覆盖；`canRefineWordDiff` 在前后缀不足时增加共有 token 阈值（Jaccard ≥.25），结构块只在不存在任何可细化行时拆为纯删除/新增，因此 `companion object` 与 `private object CleanerHolder` 可保留共同 `object` 并显示词级差异。`npm run compile`、`git diff --check` 与文件诊断通过。
- 对齐 Custom Diff 深色 word-diff 范围：`wordDiff()` 在 LCS/Myers 生成真实变化字符后重新调用 `extendToWord()`，将变化字符扩展到所属完整标识符 token；不再伪造另一侧未变化 token。因此深红/深绿与目标图的 token 范围一致，而共同 token 保持浅色行背景。`npm run compile`、`git diff --check` 与文件诊断通过。
- 增强 Custom Diff 文件交互：选中文件的 `.diff.selected` 现在高亮整个文件容器；并排右侧真实代码行渲染 `data-source-line`，仅该 pane 在 Ctrl/Cmd 悬停时显示可点击样式，Ctrl/Cmd + 左键经 generation、白名单路径和行号校验后由扩展端打开 `rootUri + path` 的工作区源码并定位对应新文件行；左侧与内联模式不注册该跳转。`npm run compile`、`git diff --check` 与文件诊断通过。
- 再次按效果图调整 Custom Diff：撤销 `extendToWord()` 的全 token 扩张，word diff 保留 LCS/Myers 的实际字符范围；仅单侧增删时，在保留侧 LCS 边界后的 token 首字符添加最小深色锚点，覆盖 `m_active → active` 和删除一侧 `private` 后的 `val` 前置小块；纯结构性新增/删除块将行、gutter、代码区统一使用深绿/深红，符合完整 `init` 块的视觉。共同 `object` 不会高亮。`npm run compile`、`git diff --check` 与文件诊断通过。
- 修复 Custom Diff 文件置顶时的选中边框缺口：sticky `.file-header` 覆盖 `.diff` 顶部边框后，`.diff.selected > .file-header` 显式使用 focus border 的 `border-top-color` 和 inset 顶边阴影，保持整个文件容器的选中高亮连续。`npm run compile`、`git diff --check` 与文件诊断通过。
- 对齐置顶 Custom Diff 标题栏与卡片几何：`.diff` 是 `3px` 边框、`8px` 圆角，`.file-header` 相应改为宽度 `calc(100% + 6px)`、左右及顶边 `-3px` 外边距、顶部 `8px` 圆角，置顶时两侧和圆角可与卡片边框贴合。`npm run compile`、`git diff --check` 与文件诊断通过。
- 补齐置顶选中 Custom Diff 标题栏的两侧高亮：`.diff.selected > .file-header` 显式设置左右 `border-color` 为 `--vscode-focusBorder`，并加入左右 `inset` 阴影，确保标题栏置顶时侧边焦点框连续。`npm run compile`、`git diff --check` 与文件诊断通过。
- 按最新截图纠正 Custom Diff hunk 分类与背景层：移除“hunk 中存在任一可细化行即不视为结构性替换”的例外；不等长或 token 相似度低于 .50 的多行 hunk 始终投影为纯删除/新增，避免 `active` 等局部共同 token 将整个 `init` 块错误变成逐行 `modified-*` 与阶梯深绿。纯新增/删除背景绘制在真实 `.line` 整行（该行已对齐 pane `scrollWidth`），而非子文本宽度或整个 block，从而实现完整矩形色块且保留 placeholder 斜纹。`npm run compile`、`git diff --check` 与文件诊断通过。
- 调整 Custom Diff 标题栏为卡片内部直角样式：`.file-header` 使用内部 `width: 100%`、`margin: 0`、无外框及圆角，仅保留底部分隔线；外层 `.diff` 继续承担卡片与选中焦点边框，因此标题栏顶部与两侧贴合卡片的深色内部区域。`npm run compile`、`git diff --check` 与文件诊断通过。
- 恢复内部直角 Custom Diff 标题栏的置顶选中顶边：`.diff.selected > .file-header` 明确添加 `border-top: 1px solid var(--vscode-focusBorder)`，同时保留底部分隔焦点色，避免 sticky 标题栏遮挡卡片顶边后高亮缺失。`npm run compile`、`git diff --check` 与文件诊断通过。
- 为置顶 Custom Diff 标题栏的顶部焦点边缘预留空间：`.file-header` 的 sticky `top` 从 `35px` 调整为 `36px`，避免与全局工具栏完全贴合而遮挡 `border-top` 高亮。`npm run compile`、`git diff --check` 与文件诊断通过。
- 填充置顶 Custom Diff 标题栏顶部的 1px 间隙：`.file-header::before` 绝对定位覆盖间隙并使用标题栏同色背景；选中时伪元素改为 `--vscode-focusBorder`，既不透出下方内容又保持顶部焦点高亮连续。`npm run compile`、`git diff --check` 与文件诊断通过。
- 统一置顶标题栏的顶部焦点宽度与 Diff 卡片：卡片使用 `3px` 焦点外框，`.file-header` sticky `top` 改为 `38px`，其 `::before` 遮罩改为 `top: -3px; height: 3px`；移除额外 `1px` 标题栏顶边，选中时仅由遮罩绘制完整 `3px` 顶部高亮，避免透底或线宽不一致。`npm run compile`、`git diff --check` 与文件诊断通过。
- 修复透明焦点色透出置顶标题栏下方内容：在 `::before` 焦点高亮之下新增同尺寸、无透明度的 `::after` 标题栏背景底板，并为 header row 设置更高层级；焦点色本身保持不变，只由底板遮住间隙后的 Diff 内容。`npm run compile`、`git diff --check` 与文件诊断通过。
- 调整 Custom Diff 标题栏下方路径栏颜色：`.pane-title` 改为以 `--vscode-editorWidget-background`（回退 active tab 背景）混合 15% 黑色的 `color-mix`，使其比标题栏稍深而不再使用过深的编辑器背景。`npm run compile`、`git diff --check` 与文件诊断通过。
- 完成 Custom Diff 词级计算重构：成对修改行由字符级 LCS/Myers 改用按 `_`、camelCase 与数字边界拆分的词元 LCS，避免不同标识符内部的相同字符被错配；`tokenWordDiff` 只在另一侧发生纯单侧词元增删时，在首个保留词元前渲染 `0.5ch` 的深色 `word-anchor`，双侧替换和共同词元不出现锚点。`npm run compile`、`git diff --check` 与文件诊断通过。
- 调整 Custom Diff 文件卡片间距：`.diff` 的下边距从 `10px` 增至 `20px`，使相邻 Diff 文件之间的垂直间隔加倍。`npm run compile`、`git diff --check` 与文件诊断通过。
- 修复删除词在对侧缺失竖杆的问题：并排和内联渲染变更行时均透传 `currentLeft.html` / `currentRight.html` 至 `codeHtml`，避免词元级 `word-anchor` 被 `innerChanges` 范围重绘覆盖；锚点宽度调整为 `0.4ch` 深色竖杆。`npm run compile`、`git diff --check` 与文件诊断通过。
- 修复词元删除对侧锚点位置：`tokenWordDiff` 在 LCS 回溯删除/新增 token 时记录另一侧当前 token 索引，渲染时仅在对应索引前插入 `word-anchor`，不再固定插入首个未变 token 前；如删除 `m_` 时右侧锚点准确落在 `active` 前。`npm run compile`、`git diff --check` 与文件诊断通过。
- 对 Custom Diff 与 VS Code 的算法对齐程度做代码核对：当前并非 VS Code 原生 diff 算法/渲染的等价实现。项目使用自定义 `tokenWordDiff`、`canRefineWordDiff` 的 `.35` 前后缀与 `.25` token Jaccard 阈值、半字符 `word-anchor` 等启发式；这些行为未对应 VS Code 的内部 advanced diff 结果，后续如要完全对齐应改为复用 VS Code 原生 Diff Editor 而不是继续调整 Webview 重实现。
- 修复 Custom Diff 上下变更跳转的跨文件置顶：`navigateChange` 记录前后目标所属 `.diff`，仅跨文件时将目标滚动位置下限限制为 `diff.offsetTop - 38 + 1`，使目标文件的 sticky `.file-header` 必然进入置顶区；同文件跳转维持原居中行为。`npm run compile`、`git diff --check` 与文件诊断通过。
- 调整 Custom Diff word diff 的垂直覆盖范围：`.word-removed`、`.word-added`、`.word-modified` 统一为 `inline-block` 并使用 `height/line-height: var(--line-height)` 与顶部对齐；`word-anchor` 同样使用完整行高，保证深色词级高亮和竖杆覆盖整行高度。`npm run compile`、`git diff --check` 与文件诊断通过。
- 对齐 Custom Diff 变更行号标记：`.line-number` 改为右对齐 flex 布局，非空删除/修改删除/移动删除行号后显示 `−`，非空新增/修改新增/移动新增行号后显示 `+`；空侧 placeholder 行号不显示符号；选择器限定为 `.pane`，内联 `.unified-pane` 不显示增删标记。每个并排 pane 渲染后按最大行号位数计算 `--line-number-width`，符号列改为内容自适应且 `min-width: 8px`，普通行也保留该列，以避免行号横向错位。默认 `.line-number` / `.line-marker` 背景改为 gutter 色混合 20% 黑色，行号使用左 `20px`、右 `2px` 内距；计算宽度增加到 `digits + 36px` 以容纳内距和符号列。所有 change block 的行号栏强制保持独立深色 gutter，不再被红绿对比高亮覆盖。`npm run compile`、`git diff --check` 与文件诊断通过。
