# 2026-08-12

- 分支弹窗关闭时仅在实际打开状态回滚未确认的暂存选择，从而将每个分支复选框恢复至打开前状态；全选按钮移除悬停与单击背景。`npm run compile` 通过。
- `CustomDiffPanel` 的 `calculateDiff` 投影层增加左右源行索引去重集：同一源行若因算法区间重叠再次出现，丢弃后续虚拟行，确保并排两侧只展示首次投影。`npm run compile` 通过，文件诊断为零。
- 分支弹窗中的暂存勾选不再即时更新 `branchs:` 后的显示；仅点击确定才更新，取消或外部关闭时该显示与勾选都会恢复打开前状态。`npm run compile` 通过。
- 根据交互反馈，分支弹窗与 `branchs:` 显示恢复为同一选择状态源：选择变化立即同步显示，取消或外部关闭时通过打开前快照重绘全部复选项及显示。`npm run compile` 通过。
- 修复分支弹窗打开期间发生状态刷新时的回滚失效：即使分支选项 DOM 被重建，仍保留打开前快照和未确认选择；非确认关闭会恢复全部选项及 `branchs:` 显示。`npm run compile` 通过。
- `CustomDiffPanel` 并排重复行排查中已撤回未命中的 Patience 锚点、源索引去重及上下文折叠范围改动，恢复原行级算法与折叠逻辑。用当前真实 Git 文本和 20000 组含重复文本的随机序列执行 `calculateDiff`，左右源行索引均无重复或倒退；`Myers` 回溯、全量 `innerHTML` 重绘、complete 门闩也已排除。用户确认仅并排出现，因此后续应聚焦左右 pane 独立 DOM 与仅并排渲染的 `placeholder` 对齐行；需要实际截图或原始/修改文本区分重复实际文本和对齐空白。`npm run compile` 通过，文件诊断为零。
- 加强分支弹窗的未确认关闭回滚：打开时深拷贝已应用选择；取消、Esc 或外部关闭时先清除暂存事务标记，再按快照重绘分支复选框、全选框及 `branchs:` 显示，避免回滚被待处理状态覆盖。`npm run compile` 通过。
- 分支弹窗回滚基准改为独立 `confirmedSelection`：扩展端状态稳定时更新该快照，打开时用快照初始化草稿，只有确定时写回快照；未确认关闭始终从该快照恢复全部分支选项和 `branchs:` 显示。`npm run compile` 通过。
- 修复分支确认时序：点击确定不再提前将 Webview 草稿写入 `confirmedSelection` 或清除回滚快照，必须等扩展端 Store 回传权威 `selectedBranches` 后才确认，避免无效/过期选择污染后续未确认关闭的还原。`npm run compile` 通过。
- 根据用户截图确认 `CustomDiffPanel` 并排实际文本重复根因：短未修改区间中 `renderRows` 的 `trailingStart` 可小于当前区间 `index`，尾部循环回退并再次输出前一 hunk 的行；已将 `leadingEnd`/`trailingStart` 限制在当前 `[index, end]`，截图对应区间原尾部起点 23 小于起点 26，修复后为 28。`npm run compile` 通过，文件诊断为零。
- 修复分支弹窗回滚未执行的断链：打开逻辑已改用 `appliedSelection`，而旧回滚仍依赖未设置的 `openSelection`，使取消与外部关闭直接跳过恢复。现改为非确认关闭无条件从 `appliedSelection` 重绘选项、全选及 `branchs:` 显示；确定操作显式跳过本地回滚并等待 Store 回传。`npm run compile` 通过。
- `CustomDiffPanel.appendChanged` 明确仅对一对一 `modified-*` 行调用 `wordDiff`；纯 `removed`/`added` 行保持整行差异样式，不输出词级 `<span>`。`npm run compile` 通过，文件诊断为零。
- `GitkViewProvider` 以 `WebviewView.visible` 判断底部 Gitk 面板是否被选中且可见：失焦时隐藏 `CustomDiffPanel`，`openDiff` 入口也二次拦截，防止后台加载完成后重开 Diff。首次解析 Gitk Webview 若复用后台已加载的 Store 数据，会主动调用可见性同步显示 Diff，弥补未触发可见性事件的路径。`npm run compile` 通过，文件诊断为零。
- `selectWorkingTreeChanges` 不再复用分支加载时缓存的 `stagedFiles/changeFiles`：每次点击 Staged Changes 或 Changes 虚拟提交都会调用 `getWorkingTreeChanges` 读取最新 Git 状态，回写两个缓存后加载所选集合的 Diff；请求可取消且错误会写入 `diffError`。`npm run compile` 通过，文件诊断为零。
- 已确认可通过 VS Code 文件系统监听实现虚拟提交可见性联动：现有 `initializeGitWatchers` 已监听当前工作区的 `.git/index`，可感知 staged 变化；还需监听当前仓库工作区文件（排除 `.git`）并防抖调用 `getWorkingTreeChanges`，再据结果刷新当前分支的 `virtualCommits` 与 `stagedFiles/changeFiles`，以空/非空控制显示。用户倾向改用串行 5 秒轮询：首次从不可见切为可见时立即执行查询，后续每轮只执行 `git status --porcelain=v1 -z` 判断两类变更存在性，查询完成后再等 5 秒；仅在底部 Gitk `WebviewView.visible` 时启动，失焦时清除下一轮计时器并 Abort 在途查询。针对选中仓库的仅存在性检测，推荐保持单进程 `git status --porcelain=v1 -z --untracked-files=normal`：一条输出即可区分 index/worktree/untracked；若产品不将未跟踪文件视作 Changes，可用 `--untracked-files=no` 降低大型仓库目录扫描开销。已实施：`getWorkingTreeChangePresence` 仅执行该 status 命令并提前退出解析；`GitkViewProvider` 在 Gitk 可见且单选仓库时首次立即、之后完成后 5 秒串行轮询。不可见、切仓、多仓库、dispose 时清 timer 并 abort 在途进程；结果使用仓库路径和 generation 校验，并只在 staged/changes 布尔显示状态改变时增量替换当前分支 `virtualCommits`，不读取 diff 元数据、分支或提交历史。`npm run compile` 通过，相关文件诊断为零。
