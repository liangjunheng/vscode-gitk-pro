# 2026-08-09

- 说明仓库切换加载态与 Cherry-pick 刷新的现状：切换路径先调用 `beginCommitReload()` 清空本地数据，但其加载态消息发送与后续刷新启动存在异步调度间隙；`runCommitAction()` 对所有成功 Git 操作统一执行 `refreshWithRetry()`，因此 Cherry-pick 后会重载提交列表以反映新生成的提交与 HEAD 变化。
- 修复仓库切换与提交操作的冗余仓库刷新：此前 `refreshInternal(skipSelectors)` 忽略了参数，始终执行 `refreshSelectors()`，导致每次切换/Cherry-pick 都递归扫描所有仓库和子模块。现改为 `skipSelectors` 时调用 `refreshSelectedRepositoryBranches()`，仅刷新目标仓库分支和提交；重试也保留该开关。`npm run compile` 通过。
- 审查 Changed Files 标题栏图标：已实现动作的 `data-action`、Webview 消息和宿主 switch 映射完整；确认待修问题包括 `copyHash` 异常未捕获、`drop` 仅提示未执行却保持可用、提交动作消息缺少仓库/提交归属校验、非提交状态未隐藏右侧空 action group，以及 checkout 等动作刷新仍可能被已选分支 refs 过滤而无法展示当前 HEAD。
- 排查仓库切换异常：`getGitRepositories()` 可借助本地 `git rev-parse` 发现工作区/子模块，但 `getGitCommits()` 与 `getCommitFiles()` 又要求仓库已被 VS Code Git API 登记，扩展扫描未就绪时会误报“未找到 Git 仓库”。切换刷新允许旧 generation 并行，但选择器、进度和错误消息缺少 generation 防护，会覆盖新仓库 UI。加载提交慢还因默认传入全部本地/远程 refs 给 `git log`，并将 `git status --untracked-files=all` 与提交读取绑定等待；子模块 BFS 扫描与 refs 缓存无单飞进一步放大耗时。
- 性能与资源优化：`gitLogProvider.ts` 的 refs 读取新增按仓库单飞 `refsInFlight`；`getGitCommits`、`getCommitFiles` 改为完全使用本地 Git，不再依赖 VS Code Git API 登记；状态签名由 `--untracked-files=all` 改为 `normal`。`gitkViewProvider.ts` 在卸载时释放状态订阅与定时器，切换仓库时清除旧防抖任务；提交文件读取与局部分支刷新均增加仓库/generation 防过期覆盖。`npm run compile` 已通过。
- 事件驱动改造：移除 `gitkViewProvider.ts` 的固定重试及动画清理定时器；Git 仓库打开/关闭、工作区文件夹和仓库状态事件现在驱动显式缓存失效与单飞 dirty 队列刷新，生命周期事件强制完整刷新选择器。新增 `invalidateGitRepositoriesCache()` 与 `invalidateGitRefsCache()`。提交图和自定义 Diff 的动画样式改由 `animationend`/`animationcancel` 清理。状态事件与自动搜索均使用 300ms 防抖；Enter、Escape、清空搜索保持即时触发。保留 `requestAnimationFrame` 供渲染批处理。`npm run compile` 通过。
- 风险评审结论：移除定时器后的优先风险为 Git 初始扫描事件遗漏导致空列表、生命周期事件等待慢刷新造成切换延迟、状态事件提前失效 refs 缓存；状态与搜索防抖已恢复。后续应优先为初始扫描增加可靠完成信号，并将 refs 失效延后到防抖刷新实际执行时。
- 进度文案清晰化：子模块发现进度由“加载子模块 (N)”改为“已扫描到子模块 N 个”，并修正 `collectSubmoduleRepositories` 回调仅统计新增子模块、不包含初始仓库；并发复用路径显示“正在等待子模块扫描完成”。自定义 Diff 的准备和生成进度统一为“已处理 N / 总数 个文件”。`npm run compile` 通过。
- 风险方案规划：高风险包括初始 Git 扫描事件遗漏、切换等待旧刷新；中风险包括生命周期事件造成多轮完整刷新、状态事件提前失效 refs 缓存。建议分别采用 Git API 初始化完成后的条件复核、可取消/不阻塞的 generation 刷新、合并生命周期事件并按仓库集合差异选择局部刷新、在状态防抖实际执行时失效 refs 缓存。
- 已实施风险缓解：`resolveWebviewView` 改为顺序执行 Git API 初始化/订阅后再首次完整刷新，避免初始扫描事件遗漏；生命周期事件按类型合并，仅仓库打开/关闭和工作区目录变更重新发现仓库并比较集合，集合未变只局部刷新。状态事件的 refs 路径暂存至集合，300ms 防抖到期并执行刷新前再按路径失效。`npm run compile` 通过。
- 刷新优先级队列：新增 `RepositorySelection > Lifecycle > RepositoryState` 调度，仓库切换会递增 refresh generation 使低优先级请求在仓库扫描、分支读取、提交读取阶段停止写回；低优先级刷新仅在高优先级任务完成后继续。`refreshSelectors` 和 `refreshSelectedRepositoryBranches` 追加统一世代校验。`npm run compile` 通过。
- 旧请求立即取消：`gitkViewProvider.ts` 以独立 `AbortController` 管理刷新、搜索、提交文件、分页和预取；任何新刷新、提交列表重载、分支切换、搜索输入或 Webview 释放均主动 abort 旧请求。`gitLogProvider.ts` 的 `git log`、`rev-list`、`for-each-ref`、`rev-parse`、`diff-tree`、`status` 和分支读取支持传递 `AbortSignal` 至 `execFile`，取消异常会静默传播而不覆盖新 UI。保留 generation 校验作为写回保护。`npm run compile` 与 `src` 诊断通过。
- 取消机制风险修复：仓库/子模块扫描现支持 `AbortSignal`，可取消调用不再复用不可控的 `repositoriesInFlight`；子模块 `.gitmodules`、工作区与子模块 `rev-parse` 都会传播中止。refs 缓存加入版本号，缓存失效后旧 `for-each-ref` 完成也不能回写过期 refs。分支直接刷新同时取消搜索、提交文件和预取；中止异常不再被显示为刷新失败。`npm run compile` 通过。
- 提交加载与标题操作修复：提交加载进度现明确显示“解析分支指向的提交”“读取提交历史与工作区变更”“整理提交图与变更文件”，多仓库模式显示已完成仓库数；分支筛选加载也区分解析分支和读取日志。`runCommitAction` 仅在 Git 命令成功实际变更仓库后才失效 refs 并刷新；取消标签/分支输入、取消 reset 确认、copy/drop 或未知操作不会重载提交。`npm run compile` 通过。
- 修复提交进度提前完成：`getGitCommits()` 的 100% 只表示日志与 refs 已读取，后续仍有构图、工作区变更整理及四个并行 Git 状态校验。`refreshInternal()` 现将这些阶段统一显示为 1/4 至 4/4 的真实进度，只有提交列表已发送给 Webview 后才显示完成；`getGitRepositoryState()`、状态签名更新均接收 `AbortSignal`，新刷新可中止旧状态校验。`npm run compile` 通过。
- 首次初始化刷新稳定性：Git API 注册仓库和状态监听会在首轮刷新中触发生命周期/状态刷新，现通过 `initializingViewGeneration` 合并首轮生命周期事件、`isLoading` 合并状态事件，防止重复读取提交。刷新进度消息不再清空已有提交，避免后续取消/失败刷新误将有效列表变为“无提交”。仅在未选仓库时发布空列表；仓库已选但分支尚未初始化时读取全部提交。`npm run compile` 通过。
- 渐进式首屏加载：`refreshInternal()` 现在只等待 `getGitCommits()`，构图后立即下发 `commits` 并结束首屏 loading；`getWorkingTreeChanges()` 与 `updateRepositoryStateSignatures()` 改由 `loadRefreshDetails()` 后台执行。后台统计返回后用 `workingTreeChanges` 消息仅更新 Changes/Staged 行，避免重绘提交图、折叠状态或滚动位置。`npm run compile` 通过。
- 其他界面渐进式更新：`refreshBranchCommits()` 选择提交后不再 await 变更文件加载；自定义 Diff 面板改为每 20 文件读取/推送一批，Webview 在首批渲染后立即显示，后续批次原位追加，完整加载后才执行默认文件选中。`npm run compile` 通过。
- 仓库切换提交区加载态：`beginCommitReload()` 现在发送 `commitsLoading`；Webview 接收后清空旧仓库的提交、选中状态和列表 DOM，并显示“正在读取提交历史...”，避免新仓库加载期间错误保留旧仓库提交。`npm run compile` 通过。
- 子模块扫描变慢的代码审查：当前实现会以 `api.repositories` 的所有已登记仓库为起点，递归 BFS 对每个仓库执行 `.gitmodules` 查询，并为每个声明的子模块再执行 `rev-parse --show-toplevel`；仓库打开/关闭和工作区变更会主动清空 10 秒仓库缓存并触发完整扫描。仓库切换本身已经走 `refreshSelectedRepositoryBranches()`，不应触发扫描。建议优化顺序：扫描前去重并排除已由上层覆盖的嵌套 API 仓库；仅对未缓存根仓库扫描；以仓库集合差异判定缓存失效，避免普通 Git API 生命周期事件清空全局缓存。
- 已实现子模块扫描优化（不含并发限流）：`gitLogProvider.ts` 仅从顶层根仓库开始 BFS，避免 API 已登记的嵌套子模块重复扫描；命中 API 已知子模块路径时直接复用根路径、跳过 `rev-parse`；按顶层根仓库缓存递归结果 10 秒，并使用 API 仓库集合签名校验总缓存。`gitkViewProvider.ts` 的仓库打开/关闭不再无条件清空缓存，工作区或 `.gitmodules` 实际变更才清空。`npm run compile` 通过。
- 子模块缓存复核：用户指出缓存应优先限定在同一次扫描任务；当前 `submoduleRepositoriesCache` 以 10 秒 TTL 跨任务复用，是为短期生命周期刷新节流而加入，但不符合“本次扫描内复用”的更精确语义。后续应将其替换为单次扫描 `Map` 或保留总仓库结果的单飞复用，并以 `.gitmodules` 变更进行失效。
- 已按复核结论修正：删除 `submoduleRepositoriesCache` 的跨任务 10 秒缓存；`getGitRepositoriesInternal()` 使用仅存活于本次扫描的 `scanTaskResults: Map<rootPath, Promise<RepositoryRecord[]>>` 复用同一顶层根仓库的发现任务。总仓库结果缓存与 `repositoriesInFlight` 单飞仍保留，分别由集合签名/变更事件及进行中的同一请求控制。`npm run compile` 通过。
- 分支切换提交区状态：`commitsLoading` Webview 处理现在同步重置 `hasMoreCommits`、`isLoadingMoreCommits` 与分页错误，立即隐藏“加载更多”页脚，并将搜索框右侧 `countLabel` 设置为“加载中...”；收到新 `commits` 后恢复真实计数和分页状态。`npm run compile` 通过。
- `src/customDiffPanel.ts` 的每个 Multi-Diff 文件标题现在按 `calculateDiff()` 的实际行类型统计并展示 `+新增行数`、`-删除行数`，覆盖普通修改、新增、删除和重命名文件；`npm run compile` 与该文件 lint 均通过。
- 提交文件加载优化：`getCommitFiles()` 的 `git diff-tree` 保留重命名检测 `-M`、移除高成本复制检测 `-C`；`CommitFilesViewProvider.setCommit()` 选中提交后仅读取并展示变更文件清单，不再自动读取该提交全部 blob 并打开/渲染完整 Custom Multi-Diff，改为用户点击文件后再按需打开。`npm run compile` 通过；相关文件无新增诊断。
- Multi-Diff 修复：标题统计移动到状态标识之后、文件路径之前，CSS 取消统计的自动右对齐以确保可见；宿主为每个加载世代防重、读取结束后发送 `complete`，失败时发送 `error`，Webview 收到 `complete` 后关闭加载视图，解决进度达到 `26/26` 仍停留“正在准备 Diff”的问题。`npm run compile` 与 `customDiffPanel.ts` lint 通过。
- Changed Files 交互与 Multi-Diff 视觉更新：文件行右键菜单可复制相对路径或基于当前仓库生成的完整路径；文件列表默认平铺，树状/平铺选择保存到 `workspaceState` 的 `gitk.filesDisplayMode` 并在扩展启动恢复。Multi-Diff 标题统计移至文件名前，使用自绘加/减 SVG 图标；隐藏行改为自绘 SVG 箭头，顶部控件向上展开、底部控件向下展开。`npm run compile` 通过；仅保留 `gitkViewProvider.ts` 既有未使用符号 hints。
- 紧凑化 Multi-Diff 标题统计：`customDiffPanel.ts` 将统计项间距设为 `4px`、单项内图标与数字间距设为 `0`，减少加减 SVG 与数值的视觉空隙；`npm run compile` 与该文件诊断均通过。
- 修正 Multi-Diff 折叠行方向图标：`contextFoldHtml` 根据折叠区上下文条件渲染按钮；仅能向上展开时只显示向上箭头，仅能向下展开时只显示向下箭头，中间两侧均可展开时同时显示上下箭头。`npm run compile` 与 `customDiffPanel.ts` 诊断通过。
- Changed Files 文件路径前增加提交行数统计：`getCommitFiles` 并行执行 `git diff-tree --name-status` 和 `--numstat`，将非二进制文件的 `addedLines` / `removedLines` 合并到 `CommitFile`；`gitkViewProvider.ts` 在树状与平铺行的路径前显示绿色 `+N` 和红色 `−N`。`npm run compile` 通过，现有未使用符号 hints 未新增。
- 选中提交首屏性能优化：`getCommitFiles()` 改为只读取 `--name-status`，Changed Files 与 Multi-Diff 不再等待 `--numstat`；`getCommitFileLineStats()` 在文件清单和 Diff 启动后异步补齐行数，且仅当提交仍有效才重渲染。`CustomDiffPanel` 首批仅读取 1 个文件，之后每批 8 个增量读取，优先展示第一个 Diff。`npm run compile` 通过，未新增诊断。
- 进一步优化变更文件读取：`getCommitFiles()` 和后台 `getCommitFileLineStats()` 均使用 `--no-renames`，避免大提交执行昂贵的重命名相似度检测；提交文件列表按仓库路径和 hash 缓存最多 100 项，重复选择同一提交直接复用结果。`setCommitFiles()` 不再等待 `openDiff()`，让 Changed Files 的选择流程不被 Multi-Diff 面板初始化阻塞。`npm run compile` 通过，未新增诊断。
- Changed Files 完整数据与首次初始化优化：`getCommitFilesWithLineStats()` 使用单条 `git diff-tree --raw --numstat --no-renames` 同时取得文件状态与行数，替代此前先后两次 `diff-tree` 遍历，解析 `--raw` 记录并与 numstat 合并；完整缓存和同 key in-flight Promise 防止预热与选择同一提交导致重复加载。`setCommitFiles()` 仅在完整数据就绪后渲染一次。提交列表仍先显示、默认提交优先加载，其余提交以 3 路后台预热。`npm run compile` 通过，未新增诊断。
- 提交切换与 Multi-Diff 视觉修复：`selectCommit()` 在请求文件前立即清空旧文件、发送 `filesLoading` 并隐藏旧 Diff；Webview 以 hash + repositoryPath 对所有提交行重设唯一 `.selected`，修复双高亮。Multi-Diff 的加减图标改为 11px 实心 SVG，移除描边透明感，负右边距与紧缩字距使其贴近数字。`npm run compile` 通过；`gitkViewProvider.ts` 仅保留既有未使用符号 hints。
- Multi-Diff 文件标题视觉微调：`customDiffPanel.ts` 的 `.file-header` 字号由基础字号的 `0.89` 倍调至约 `1.19` 倍（放大约三分之一）；`.line-stat` 图标与数字改为明确 `2px` 间隔，移除负边距和负字距。`npm run compile` 与该文件诊断均通过。
- 提交切换请求治理：`selectCommit()` 现在先中止旧的 `commitFilesAbortController`、关闭并终止旧 Custom Multi-Diff 的 Git 子进程，再发布新 commit 选择与 `{ filesLoading, message }`；Changed Files Webview 立即替换为带动画 spinner 的“正在加载变更文件...”状态。现有 `setCommitFiles()` 的 AbortSignal 与 hash/仓库路径校验会阻止旧响应回写，避免跳变。`npm run compile` 通过；仅保留既有未使用符号 hints。
- 快速切换 Changed Files 修复：新增 `commitFilesGeneration`，每个提交/工作区选择均使旧加载世代失效；`setCommitFiles` 在进度、成功和错误写回前都校验 generation，Webview 也以 hash + repositoryPath 拦截过期加载/错误消息。可取消的前台文件请求不再复用预加载 in-flight Promise，避免旧 signal 取消当前选择。
- Changed Files 跟手性能优化：提交选择会取消后台 `preloadCommitFiles`，避免最多 3 路预取与当前点击竞争 Git/磁盘资源；首屏预取也排除默认选中提交，防止重复扫描。`getCommitFilesWithLineStats()` 移除为提前取得准确总数而执行的 `diff-tree --name-only`，仅保留一次 `diff-tree --raw --numstat --no-renames`；加载时为不定量 `0/0`，完成后更新为实际 `N/N`。`npm run compile` 通过，诊断仅保留既有未使用符号 hints。
- 高频提交切换响应优化：`selectCommit()` 发布当前 commit 的选中与不定量加载态后，先让出一个事件循环轮次，再检查 `commitFilesGeneration`；短时间内被覆盖的选择不会创建 Git 请求，最新点击可先完成 Webview 绘制再开始文件读取。加载文案固定为“正在加载变更文件...”，不显示数量。`npm run compile` 通过。
- Multi-Diff 增删统计紧凑化：`.line-stat` 文字缩至当前标题字号的 `5/6`，SVG 图标由 `11px` 缩至 `9px`（约六分之一），图标与数字间隔改为 `1px`。`npm run compile` 及该文件诊断通过。
- Changed Files 与 Multi-Diff 同步：`CustomDiffPanel` 新增 `MultiDiffLoadEvent` 回调，在每个批次读取完成、完成或错误时通知 `GitkViewProvider`。提交切换后 `setCommitFiles()` 仅暂存文件并启动 Multi-Diff，Changed Files 区显示“正在加载 Diff (x/N)...”进度；仅当前 hash/仓库/generation 的 Diff `complete` 事件才渲染文件列表。切换 commit 与刷新会清除 `pendingFiles`，阻止旧完成事件回写。
- Multi-Diff 完成态时序修正：扩展端完成读取后不再直接触发 `complete`；先向 Webview 发送 `sourceComplete`，Webview 在下一动画帧内确认所有 Diff DOM 批次已渲染后，发回 `renderComplete`，扩展端才发出 `MultiDiffLoadEvent.complete` 并解除 Changed Files 加载态。`npm run compile` 通过；`customDiffPanel.ts` 无诊断，主视图仅有既有未使用符号 hints。
- 复用 Multi-Diff 面板与取消旧请求：`selectCommit()` 改用 `customDiffPanel.cancelPending()` 替代 `hide()`，不再 `dispose()` 面板，而是递增 `requestGeneration`、终止子进程、清空 `loadingGeneration` 并向 Webview 发送 `loading` 消息（更新 generation 并显示加载态）。`show()` 复用已有面板，仅在新 commit 时递增 generation 并发 `reset`。Webview 的 `loading` handler 更新 `generation` 后，旧 `diffs`/`progress`/`sourceComplete` 消息因 `message.generation !== generation` 被忽略，修复快速切换内容错乱。无文件时 `setCommitFiles` 调用 `hide()` 释放面板。`npm run compile` 通过。
- Changed Files 先于 Multi-Diff 渲染：`setCommitFiles()` 在文件读取完成后立即调用 `renderFiles()` 显示文件列表，再启动 `openDiff()`；不再发送 `filesLoading` Diff 进度消息覆盖列表。`handleMultiDiffLoadEvent` 的 `progress` 分支改为 no-op（Multi-Diff 面板自身显示进度），`complete` 分支不再重复 `renderFiles()`（已在 `setCommitFiles` 渲染），仅清空 `pendingFiles`。修复 Multi-Diff 加载时选中文件高亮失败的问题。`npm run compile` 通过。
- 架构重构：Multi-Diff 面板改为纯渲染器，Diff 准备流程移至 Changed Files。`customDiffPanel.ts` 移除 `readAllDiffsIncrementally`/`readDiffs`/`readWorkingTreeDiffs`/`readWorkspaceFile`/`readGitObjects`/`stopChildProcess` 及 `childProcess`/`loadingGeneration` 字段和 `loadAll` 消息处理；新增 `getGeneration()`/`appendDiffs()`/`sendProgress()`/`completeLoading()`/`sendError()` 公共方法；`show()` 返回 `Promise<boolean>` 指示是否为新 commit。Webview 移除 `requestAll()` 和 `loadAll` 往返。`gitkViewProvider.ts` 新增 `diffChildProcess` 字段及 `prepareDiffs()`/`readDiffs()`/`readWorkingTreeDiffs()`/`readWorkspaceFile()`/`readGitObjects()`/`stopDiffChildProcess()` 方法；`openDiff()` 在 `show()` 返回 true 时调 `prepareDiffs()`；所有 `cancelPending()` 调用前加 `stopDiffChildProcess()`。`npm run compile` 通过。
- 项目重构：按功能拆分文件夹 + 单一数据源 Store。新目录结构: `types/` (git.ts, state.ts, index.ts), `state/` (store.ts), `git/` (gitLogProvider.ts, diffReader.ts), `services/` (refreshScheduler.ts, gitActions.ts), `webview/` (gitkViewProvider.ts, customDiffPanel.ts, commitFilesViewProvider.ts, diffContentProvider.ts)。`types/git.ts` 统一定义所有共享类型; `state/store.ts` 实现 Observable Store (subscribe/notify/subscribeSelector); `git/diffReader.ts` 提取 DiffReader 类; `services/refreshScheduler.ts` 提取优先级队列; `services/gitActions.ts` 提取 GitActionRunner。`gitkViewProvider.ts` 使用 DiffReader/GitActionRunner 替代内联方法, 通过 `syncToStore()` 在 setLoading/renderFiles/selectCommit/refreshInternal 后同步状态到 Store。`gitLogProvider.ts` 类型改为从 `../types` 导入并 re-export。`npm run compile` 通过。
- 清理编译缓存：`npm run compile` 实际通过 (exit code 0), VS Code Problems 面板显示的 18 个错误全部来自已删除的旧文件 `src/gitkViewProvider.ts` 和 `src/gitLogProvider.ts` (TS language server 缓存)。清理了 `out/` 目录中残留的旧编译产物 (commitFilesViewProvider/customDiffPanel/diffContentProvider/gitkViewProvider/gitLogProvider 的 .js + .map)。用户需 Reload Window 清除 VS Code 旧文件诊断缓存。
- Store 升级为真正的单一数据源：`store.ts` 新增 `batch()` 方法支持批量更新 (收集多次 setState, 退出时仅 notify 一次)。`state.ts` 新增 `hasBranchSelection` 字段, `displayMode` 默认值改为 `'flat'` 匹配 workspaceState 默认。`gitkViewProvider.ts` 移除 24 个本地数据字段 (), 改为 Store getter/setter 直接读写; 移除 `syncToStore()` 方法及 4 处调用; 新增 `updateViewVisible()` 在 resolveWebviewView/onDidChangeVisibility/onDidDispose 调用; `loadMoreCommits` 的 `push` 改为 spread 重新赋值; `buildGraph` 原地修改 `graphState` 后浅拷贝触发 Store 通知; 多字段设置点用 `store.batch()` 包裹 (selectCommit/selectWorkingTreeChanges/refreshInternal/refreshBranchCommits/refreshSelectors/refreshBranchSelectors/selectRepositories/selectBranches/clearSearch/refreshSearchCleared/loadMoreCommits)。`extension.ts` 显式 import store 并在 activate 日志初始化。`npm run compile` 通过。
- MVI 架构审查与修复: (1) 删除死代码 `commitFilesViewProvider.ts` (无外部引用, 4 个本地字段复制 Store)。(2) 统一 `gitApi` 数据源: `gitLogProvider.ts` 导出 `getGitApi()` 和 `GitApi` 接口 (新增 `onDidOpenRepository?`/`onDidCloseRepository?` 事件属性), `gitkViewProvider.ts` 从 `../git/gitLogProvider` 导入 `GitApi` 类型 (移除 `../types` 的 `GitApi`/`GitApiRepository`/`GitExtensionApi`), `loadGitApi()` 改用 `getGitApi()` 替代独立 `vscode.extensions.getExtension`。(3) `statusBar.ts` 重写: 订阅 Store `repositories` 变化驱动可见性 (`store.subscribeSelector`), 移除独立 `gitApi` 字段, `checkVisibility` 改为 private 接收 `hasRepo` 参数, `dispose()` 调用 `unsubscribe()`。(4) `extension.ts` 移除 `onDidChangeWorkspaceFolders` → `statusBar.checkVisibility()` 调用 (已由 Store 订阅替代)。未修复 (需大规模重写): Webview JS 12+ 本地变量镜像 Store 状态, 62 个散落 `postMessage` 推送 (命令驱动非数据驱动)。`npm run compile` 通过。
- 数据碎片第二轮审查与修复: (1) 移除 `activeRefreshPriority` 死代码 (仅写不读)。(2) 移除 `RefreshScheduler` 类 (从未实例化的死代码), `refreshScheduler.ts` 仅保留 `RefreshPriority`/`RefreshPriorityValue`/`QueuedRefresh` 导出。(3) 移除 `gitkViewProvider.ts` 内重复的 `QueuedRefresh` 接口, 改从 `refreshScheduler.ts` 导入。(4) `gitLogProvider.ts` 新增 `getCachedGitApi()` 同步获取器, `gitkViewProvider.ts` 移除 `private gitApi?: GitApi` 字段, `updateRepositoryStateSubscriptions()` 改用 `getCachedGitApi()`。(5) `pendingFiles` 类型移除未使用的 `files: CommitFile[]` 字段。可接受的二级缓存: `customDiffPanel.ts` 显示快照 (files/hash/changeSetMode/rootUri), `gitLogProvider.ts` 的 refsCache/repositoriesCache/completeCommitFilesCache (HTTP-like), `repositoryStateSignatures` Map (优化用)。`npm run compile` 通过。
- Webview 数据驱动重构 (MVI): 将 Webview 从命令驱动改为数据驱动。(1) `state.ts` 新增 `loadingMessage: string | undefined` 和 `commitPageError: string` 字段。(2) `gitkViewProvider.ts` 新增 `pushStateToWebview()` 方法收集 Store 全部业务状态 (commits/files/repositories/branches/selection/isLoading/loadingMessage/isSearchResult/searchMatchCount/commitPageError 等) 发送 `stateUpdate` 消息; `schedulePushState()` 用 `queueMicrotask` 防抖; `resolveWebviewView` 内 `store.subscribe(() => schedulePushState())` 订阅 Store 变更自动推送; `onDidDispose` 和 Disposable 清理订阅。(3) Webview JS 新增 `stateUpdate` 处理器, 更新所有本地变量并调用 render/renderFiles/renderSelectors/renderCommitFooter。(4) 移除 10 类冗余 postMessage 调用 (selectors/commits/files/selectedCommit/commitPageState/updateTotalCommits/workingTreeChanges/loading/error/commitsLoading), 改为写 Store 触发自动推送; `setLoading(false)` 清空 `loadingMessage`。(5) 移除 Webview 10 个冗余消息处理器, 仅保留 branchesLoading/appendCommits/loadingProgress/refreshUnchanged/refreshing/filesLoading/filesError 瞬态消息。`npm run compile` 通过。
- "谁最新用谁"数据源改造: `gitLogProvider.ts` 的 4 个数据获取函数全部改为优先读 VS Code Git API `repository.state`, API 不可用时回退 git CLI。(1) `GitRepository` 接口 `state` 新增 `HEAD?`/`workingTreeChanges?`/`indexChanges?` 字段; `types/git.ts` 的 `GitApiRepository` 同步更新并补充 `GitRefApi`/`GitChangeApi` 定义。(2) 新增适配器: `apiRefsToRefRecords()` 将 API refs 转为 `GitRefRecord[]`; `apiChangesToCommitFiles()` 将 API changes 转为 `CommitFile[]` (用 `path.relative` 转相对路径, `originalUri` 判重命名)。(3) `getGitRepositoryState()`: API 路径从 `repo.state.HEAD`/`refs`/`workingTreeChanges`/`indexChanges` 序列化签名, 零 git 命令; 回退仍跑 4 条 git CLI。(4) `getWorkingTreeChanges()`: API 路径直接转 `indexChanges`+`workingTreeChanges`; 回退仍跑 `git status --porcelain`。(5) `getGitRefs()`: API 路径从 `repo.state.refs` 转 `GitRefRecord[]` 并更新缓存; 回退仍跑 `git for-each-ref`。(6) `getCurrentGitBranch()`: API 路径从 `repo.state.HEAD` 取分支名; 回退仍跑 `git symbolic-ref`。提交历史 (`git log`) 和提交文件变更 (`git diff-tree`) 仍走 CLI (VS Code 不提供等价数据)。`npm run compile` 通过。
- 竞速 + 局部刷新改造: (1) `gitLogProvider.ts` 新增 `raceSources<T>(apiFn, cliFn)` 竞速函数: API 同步返回有效数据时直接 resolve, 否则走 CLI; 4 个函数 (`getGitRepositoryState`/`getWorkingTreeChanges`/`getGitRefs`/`getCurrentGitBranch`) 全部从 "API 优先 + CLI 回退" 改为竞速模式; `findRepository` 和 `apiChangesToCommitFiles` 改为 export。(2) `gitkViewProvider.ts` 签名拆分: `repositoryStateSignatures` → `repositoryCommitSignatures` (head+branch+refs) + `repositoryWorkingTreeSignatures` (status); `getRepositoryStateSignatures()` 返回 `{commitSig, workingTreeSig}`; `refreshIfChanged()` 分别对比, 仅工作区变化时走 `refreshWorkingTreeOnly()` 不重载提交历史。(3) `onDidChange` 零延迟响应: 新增 `pushWorkingTreeFromApi()` 在 `repository.state.onDidChange` 回调中立即从 API 读 `indexChanges`+`workingTreeChanges` 推入 Store (经 `schedulePushState` → Webview 增量渲染), 同时仍触发 300ms 防抖全量签名校验。(4) `pushStateToWebview()` 改为增量推送: `lastPushedSnapshot` 记录上次推送的 21 个字段值, 仅发送变化的字段 (`delta`), 无变化时跳过 postMessage。(5) `sameRepositoryStateSignatures` 方法移除。`npm run compile` 通过。
