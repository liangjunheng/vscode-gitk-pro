# 2026-09-06

- 已验证并提交 Gitk Webview 提交列表工具栏布局调整：刷新、同步操作与搜索统一置于提交列表列头，移除变更文件列表中重复的 push 入口。
- 首次初始化加载态的上一轮修复尝试未解决实际现象，已撤销；后续需继续依据运行时事件链定位 `_isLoading` 未清除的真实来源，不能将“仓库选择早于分支”单独视为已确认根因。
- 第二轮将 Webview `loadingProgress` 与 `stateUpdate.isLoading` 分离的方案同样未解决现象，已完整撤销。静态核对确认“正在加载历史提交列表”仅由 `GitCommitController.onCommitsLoadingChanged(true)` 发出；当前项目的同等 `git symbolic-ref`、`rev-parse`、`git log` 命令可正常结束，后续必须针对控制器运行时 generation/分支事件链取证。
- 第三轮根因定位与修复（已落地）：
  - 取证手段：在 `%TEMP%\gitk-probe` 用 vscode 桩件（拦截 `Module._load` 的 `require('vscode')`）+ 真实 `out/` 编译产物，直接实例化 `GitkViewProvider` 并捕获其 postMessage（`probe2.cjs`）。桩件要点：`Uri.parse/file` 用 `fileURLToPath/pathToFileURL` 往返、`EventEmitter` 按注册顺序同步 fire、其余 API 用 Proxy 自动桩。该探针可复现任意工作区的首次初始化消息序列。
  - 根因：`GitCommitController.selectRepositories` 在仓库选择变化时就置 `_isLoading=true` 并停在那里等分支输入，而唯一能结束等待的 `refresh()` 只由 `selectBranches()` 启动；`GitBranchesController` 仅在 `defaulted || headChanged` 时下发选择快照，命中缓存但无当前分支、或 HEAD 读不到（空仓库/无提交仓库）时永不补发 ⇒ 加载态永久停在“正在加载历史提交列表”。空仓库工作区已用探针复现（isLoading 20s 仍为 true）。
  - 修复：`gitBranchesController.ts` 新增 `pendingSelectionDelivery`（仓库选择变化时置 true，`fireSelected()` 内清除）；分支读取结束(`onBranchesLoadingChanged(false)`)仍未下发则补发一次选择快照；`selectRepositories` 在 `defaulted || !this._isLoading` 时也下发（覆盖命中缓存无当前分支的情况）。`selectedRepoTotalBranchWatcher.ts` 把 `enqueueRefresh` 提到 `await createRepositoryWatcher` 之前，消除“分支读取在途但 loading=false”的假空闲窗口，否则下游会误判已定型而闪空列表（先前的无条件补发会导致分支补全 upstream 后多一次 git log，已用该标记收敛）。
  - 验证：正常仓库单次加载周期结束且 commits=50；空仓库 6.5s 内 isLoading 转 false 且 commits=0；多根+子模块正常；手动 refresh 后加载态正常收尾；连跑三次无竞态。
- 第四轮（修正第三轮的回归，最终版）：第三轮把“选择快照补发”挂在 `onBranchesLoadingChanged(false)` 上后，用户反馈变成“无论什么时候都正在加载”。
  - 回归根因：分支加载态根本不可作为“输入已定型”的判据。`GitRepositoryOption.equals` 是严格比较（含 `hasSubmodules`/`ancestry`），子模块发现过程中仓库列表会被反复重新发布 ⇒ `SelectedRepoTotalBranchWatcher.syncRepositories` 反复重绑并 `enqueueRefresh`（无缓存分支时 force=true）⇒ `drainRefresh` 的 `needsRefresh` 循环使 `loadingCount` 长期 >0 ⇒ 分支 loading 迟迟不落 false ⇒ 补发永不发生 ⇒ 提交读取永久等待。教训：分支读取的加载态只表示“还有活在做”，不能表示“选择未定型”。
  - 最终修复（仅 `gitBranchesController.ts`，`selectedRepoTotalBranchWatcher.ts` 已完整回退）：保留 `pendingSelectionDelivery`，改用确定性判据 `hasAllSelectedSnapshots()`（经 `totalBranchWatcher.getRepositorySnapshots()` 判断所有选中仓库是否都已有分支快照；`totalBranches` 只在 HEAD 读完之后才写入，故“有快照”等价于“该仓库 HEAD 已确定”）。`selectRepositories` 用 `defaulted || hasAllSelectedSnapshots()`，`applyTotalBranches` 用 `headChanged || defaulted || (pending && hasAllSelectedSnapshots())`，`fireSelected()` 内清除 pending。
  - 验证：正常仓库单次周期结束 commits=50；空仓库 4.5s 内 isLoading 转 false 且 commits=0；多根+子模块 commits=2；空仓库+真实仓库组合正常；手动 refresh 正常收尾；真实工作区连跑两次均正常。
- 第五轮（真正的根因，已修复）：前四轮都判错了方向——现象不由控制器状态产生，而是 **webview `stateUpdate` 处理函数在渲染前抛异常**。
  - `gitkWebviewDocument.ts` 的 `stateUpdate` 分支里仍在操作 `commitPrimaryBtn` / `commitSplitGroup`；这两个 id 是提交按钮（已随 53e1911 重构迁到 Commit 面板 / `commitPanelDocument.ts`），gitk 页面模板里早已不存在 ⇒ `document.getElementById(...) === null` ⇒ 每次 stateUpdate 都在 `isCommitLoading` 赋值之后、`loading.style.display='none'` 与 `render()` 之前抛 `TypeError` ⇒ 加载覆盖层永不隐藏、提交列表永不渲染，表现为“一直正在加载历史提交列表”，且与控制器状态无关（所以前几轮改控制器毫无效果）。
  - 修复：删除这三行死引用（`commitEditorLoading` 在 gitk 页已无消费者；store 字段保留）。
  - 取证/防复发手段：`%TEMP%\gitk-probe\probe3-dom.cjs` / `probe4-panel-dom.cjs` 用真实 `renderGitkWebviewHtml` / `renderCommitPanelHtml` 生成 HTML，正则比对「模板 `id="..."`」与「脚本 `getElementById('...')`」，列出引用了但不存在的 id。修复前 gitk 页有 2 个（commitPrimaryBtn、commitSplitGroup），修复后为 0；Commit 面板为 0。
  - 教训：webview 侧「加载态不消失」先查脚本是否抛异常（null 元素引用），不要直接怀疑后端控制器；模板与脚本拆分后必须做 id 一致性校验。
