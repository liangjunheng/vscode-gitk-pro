# 2026-08-17

- 链路复核结论：当前 `SingleDiffPanel` 没有任何 `new SingleDiffPanel(...)` 实例化；`MultiDiffPanel` 只复用其 `MONACO_DIFF_OPTIONS` 与 `MONACO_DIFF_LANGUAGES`，实际运行是自身 webview。数据准备链正常（DiffReader.prepare 完成写 `{files:data,diffLoading:false}`）。为移除反复失效的 reset/generation/ready 状态机，MultiDiffPanel 已重写为完整 Store 快照协议：仅订阅 diffLoading/diffError/files，webview ready 后 Host 发布递增 revision 的 loading/error/render 全量 DiffPayload[]；webview 仅接受递增 revision，Monaco 未就绪保留最后一条 render，加载后渲染全部 Monaco DiffEditor。`tsc -p tsconfig.json` 已刷新 out，lints 通过。
- 面板协议重构（参考已验证的 CustomDiffPanel 原则）：MultiDiffPanel 改为每次 Store 变化只发送一个原子 `DiffSnapshot {revision,loading,error,diffs}`，Host 的 postMessage 用 postQueue 串行，webview 仅接受严格递增 revision；Monaco 未就绪保存最后完整 snapshot，加载后渲染；订阅补齐 diffProgress。移除拆分 loading/error/render 消息互相覆盖的风险。SingleDiffPanel 的 ready 从首段队列建立阶段延后至正式 receive 注册、队列回放完成后。注意：SingleDiffPanel 仍未被 MultiDiffPanel 实例化，只共享 Monaco 配置。`tsc -p tsconfig.json` 和 lints 通过。
- 全链路复核后修复的面板错误：CustomDiffPanel 在 `diffLoading=false` 后无条件发 complete（即使 diffs 为空），而 MultiDiffPanel 之前写成 `loading: state.diffLoading || (!error && diffs.length===0)`，把“已完成的空 DiffPayload 快照”永久重新解释成「正在读取 Diff 数据」。现 loading 仅由 Store.diffLoading 决定；完成但空的 snapshot 显式显示“没有可显示的 Diff 内容”；Monaco render 加 try/catch 显示渲染错误，不再保留旧 loading 页面。`tsc -p tsconfig.json` 与 lints 通过。SingleDiffPanel 仍未实例化，仅共享配置。
- 根因已实锤并修复：面板显示 `require is not defined` 时检查发现 `media/monaco/vs/loader.js` 根本不存在，两个面板引用 `${monacoUri}/loader.js` 都会失败，AMD require 永不建立；此前所有“正在读取/准备”均是 loader 缺失掩盖的数据链路。新增 `scripts/copy-monaco.cjs` 将 `node_modules/monaco-editor/min/vs` 完整同步到 `media/monaco/vs`；package compile/watch 前先执行同步；将 `monaco-editor@^0.52.2` 加入正式 dependencies/lockfile，确保干净安装/打包不再丢资源。确认 loader.js 现存在，`npm run compile` 成功，lints 通过。
- 用户提供的运行日志已证实修复后的数据与渲染链路完整成功：`publish #1 loading=false progress=7/7 diffs=7` → webview `receive #1` → `render #1: 7 diffs`；无 `gitk-multi-diff` 错误。若视觉上仍空白，问题已缩小为 Monaco/DOM 布局或查看的非当前面板实例，不是读取、Store、postMessage 或 Monaco 初始化。已追加 render 后 DOM 诊断日志：cards 数量、loading/list hidden 标志、首个 `.editor` 的实际宽高；`npm run compile` 与 lints 通过。
- MultiDiffPanel 持续显示读取态时增加了非侵入诊断：DiffSnapshot 新增 completed/total；Host publish 输出 `[gitk-multi-diff] publish #... loading/progress/diffs/error`；webview 回传 receive/render 阶段日志到 extension console。页面文案显示实际 `(completed/total)`。`npm run compile` 与 lints 通过。
- 用户最新日志已严格证明当前渲染链路在后续请求完成：#15 loading=true progress=7/7→#16/#17 loading=false,diffs=7→webview receive #16/#17→render cards=7, loading.hidden=true, list.hidden=false, firstEditor=1070x360。最终 UI 根因更正：不是旧 Webview，而是 CSS `#loading{display:grid}` 的 ID specificity 高于浏览器 `[hidden]{display:none}`，因此 JS 设 `loading.hidden=true` 仍保留 `min-height:100vh` 整屏占位，真实 Diff 列表仅在向下滚动后可见。已在 MultiDiffPanel 与 SingleDiffPanel 都加 `#loading[hidden]{display:none}`；`npm run compile`/lints 通过。
- 补齐两面板错误可观测性：SingleDiffPanel 与实际入口 MultiDiffPanel 均将 Monaco `require` 放入 try/catch 并注册 AMD error callback；model/editor render 包 try/catch；失败时 UI 显示 `Diff 渲染失败: <message>`，并经 webview `{type:'error'}` 回传 extension console（`[gitk-single-diff]` / `[gitk-multi-diff]`）。tsc/lints 通过。
- `SingleDiffPanel` 已迁移 CustomDiffPanel 的有序数据协议：HostMessage 改为 reset/loading/progress/error/render 并均携带 generation；show/ready 经过 await reset 后才设 webviewGeneration 并补发 Store 快照；扩展转发须同时匹配 Store diffGeneration/requestGeneration/webviewGeneration；webview reset 更新本地 generation，所有非 reset 消息按 generation 丢弃；Monaco 未就绪仅保存当前 generation 的 pending render。cancelPending 同时推进 Store diffGeneration、清空 loading/error/progress，从而让旧 DiffReader 结果失效。保持一次仅渲染选中文件和原生主题色。tsc/lints 通过。
- `DiffReader.prepare` 批次从「首批 1 个 + 后续 8 个」改为统一每次 128 个文件（batchSize=128，去掉首 1 特殊处理）。tsc/lints 通过。
- 完成旧面板替换：新增 `multiDiffPanel.ts`，在一个 WebviewPanel 中按 `DiffPayload[]` 创建多个 Monaco `DiffEditor`（每文件一张卡片、双栏、词级比较）；`singleDiffPanel.ts` 保留单文件 Webview+Monaco Diff，并导出共享 `MONACO_DIFF_OPTIONS`（VS Code 原生 diff 参数）与 `MONACO_DIFF_LANGUAGES`，MultiDiffPanel 注入并复用它们。MultiDiffPanel 订阅 Store，以 generation 过滤 `reset/loading/error/render` 消息；`gitkViewProvider.ts` 已由 `CustomDiffPanel` 改为 `MultiDiffPanel` 并纳入 subscriptions；删除无引用的 `customDiffPanel.ts`，DiffReader 注释同步。全项目 tsc/lints 通过。
- 修复 MultiDiffPanel 仍停留「正在准备 Diff...」的 webview 初始化竞态：首段内联脚本原来在仅注册 queue listener 后即 post ready，Host 收到后会发 reset/snapshot，但第二段脚本尚未注册 receive handler；虽 queue 试图缓冲，但 ready 与 postMessage 时序仍造成协议不确定。现新增 webviewInitialized 门控，并把 ready 移到第二段脚本完成 `addEventListener('message')`、回放 gitkQueue、将 queue.push 设 no-op 后才发送；Host 仅在这之后执行 CustomDiffPanel 式 reset→snapshot。已 `tsc -p tsconfig.json` 刷新 out，lints 通过。
- MultiDiffPanel 已回归并严格复用 CustomDiffPanel 已验证的协议（前一版移除 reset 是错误方向）：Store 订阅拆为 progress/loading/error/files；synchronize 用 reset 入队后才开放 webviewGeneration，并依次补转发 progress/loading/error/complete 完整快照；加 synchronization 序号防 show/ready 并发；webview 只允许 reset 建立 generation，之后严格 `message.generation === generation`，complete 才调用 Monaco 多文件 render。后续发现并补齐 CustomDiffPanel 的决定性语义：diffLoading 从 true→false 时，forwardLoading 发送 `loading:false` 并立即 forwardCompleteSnapshot；cancelPending 发送 reset 前同步设置 webviewGeneration，避免完成快照被 canForward 拒绝。最后一次尝试移除 ready 门控时遗漏 synchronize/canForward 中的两个 `this.ready` 引用，造成源代码与构建产物不一致（必然导致旧 out 继续运行）；现已彻底移除残留，执行 `tsc -p tsconfig.json` 成功刷新 out，lints 通过。
- 滚动行为调整：`MONACO_DIFF_OPTIONS` 关闭 Monaco 内部 horizontal/vertical scrollbar 与 mouse-wheel，关闭 scrollBeyondLastLine；MultiDiffPanel 页面 `html/body` 成为全局 overflow 容器，每个 editor 根据 original/modified content height 与 scroll width 动态设置 host 尺寸并调用 `editor.layout`。SingleDiffPanel 容器补 VS Code editor 字体/字号和 overflow。`npm run compile`/lints 通过。
- 修复主题与双栏宽度：实际运行入口是 MultiDiffPanel，之前未应用 SingleDiffPanel 的 `gitk-vscode-diff` CSS 变量主题，故 Monaco 默认浅色；现 MultiDiffPanel 也定义/应用相同 VS Code 主题映射。此前 fit() 以 `left/right scrollWidth` 撑开 host，破坏双栏均分；现列表/卡片/editor 用 box-sizing+width:100%+min-width:0，禁用页面横溢，fit 使用 host.clientWidth / 视口宽度进行 layout，Monaco 在父容器内均分左右栏。`npm run compile`/lints 通过。
- 用户要求仅保留背景和字体主题效果：MultiDiffPanel 主题覆盖已收窄为 `editor.background` 与 `editor.foreground` 两项 VS Code CSS 变量，并以 updateOptions 应用 `--vscode-editor-font-family/font-size`；移除 diff、gutter、行号、选区、滚动条、minimap 等所有额外颜色映射，使其恢复 Monaco 基础 `vs`/`vs-dark` 的原始颜色。SingleDiffPanel 原本仅继承容器背景/前景/字体，Diff 色未覆盖。`npm run compile`/lints 通过。
