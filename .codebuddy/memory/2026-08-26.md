- 已撤回上一轮对仓库扫描提前发布、控制器默认选择和直接状态推送的改动；当前只保留用户要求的新增回调步骤，未改变仓库扫描、默认选择及其他下游逻辑。恢复后 `npx tsc -p ./ --noEmit` 与相关 lint 通过（2026-08-26）。

- 对 `SelectedRepoTotalBranchWatcher` 与 `GitBranchesController` 做只读审查（未修改）：编译通过，但存在阻断级闭环问题和性能风险。(1) 新 watcher 的 `getGitBranches()` 只返回已提交 `snapshot`，而 `GitBranchesController.applyTotalBranches()` 要求 `this.repositories` 已先更新；选中仓库事件中 watcher 的 `syncRepositories()` 与 controller 的 `selectRepositories()` 都是异步回调，且 watcher 订阅先注册，snapshot 可能先到，此时 controller 丢弃分支快照，之后没有第二次发布，导致新选中仓库分支列表为空。(2) `GitBranchesController.refresh()` 已改成不再读取分支，但 `applyCurrentBranch`/`withCurrentHead`/`getHeadBranchByRepo` 的旧逻辑仍留存，属于死代码；force 刷新中对每个 repository 重复调用 `refreshSelectedRepositories()`，而 watcher 方法本身会遍历所有 slot，导致仓库数为 R 时变成 O(R²) 次入队。(3) watcher 用 `require('child_process').execFile`，破坏现有静态 import/promisify 风格；且 `getGitBranches()` 内部会重新构造 `GitRepositoryOption`，watcher 再映射 repoOption，增加无谓对象分配。(4) refs watcher 使用三个递归/文件监听器，正常一次 Git 操作可能触发多次 refresh，但 slot 会再次执行 Git 查询；需观察是否需要按仓库 pending 合并（当前 running 期间合并，但读取完成前事件会再读一次，语义正确但可能有额外 I/O）。(5) 当前 workspace git 实例/文件重命名存在大范围非目标删除与新增，需先确认这些改动是否为用户本轮预期，否则不可安全交付。
- 最终交付复核：`npm run compile` 与 `git diff --check` 通过；但 `git status` 仍包含分支流程之外的大范围变更：commit editor 文件删除/迁移、HEAD watcher 删除/重命名、`extension.ts`/`gitActions.ts`/`uncommittedFilesWatcher.ts`/Provider 改动，以及新增 `selectedRepoTotalBranchWatcher.ts`。不能在未确认范围前将整个工作区作为本次功能交付。- 已确认仓库职责边界：`GitRepoController` 缓存并对外提供完整 `totalRepoList`，`RepoSubmoduleWatcher` 仅负责扫描与维护仓库拓扑；无需修改。
- 修复「初始化未默认选中当前仓库的当前分支」根因：`GitRepoController.initialize()` 有「默认选第一个仓库」逻辑，但 `GitBranchesController` 缺少对称的「默认选当前分支」逻辑——`selectRepositories()` 只 prune 从不填默认，导致 `selectedBranches` 恒空、`GitCommitController.readCommits()` 因 refs 为空返回 `[]`，提交列表加载不出来。已在 `GitBranchesController` 加 `hasUserSelection` 标记（用户手动 `selectBranches()` 后置 true）与 `ensureDefaultSelection()`：未手动选过时为已选仓库中尚无勾选的仓库自动选中 `kind==='current'` 分支。因当前分支 HEAD 是异步读入，默认回填在两处触发：`selectRepositories()`（HEAD 已缓存的仓库）与 `applyTotalBranches()`（HEAD 此刻才到达）；两处按需 `fireSelected()`。`npm run compile` 与 diagnostics 通过。
- 分支标签图标改多图标 + origin/HEAD 特例（`src/webview/gitkViewProvider.ts`, 2026-08-26 续）：`origin/HEAD` 无图标的根因——`for-each-ref` 里它 `%(refname:short)` 被 git 简化为 `origin`(不是 `origin/HEAD`), 故 branches 里 label='origin', 而 `git log %D` 文本是 `origin/HEAD`, `label===ref` 精确匹配必败。`tag: v1.0` 无图标根因——tag 不在 branches(getGitBranches 只读 refs/heads/refs/remotes 不含 refs/tags)。解决: `refIconFor` 改名 `refIconsFor` 返回**图标数组**(支持一标签多图标); 远程 HEAD 指针按 git 规范 `/HEAD$` 正则识别(模板字符串里写 `/\\/HEAD$/` 输出为 `/\/HEAD$/`)返回 `['cloud','target']`(既远程又 HEAD 指针); tag 匹配不到返回 [] 维持纯文字。渲染端遍历数组, chip `.col-message-head-ref` 的 `gap:3px` 自动分隔多图标。用户决策: origin/HEAD=靶子+云双图标(靶子在第一位, 返回 ['target','cloud']); tag=保持无图标。后续: 当前分支(kind==='current')既是当前又是本地, 返回 ['target', <本地图标>](靶子在前)。本地分支图标最终定为 `device-desktop`(显示器), 替换原 `git-branch`; 故 current=['target','device-desktop'], local=['device-desktop'], remote=['cloud'], remote HEAD=['target','cloud'], tag=[]。
- 描述紧贴每行泳道 + 本地分支图标（`src/webview/gitkViewProvider.ts`, 2026-08-26 续）：用户确认要「每行描述各自紧贴本行泳道」的锯齿式对齐(gitk 传统风格)。做法: 新增 `rowGraphW(c)=(rowMaxSwimlane(c)+1)*LANE_W+10` 按行算 SVG 宽度; `buildCommitRowHTML`/`setupRow`/`applyCommitSelection` 三处 SVG 的 width+viewBox 都改用每行 rowW(同值 1:1 不缩放); `.col-main` 第一列由固定 `var(--graph-lane-width)` 改为 `auto`, 描述紧随本行 SVG 右边缘。泳道竖线坐标绝对仍跨行对齐。`refIconFor` 增加 `kind==='local'` → `git-branch` 图标(codicon \ea68); 现当前=target/远程=cloud/本地=git-branch。另修正: 静态 HTML 表头(index.html 内联)漏改致空列表(commits.length===0 提前 return 不重建 header)时显示旧 5 格错乱, 已把静态表头与动态 headerCell 统一为「描述/作者/Commit ID/时间」4 格(表头首格文案「描述」, key=main)。教训: 空列表分支跳过动态表头重建, 静态 HTML 表头必须与动态结构同步。
- 按用户要求修改初始化时序：根仓库层识别后 `RepoSubmoduleWatcher` 立即 `applyTotal()`，`GitRepoController` 首次收到仓库列表立即选择首项并触发现有 `onSelectedRepoListChanged`；Provider 在仓库选择回调中新增 `pushStateToWebview()`。当前分支由既有 HEAD/`ensureDefaultSelection()` 产生后触发现有 `onSelectedBranchesChanged`，Provider 在该回调完成显示快照后新增 `pushStateToWebview()`。未改变提交加载及其他下游逻辑；lint、`npx tsc -p ./ --noEmit` 通过（2026-08-26）。
- 分支标签加图标（历史，已被上一条合并列方案覆盖）：SVG 图区每行的分支椭圆标签（`drawSvg` 渲染 `c.refs`）此前只有纯文字。需求：当前分支标签文字前加靶子(target)图标，远程分支标签文字前加云(cloud)图标。根因约束：`c.refs` 是 `git log %D` 纯文本，不能靠文本猜 kind，必须复用带 `kind` 的 `branches` 数据（label 与 `%(refname:short)` 同源，可按 `repoOption.path`+`label` 匹配）。实现：新增 `refIconFor(ref,repoPath)`（current→target, remote→cloud）、`refIconChar(icon)`（codicon 字形实体 cloud=&#xebaa; target=&#xebf8;）；`refLabelItem(ref,icon)` 有图标时预留 14px；`calcGraphCanvasW`/`drawSvg` 传图标，图标画在文字前、文字右移 14px；新增 `.graph-ref-icon { font-family: codicon; }` CSS。SVG 是内联 DOM（`svg.innerHTML`）与 webview 同文档共享已加载的 codicon 字体。`tsc --noEmit` 通过、无 lint。
- 修复默认选择等待全部子模块扫描的根因：`RepoSubmoduleWatcher.scan()` 原本只在递归扫描结束后执行 `applyTotal()`，`GitRepoController.initialize()` 又等待整个 `startQueuedScan()` 返回才默认选择，因此初始化必须等所有仓库完成。现改为全量扫描拿到工作区根仓库后立即 `applyTotal()` 发布；`GitRepoController` 的首次列表事件直接选择首个根仓库，后续递归完成只更新总列表并保留路径选择；分支控制器随后按异步到达的 HEAD 自动选当前分支。`npx tsc -p ./ --noEmit` 与相关 lint 通过（2026-08-26）。
- 为初始化默认选择关键步骤补充注释，明确根仓库发布不阻塞子模块扫描、首次列表事件负责默认选仓库，以及仓库/分支选择回调必须立即推送 UI，防止后续回退造成问题反复；lint 与 `npx tsc -p ./ --noEmit` 通过（2026-08-26）。
