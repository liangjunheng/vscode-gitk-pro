- 用户确认分支架构边界：`BranchController` 仍需保留总分支列表读取/聚合接口；`SelectedRepoTotalBranchWatcher` 只负责监听选中仓库 refs、按仓库维护原始 `totalBranchList` 快照并发布变化，BranchController 消费快照后继续对外提供 `getBranches()`、selected branch 和 HEAD 合并。
- 对 `SelectedRepoTotalBranchWatcher` 与 `GitBranchesController` 做只读审查（未修改）：编译通过，但存在阻断级闭环问题和性能风险。(1) 新 watcher 的 `getGitBranches()` 只返回已提交 `snapshot`，而 `GitBranchesController.applyTotalBranches()` 要求 `this.repositories` 已先更新；选中仓库事件中 watcher 的 `syncRepositories()` 与 controller 的 `selectRepositories()` 都是异步回调，且 watcher 订阅先注册，snapshot 可能先到，此时 controller 丢弃分支快照，之后没有第二次发布，导致新选中仓库分支列表为空。(2) `GitBranchesController.refresh()` 已改成不再读取分支，但 `applyCurrentBranch`/`withCurrentHead`/`getHeadBranchByRepo` 的旧逻辑仍留存，属于死代码；force 刷新中对每个 repository 重复调用 `refreshSelectedRepositories()`，而 watcher 方法本身会遍历所有 slot，导致仓库数为 R 时变成 O(R²) 次入队。(3) watcher 用 `require('child_process').execFile`，破坏现有静态 import/promisify 风格；且 `getGitBranches()` 内部会重新构造 `GitRepositoryOption`，watcher 再映射 repoOption，增加无谓对象分配。(4) refs watcher 使用三个递归/文件监听器，正常一次 Git 操作可能触发多次 refresh，但 slot 会再次执行 Git 查询；需观察是否需要按仓库 pending 合并（当前 running 期间合并，但读取完成前事件会再读一次，语义正确但可能有额外 I/O）。(5) 当前 workspace git 实例/文件重命名存在大范围非目标删除与新增，需先确认这些改动是否为用户本轮预期，否则不可安全交付。
- 最终交付复核：`npm run compile` 与 `git diff --check` 通过；但 `git status` 仍包含分支流程之外的大范围变更：commit editor 文件删除/迁移、HEAD watcher 删除/重命名、`extension.ts`/`gitActions.ts`/`uncommittedFilesWatcher.ts`/Provider 改动，以及新增 `selectedRepoTotalBranchWatcher.ts`。不能在未确认范围前将整个工作区作为本次功能交付。- 已确认仓库职责边界：`GitRepoController` 缓存并对外提供完整 `totalRepoList`，`RepoSubmoduleWatcher` 仅负责扫描与维护仓库拓扑；无需修改。
- 修复「初始化未默认选中当前仓库的当前分支」根因：`GitRepoController.initialize()` 有「默认选第一个仓库」逻辑，但 `GitBranchesController` 缺少对称的「默认选当前分支」逻辑——`selectRepositories()` 只 prune 从不填默认，导致 `selectedBranches` 恒空、`GitCommitController.readCommits()` 因 refs 为空返回 `[]`，提交列表加载不出来。已在 `GitBranchesController` 加 `hasUserSelection` 标记（用户手动 `selectBranches()` 后置 true）与 `ensureDefaultSelection()`：未手动选过时为已选仓库中尚无勾选的仓库自动选中 `kind==='current'` 分支。因当前分支 HEAD 是异步读入，默认回填在两处触发：`selectRepositories()`（HEAD 已缓存的仓库）与 `applyTotalBranches()`（HEAD 此刻才到达）；两处按需 `fireSelected()`。`npm run compile` 与 diagnostics 通过。
- 分支标签加图标（`src/webview/gitkViewProvider.ts`）：SVG 图区每行的分支椭圆标签（`drawSvg` 渲染 `c.refs`）此前只有纯文字。需求：当前分支标签文字前加靶子(target)图标，远程分支标签文字前加云(cloud)图标。根因约束：`c.refs` 是 `git log %D` 纯文本，不能靠文本猜 kind，必须复用带 `kind` 的 `branches` 数据（label 与 `%(refname:short)` 同源，可按 `repoOption.path`+`label` 匹配）。实现：新增 `refIconFor(ref,repoPath)`（current→target, remote→cloud）、`refIconChar(icon)`（codicon 字形实体 cloud=&#xebaa; target=&#xebf8;）；`refLabelItem(ref,icon)` 有图标时预留 14px；`calcGraphCanvasW`/`drawSvg` 传图标，图标画在文字前、文字右移 14px；新增 `.graph-ref-icon { font-family: codicon; }` CSS。SVG 是内联 DOM（`svg.innerHTML`）与 webview 同文档共享已加载的 codicon 字体。`tsc --noEmit` 通过、无 lint。