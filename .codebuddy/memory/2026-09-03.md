
- CommitPanel 提交成功、commit 列表已出现新记录，但 staged changes 仍不消失的根因（split-brain HEAD 身份）：CommitPanel 的 staged 是 `store.commitRepositories[].staged` 投影，其数据由 `UncommittedFilesWatcher` 按 `branch.hash` 缓存(`getCachedUncommittedFilesByHeadBranch`)。该 watcher 的 HEAD 身份只经 `RepoHeadBranchWatcher.createHeadWatcher()` 的 `.git/HEAD` 文件监听器(eachRepoHeadBranchWatcher.ts:134-136) 换代。而普通 `git commit` 只改写 `refs/heads/<branch>`、`logs/HEAD`、`index`，不触碰 `.git/HEAD`(符号引用)，故 HEAD 文件监听器不触发→`applyCurrentHeadBranch` 不运行→槽位仍停在提交前 hash 并保留旧 staged 缓存。commit 列表则由另一套 refs 监听器(`selectedRepoTotalBranchWatcher` 监听 `refs/heads/**`)推进，于是列表更新而 staged 不更新。`forceRefreshCurrentSelection` 也无效：refs watcher 已把 `commitController.branches` 推到新 hash 后，`refreshUncommittedFilesByHeadBranch` 的 `slot.branch?.hash !== branch.hash` 守卫抛错被吞。根治：新增 `RepoHeadBranchWatcher.refreshHeadBranch(repositoryPath)` 强制重读 HEAD；`runCommit` 收集 `committedRepositoryPaths`，提交成功后 `await Promise.all(...refreshHeadBranch)` 再 `forceRefreshCurrentSelection`，让新 hash 经事件下发使 watcher 换代并整表刷新，清除过期 staged。`tsc --noEmit`、`git diff --check`、lint 通过。

- CommitPanel 的 Staged Changes 标题栏已移除树状/平铺显示方式按钮；一并删除其 DOM、点击消息 `toggleDisplayMode`、卡片刷新时的图标更新和专用 CSS，避免访问已删除节点。文件列表仍沿用现有全局 `displayMode` 渲染状态；`tsc --noEmit`、`git diff --check`、lint 通过。

- 仓库图标与状态栏 `$(repo)` 不一致的根因是 Webview 使用项目内旧版 `media/codicons/codicon.ttf`，而状态栏由当前 VS Code 内置字体 `resources/app/out/media/codicon.ttf` 渲染；两边 `.codicon-repo:before` 均为 `\ea62`，但 ttf 哈希不同。已用当前 VS Code 的字体覆盖项目字体并以 SHA-256 `A832273CE3A515050017A0FFB36B1B8CBD9C9CFA11B025CDD9DFC0D456940225` 验证一致。父仓库/独立仓库均为 `repo`，仅不包含子仓库的子仓库为 `archive`；`tsc --noEmit` 与 `git diff --check` 通过。

- Diff 面板偶现“加载中但无卡片/无空态”而 Commit 面板有变更的真实根因：`GitkViewProvider.selectWorkingTreeChanges()` 在任何前置校验之前就无条件 `++this.commitFilesGeneration`(原 504 行)，但直到 507 行才判断是否处于工作区上下文。当一次瞬时调用(如 `refreshPendingGitlinkDiff()`→`selectWorkingTreeChanges(undefined,false)`, 或提交切换过程中 `selectedCommit` 短暂为 null)在 507 行 return 时，它已把 generation 推进，从而把一个正在进行且健康的工作区读取请求无谓作废，而它自己不产生任何终态；被作废请求回来后在 577-579 行因 `generation !== commitFilesGeneration` return，`diffLoading` 永远停在 true，面板既无卡片也无空态。CommitPanel 用 watcher 写入的 `store.commitRepositories`(542-559 行) 不受此代次影响故仍显示文件。修复：把 `const generation = ++this.commitFilesGeneration;` 移到 `if (!selectedBranch || !isWorkingTreeHash(selectedHash)) return;` 之后，只有确认要真正执行 Diff 工作时才领取新代次。`tsc --noEmit`、`git diff --check`、lint 通过。
（`src/webview/commitPanel.ts`）：`refs.commitBtn.textContent=card.amend?'amend':'commit'`（808 行），amend 态由大写 `Amend` 改为小写 `amend`。
  - 约定（本文件既成体系）：**按钮一律小写** —— `commit`（633 行模板初值）、`push`（636 行）；**复选框标签保持首字母大写** —— `Amend`（631 行）、`Pull`（637 行）。所以只改了按钮、未动 631 行的复选框标签。
  - 坑：按钮文案在 `updateCard()` 里被覆盖（808 行），只改模板初值会在刷新后回退；同理上一轮改小写时也必须改这一行。
  - `npm run compile`、lint 通过，产物确认仅剩复选框的 `Amend` 为大写（605 行）。

- 已修复暂存、取消暂存、还原操作的进度条提前结束问题（`src/webview/gitkViewProvider.ts`）。`processWorkingTreeActionQueue()` 现在在同一 `withProgress` 中依次执行 Git 写入、`endWorkingTreeMutation()` 的权威状态重读，并显示“正在同步 Git 工作区状态...”和“正在刷新变更文件和差异视图...”文案；仅在当前虚拟提交的 Store/CommitPanel/Diff 投影完成后关闭进度。为保持 watcher 是唯一状态读取者，主动操作期间先暂存 watcher 回传快照，再由事务统一 `await applyWorkingTreeChanges()` 消费，避免重复异步刷新。`npm run compile` 与 `gitkViewProvider.ts` lint 通过。

- 拉取后子模块进度改为显示当前模块名：`runGitSync()` 从拉取前后 HEAD 的 gitlink diff 保留非删除项 `submodulePaths`，`GitActionRunner` 传给 `updateGitSubmodules()`；后者按这些路径逐个运行 `git submodule update --init --recursive -- <path>`，在命令前后报告“正在/已完成（i/n）：<path>”。根因是旧实现只用布尔值丢弃了已解析的 gitlink 路径，并把全部递归更新作为单一命令执行，无法报告实时目标；`npm run compile` 和 `git diff --check` 通过。

- 将“上一个差异”快捷键由 `F8` 改为 `Shift+F7`：`package.json` keybindings 中 `vscode-gitk.multiDiff.previousChange` 的 `key` 从 `f8` 改为 `shift+f7`（`when` 不变）；`README.md` 快捷键表与导航说明同步更新。`F7`（下一个差异）保持不变。

- 统一三处文件列表在树状模式下的路径可见性：`gitkViewProvider.ts` 中普通已提交记录和虚拟提交（Unstaged/Staged）的树状文件行，均改为显示 `文件名 + folder/`；`commitPanel.ts` 的共享 `fileRowHtml` 移除平铺模式限定，因此 Unstaged、Staged、Committed 的树状文件行也显示相同路径。保留既有文件夹分组行与折叠交互。相关文件无 lint 诊断，`npm run compile` 通过。

- 已提交 `d0c4392`（`统一变更文件路径展示`），同时包含提交后显式刷新 HEAD 与未提交文件缓存的既有根因修复。

- 扩展版本升级至 `1.2.6`：`package.json` 从 `1.2.5` 递增；并将滞后的 `package-lock.json` 根包和工作区包版本从 `1.2.4` 同步至 `1.2.6`。
