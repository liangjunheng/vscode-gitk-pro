- 修复切换仓库后的分支刷新异常：删除首次初始化中冗余的后台仓库/分支刷新方法及调用；切换仓库先立即读取并显示新仓库当前分支，再独立异步刷新该仓库完整分支列表，合并结果时保留当前已选择分支；`npm run compile` 与 lint 均通过。
- 按最新需求恢复仓库切换后的独立异步刷新：显示新仓库当前分支后，同时启动当前仓库完整分支刷新和全量仓库刷新；仓库刷新通过阶段回调增量更新并保留当前选中仓库，分支刷新保留已选分支；首次初始化不启动这两项冗余刷新。
- 初始化流程更新：当前仓库和当前分支确定后，立即独立启动 `refreshBranchesAfterRepositorySelection` 与 `refreshRepositoriesAfterRepositorySelection`，两者均不覆盖当前选中项，并保持仓库/分支弹窗 loading 期间的增量数据；编译和 lint 通过。
- 切换仓库流程按最新图确认：仅按选中仓库异步刷新分支，不再重新扫描仓库列表（移除 switch 中的 `refreshRepositoriesAfterRepositorySelection`）；初始化仍保留独立仓库与分支刷新；编译与 lint 通过。
- 复盘切仓库分支列表未即时清空的根因（未改代码，仅出方案）：`beginCommitReload` 不清 `branches/selectedBranches`，而 `selectRepositories` 先同步推 `branchesLoading:true` 再 await 两个 git 调用，窗口期内 Webview 仍渲染旧仓库分支；`refreshSelectors` 内那段带"清空旧仓库分支候选项"注释的代码受 `repositoryOptionsChanged` 保护，而切仓库走 `refresh(false, true)`（`reloadSelectors=false`）使其恒为 false，对切仓库路径不可达；`getCurrentGitBranch` 无 signal 参数不可中止；`publishCurrentBranchLoading` 的 `!branchName || !headHash` early return 被外层 `.catch` 吞掉且不收尾，会残留旧分支。
- 拟定方案（五步，待确认后实施）：①`selectRepositories` 同步段清空分支三元组并新增 `branchesReset` 消息让 Webview 同时清 `branches/selectedBranches/initialBranchDisplay`（必须联动，否则 `setBranchLoading` 的 `initialBranchDisplay` fallback 会显示更早仓库的分支名）；②给 `getCurrentGitBranch` 补 signal，`selectRepositories` 建专属 AbortController 贯穿快路径；③`publishCurrentBranchLoading` 失败分支显式收尾并交由 `refreshBranchesAfterRepositorySelection` 兜底；④`refreshSelectors` 那段保留但抽成与快路径共用的 helper 并澄清 `repositoryOptionsChanged` 语义；⑤同步修正 `repository-switching-sequence.md` 中与 745 行短路逻辑矛盾的第 40-48 步。
- 已实施切仓库分支即时清空 + detached HEAD 支持（`gitLogProvider.ts` + `gitkViewProvider.ts`，`npm run compile` 通过）：
  - `readBranchRefsFromCli` 增加并行 `rev-parse HEAD` 返回 `detachedHead`；新增导出 `buildDetachedHeadBranch(hash)` 造 `{name: hash, label: '(HEAD detached at xxxxxxxx)', kind: 'current'}`；`getGitBranches` 在无 `currentBranch` 时用它产出 current 项。裸 hash 是合法 ref，可直接传 `git log`。
- 修复 `GitBranchesController.selectRepositories` 切仓库时旧 selectedBranches 闪现：替换 repositories 后，在首次 `onBranchesMapChanged` 前按 repository path 剔除已移出仓库的 selected 分支并同步 fire selection 事件；异步 current 默认选择随后才追加。这样 Provider 不会再渲染新分支列表配旧仓库勾选。同步更新分支控制器流程文档；`npm run compile`、`git diff --check` 和代码/文档诊断均通过。
  - `getCurrentGitBranch` 补 `signal` 参数，并在 catch 中重抛 AbortError，否则中止会被误判为 detached HEAD。
  - 新增 `GitkViewProvider.readCurrentBranchOption(rootUri, signal)` 统一当前项读取，判据从 `!branchName || !headHash` 收窄为仅 `!headHash`（只有空仓库无 HEAD 才放弃）。三处调用点（`publishInitialRepositorySelection` / `refreshSelectors` / `publishCurrentBranchLoading`）已全部改为复用它。
  - `selectRepositories` 在任何 await 之前同步清空 `branches/selectedBranches/hasBranchSelection` 并发 `branchesReset`；Webview 侧该消息还需清 `initialBranchDisplay` 与 `branchDropdown.selected/appliedSelection/pendingSelection/openSelection`（后两个不清会让 `renderBranchOptions` 的 `keepPendingSelection` 保留旧仓库草稿选择）。
  - 新增 `selectRepositoryAbortController`（独立于 `refreshAbortController`，否则紧随的 `refresh` 会 abort 自己的前任而连坐中止快路径），已接入 `cancelActiveRequests`。
  - **重要竞态修复**：`refreshBranchesAfterRepositorySelection` 原先捕获 `refreshGeneration` 做门禁，但它由快路径 fire-and-forget 发出，紧随的 `refresh` 会 `++refreshGeneration`，导致全量分支结果被丢弃。旧代码因分支列表残留旧仓库选项而看不出问题；清空后会卡在只剩一个当前分支。已改用独立 `branchesRefreshGeneration` + `selectedRepositoryPath` 双门禁。
  - `repositoryOptionsChanged` 注释澄清：仅指仓库下拉选项集合变化（如新发现子模块），不含用户主动切仓库；切仓库走 `refresh(false, ...)` 使其恒为 false。
- **`npm run lint` 在本仓库恒失败**（与改动无关）：ESLint 已是 v10.8.1，要求 `eslint.config.js` flat config，而项目里既无 flat config 也无 `.eslintrc*`。验证只能依赖 `npm run compile`。此前记忆里"lint 通过"的说法已不成立。
- detached HEAD 标签改为直接显示 8 位短 hash（`buildDetachedHeadBranch` 去掉 `(HEAD detached at ...)` 包装）。
- 修复「当前分支已取到但要等全量分支列表才显示」：根因是 `schedulePushState` 用 `queueMicrotask` 合并推送，`publishCurrentBranchLoading` 里 `store.batch` 之后紧跟的 `refreshBranchesAfterRepositorySelection` 同步头部会立刻 `store.setState({branchesLoading:true})`，于是 microtask 真正 push 时快照已带 loading，「当前分支 + 非 loading」那一帧从未到达 Webview；而 `setBranchLoading` 会给主标签挂 spinner，视觉上就像一直在等。修法两处：①`store.batch` 内一并置 `branchesLoading:false` 并紧跟同步 `pushStateToWebview()` 强推一帧；②`setBranchLoading` 不再给主标签挂 spinner（与仓库选择器"加载态仅在下拉列表展示"的既有设计对齐），加载态改为在分支下拉列表顶部插入 `.dropdown-progress` 条，非 loading 时移除；`initialSelection` 分支的 spinner 也一并去掉。
- 关键机制备忘：`store.setState/batch` 触发的推送是 `queueMicrotask` 延迟合并的，同一同步块内后续的 setState 会覆盖前面的中间态。需要某个中间态确实到达 Webview 时，必须显式调 `pushStateToWebview()` 同步推。
- `renderBranchOptions` 主体不写 label，label 由 `setBranchLoading` 或事件回调里的 `updateSelectionUi` 决定；`canUpdateInPlace` 按 `input[type=checkbox]` 计数判定，插入 div 进度条不影响它。
- 按用户后续要求回调：分支弹窗 loading 时显示栏的分支名也要保持 loading 态，故 `setBranchLoading` 恢复给主标签挂 spinner（分支名立即显示的修复保留，即 `store.batch` 内置 `branchesLoading:false` + 同步 `pushStateToWebview()`）。配套在 `renderSelectors` 的非 loading 分支清 spinner：仅当 `selectedBranches.length === 1` 时写回单个分支名，多选场景的汇总文案仍由 `updateSelectionUi`/`selectedLabel` 负责，不能覆盖。
- **修复回归「刷新结束后分支列表只剩当前分支」**：`refreshSelectors` 入口就把 `this.branches` 拍成局部快照（`let branches = this.branches`），切仓库时 `reloadRepositories=false` 使 1141 行重算分支的分支不进，该快照原样返回；而 `refreshInternal` 无条件 `this.branches = preparedSelectors.branches`，于是后台 `refreshBranchesAfterRepositorySelection` 先落地的全量分支被过期快照压掉，只剩快路径写入的单个 current。改动前因不清空 `branches`，同一覆盖路径表现为「显示旧仓库分支列表」，是同一 bug 的两种表现。修法：`refreshSelectors` 返回值新增 `branchesReloaded: boolean`（= `repositoryOptionsChanged`），未重算时 `branches` 改取 `this.branches` 当前值，且 `refreshInternal` 三处回写（751 短路分支、rootUris 为空分支、815-833 完整加载分支）统一按该标记决定是否写回。原 751 行就地重算的 `repositoryOptionsChanged` 已删除，改用统一标记。
- 教训：`refreshSelectors` 这类「入口拍快照 → await IO → 返回快照」的函数，凡是有并行后台任务会改同一状态的字段，都不能无条件回写；需显式标记本轮是否真的重算过该字段。
- **收尾重构（最终形态，已取代上面的 `branchesReloaded` 标记方案）**：确立「分支写入单一归口」原则 —— `branches` 只由 `publishCurrentBranchLoading`（快路径）、`refreshBranchesAfterRepositorySelection`（后台全量）、`refreshSelectors` 内仓库选项变化分支这三处写入，`refreshInternal` 一律不碰。具体删除：
  - `refreshSelectors` 返回类型去掉 `branches` 与 `branchesReloaded` 两个字段，局部变量 `let branches` 也删除（改为直接写 `this.branches`）；末尾 `currentBranch` 改读 `this.branches` 实时值。
  - `refreshInternal` 三处 `this.branches = preparedSelectors.branches` 全部删除（含 751 短路分支里那段 virtualCommits 保留逻辑 —— `this.branches` 本就含 virtualCommits，重新拼是多余的）。
  - 843 行 `loadVirtualCommits` 的入参从 `preparedSelectors.branches` 改为 `this.branches`。
  - `refreshSelectors` 内仓库选项变化分支里重复实现的「后台全量分支 + 合并保留已选」整段（含自己的 `getGitBranches` promise、catch、finally 与 `branchesLoading` 收尾）删除，改为直接 `void this.refreshBranchesAfterRepositorySelection(repositoryPath)`；顺带消除了该处仍用会被超车的 `refreshGeneration` 做门禁的隐患。
  - `hasSameSelection` 只依赖 `selectedRepositoryPaths` / `selectedBranches`，无需改动。
- **同一 bug 的第三种形态及最终修复（点刷新按钮后分支塌成一条）**：上条重构只替换了 `refreshSelectors` 仓库选项变化分支里「后台补全」那半段，却留下了前半段把 `this.branches` 砍成「已选 + 当前分支」的赋值（单选当前分支时去重后只剩一条）。原代码里砍短是安全的，因为紧接的 `fullBranchesPromise.then` 共享同一 signal 会立刻补回；替换成独立的 `refreshBranchesAfterRepositorySelection`（且当时未传 signal）后，砍短与补全之间的窗口暴露出来。触发时机正是子模块扫描完成使 `repositoryOptionsChanged` 翻 true 的那一刻，故表现为「仓库刷新结束后分支塌成一条」。最终修法：删掉整段清空重建，只留 `void this.refreshBranchesAfterRepositorySelection(selectedRepositoryPaths[0], signal)`；理由是该场景下当前仓库并未改变，分支根本不需要清空，`getGitBranches` 返回的就是完整列表。同时给该方法补 `signal` 参数并纳入 `isCurrent()` 门禁。
- **根本教训（三次返工的共同错误）**：每次都在修「中间态显示成什么」，而没有质疑「这个中间态是否必要」。凡是「先砍短/清空 → await IO → 再补全」的写法，中间态存在多久就会闪多久；若该场景下数据本无需清空（如同仓库内的分支），正确做法是直接等完整结果整体替换。切仓库快路径的单条当前分支是必要中间态（旧仓库分支必须立即清除），予以保留。
- **第四种形态根因（切仓库后偶现只剩当前分支）—— 已定位但修复未落盘**：根因不在 generation 门禁，而在 `branchesLoading` 无所有者。`refreshInternal` 有四处（1118、751 短路、779 rootUris 空、822 完整加载）无条件 `store.setState({branchesLoading:false})`，而 Webview 的 `renderSelectors` 完全由该标记驱动：收到 false 就摘 spinner 并把当时的 `branches` 当最终结果定稿。切仓库时序：快路径写单条分支 → 后台刷新置 true 开始跑 `getGitBranches` → `refreshInternal` 走短路置 false → Webview 把「只有当前分支」定稿。冷缓存（首次访问的仓库）时 `for-each-ref` 慢于几乎不做 IO 的 `refreshSelectors`，故偶现。
- **待应用的四步修复**：①`refreshInternal` 四处只留 `reposLoading:false`，`branchesLoading` 归 `refreshBranchesAfterRepositorySelection` 独占；②去掉该方法的 `signal` 参数（加 signal 是错的，等于把好不容易与 `refreshGeneration` 解耦的逻辑又绑回 refresh 生死），`getGitBranches` 亦不传 signal（有单飞+缓存，重复调用便宜）；③新增 `branchesRefreshPath` 做同仓库在途去重，`finally` 中仅当 `generation === branchesRefreshGeneration` 才释放，且 `selectRepositories` 入口主动清除；④快路径不再置 `branchesLoading:false`，分支名靠同步 `pushStateToWebview()` 立即显示，spinner 由 `branchesLoading` 保持。
- **核对结论：上述四步修复实际未落盘**。当前 `gitkViewProvider.ts` 无 `branchesRefreshPath`，四处 `branchesLoading:false` 越权仍在，`refreshBranchesAfterRepositorySelection` 仍带 `signal`（1302），快路径 1261 仍置 false。前三轮改动均在（detached HEAD、`branchesReset`、`readCurrentBranchOption`、`branchesRefreshGeneration`、`selectRepositoryAbortController`）。「偶现只剩当前分支」仍未修复。
- **状态所有权原则**：成对的「数据字段 + 该数据的 loading 标记」必须由同一个所有者管理。只给数据字段加代次/门禁保护而放任 loading 标记被他人置位，会留下「数据未到但状态已完成」的窗口，UI 会把中间态当最终结果。
- **简化 `gitkViewProvider.ts`：2913 → 2474 行，编译错误 114 → 0**（此前该文件卡在「控制器化」半迁移状态，顶部已改为读三个控制器的只读 getter，但下半部仍留着整批旧方法赋值只读 getter、引用已删除的 store 字段、调用已不再导入的函数）：
  - 删除 727-1138 行整块陈旧方法：`startInitialRepositoryLoad`/`publishInitialRepositorySelection`/`loadInitialBranchCommits`/`refreshSelectors`/`performSearch`/`refreshSearchCleared`/`beginCommitReload`/`readCurrentBranchOption`/`publishCurrentBranchLoading`/`refreshRepositoriesAfterRepositorySelection`/`refreshBranchesAfterRepositorySelection`/`selectRepositories(paths)`/`loadBranchCommitsNow`，连带 `commitPageGeneration`/`searchGeneration`/`rawCommits`/`selectRepositoryAbortController`/`branchesRefreshGeneration` 等字段一并消失。另删 3 个已成死代码的比较辅助（`hasSameRepositoryOptions`/`keepSelectedRepositoryOption`/`hasSameSelection`）与 `refresh` 的 `forceCommitReload` 形参。
  - `runEffect` 四个分支改为纯委派：`selectRepositories` → `repoController.selectRepositories(选项对象)`（原先传 path 数组）；`reloadBranches` → 改名 `selectBranches`（store 里实际就是这个 effect 名，旧代码那个 case 根本不可达）→ `branchesController.selectBranches`；`search` → `commitController.search`；`loadMoreCommits` 暂空转（分页未纳入控制器职责）。
  - 补回被误删但仍有 9 处调用的 `getRepoRootUri`；`hasPreparedInitialData` 从读 `state.repositories/branches/commits` 改为回读三个控制器；`initializeGitWatchers` 去掉已不存在的 `invalidateGitRepositoriesCache`（仓库缓存现由 `GitRepoController` 自持）；补 `getCommitFiles`/`getWorkingTreeChangePresence` 两个 import。
  - **工具事故**：用 PowerShell 按行号切片重写文件（`$out+=$ls[0..725]` 方式）虽然结果正确，但让 IDE 语言服务持续报「File appears to be binary / line 672 invalid character / TSD-Header」等 27 个幻象错误，而 `[System.IO.File]::ReadAllBytes` 校验 badBytes=0、`tsc --noEmit` exit=0。**结论：大块删除也应优先用文件编辑工具；IDE 诊断与 tsc 冲突时以 tsc 为准。**
- **完全重构 `src/git/gitBranchesController.ts`（按 `doc/gitbranchscontroller.md` 从零重写，约 240 行）**：
  - 内部 `branches: Map<string, GitBranchOption[]>` 以 **path 为 key**（选项不可变、`copyRepositoryOption` 后引用变，原生 Map 引用判等必然 miss），对外 `onBranchesMapChanged` 现场转成 `Map<GitRepositoryOption, GitBranchOption[]>`。
  - `refresh(repositories, force)` 单一私有流程同时服务 `selectRepositories` 与 `forceRefresh`，差别只在 `force`：跳过去重、targets 取全部入选仓库、不走快路径。`isLoading` 在任何 await 之前同步置位。
  - 快路径 `applyCurrentBranch` 同时写 `branches` 分片与 `selected`，两个事件都 fire；不追加的两个条件：`hasUserSelection` 为 true、该仓库已有归属项。全量落地一律不碰 `selected`。
  - `readCurrentBranch` 判据只用 `!headHash`（detached 时 `symbolic-ref` 必失败）。
  - `resolveBranch` 单次遍历三级匹配（`equalsBranchOption` → name+hash → 仅 name），`hashMatch ??=` / `nameMatch ??=` 记录候选。
  - 两处 await 后都重新确认仓库仍在 `this.repositories` 中，防止期间被移出。
- **Provider 适配三处**：`onBranchesChanged` → `onBranchesMapChanged`；`onSelectedRepositoriesChanged(selected)` → `selectRepositories(selected)`；`refreshCurrentHeadBranch` 从 `async + await reload(selected)` 改为同步 `forceRefresh()`（仓库集合没变而分支变了，去重会拦住 `selectRepositories`，只能走 `forceRefresh`），且条件从 `length !== 1` 放宽为 `length === 0` 才 return。
- **工具写入被回滚的现象**：`replace_in_file` 报成功但文件内容未落盘（OneDrive 目录下），第二轮重做才生效。改完关键文件后应立刻用 `git diff` 或 grep 复核实际内容，不能只信工具返回值。
- **`gitkViewProvider.ts` 尚存 114 个既有编译错误**（与本次重构无关，全部集中在 736-1290 行的陈旧方法区）：该文件正处于「控制器化」半迁移状态，顶部已改为读 `repoController`/`branchesController`/`commitController` 的只读 getter，但 `refreshInternal`/`refreshSelectors`/`publishInitialRepositorySelection`/`loadInitialBranchCommits`/`performSearch`/`readCurrentBranchOption`/`publishCurrentBranchLoading`/`refreshBranchesAfterRepositorySelection`/`refreshRepositoriesAfterRepositorySelection`/`selectRepositories(paths)`/`loadBranchCommitsNow`/`beginCommitReload` 等旧方法仍在，它们赋值已变只读的 getter（`this.commits`/`this.branches`/`this.repositories`/`this.selectedBranches`）、引用已从 store 删除的字段（`reposLoading`/`branchesLoading`）、调用已不再导入的 `getGitCommits`/`buildGraph`/`getGitRepositories`/`getCommitFiles`/`searchCommits`/`COMMIT_PAGE_SIZE` 等。**下一步应删除这批陈旧方法，把职责整体交给三个控制器**。
- **重写 `doc/gitbranchscontroller.md`（用户要求剥离 Provider 逻辑，只按四条规则走）**：从 12 节压到 7 节 + 4 张 mermaid 图。四条规则=监听 repo 变化 / 内容不一致才刷新 / 在途则忽略请求 / 保证用户已选分支不变。与 `gitcommitcontroller.md` 的简化方向对齐。
  - **删掉 `hasUserSelection` 字段**：规则 4 说「已选分支不变」，就不存在「该不该跟随默认值」的判别，也不存在作用域重置（旧稿规则 5.7「仓库集合变化时重置」直接与规则 4 冲突）。`selectedBranches` 只剩两个变化来源：为空时的首次默认赋值 + 用户 `selectBranches`。
  - **删掉失效剔除与补充项**（旧稿 5.3/5.4）：允许 `selectedBranches` 含不在 `branchesMap` 中的分支——`getGitCommits` 的 refs 只吃名字，删掉的 ref 读不到提交即如实为空；UI 表现为该项不高亮，是正确的。与 `GitCommitController` 第 6 节「selectedCommit 可指向列表外提交」同构。
  - **`loadingRepos: Set` 降为单个 `isLoading: boolean`**：改用「在途即丢弃」后一次刷新内所有仓库统一收尾，不再有「先完成者提前抹掉加载态」的问题。
  - **删掉代次机制与两阶段快路径**：旧稿第 8 节论证「分支做不到像仓库那样免代次」，前提是「切仓库必须立刻发新请求」；改为在途即丢弃后该前提不成立，`isLoading` 单门禁即可，与 `GitRepoController` 第 6 节结论合流。快路径（先落地当前分支再全量替换）属中间态优化，非四条规则所需，一并剥离。
  - **新增关键约束：`repositories` 快照的替换必须与刷新绑定**，不能在门禁之前替换。否则被丢弃的那轮留下「快照已是新值但分支仍是旧仓库」的错位，之后同内容事件都会被去重规则判为一致而永不修正。
  - 保留的部分：分片按 `repositoryPath`（非选项对象，copy 后引用会变）、已选分支带归属二元组、`selectBranches` 三级匹配（`equalsBranchOption` → `name`+`hash` → 仅 `name`，第 2 级为挂 `virtualCommits` 后的对象保命）、事件负载去快照、加载态与列表分开发事件、返回 `boolean` 避免假 loading。
- `doc/gitbranchscontroller.md` 二次修订（用户两条追加要求：`hasUserSelection` 加回 + 快路径与分支类型分类）：
  - **`hasUserSelection` 加回，但语义改为「一经置位永不重置」**。它解决的是规则 4 管不到的空档：规则 4 说「已选分支不变」，但「用户还没选过」时需要默认值。不能用 `selectedBranches.length === 0` 代替——该判据把「从未选过」（应填默认值）与「用户显式清空」（应保持空）混为一谈，只看长度则下次刷新会把当前分支自动填回，用户的清空意图被无声撤销。故 `selectBranches([])` 明确为合法入参。旧稿「仓库集合变化时重置」的规则与规则 4 直接冲突（换仓库即撤销用户选择），保持删除。字段不进对外接口：调用方读它只能重复控制器已做的判断，写它等于替用户宣称选过。
  - 第 5 节两条规则改为按 `hasUserSelection` 分流（false 跟随各仓库当前分支、HEAD 切换自动更新；true 一律不动含空选择），并补「默认态下与旧选择逐项全同则静默同步不 fire」——多仓库刷新后当前分支常未变，无条件 fire 会让 `GitCommitController` 白跑一轮（其去重按 name 集合拦不住内容确实不同的两次）。判等走 `equalsBranchOption`。
  - **恢复两阶段加载（快路径）**，条件收窄为「仅对 `branchesMap` 中不存在分片的仓库」。已有分片的仓库不走——退回「只有一条当前分支」是把有效数据换成更差的数据，比空白更糟。理由：`getGitBranches` 走 `for-each-ref refs/heads refs/remotes`，远程分支多时可感知耗时，无快路径则切仓库后选择器空到全量返回。
  - **快路径放弃判据只能是 `!headHash`，不能是 `!branchName || !headHash`**：detached HEAD 时 `symbolic-ref` 按 Git 约定返回非零使 `branchName` 为 undefined，算作失败会让 detached 下选择器一直空着。`hash` 必须取 `rev-parse HEAD`（`symbolic-ref` 只给 ref 名无 hash），而 hash 是 `selectBranches` 三级匹配第 2 级的依据。
  - **快路径中间态不参与已选分支收敛**：加 `isFullLoad` 标记，快路径传 false 时只更新 `branchesMap` + fire `onBranchesChanged`，不碰 `selectedBranches`。否则默认态下会先按单条当前分支算一次默认选择再算一次，下游白跑两轮。
  - **新增「GitBranchOption 的类型分类」小节**：`kind: 'current'|'local'|'remote'` 判据全部来自 `name` 前缀（`refs/heads/` → local、`refs/remotes/` → remote 且滤掉 `*/HEAD`、`symbolic-ref`/detached hash → current），零额外 IO。关键澄清：**`current` 与 `local` 不互斥而是同一 ref 的两个身份**——`getGitBranches` 返回 `[...current, ...branches]`，`branches` 由 `[...local, ...remote]` 映射且包含当前分支那条，`current` 又是从 `local` find 出来的，故非 detached 时当前分支在列表里出现两次（一条 current 排最前、一条 local 在本地区）。这是既有行为，控制器不改；排序与去重属展示层决策。detached 是唯一例外（无 `refs/heads/*` 指向 HEAD，仅一条裸 hash 项）。
  - `getBranches()` 改为 `getBranches(kind?: GitBranchKind)`，支持按类过滤。
  - 用户手改的状态表把 `branchesMap` key 与已选分支归属改成 `GitRepositoryOption` 对象，与正文「key 用 path」冲突；已按表格对齐并补约束：`copyRepositoryOption` 后同一仓库是两个实例（`hasSubmodules` 翻 true 时），原生 `Map` 按引用判等必然 miss，故对象只是对外表达形态、查找一律按 `path` 命中，否则「扫描完成后同一仓库变两个 key、已加载分支查不到、旧分片删不掉」。
  - 验收表新增 12 行（快路径/分类/默认态跟随/静默同步/显式清空保持空等）。
- `doc/gitbranchscontroller.md` 三次修订（用户两条新要求 + 对齐用户手改的接口命名）：
  - **第 5 节改写为「branchesMap 变化不影响 selectedBranches」**：两状态彻底解耦，唯一例外是「从未选过时填初值」——那不是「影响」而是「初始化」。**连带推翻上一稿的「默认态跟随 HEAD 切换」**：默认态下 HEAD 切换后 `selectedBranches` 仍指向切换前的分支，不为「看起来更聪明」而破例；需要跟随由调用方显式调 `selectBranches`。`hasUserSelection` 语义收窄为「仅用于判断是否需要初值」，置 true 后控制器再也不主动写 `selectedBranches`，标记退化为只读事实。加了一张「触发 → selectedBranches」六行对照表。
  - **新增「UI 勾选态由 selectedBranches 决定」**：列表项读 `getBranches()`、勾选态读 `selectedBranches`，两者职责不混。**禁止从 branchesMap 反推勾选态**（如「kind==='current' 默认打勾」）——那等于在 UI 层复制一份默认选中逻辑，与控制器的判断各算一次，用户清空选择后 UI 仍会把当前分支画成勾选。正反两种脱节都允许：已选分支不在列表里则一个勾不打、列表有大量分支而选择为空同样不打勾。
  - **新增 2.1 节 `forceRefresh(): void`**：第 2 节去重按仓库集合判等，导致「仓库不变但分支变了」无入口刷新（别处 checkout/branch -d/fetch、`gitActions` 调 `invalidateGitRefsCache` 后、watcher 检测 HEAD 变化）。关键论证：**清缓存本身不触发读取**，`invalidateGitRefsCache` 只让「下次读」拿新数据，没人读则永远停在旧值，`forceRefresh` 就是那个「去读」的入口。与 `selectRepositories` 只差两点（跳过去重、对全部入选仓库重读），其余共用。四条约束：不清空已有分片（旧数据仍可用，清空是纯退步）、不走快路径（已有完整分片，退回单条是把有效数据换成更差的）、共用同一个 `isLoading`（写的是同一个 `branchesMap`，各自判在途会出现「仓库切换正在写分片时被覆盖」的交叉）、缓存失效由调用方负责（清缓存时机取决于刚做了什么 git 操作，属 `gitActions` 的知识；控制器自己清等于让 `getGitBranches` 的缓存形同不存在）。返回 `void` 而非 `boolean`：被丢弃时调用方无需善后，加载态由 `onBranchesLoadingChanged` 驱动，且它幂等。
  - **`onBranchesChanged` → `onBranchesMapChanged`**（用户手改，负载改为 `Map<GitRepositoryOption, GitBranchOption[]>`），`onSelectedRepositoriesChanged` → `selectRepositories`（用户手改）。补论证：两个事件**必须分开发**，这是「两状态独立」在事件层的体现——`branchesMap` 刷新只 fire 前者，订阅方据此重画列表而不动勾选；合成一个则订阅方无法区分「可选项变了」与「选择变了」，勾选态会跟着列表刷新而抖动。
  - 第 0 节四条规则扩为五条；验收表新增 11 行（UI 勾选态、清空后不补勾、forceRefresh 六种情形），并修掉两行与新第 5 节冲突的旧用例（「默认态 HEAD 切换自动跟随」改为「不跟随」、删掉「默认态静默同步」）。
- `doc/gitbranchscontroller.md` 四次修订（用户要求：新仓库快路径同时写 `selectedBranches`）：
  - **快路径从「绝不碰 selectedBranches」改为「唯一写入时机」**。上一稿（三次修订）把两状态彻底解耦、初值放在全量落地后算；本轮改为快路径读到当前分支后**同一步写两个状态**（写 `branchesMap` 分片 + 追加进 `selectedBranches`），全量落地一律不重算。三条理由：①提交读取提前一整个 `for-each-ref` 的时间，与 `getGitBranches` 并行而非串行；②`onSelectedBranchesChanged` 全程只 fire 一次，避免下游 `GitCommitController` 白读一轮（其去重按 name 集合，「一条」vs「若干条」内容确实不同拦不住）；③全量加载失败时该仓库仍有可用选择，提交列表照常出来。
  - **写入范围限「新增仓库」，按仓库粒度判断**：只往 `selectedBranches` 追加新仓库的当前分支，已在选仓库的选择原样保留（多选 2→3 时前 2 个不动）。两个不追加的条件：`hasUserSelection === true`（用户已显式选过，新增仓库的当前分支不该挤进去）、该仓库在 `selectedBranches` 里已有归属项（仓库移出再加回的情形）。
  - **`forceRefresh` 因此不写 `selectedBranches`**：写入只发生在快路径，而 `forceRefresh` 不走快路径。上轮全量失败只剩单条分片的仓库，`forceRefresh` 补齐列表但不重复给选择。
  - 第 5 节表格改为 8 行，新增「全量分支落地」「已在选仓库分片被替换」两行显式标注不动；点明「第二行是本节与第 4 节的接缝，容易写错」。快路径小节标题由「中间态不参与已选分支收敛」改为「是 selectedBranches 的唯一写入时机（除 selectBranches 外）」。
  - 时序图新增 `GitCommitController` participant，标出提交读取在快路径 fire 后即开跑；验收表新增 9 行（快路径写两状态、全量只写一个、全程只 fire 一次、提交读取起点、2→3 仓库只追加、hasUserSelection 时不追加、移出再加回不重复、全量失败仍可用、forceRefresh 补齐不重写）。
- 二次优化 `doc/gitrepocontroller.md` 并加入 5 张 mermaid 图（状态机、初始化时序、收敛规则 flowchart、并发控制 flowchart、职责划分图）。采纳用户手改的三条并补足论证：4.4 按 `hasUserSelection` 分流（false 回退当前仓库 / true 变未选择）；6.1 `scanning` 时外部刷新直接丢弃；6.3 进入扫描流程第一件事同步置 `scanning=true`（必须在任何 await 之前，否则两个请求都能通过 6.1 检查）。据此推出：6.1+6.3 使控制器**无需 generation 机制**，因同一时刻只可能有一次扫描，不存在后发覆盖先发的竞争，比引入代次更简单且避免「后发者作废先发者、自己又失效」的双输。另补充：`Scanning` 态下 `selectRepositories` 仍生效（用户操作不被扫描阻塞）、`keepSelectedRepositoryOption` 应在控制器化时删除、异常路径也必须把 `scanning` 置回 false。
- 重写 `doc/gitbranchscontroller.md`（原文只有 5 行要点），按 `gitrepocontroller.md` 同构结构展开为 12 节 + 4 张 mermaid 图，全部结论用现有代码论证。关键新增：①`branchesMap` 必须是 `Map<repoPath, GitBranchOption[]>` 而非扁平数组，因多仓库合并视图下子模块间分支名会重名；②去重必须按值比较——`GitRepoController.selectedEmitter.fire([...this.selected])` 每次都是新数组，引用比较恒不等；③`converge` 会额外 fire 一次内容常相同的事件，不去重则每次子模块扫描结束都白跑全量分支加载；④`loadingRepos` 按仓库粒度而非全局布尔，否则多仓库并行时先完成者提前抹掉加载态；⑤加载态与分支列表必须**分开发事件**（`onBranchesChanged` / `onBranchesLoadingChanged`），从事件层根治现有靠 `pushStateToWebview()` 强推一帧抢覆盖顺序的做法；⑥控制器持有自己的 `branchesGeneration` 且门禁需叠加「repo 仍在 currentSelectedRepoList 中」，对应现有 `isCurrent()` 三重判断；⑦规则 5.4 保留「已选但全量结果中缺失」的分支为列表尾部补充项（沿用 `refreshBranchesAfterRepositorySelection` 的合并写法）；⑧新增第 11 节迁移表，列出 8 处现有职责的归属，并指出 `virtualCommits` 属工作区状态应留在 Provider、多仓库下 `getGitCommits` 不按分支过滤的行为不变。
- `doc/gitcommitcontroller.md` 七次修订：加 `virtualCommitMap: Map<GitBranchOption, VirtualCommit>` 与 `getVirtualCommitContent(mode, repositoryPath)`。关键论证：①**key 为什么是分支**——Changes/Staged 只挂 `kind==='current'` 分支下（工作区状态属某仓库 HEAD，HEAD 即当前分支），多仓库合并视图有多个当前分支且工作区互不相干，必须按分支分片；非当前分支不在 map 中，查不到即两行不显示。②**`VirtualCommit` 只存 `{changes, staged}` 两个布尔，不含 Diff 内容**——显示与否可缓存（刷新流程统一更新），内容不能，因工作区随编辑/保存/git add/外部工具随时变，缓存失效时机无法确定；这正是 `getVirtualCommitContent` 每次重新读取的依据。③**`Map` 用 `GitBranchOption` 作 key 的坑**（同 `branchesMap` 不能用 `GitRepositoryOption`）：Map 按引用判等而分支对象经 `copyBranchOption` 后引用会变，不能 `map.get(branch)`，须按 name 线性查或以 name 作内部索引键。④`virtualCommitMap` 只在**分支变化**时刷新（关键字过滤不改变分支集合也不改变工作区状态），刷新方式为对每个当前分支调 `getWorkingTreeChangePresence`（只跑一次 `git status --porcelain`，代价远低于列表读取，可并入同一流程不必单设门禁）。⑤**推翻上一稿「工作区提交不纳入」的结论**：改为 `getCommitContent` 与 `getVirtualCommitContent` 两个方法分开，而非用 `ChangeSetMode` 在一个方法里分流——后者会让签名同时存在两个互斥入参（调用方得传假 hash 再靠 mode 覆盖）。⑥两侧正文取法差异是该方法存在的根本原因：`staged` 为 HEAD→索引，`changes` 为索引→磁盘工作区文件，读磁盘那一路不经过 git，是 `getCommitContent` 完全没有的分支。⑦开关为 false 时仍调用**不拒绝**——开关是上次刷新的快照，此刻工作区可能已有新变更，读真实结果比按过期开关拒绝更正确。⑧「点的是哪一行」的判断在调用方，控制器不提供「这个 hash 是否虚拟提交」的辅助方法（虚拟 hash 是展示层协议）。另修正用户接口块里 `Event<GitBranchOption，VirtualCommit>` 的中文逗号 + `Event<T>` 只接受单类型参数问题，改为 `Event<ReadonlyMap<GitBranchOption, VirtualCommit>>`；并把正文 5 处旧事件名 `onCommitsChanged`→`onTotalCommitsChanged`、删掉已被用户移除的 `onSelectedCommitChanged` 引用（改为「随 onSearchedCommitsChanged 一并可读」）。
- `doc/gitcommitcontroller.md` 六次修订：`selectedCommit` 从存 `{hash, repositoryPath}` 标识改为**存 `RepositoryCommit` 完整对象**（与用户此前对分支定的「直接回传对象不要只回传名字」一致）。理由：调用方拿到选中项通常还要用 `subject`/`author`/`date` 渲染标题栏，只给标识会迫使调用方回列表反查，而该提交可能已不在列表中（第 6 节规则 2 允许）。`RepositoryCommit` 自带 `repositoryPath`，多仓库同 hash 的定位问题天然解决。**代价与新增硬约束**：对象里的 `lane`/`laneColor`/`inputSwimlanes` 等图形字段由 `buildGraph` 写入、随列表变化，列表刷新后 `selectedCommit` 持有的是某一帧的旧布局 → 规定「图形字段不可信，任何渲染不得读取」，需布局信息时回 `searchedCommitList` 按 hash+repositoryPath 查当前项；判等**只比 hash + repositoryPath**，全属性比较会因图形字段差异把同一提交判为不同。`selectCommit(commit)` 入参也改收对象——既然不做存在性校验，控制器无法把标识补全成对象（列表里可能查不到），只能要求调用方直接给。
- `doc/gitcommitcontroller.md` 五次修订（用户四条新规则）：**`searchedCommitList` 语义从「搜索态才有」升为「UI 唯一渲染的展示列表」**——关键字空时它是全部提交、非空时是过滤结果，永不为「未搜索故为空」；调用方不再需要「该渲染哪个列表」的判断，`totalCommitList` 退为不参与展示决策的全量数据。两个刷新入口（分支变化 / 关键字变化）走同一条流程，因为「不过滤」只是「过滤条件为空」的特例，两套逻辑迟早分叉；`totalCommitList` 只在分支变化时重读，关键字变化不 fire `onCommitsChanged`。**第 6 节彻底重写：`selectedCommit` 只有两条规则**——首次（`=== undefined`）取 `searchedCommitList` 首条；刷新列表时一律不动，即使已不在新列表中。因此**删掉了上一稿的「回退首条」收敛规则**（那是我按 Provider 现有行为写的，与用户规则 4 冲突）。正视后果并写入文档：`selectedCommit` 可能指向列表外的提交，这是允许的——`getCommitContent` 是对 git 的直接查询不依赖列表，UI 侧表现为无高亮（正确，该提交确实不在当前视图）。连带 `selectCommit` **去掉「是否在列表中」的校验**（既然允许指向列表外，入口就无理由拒绝），只保留「与当前相同则返回 false」；工作区虚拟提交 hash 因此天然可用。新增取舍说明：关键字变化在「在途即丢弃」下会丢请求（用户连续输入时列表停在旧关键字结果），缓解手段是**调用方防抖**而非控制器排队——排队要处理「排队中又来新请求」的合并，复杂度远高于防抖。第 10 节强化：不得让两列表共享提交对象，`searchedCommitList` 必须来自独立读取，若为省 IO 复用 `totalCommitList` 数据必须先逐对象复制再建图。
- `doc/gitcommitcontroller.md` 四次修订：加 `getCommitContent`（返回 `DiffPayload[]`）、加 `selectedCommit` 字段、`commitList` 更名 `totalCommitList`。关键论证：①**`getCommitContent` 是纯查询**——不写状态不发事件，这是它豁免 `isScanning` 门禁的前提（否则列表加载期间点提交什么也看不到），也是它与列表读取的根本区别。②**不复用 `DiffReader.prepare`**——该方法直接 `store.setState({files, diffLoading})` 并依赖 `store.getState().diffGeneration` 做代次，复用等于通过 Store 产生副作用；需要的是 `readDiffs` 那层「组装 DiffPayload[] 并返回」的能力。另两条：不做进度上报（属展示需求）、单文件失败写 `DiffPayload.error` 继续而非抛。③**`selectedCommit` 只存 `{hash, repositoryPath}` 不存对象**——提交对象含 `lane`/`laneColor` 等随列表变化的图形字段，存对象等于存布局快照，列表重建即过期；`repositoryPath` 不能省，多仓库下不同仓库可能有相同 hash（cherry-pick 或子模块指向同一上游）。④**收敛规则与前两个控制器显式不同**：仓库/分支在用户选择失效后变「未选择」，提交必须回退首条（内容面板需选中项才有内容，空选中态 UI 无意义）。⑤清空搜索时也要收敛——搜索是全量扫描而普通列表只取首页，搜索态选中的提交可能不在 `totalCommitList` 中。⑥**内容读取由事件驱动不由控制器串联**——`selectedCommit` 变化只 fire 事件，绝不自己调 `getCommitContent` 再推结果，否则「谁需要内容、何时需要」的决定权被收进控制器。⑦工作区虚拟提交（staged/changes）不纳入 `getCommitContent`——比对索引与工作区而非两个 commit，`ChangeSetMode` 分支判断会把工作区语义带进来。
- `doc/gitcommitcontroller.md` 三次修订：加入 `searchedCommitList`（原字段 `commits` 顺势更名 `commitList`），新增 `searchKeywords`，第 6 节「搜索」+ 第 7 节接口 `search(keywords, repositories)`。关键论证：①两个列表必须分开存——搜索结果若覆盖 `commitList` 则清空搜索只能重新发 IO，而这次 IO 又会被第 4 节的在途丢弃拦住，UI 就停在搜索结果上；分开存则清空搜索零 IO。②普通读取与搜索**共用同一个 `isScanning`**，不各设一个——两者写相邻状态（搜索清自己、普通读取清搜索结果），独立门禁会出现「搜索正写 searchedCommitList 时普通读取把它清空」的交叉。③清空搜索（`search([])`）是 `isScanning` 的例外，不置位也不受限，否则读取在途时用户无法退出搜索态。④分支变化时清空搜索但**不自动重跑**——自动重跑会让一个事件产生两条并发写入路径，谁先落地成时序问题。⑤两个列表各自发事件（`onCommitsChanged`/`onSearchedCommitsChanged`），合成一个则订阅方无法区分、只能全量重渲染。⑥`searchCommits` 是 `git log` 全量输出 + 本地 filter，无分页语义故搜索结果不涉及分页状态；`refs` 为空时它内部回退 `HEAD`，控制器不重复实现。⑦第 9 节补：不能拿 `commitList` 过滤出搜索结果再建图，那会改写 `commitList` 里对象的 lane，搜索结果必须来自 `searchCommits` 独立读取。
- `doc/gitcommitcontroller.md` 二次修订：①用户指出「用户已选项不见了」——原稿把该状态叫 `currentSelectedBranchList` 并描述为「已消费的快照」，语义偏了；改回 `selectedBranches` 并新增「selectedBranches 的角色」小节，明确它是用户已选分支在控制器内的只读副本、承担去重依据 + refs 来源两个职责、所有权仍在 `GitBranchesController`，且规则 4 在实现上就是「除入口赋值外代码里不存在第二处写它的地方」；对外接口补 `readonly selectedBranches`。②`isFetching` 全文更名 `isScanning`（与 `GitRepoController.scanning` 命名一致），含状态机 `Scanning` 态、三张图、验收场景。
- **重写 `doc/gitcommitcontroller.md`（用户要求剥离 Provider 逻辑，只按四条规则走）**：从原 12 节压到 9 节。四条规则=监听 branches 变化 / 内容不一致才刷新 / 在途则忽略请求 / 保证用户已选分支不变。剥掉的 Provider 派生内容：分页（`loadMore`/`pageStates`/`prefetchNextCommitPage`/`COMMIT_PAGE_REQUEST_SIZE` 探测下一页）、搜索（`search`/`searchKeywords`/与普通列表互相覆盖）、选中提交（`selectedCommit`/`hasUserSelection`/收敛回退首条）、多仓库分片（`commitsMap` 按 repoPath）、`GitCommit` 不可变改造方案 A/B、代次机制。
  - **「在途即丢弃」使控制器无需代次**：同一时刻只可能一次读取，不存在后发覆盖先发的竞争。文档写明这比引入代次更简单，且避免「后发者推进代次作废先发者、自己又失效」的双输。
  - **`isFetching = true` 必须在任何 await 之前同步置位**，否则两个请求都能通过检查；`false` 必须在 `finally`，否则异常后控制器永久拒绝一切请求（规则 3 会全丢）。
  - **去重比较三条禁止**：不比数组引用（上游每次 fire 都是 `this.selected.map(...)` 新数组）、不比全属性（挂 `virtualCommits` 后属性变但不该重载）、不依赖顺序（上游按仓库遍历序构建）。去重是必需而非优化——上游一次仓库切换会 fire 两次（快路径 + 全量落地收敛）。
  - **第 5 节「不碰分支选择」列 4 条禁止项**：不得因无提交而改选/清空、不得因失败回退其他分支、不得调 `selectBranches`、不得替用户挑分支重试。
  - 入参需带 `repositories`（`getGitCommits` 要 `rootUri`），但控制器**不订阅仓库事件**——那会形成两个输入源产生时序竞争，由调用方转发时一并传入。
  - `onCommitsChanged` 负载为 `void` 不带快照（同分支控制器结论：发快照会诱导订阅方缓存，导致过期快照压掉后台结果）。
  - 保留 `buildGraph` 副作用说明：原地改 5 个图形字段并返回同一数组；控制器每次对新读取数组建图并整体替换故不受影响，但不得把已建图数组再次传入。
- **简化 `GitBranchesController` 冗余逻辑（`npx tsc --noEmit` 通过）**：先用 grep 核对实际调用点（仅 9 个成员被 Provider 使用），据此收敛接口。
  - **删掉零调用的公开成员** `branchesMap`/`loadingRepos`/`getBranchesOf`/`isRepositoryLoading`：它们只服务假想的多仓库 UI，留着会诱导后续实现依赖 `GitRepositoryOption` 作 map key（对象身份不稳定，文档 1.1 已论证会出错）。连带删掉 `toRepoKeyedMap`/`toLoadingSet`/`fireBranches` 三个仅供它们使用的私有方法。
  - **删掉 `repoByPath` 字段**：与 `currentSelectedRepoList` 是同一份数据两处存，改为 `isSelected(path)` 线性查（选中仓库个位数）。字段更名 `repositories`。
  - **事件负载去快照**：`onBranchesChanged` 从 `Event<ReadonlyMap<...>>` 改 `Event<void>`——发快照会诱导订阅方缓存，正是 `refreshSelectors`「过期快照压掉后台刷新结果」的病根；`onBranchesLoadingChanged` 从 `Event<ReadonlySet<GitRepositoryOption>>` 改 `Event<boolean>`——UI 只需知道要不要转圈，按仓库粒度是内部并发门禁不该成为契约。
  - **`resolveBranch` 三趟遍历改单次遍历**：原实现三个 `findSelected` 回调各扫一遍所有分片，改为一趟记录 `hashMatch`/`nameMatch` 候选，逻辑等价但少两趟扫描且优先级集中在一处。
  - **`convergeSelected` 去掉冗余分支**：原 `if(!hasUserSelection){...} else if(kept.length===0){next=[]}` 里 else-if 分支恒等于 `next=kept`（kept 为空时二者相同），合并为 `const next = fallback.length > 0 ? fallback : kept`；同时去掉「静默同步」与「通知」两条重复的赋值路径。
  - 其他：`sameRepositories` 从双向 `every+some`（O(n²)）改 `Map<path>` 单向查（path 唯一，同长度+逐项命中即等价）；剔除逻辑抽成 `pruneRemoved(keep)`；`applyBranches` 预算 `loadedNames` 集合避免 filter 内重复 `some`；`sameBranches`/`sameSelected` 去掉多余的提前 return。
- **`GitBranchesController` 自检并去 Provider 污染（`npx tsc --noEmit` 通过）**，修掉四处不一致 + 两个功能性 bug：
  - **多仓库同名分支错配（bug）**：原 `selectedBranches` 存裸 `GitBranchOption` 靠 `findBranchByName` 跨仓库按名字查。多子模块都有 `refs/heads/master` 时错两次——仓库移出选择时按名字查会命中另一仓库同名分支而误留；A 仓库 master 被删但 B 仓库还有时存在性判定仍命中、该项不被剔除。改为内部存 `{repositoryPath, branch}` 二元组，剔除与存在性判定按二元组精确匹配；对外 `selectedBranches` 仍映射为 `readonly GitBranchOption[]`（下游 `getGitCommits` 只消费名字），归属是内部细节。
  - **`branchesMap` 的 key 失效不通知（bug）**：原来只在有条目被删时 `fireBranches()`。但能走到该分支说明仓库选项对象已变，而 `branchesMap` 的 key 正是这些对象，订阅方拿旧 key 查不到分支。改为无条件 fire。
  - **`getBranches(repositories)` 入参是 Provider 逻辑的倒影**：Provider 手里有 `repoController.selectedRepoList` 就顺手传入，等于承认控制器状态不可信，还留下「传入集合与内部集合不一致」的口子。改为无参 `getBranches()` + `getBranchesOf(repository)`。
  - **`hasUserSelection` 缺作用域重置**：原来一旦 true 永久 true，用户在仓库 A 选了 feature-x 后切到 B，B 的当前分支不会被默认选中。改为仓库集合本身变化时重置（仅 `hasSubmodules` 等元数据变化不重置）。
  - **快路径中间态参与了剔除判断**（违反文档规则）：快路径与全量结果都走 `applyBranches`→`convergeSelected`，「只有当前分支」的中间态会把用户已选的其他分支剔掉。加 `isFullLoad` 参数，快路径传 `false` 时该仓库已选分支一律保留。
  - 默认模式跟随逻辑简化（去掉从 `resolveBranchSelection` 带来的 `!kept.some(kind==='current')` 补丁思路）：`hasUserSelection===false` 就始终跟随各仓库当前分支，`===true` 且全部失效才变未选择，默认模式无空选择态。另补 `generations` 在仓库移出后的清理。
  - **Provider 侧死代码**：`hasSameBranchSelection(selectedBranches)` 的实参就是 `this.selectedBranches`（同一 getter），恒为 true，短路条件退化成「有提交就跳过重载」。已删除该方法并把 `refreshInternal` 短路改为 `!forceCommitReload && this.commits.length > 0`。
  - **`selectBranches` 改返回 `boolean`**：调用被忽略（校验不通过/选择未变）时调用方必须知道，否则 `runEffect` 里先 `beginCommitReload` 进的加载态永不结束。且清态必须排在调用之前——控制器同步 fire 选择变化并在其中发起重载。
  - **入参保持 `GitBranchOption[]` 对象（用户明确要求，非名字）**：控制器内 `resolveBranch` 三级逐步放宽匹配 `equalsBranchOption` → `name`+`hash` → 仅 `name`。第 2 级是关键：对象经 `copy`（挂 `virtualCommits`）后属性已变但业务身份未变，若只有第 1、3 级则挂过 virtualCommits 的当前分支会跳过精确匹配落到按名字兜底，多仓库下仍会错配。
- **新建 `doc/gitcommitcontroller.md`**（按 `gitbranchescontroller.md` 同构结构，12 节 + 5 张 mermaid 图，全部结论用现有代码论证）。关键论证：
  - **`buildGraph` 原地改属性与不可变约束直接冲突**：`gitLogProvider.ts` 的 `buildGraph(commits)` 对入参 `commit.lane`/`laneColor`/`laneStartsHere`/`inputSwimlanes`/`outputSwimlanes` 五个字段原地赋值并返回同一数组。故 `loadMoreCommits` 传 `rawCommits` 建图后原始数据已被改写，`refreshSearchCleared` 再次建图靠「反正会被覆盖」侥幸成立。文档给出方案 A（推荐，`GitCommit` 全 readonly + 布局结果外移为 `CommitGraphRow`，`buildGraph` 改签名返回新数组）与方案 B（本体字段 readonly、图形五字段留可变）二选一。
  - **`rawCommits` 扁平数组是多仓库不支持分页的根因**（非设计取舍）：`refreshInternal` 里 `isSingleRepository ? successful[0].raw.slice(...) : []`，多仓库恒为空 → `hasMoreCommits` 恒 false + `loadMoreCommits` 守卫 `paths.length !== 1` 直接 return。分页游标每仓库独立，扁平数组存不下多个 skip，故 `commitsMap` 必须按 repoPath 分片。
  - **`commitPageGeneration` 被四条流程共享**（`refreshInternal` 两处 ++、`beginCommitReload`、`reloadCommitsForBranchSelection` 都推进，`loadMoreCommits` 只读比较），任一流程推进会连带作废其他三条在途结果，与分支维度 `refreshGeneration` 是同一 bug 模式。改为按仓库分片代次。
  - **搜索只覆盖 `graphCommits`、绝不写 `commitsMap`** 要从隐式约定升为显式规则（现有 `performSearch` 只写 `commits` 不动 `rawCommits`，`refreshSearchCleared` 才能恢复）。同时指出 `refreshSearchCleared` 用 `rawCommits.length === COMMIT_PAGE_SIZE` 推断 hasMore 是 bug（总数恰为 50 倍数时误判），应改为从 `pageStates` 读回而非二次推断，迁移时不要移植。
  - **选中提交收敛规则与仓库/分支控制器显式不同**：那两个在用户显式选择失效后变「未选择」，提交必须回退首条（Changed Files 需要选中项才有内容，空选中态 UI 无意义）。现有 `?? commits[0]` 回退方向对，但缺 `hasUserSelection` 且 `beginCommitReload` 每次清 `currentHash=''` 使 find 必然失败、必然打回首条 —— 用户翻到第 200 条会被任意 watcher 刷新打回。
  - **最大解耦点**：现有 5 处提交加载完成后 `await this.selectCommit(...)`，而该方法做「设选中 + 加载变更文件 + 驱动 Diff + 推进 commitFilesGeneration」四件事；控制器只能 fire `onSelectedCommitChanged`，文件与 Diff 由 Provider 订阅后做。另需断开 `getRepoRootUri` 默认参数依赖 `currentRepositoryPath` 形成的循环（选中态由提交列表写入）。
  - `loadBranchCommitsNow`（983-1019）在上一轮抽 `GitBranchesController` 时随 `publishCurrentBranchLoading` 失去调用点，已是死代码，标注为直接删除而非迁移。
  - 其他：`isLoadingMoreCommits` 现有 7 处写入点但只有 `loadMoreCommits` 是真所有者（第 7 节状态所有权）；`COMMIT_PAGE_REQUEST_SIZE = PAGE_SIZE + 1` 多取一条探测下一页的约定要保留；全部落地后才整表重建图（`buildGraph` 泳道状态机跨提交连续，逐仓库增量会错乱 lane）；`onCommitsChanged` 与 `onPageStateChanged` 必须分开发（分页状态变化远比列表频繁，合发会让 Webview 全表重渲染，现靠 webview 侧比较 hash 序列补漏）。
- **不可变约束落地 `src/types/git.ts`（用户要求 `GitRepositoryOption`/`GitBranchOption` 属性禁改 + copy 函数 + 属性全同即同一对象）**：`GitRepositoryOption` 由用户自行改好（readonly + `copyRepositoryOption`/`equalsRepositoryOption`）；我补了 `GitBranchOption` 全字段 readonly + `copyBranchOption`/`equalsBranchOption`，并把嵌套的 `GitBranchVirtualCommit` 一并 readonly 化——内层可变则外层不可变形同虚设，改 `virtualCommits[0].enabled` 不产生新对象，变化无法检测，故 `equalsBranchOption` 需逐项比较 `virtualCommits` 而非比数组引用。连带必须修的两处引用相等（否则 `virtualCommits` 挂不上、工作区 Changes/Staged 两行消失）：`applyWorkingTreePresence` 与 `withCurrentBranchVirtualCommitPresence` 原用 `branch === currentBranch`，已改 `equalsBranchOption` + `copyBranchOption`。`types/index.ts` 是 `export *`，新函数自动可见。
- **落地 `GitBranchesController`（`src/git/gitBranchesController.ts` 新建，`npx tsc --noEmit` 通过）**，按用户四条门禁「禁止 Provider 处理 branches 逻辑 / 禁止把 Provider 原有 branches 逻辑与边界移植进控制器（直接废弃）/ Provider 只能调控制器方法与监听事件 / 保证控制器纯净不被污染」：
  - 控制器自持 `currentSelectedRepoList`/`branches`/`repoByPath`/`loading`/`selected`/`hasUserSelection`/`generations`。对外 `onSelectedRepositoriesChanged()`（唯一输入入口）+ `selectBranches()` + `reload()` + `getBranches/getCurrentBranch/isRepositoryLoading`，三个事件 `onBranchesChanged`/`onSelectedBranchesChanged`/`onBranchesLoadingChanged`。零 store / 零 postMessage / 零 Provider 引用。
  - **内部 Map/Set 一律以 `path` 为 key**（文档 1.1 节论证的实现代价）：对外 getter 才现场转成 `Map<GitRepositoryOption, ...>`/`Set<GitRepositoryOption>`。`generations` 按 path 各自独立——多仓库并行加载天然多在途，无法像 `GitRepoController` 那样靠单例 `scanning` 免掉代次。
  - **存在性判定按 `name`，对象替换判定按 `equalsBranchOption`**，两者不可混用：全量结果里的对象尚未挂 `virtualCommits`，用全属性比较会把已选分支误判为「不在」而清空。
  - 两阶段：`loadRepository(repo, withFastPath)`，快路径读当前分支立即落地，再 `getGitBranches` 整体替换；`reload` 传 `withFastPath=false`，已有列表不退回单条中间态。`finally` 里 `loading.delete` 必须收敛否则 UI 永久 loading。
  - Provider 侧废弃（非移植）：`refreshBranchCommits`、`publishCurrentBranchLoading`、`refreshBranchesAfterRepositorySelection`、`readCurrentBranchOption`、`resolveBranchSelection`、`loadVirtualCommits`、`withCurrentBranchVirtualCommitPresence`、`reloadAfterRepositorySelection`、字段 `branchesRefreshGeneration`/`selectRepositoryAbortController`，以及 `refreshInternal` 三处分支写入与四处越权 `branchesLoading:false`。新增 `reloadCommitsForBranchSelection`（只读控制器分支、只管提交）与 `onSelectedBranchesChanged`。
  - `refreshCurrentHeadBranch` 只剩委派 `branchesController.reload()`；但 reload 后分支名常未变而不 fire 事件，HEAD 已移动却不重载提交，故显式补调一次 `reloadCommitsForBranchSelection`。
  - `virtualCommits` 留 Provider 但**不能回写控制器**：新增 `workingTreePresence` 字段 + `branchesWithVirtualCommits()` 只在 `pushStateToWebview` 帧内临时合成（文档第 10 节已声明这条代价）。
  - Store 清理：`AppState` 删 `branches`/`selectedBranches`/`hasBranchSelection`/`branchesLoading`；`StoreEffect` 的 `reloadBranches` 改为 `selectBranches{names}`，`dispatch` 只做类型过滤后转发，校验归口控制器。
- 二次优化 `doc/gitbranchscontroller.md`（对齐用户把 `selectedRepoList` 改成 `GitRepositoryOption[]` 后的 `gitrepocontroller.md`，并补回用户删掉的第 0 节首句、第 7 节代次条目、断档的第 8/11 节编号）。核心新增论证：
  - **`GitRepositoryOption` 不能作 Map/Set key**。`toOptions` 每次调用都 `records.map(record => ({...}))` 返回全新对象字面量；`mergeIntoTotal` 每批子模块调一次并 `merged[index] = option` 原地替换，`converge` 又整体重建，故同一仓库一次扫描内会有多个实例。控制器自己已承认这点——`converge` 三处都 `options.find(item => item.path === ...)` 重新绑定。Map/Set 按引用判等，用对象作 key 会导致扫描完成后同一仓库变两个 key，已加载分支查不到、`loadingRepos.delete` 删不掉、加载态永久残留。结论：内部一律以 `option.path`（`vscode.Uri.file(rootPath).toString()`，跨批次稳定）为 key。
  - `samePaths` 是**按下标逐项比较**，依赖两侧有序。`converge` 里两侧都过 `sortOptions` 故成立，但 `selectRepositories` 按用户传入顺序构建 `next`，控制器侧不能照搬，需先按 path 排序再逐项比。
  - `converge` 已用 `samePaths` 在路径一致时静默返回（新增的元数据同步分支），这层保护可依赖但不能作唯一防线：`selectRepositories` 与 `runScan` 两处 fire 都不经过它。
  - `selectBranches` 入参定为**分支名而非 `GitBranchOption`**（与 `selectRepositories` 收对象不同）：分支名本身是稳定标识，下游 `getGitCommits` 直接消费名字，传对象反而撞上身份不稳定问题。
  - 新增第 7 节「状态所有权」独立成节，把 `branchesLoading` 被 `refreshInternal` 四处越权置 false 导致「偶现只剩当前分支」的完整时序写进文档，并升格为通则：成对的「数据字段 + loading 标记」必须同一所有者。
  - 第 8 节明确**分支控制器与仓库控制器的差异**：仓库靠「同一时刻只有一次扫描」免掉代次，分支做不到（多仓库并行天然多在途 + 切仓库必须立刻发新请求而非丢弃），故代次机制必需，不能照搬简化结论。同时补入「不接受外部 AbortSignal」（曾给 `refreshBranchesAfterRepositorySelection` 加 signal 是错的，等于把刚与 `refreshGeneration` 解耦的逻辑绑回 refresh 生死）。
  - 第 4 节第 1 步补充：只删「不再入选」的条目，仍在选且已加载的仓库条目必须原地保留（多选 2→3 个仓库时前 2 个不该重载）；验收场景同步加该用例与「hasSubmodules 翻 true 使选项对象被重建」用例。
- **`GitRepositoryOption` 改为不可变值对象（文档新增 1.1 节 + 代码落地，`tsc --noEmit` 通过）**：
  - `src/types/git.ts` 四个属性全部加 `readonly`，新增 `copyRepositoryOption(source, changes)`（唯一改属性入口，返回新对象）与 `equalsRepositoryOption(left, right)`（属性全同即同一对象）。`types/index.ts` 已 `export *`，无需改。
  - 控制器内所有判等改走 `equals`，禁用 `===`：`sameOptionList` 替代原 `samePaths`（原先只比 path，会漏掉 `hasSubmodules` 变化）；`selectRepositories` 校验 2 改 `equals` 逐项比较；`mergeIntoTotal` 先 `equals` 判断，有变化才 `copyRepositoryOption` 造新对象替换。
  - 新增 `applySelected()` 与 `applyTotal()` 对称，两者都先 `sameOptionList` 判断再 fire。三处原先手写 `this.selected = x; fire(...)` 的地方（`runScan` 默认选中、`selectRepositories`、`converge`）全部收敛到 `applySelected`，消除了此前 `converge` 里「路径同但元数据变则静默同步不通知」的特殊分支——该分支在值判等下已不需要。
  - 文档 1.1 节记录三条连带要求：数组元素一经创建不可改（改用 copy 替换）、规则 4.3/7 判等用 equals、copy 后属性全同视为同一对象不触发通知。验收表补两行。
- **纯净性复审（用 `git show HEAD:` 取旧版逐条比对）**：发现并清除唯一一处真实移植——控制器的 `sameOptions()` 是 Provider 已删除的 `hasSameRepositoryOptions()` 的逐字节复制，其存在理由（Store 全量重推的去重）在事件驱动模型下不成立，已删除，`applyTotal` 改为直接 fire。同时删掉 `converge` 里 `hasSubmodules` 那条 4.5 分支：按路径重映射到新 options 已天然拿到最新 `hasSubmodules`，该分支永远得到与重映射相同的结果，属死代码。保留的 `repoKey`/`RepoRecord`/`sortOptions` 与旧 `gitLogProvider` 同形但属 git 语义常量（Windows 大小写不敏感、subrepo 排后），非 Provider 编排逻辑。确认控制器不 import store/webview/gitLogProvider，无 postMessage、无 generation、无 AbortSignal、无 branch/commit 引用。
- **落地 `GitRepoController`（`src/git/gitRepoController.ts` 新建，`npx tsc --noEmit` 通过）**，按用户要求「禁止 Provider 处理 repo 逻辑、禁止把 Provider 原有边界移植进控制器、直接废弃」：
  - 控制器自持 `total/selected/isScanning/hasUserSelection/currentRepo`，`selectedRepoList` 按最终文档定为 `GitRepositoryOption[]`（非 path 数组）。对外仅 `initialize()/rescan()/selectRepositories()` + `ontotalRepoListChanged/onSelectedRepoListChanged/onScanningChanged`。`scanning` 需要可观测才能驱动仓库加载态，故额外加了 `onScanningChanged`（文档接口未列）。
  - `runScan` 第一件事同步置 `isScanning=true`（在任何 await 之前），据此免掉 generation；`finally` 无条件置回 false，异常时 `total` 保留已发现部分。不接受任何外部 AbortSignal。
  - 自行实现 `resolveWorkspaceRepositories` + 逐层 `scanSubmodules`（`git rev-parse --show-toplevel` / `git config --file .gitmodules`），未复用 `gitLogProvider` 的 `getGitRepositories`。未初始化的子模块判据：`rev-parse` 结果与候选路径不相等（会落到父仓库根）。
  - 增量只走 `mergeIntoTotal`（只追加或原地更新，绝不清空/收缩）；`converge` 才做已选项剔除与 4.4/4.5 分流。`converge` 中路径相同但元数据变化时静默同步不 fire，避免白跑下游加载。
  - **废弃并删除**（未移植）：`gitLogProvider` 的 `getGitRepositories`/`getGitRepositoriesInternal`/`invalidateGitRepositoriesCache`/`repositoriesCache`/`getWorkspaceSourceKeys`/`getTopLevelRepositories`/`toGitRepositoryOptions`/`isNestedRepository`/`getFirstRepoPath`；Provider 的 `keepSelectedRepositoryOption`/`hasSameRepositoryOptions`/`hasSameSelection`/`refreshSelectors`/`startInitialRepositoryLoad`/`publishInitialRepositorySelection`/`refreshRepositoriesAfterRepositorySelection`/`loadInitialBranchCommits`。注意 `awaitWithAbort` 被 commit/branch 路径复用，误删后需恢复。
  - `AppState` 移除 `repositories/selectedRepositoryPaths/hasRepositorySelection/reposLoading` 四个字段；Store 的 `selectRepositories` case 退化为纯类型过滤转发，不再改状态。Provider 三个 getter 改为只读代理控制器，`pushStateToWebview` 的 `reposLoading` 取 `repoController.scanning`。
  - Provider 侧仅剩：`requestRepositoryScan()`（首轮 initialize、其后 rescan，不 await 不传 signal）与 `onSelectedRepoListChanged()`（推状态 + 发 `initialSelection` + 触发分支/提交加载）。`refreshInternal` 不再 await 仓库扫描，无选择时直接 return，等控制器事件驱动；首轮默认选中与用户切仓库合并为同一条下游路径。`gitActions` 的 `invalidateGitRepositoriesCache` 调用一并删除。
- 首版重写 `doc/gitrepocontroller.md` 的新增内容：`hasUserSelection` 标记（区分默认选中与用户显式选择，否则无法实现「用户改动后不随 totalRepoList 变化」）；禁止扫描期间清空 `totalRepoList`/`selectedRepoList`；扫描结束收敛规则明确「仅最终结果参与已选项剔除，中途增量结果不参与」，并指出现有 `keepSelectedRepositoryOption`（562-569）行为与需求相反——它把缺失的已选项塞回列表，需求要求移除；`scanning` 标记所有权约束；控制器不接受外部 AbortSignal + 自有 `scanGeneration` + 在途去重；控制器不得 postMessage、调用方不得回写仓库状态；6 条验收场景。
- 完成 HEAD 提交变化链路：`buildGraph` 与颜色池从 `gitLogProvider` 移入 `GitCommitController` 私有实现；`GitBranchesController` 比较全量刷新前后 current 分支 hash，新增 `onBranchHeadCommitChanged`；`GitCommitController` 构造时订阅该事件，维护按 repository path 索引的 `branchHeadCommitMap`，并通过 `forceRefresh` 重读提交，在途事件用 `pendingForceRefresh` 合并补跑。
- `GitkViewProvider` 改为 `new GitCommitController(branchesController)`；继续监听 `onVirtualCommitChanged` 推状态，并在 Provider 合成及 Webview 渲染两层保证只有 HEAD 分支被勾选时才显示 Changes / Staged 两个虚拟提交。同步更新 `doc/gitbranchscontroller.md`、`doc/gitcommitcontroller.md`；`npm run compile`、`git diff --check` 通过，git 目录无诊断错误，Provider 仅有既有未使用 import/getter hints。
- 按追加要求调整提交控制器事件顺序：每次 `searchedCommitList` 更新并 fire `onSearchedCommitsChanged` 后，立即用当前 `virtualCommitMap` 快照重发 `onVirtualCommitChanged`；分支变化流程随后读取工作区最新状态并可能再次 fire。同步更新 `doc/gitcommitcontroller.md`，编译、diff check 与该文件诊断均通过。
- `GitBranchesController.selectRepositories` 默认选择规则改为：并行读取所有入选仓库 current 分支（已有分片也读），汇总追加到 `selectedBranches`，按 `repository.path + branch.name` 去重，并统一、无条件 fire 一次 `onSelectedBranchesChanged`；无分片仓库仍先写单条快路径分片。删除分支控制器的 `hasUserSelection`，同步重写 `doc/gitbranchscontroller.md` 相关规则；编译、diff check 与诊断通过。
- 按用户新改的分支控制器契约完成 Map API：`selectedBranches` getter 返回防御性 `ReadonlyMap<GitRepositoryOption, GitBranchOption>`；`onSelectedBranchesChanged` 发送按仓库分组的 `Map<GitRepositoryOption, GitBranchOption[]>`。`GitkViewProvider` 从 getter values 生成 Webview 名称列表，并把事件 map 展平后传给 `GitCommitController`；同步修正文档状态表与接口。编译、diff check、分支控制器诊断通过，Provider 仍仅有既有 hints。
- Map 对象键设计结论：JavaScript 原生 `Map` 对对象只按引用（SameValueZero）判等，项目的 `equalsRepositoryOption` / `equalsBranchOption` 不会参与 Map 去重。仓库业务身份应使用 `repository.path`，分支业务身份应使用 `repository.path + branch.name`；若外部契约保留对象键，只能用控制器持有的规范对象构造快照，不能保证调用方用属性相同的新对象 `get` 命中。
- 修复 `selectBranches` 包含 current 分支但虚拟提交消失：根因是 `selectedBranches: Map<Repo, Branch>` getter 每仓库只保留第一条，多选顺序为普通分支在前、current 在后时 Provider 判定 HEAD 未勾选。Provider 改为缓存完整 `onSelectedBranchesChanged: Map<Repo, Branch[]>` 防御性快照，Webview selected names、checked HEAD 与 `branchesWithVirtualCommits` 全部从该快照读取；事件处理先更新快照再推帧。编译与 diff check 通过，仅保留既有未使用 hints。
- 分支状态所有权再调整：`virtualCommitMap`、`branchHeadCommitMap`、`onVirtualCommitChanged`、工作区 presence 刷新全部迁入 `GitBranchesController`，内部按 repository path 存储；`GitCommitController` 删除这些 map/事件/刷新方法，仅监听 HEAD 事件做 `forceRefresh`，并在 searched list 变化后调用分支控制器 `notifyVirtualCommitChanged()` 保留重发约定。`GitkViewProvider` 全部改读/订阅分支控制器。
- `GitBranchesController.selectBranches` 内容变化时先同步置 loading 并 fire `onBranchesLoadingChanged(true)`，再发布选择和读取虚拟状态，finally fire false；已有分支加载在途时统一返回 false，避免 loading 竞争。同步更新两份控制器文档；`npm run compile`、`git diff --check` 和两个控制器诊断通过，Provider 仅有既有 hints。
- 清理迁移后的冗余：彻底删除无消费者的 `branchHeadCommitMap`/`branchHeads`/转换 helper，HEAD 事件改成同轮合并的 `Event<void>`；虚拟 entry 删除重复 repository 字段。`GitCommitController` 不再持有/订阅 `GitBranchesController`，Provider 成为唯一协调者：HEAD 通知调 `forceRefresh`，searched 通知调分支控制器 `emitVirtualCommitChanged`。相关 Webview 推送统一走 microtask 合并，删除旧桥接命名和全部残留文档；编译、diff check、控制器诊断通过，Provider 仅有既有 hints。
- 完成虚拟 map 去除：删除 `virtualCommitMap`、`VirtualCommit`、`onVirtualCommitChanged`、`reloadVirtualCommits`、`emitVirtualCommitChanged` 及 Provider 工作区轮询。`GitBranchOption` 新增 `hasChangeFiles` / `hasStagedChangeFiles`，并纳入值判等；`GitBranchesController` 在当前分支上以 immutable copy 更新这两个标记，通过既有 `onBranchesMapChanged` 发布。控制器内部监听工作区文本编辑，并为入选仓库 `.git/index` 建 watcher，300ms 防抖刷新标记；Provider 只根据已勾选 current 分支的标记临时合成虚拟行。同步更新两份文档；编译、diff check、分支控制器诊断通过，Provider 仅留原有未使用 `searchKeywords` hint。
- 诊断 selectRepositories 期间 selectedBranches 闪现旧值：refresh 先替换 repositories、prune 分支并 fire `onBranchesMapChanged`，但不清理 selected；Provider 因此先按缓存的旧 selectedBranchesMap 推一帧。随后快路径在 await 后以 `[...this.selected, ...defaults]` 追加并 fire selected，旧选择还会混入新默认选择。要消除闪烁与语义残留，仓库选择变更必须在首次 branches event 前原子重置/过滤 selected，并立即 fire 新选择快照，再开始异步快路径。
- GitLogProvider 缓存移除完成：删除 branch cache/in-flight/version、commit page cache/in-flight/version、commit-file cache/in-flight 及 `invalidateGitRefsCache`；`getGitBranches`、`getGitCommits`、`getCommitFiles` 每次直读 Git CLI。删除 Provider 和 GitActionRunner 的失效调用，刷新改为直接重读控制器状态；更新分支控制器文档。`npm run compile` 与 `git diff --check` 通过，GitLogProvider 无诊断错误。
- 提交列表加载阶段提示已补齐：`GitkViewProvider` 将仓库控制器扫描、分支控制器加载和提交控制器加载事件分别映射为“正在获取仓库...”“正在获取分支...”与“正在加载历史提交列表...”，统一通过既有 `loadingProgress` 投递到提交列表区域；`npm run compile` 通过。
- 虚拟提交状态刷新调整：Gitk 面板可见且存在已选仓库时，Provider 调用 `GitBranchesController.refreshVirtualCommitStates()`；每轮 `git status` 状态读取完成后等待 1 秒再发下一轮，使用代次门禁避免隐藏或切换后的旧异步回调重启轮询。点击 `Changes` / `Staged Changes` 不再按缓存标记拒绝，始终直接调用 `getWorkingTreeChanges()` 从本地读取，避免过期状态造成文件缺失；`npm run compile` 通过。
- 提交列表初始化与选择切换状态提示补充：`onSelectedRepoListChanged` 立即显示“正在获取选中仓库...”（无选择时为“正在获取仓库...”），`onSelectedBranchesChanged` 显示“正在获取分支信息...”；随后已有提交控制器加载事件覆盖为“正在加载历史提交列表...”。三者都复用 `loadingProgress`，`npm run compile` 通过。
- 虚拟提交状态轮询职责已迁回 `GitBranchesController`：新增 `setVirtualCommitStatePolling(enabled)`，控制器内部持有轮询开关、定时器和代次；启用后立即读取 current 分支的工作区/暂存区状态，读取结束后等待 1 秒再继续。禁用或 dispose 会取消定时器并使旧回调失效。`GitkViewProvider` 只按“面板可见且存在已选仓库”传启停条件，不再持有定时器或调度读取；`doc/gitbranchscontroller.md` 已同步，`npm run compile` 和 `git diff --check` 通过。
- 修复仓库/分支选项变化时提交列表仍显示空历史：Webview 的 `stateUpdate` 以 `state.isLoading` 决定是否隐藏 `commitList`，但原先该值只来自 Provider Store，可能被旧刷新流程置 false，而仓库/分支控制器仍在加载。Provider 现在聚合 `store.isLoading || repoController.scanning || branchesController.isLoading || commitController.isLoading`，并按提交→分支→仓库优先级生成文案，空提交时会显示对应状态而非“暂无历史提交”；`npm run compile` 通过。
- 修复仓库/分支交互后 loading 迟显：`showLoadingProgress` 原先只有 `commits.length===0` 才显示加载层，有旧提交时即使收到阶段事件也不覆盖列表；同时 UI 要等待 Webview→扩展端→控制器事件→Webview 的异步往返。现改为加载阶段始终隐藏提交列表并显示进度层，且仓库 radio change、分支确认按钮在发送消息前同步调用 `showLoadingProgress`，分别显示“正在获取选中仓库...”和“正在获取分支信息...”；`npm run compile` 通过。
- 追查“加进度后变慢”的实际根因并优化：进度 DOM 本身不阻塞，暴露的是原数据链路耗时。`GitBranchesController.selectBranches` 原先把 `git status --porcelain` 纳入 `isLoading` 收尾，工作区扫描会阻塞分支选择完成；现选择事件发出后立即结束 loading，虚拟状态交由内部 watcher/轮询异步维护。`GitCommitController.refresh(true)` 原先关键字为空也连续执行两次相同 `git log`（searched、total 各一次）；现只读一次，`total` 通过逐对象复制隔离引用。两份控制器文档已同步，`npm run compile`、`git diff --check` 和控制器诊断通过。
- 按状态所有权移除仓库/分支异步加载在提交列表中的提示：`stateUpdate.isLoading` 现在仅取 `commitController.isLoading`，仓库扫描与分支加载继续通过各自选择器的 `reposLoading` / `branchesLoading` 展示；移除了控制器事件、选择回调和 Webview 点击处向提交列表发送 repository/branch loading 的重复写入。实际提交历史开始读取后仍显示“正在加载历史提交列表...”。显式切换 Git 当前分支的操作进度不属于异步加载列表，保留原提示。`npm run compile` 与 `git diff --check` 通过。
- 进一步明确“异步加载列表”与“提交数据源切换”的区别并修复空窗：仓库点击时 Webview 同步将分支选择器置为 loading，解决等待 Webview→Extension 消息往返才出现“加载分支”的延迟；Provider 新增 `selectionLoadingPhase` 覆盖仓库/分支选择到 CommitController 真正 loading 之间的状态空窗。提交列表依次显示“正在获取当前仓库...”→“正在获取当前分支...”→“正在加载历史提交列表...”，因此不会短暂显示“暂无历史提交”。无效或被控制器拒绝的选择会立即清除阶段，防止假 loading。`doc/gitbranchscontroller.md`、`doc/gitcommitcontroller.md` 已同步，`npm run compile` 与 `git diff --check` 通过。
- 确认虚拟提交状态轮询已是非阻塞异步串行轮询：`setVirtualCommitStatePolling` 通过 `void pollVirtualCommitStates()` 立即返回；每轮 `await getWorkingTreeChangePresence` 完成后才等待 1 秒开启下一轮，不使用 `setInterval`，不会重叠。轮询增加 `try/finally`，确保异常不产生未处理 Promise rejection，并在仍启用时继续调度；编译、格式检查和控制器诊断通过。
- 用户随后要求完全移除虚拟提交状态轮询：已删除 `GitBranchesController` 的 polling 开关、timer、generation、`refreshVirtualCommitStates`、`setVirtualCommitStatePolling`、`pollVirtualCommitStates`，并删除 Provider 按面板可见性启停轮询的调用。Changes/Staged 状态改为纯事件驱动：工作区文本变化、选中仓库 `.git/index` watcher、分支选择后的异步刷新，继续共享 300ms 防抖和实际变化才发布的逻辑。`doc/gitbranchscontroller.md` 已同步；编译、格式检查和控制器诊断通过。
- `GitBranchesController.selectRepositories` 并发策略调整：当传入仓库集合与当前快照不一致时，不再因 `isLoading` 丢弃，而是先 abort 上一轮“获取当前分支”和“获取分支列表”，再创建新内部 AbortController 与 generation 执行新仓库流程。信号透传 `getCurrentGitBranch`、`getCurrentGitHeadHash`、`getGitBranches`；每个 await 阶段和旧流程 finally 均校验 generation，旧结果不落地且不能关闭新流程 loading。`forceRefresh` 仍在 loading 时丢弃。`doc/gitbranchscontroller.md` 并发章节与边界表已同步；编译、格式检查和控制器诊断通过。
