# 2026-09-02

- 修改 `src/webview/gitkViewProvider.ts`：启动时若 Gitk Webview 已可见，先记录 Diff 展示意图；默认提交选中后立即创建并显示 Diff panel，不再等待 Diff 文件数据加载完成，面板通过现有 Store 快照订阅显示加载过程。编译与 lint 均通过。
- 首次初始化卡死的精确根因：`GitkViewProvider` 已订阅 `repoController.onSelectedRepoListChanged`，却又在 `ontotalRepoListChanged` 回调中手动调用 `onSelectedRepoListChanged(this.repoController.selectedRepoList)`。子模块扫描更新总仓库列表时，这条重复调用会无条件取消 Diff 请求并重置 `currentHash`、`filesLoading`、`diffLoading`；但 `GitCommitController.selectRepositories()` 发现仓库选择未变后直接返回，未重新触发默认提交选择与 Diff 读取，因此 Changed Files 和 Diff panel 永久停在加载态。已移除总列表回调中的重复调用，仅让真实选择事件驱动该生命周期；lint 和 `npm run compile` 均通过。