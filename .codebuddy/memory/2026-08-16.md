# 2026-08-16

- 审查截图差异：`kBorderWidth` 由 `3.0f` 变为 `1.0f`，移除 `kTextureSize = 128`，并新增 `kFillAlpha = 0x10`；当前 vscode-gitk 工作区未找到对应 C/C++ 源码，结论仅基于截图，需在实际工程确认常量使用点。
- 用户期望 SingleDiffPanel 的行级对齐：旧文件首行删除应独立红色高亮，随后旧 `kBorderWidth=3.0f` 与新 `kBorderWidth=1.0f` 应配对显示为修改行；需要按内容相似度跨越删除行配对，而不能将连续 delete/insert 按数组索引硬配。
- 已将 `GitkViewProvider` 的单仓库切换改为复用初始化流程：先发布选中仓库/当前分支并加载首屏提交，再后台补全仓库、子模块与完整分支选择器；移除了原切换专用的 `publishCurrentBranchLoading` / `loadBranchCommitsNow` 重复路径。`npm run compile` 通过。
- 后续需处理切换流程边界：空仓库或首次提交前无 HEAD 时，`loadInitialBranchCommits` 无提交即返回且后台补全只在有提交时关闭 loading，可能导致持续加载；首屏提交读取失败也有同类收尾问题。多仓库切换当前回退到完整 `refresh(false, true)`，语义不同于单仓库的快速路径。
- 已修复空仓库和首屏提交加载异常的收尾：`loadInitialBranchCommits` 显式区分 ready/empty/failed/cancelled，空分支或空提交时清空分页并结束 loading，异常时记录 `commitPageError` 并结束 loading；`refreshSelectorsInBackground` 无论提交结果均完成收尾，选择器补全失败也结束主 loading。`npm run compile` 通过。
- 仓库切换的 `publishRepositoryCurrentBranch` 已改为接收明确的 `GitRepositoryOption`；`selectRepositories` 以 `paths[0]` 精确查找目标仓库并先发布该仓库和其 HEAD 分支，避免因状态变化误填其它仓库的默认分支。`npm run compile` 通过。
- 仓库切换先同步目标仓库、清空旧分支并设置 `branchesLoading`，防止 HEAD 查询期间显示前一仓库分支；HEAD 查询失败时仅记录告警并返回，由调用方继续执行 `refreshSelectorsInBackground` 完整加载重试，避免流程中断和 loading 卡住。`npm run compile` 通过。
- 已确认“未选择分支”的潜在根因：`getCurrentGitBranch` 捕获所有异常并返回 undefined，误将真实读取失败当作 detached HEAD；切换流程用 `Promise.all` 同时读取分支名和 HEAD hash，任一 `rev-parse HEAD` 失败都会丢弃已经取得的分支名，界面因此暂时或持续保持空 `selectedBranches`。后续修复应仅将 detached HEAD 降级为空，并将分支名与 hash 独立处理。
- Diff 面板系列改动: SingleDiffPanel 定稿为独立 WebviewPanel+Monaco diff (词级高亮 `gitk-word-diff` 主题, 整行/gutter 底色 #00000000), MultiDiffPanel 为纯委托包装; 数据链 gitkViewProvider.loadDiffData → DiffReader.prepare 分批写 store (files+diffLoading=false) → 面板订阅渲染; webview 消息经 gitkQueue 中转 + ready/monacoReady/pending 三重兜底。
- Monaco 0.52.2 min 分发关键结论: media/monaco/vs 与 npm min/vs 完全一致; `vs/css` 插件与 `vs/nls.messages-loader` 内嵌在 editor.main.js 里 (define 模块, 非 vs/css.js 独立文件), css 插件用 `<link>` 注入而非 XHR, 当前 CSP 不拦 — Monaco 加载链排除 CSP 嫌疑。
- `正在读取 Diff 数据 (7/7)` 的决定性结论与修复: 7/7 证明最后 batch 已完成，非 Git I/O 卡死；prepare 原来先单独 set diffProgress=N/N (仍 diffLoading=true)，再 set files+diffLoading=false，SingleDiffPanel 因 progress 订阅会先 post loading，webview 异步消息可令旧 loading 覆盖后续 render。现最后 batch 不再单独发布 N/N，而是原子 set `{files,data,diffLoading:false,diffProgress:N/N}`；DiffReader.stop 同步 reset `diffLoading:false, diffProgress:0/0`，因为失效的 prepare 会静默 return。tsc/lints 通过。
- 「正在读取 Diff 数据」根因进一步收敛并修复: SingleDiffPanel 仅在 store.diffLoading=true 显示该文案; loadDiffData 先置 true 再 await DiffReader.prepare; readGitObjects 的 `git cat-file --batch` 原来只等 error/close 且无 deadline, 任一子进程/FS Promise 不结算即永久卡 loading。现 DIFF_READ_TIMEOUT_MS=15s: cat-file 加单次结算 finish + timeout kill/reject, workspace.fs.readFile 经 withTimeout 包装; prepare catch 自动置 diffError + diffLoading=false。切换 commit/staged/changes 时同步复位 diffLoading/error/progress, 避免旧请求状态污染新文件清单阶段。保留 [gitk-diff] 诊断日志和 progress 数字。tsc/lints 通过。
- 「正在读取 Diff 数据」仍卡死 (第三次): 静态分析无法定位, 全面注入可观测性 — diffReader.prepare 记 start/done/superseded(mid/end/error); loadDiffData 记 enter/finish; SingleDiffPanel 新增 diffProgress 订阅, loading 文案带 (completed/total); webview 阶段回报 (inline scripts ready/monaco loaded/monaco error 含 pluginId:moduleId/render ok/error) 经 {type:'log'} 转发到扩展 console ([gitk-diff] 前缀, Debug Console 可见)。排查方法: 看日志链在哪一环断掉。教训重申: 外层模板字符串内严禁再写反引号模板 (TS1005), 嵌入 JS 只用单引号拼接。
- SingleDiffPanel 配色重构: 废弃「整行透明 #00000000 + 词级继承 monaco 默认」方案, 改为 varMap 全量映射 VS Code 当前主题 CSS 变量 (editor 基础色/行号/选中/缩进 guide/minimap/滚动条 + diffEditor 整行/词级/边框/gutter/概览标尺/diagonalFill/unchangedTextBackground 共 27 项), gutter 空值 ??= 继承整行色, 空值跳过保留 fallback, 主题定义失败仍回落 vs/vs-dark。tsc 通过。
- 修复「正在读取 Diff 数据」卡死 extension 侧根因: DiffReader.prepare 被打断时静默 return 不复位 diffLoading, 而 beginCommitReload (切仓库/刷新) 的 batch 漏复位 → 已补 diffLoading:false + diffError/diffProgress 复位; 同步更正过时注释 (cancelPending 早已不设 diffGeneration); webview 侧 gitkQueue drain 后 push 置 no-op 防消息堆积。customDiffPanel.ts 为死代码 (无引用), diffGeneration 无人递增形同虚设, 可择机删除。tsc 通过。
- `src/webview/customDiffPanel.ts` 的 `appendChanged` 已增加 token Jaccard 相似度（阈值 0.5）与保序动态规划配对：多行 replacement 中不相似的前置删除/新增拆为独立 hunk，只将相似行渲染为 `modified-*`；实现截图所需的纯删除后接修改行。`npm run compile` 与该文件诊断均通过。
- 已按用户要求撤回上述 token 相似度/动态规划配对方案，`appendChanged` 恢复为原先按 LineRangeMapping 处理 replacement 的逻辑；文件诊断与 `npm run compile` 通过，待选定替代方案后再实现。
- 验证方案一的数据链路后确认：`DiffReader` 可新增 `git diff -p` / `git diff-tree -p` 获取 unified patch 并透明传入 `DiffPayload`，但 Git hunk 仅包含 context、删除、增加行，并不表达“修改行”；截图会被分类为两条删除与两条新增，不能单凭方案一满足“首行删除、次行修改”。如继续需叠加声明名/相似度配对规则。
- 已在 `src/webview/customDiffPanel.ts` 接入 Patience diff：为区间内双方各仅出现一次的规范化行构建候选，以 LIS 筛出保序锚点，各锚点间回退现有 Myers；`emitRegion` 改为经 Patience 生成操作再走原有优化、行内高亮与渲染。该方案改善重复行/大块代码插入时 hunk 稳定性，不单独保证截图中的修改配对。诊断与 `npm run compile` 通过。
- 已按用户反馈撤回 Patience diff：删除 `patience()`，`emitRegion` 恢复直接调用 `myers()`；文件诊断与 `npm run compile` 通过。
- `src/webview/customDiffPanel.ts` 的 `appendChanged` 已实现保守顺序拆分：当多行 delete/insert replacement 的删除末行和新增首行可由现有 `canRefineWordDiff` 细化时，前置删除、该修改对、后置新增分别输出；用于将截图中的独立删除、`kBorderWidth` 修改和独立新增分开显示。诊断与 `npm run compile` 通过。
- 用户要求不影响原流程，已撤回上述顺序拆分；`appendChanged` 恢复为原有 LineRangeMapping 分类，Myers、hunk、映射、统计和移动行检测均未改动。文件诊断与 `npm run compile` 通过。
- 已安装 `diff2html@3.4.56` 并新增 `src/webview/singleDiffPanel.ts`：面板订阅现有 Store，仅从选中文件的 original/modified 用 `createTwoFilesPatch` 构造单文件 patch，再以 diff2html side-by-side HTML 渲染；未改动 DiffReader、Store、Myers 或 CustomDiffPanel 的算法。`gitkViewProvider.ts` 的面板实例切至 SingleDiffPanel，沿用 show/cancel/hide 和文件选中回调；`npm run compile` 通过。
- 已将 `singleDiffPanel.ts` 重命名为 `multiPagePanel.ts`，类、Webview 标识及 GitkViewProvider 字段同步为 `MultiPagePanel` / `multiPagePanel`。diff2html CSS 变量映射至 VS Code Webview 主题变量（编辑器背景、边框、行号、git added/deleted、diff 插入/删除与未改区域），主题切换自动继承。`npm run compile` 与文件诊断通过。
- 已将 `multiPagePanel.ts` 重命名为 `multiDiffPanel.ts`，类、Webview 标识及 GitkViewProvider 字段同步为 `MultiDiffPanel` / `multiDiffPanel`。数据链路已确认：`loadDiffData` → `DiffReader.prepare` 写入完整 `DiffPayload` 到 `store.files` 且 `diffLoading=false` → MultiDiffPanel 的 Store 订阅调用 `renderCurrentDiff` → `postMessage(render)`；仅当 `show()` 设置的 selectedPath 在完成数据中存在时渲染。`npm run compile` 与面板诊断通过。
- MultiDiffPanel 已固定为 diff2html side-by-side 双栏：新增“原始文件/修改后文件”栏头，grid 双栏、独立滚动区、分隔线和行号 gutter；插入、删除、行内高亮、空白占位、背景和边框均映射 VS Code 主题变量。未改动数据读取链路；`npm run compile` 与面板诊断通过。
- MultiDiffPanel 的字号、字体、字重、行高、行号 gutter 宽度、双栏最小宽度、栏头高度和代码行高度现均从 VS Code 编辑器变量派生；移除固定 720/640px 布局阈值，颜色持续继承 VS Code 主题。`npm run compile` 与文件诊断通过。
- 已重新抽离 `src/webview/singleDiffPanel.ts`：`SingleDiffPanel.render(DiffPayload)` 独立构建安全转义的单文件标题和 diff2html side-by-side HTML。`MultiDiffPanel` 现收集 Store 中全部完整 `DiffPayload` 并组合多个单文件区块连续渲染，仍按 selectedPath 滚动定位。代码行改为 `pre-wrap` 与内容自适应高度；渲染后将 diff2html 的 `+/-` 前缀移到行号单元格末尾。`npm run compile` 及两文件诊断通过。
- MultiDiffPanel 的每个 SingleDiffPanel 现有 VS Code 风格交互标题栏：显示文件状态、路径与行数净变化，点击标题同步 Change Files 选中状态；点击右侧（修改后）行号会按仓库 root URI 打开工作区文件并定位到对应行。`npm run compile` 和相关文件诊断通过。
- 已修正 MultiDiffPanel 的双栏布局：移除按视口计算的双栏最小宽度和 `min-width:max-content`，两栏改为 `repeat(2, minmax(0,1fr))` 与固定表格布局，避免左栏被挤出；移除固定/最小代码行高，保留内容自适应换行，并在 Webview 渲染和窗口 resize 后同步同索引左右行的最大实际高度，使两侧对齐。`npm run compile` 与面板诊断通过。
- 架构结论：单文件的 diff2html DOM 改写、双栏布局、标题栏、行高/前缀/行号交互应下沉至 SingleDiffPanel；MultiDiffPanel 只应管理面板生命周期、Store 聚合、消息分发和当前文件定位。后续宜将 SingleDiffPanel 从静态 HTML 字符串渲染器升级为带 HTML/CSS/客户端初始化片段的独立单文件视图组件。
- 已实现上述拆分：`SingleDiffPanel.clientScript()` 现在封装单文件双栏栏头注入、`+/-` 行号后移及左右行高同步；MultiDiffPanel 的 Webview 仅注入该组件脚本、聚合多文件内容、绑定跨组件消息和定位当前区块。`npm run compile` 及两文件诊断通过。
- 修复 MultiDiffPanel 每行前后异常空白：diff2html 容器含模板换行/缩进文本节点，容器的 `white-space: pre-wrap` 会将其渲染为可见空白；现仅将 `pre-wrap` 应用到 `.d2h-code-line-ctn` 实际代码内容，容器恢复 normal white-space。保留行内代码空格与长行自动换行；诊断和 `npm run compile` 通过。
- 为接近 VS Code 内置 Diff Editor 默认排版，MultiDiffPanel 已关闭代码软换行：`.d2h-code-side-line` 与 `.d2h-code-line-ctn` 改为 `white-space: pre`，取消任意断词；单栏代码表改为 `width:max-content; min-width:100%`，通过 `.d2h-code-wrapper` 横向滚动展示长行。保留左右对应行高同步；诊断与 `npm run compile` 通过。
- MultiDiffPanel 已移除每个代码栏的横向滚动：`#diff` 负责唯一的全局底部横向滚动，左右栏采用 `max-content` grid 并允许内容宽度上浮至外层，因此拖动一次会同步移动两栏。`npm run compile` 与面板诊断通过。
- 已移除 `diff2html` 与 `diff`，改装 `monaco-editor@0.52.2` 并将 `node_modules/monaco-editor/min/vs` 复制至 `media/monaco/vs` 供 Webview 本地加载。`SingleDiffPanel` 现仅生成单文件 Monaco DiffEditor 的数据和容器；`MultiDiffPanel` 在 Webview 加载 AMD Monaco、为全部 DiffPayload 创建只读并排 DiffEditor、保持标题选择和修改侧行号跳转。CSP、worker URL 及 Monaco 就绪前的渲染缓存均已处理；`npm run compile` 与面板诊断通过。
- 修复 MultiDiffPanel 一直显示“正在准备 Diff”：首次 `show()` 在 Webview 注册 message 监听之前下发 render，消息会丢失。扩展侧增加 `webviewReady`，Webview 注册监听后 `postMessage({type:'ready'})`，收到握手后再下发当前 Store diff 数据；`npm run compile` 与文件诊断通过。
- 为继续定位 Monaco 仍停在加载态，Webview 已添加本地错误展示：捕获 script error、unhandled rejection、AMD loader 缺失和 Monaco 模块加载失败；另加 8 秒 `editor.main` 初始化超时，直接在面板显示资源路径/CSP 提示，同时仍回传扩展侧。`npm run compile` 与面板诊断通过。
- 进一步修复首次 Webview 消息丢失：在外部 `loader.js` 前以内联脚本注册 `ready` 和 `window.__gitkMessages` 队列，Monaco 初始化脚本随后消费缓存消息；扩展侧不再静默 return，`diffLoading` 显示“正在读取 Diff 数据...”，无完整 DiffPayload 显示 Store files/generation 状态。`npm run compile` 与面板诊断通过。
- 用户反馈 Monaco `editor.main` 8 秒加载超时。已确认本地 `media/monaco/vs/editor/editor.main.js` 存在；修复扩展侧先注册 `onDidReceiveMessage` 再设置 Webview HTML，补齐 CSP 的本地脚本/样式/字体/connect/worker 来源和 Monaco AMD 所需 `unsafe-eval`，并增加 loader 脚本/AMD 模块错误路径显示。`npm run compile` 与诊断通过。
- Monaco 不会自动继承 Webview 的 CSS 主题变量，故此前差异无颜色。已在 `MultiDiffPanel` 读取实际 `--vscode-*` 色值并 `defineTheme('gitk-vscode')` / `setTheme`，显式映射编辑器、gutter、插入/删除行、行内变更、斜纹占位和 overview 颜色；监听 body 主题类/style 变化后重新应用。`npm run compile` 与面板诊断通过。
- 用户提供 worker 错误：Blob worker 内 `importScripts(vscode-resource workerMain.js)` 被跨源策略阻断。已移除 Blob/importScripts 包装，`MonacoEnvironment.getWorker` 直接以 Webview 本地 `media/monaco/vs/base/worker/workerMain.js` URL 创建 Worker，从而保留本地相对模块路径且规避跨源 importScripts。`npm run compile` 与面板诊断通过。
- 修复多文件 Monaco 刷新时 `TextModel got disposed before DiffEditorWidget model got reset`：`disposeEditors` 现先 `editor.setModel(null)`、销毁 DiffEditor，再释放 original/modified TextModel，避免模型仍被组件引用时提前释放。`npm run compile` 与面板诊断通过。
- Monaco MultiDiffPanel 已启用 `diffAlgorithm:'advanced'`、`ignoreTrimWhitespace:false` 与 `renderIndicators:true`，支持高级字符级差异及行内高亮；启用 folding。各 SingleDiffPanel 的 Monaco 容器按 original/modified 最大 `getContentHeight()` 自动撑高，隐藏内部横纵滚动条，改由外层页面统一垂直滚动。差异 fallback 色改为低饱和半透明红绿，优先使用 VS Code diff token；渲染时先显示容器再测量高度。`npm run compile` 与文件诊断通过。
- 已按用户要求收敛为 VS Code 原生 Diff Editor 风格：启用可拖动双栏分隔线、窄宽度自动内联、内部纵横滚动、CodeLens、hover 折叠控件和 overview ruler；恢复固定编辑器视区高度，取消先前的自动撑高/隐藏滚动条配置。高级 diff 与 VS Code 主题映射保留。`npm run compile` 与诊断通过。
- 架构边界确认：VS Code Workbench 原生 DiffEditor 无法嵌入 Webview 的 SingleDiffPanel DOM；若要完全原生的多文件 diff，必须由 MultiDiffPanel 改为调用 Workbench `workbench.input.multiDiffEditor`（`vscode.open`）而非保留 Webview/Monaco。Webview 路径只能使用 Monaco 近似原生行为。
- 继续收敛 Webview Monaco 行为：补齐原生风格的行号最小位数、装饰栏宽、自动折叠策略、缩进导引、选择/括号/空白渲染、滚动条尺寸与阴影、可移动代码显示、超大文件限制及字体连字；保留高级 diff、双栏/窄屏内联、可调整分隔线和主题映射。`npm run compile` 与诊断通过。
- 已核查 `customDiffPanel.ts` 无固定颜色或 CSS 变量定义，配色依赖 VS Code 主题渲染链路。MultiDiffPanel 的 Monaco 主题现统一从 Webview 已解析的 `--vscode-diffEditor-*` token 采样，inserted/removed 的 gutter 和 overview token 缺失时复用对应行色或 Git decoration 色，避免不同 token fallback 造成色彩不一致。`npm run compile` 与诊断通过。
- 已重新分层 Monaco 差异配色：红绿基色改为 VS Code Git added/deleted 前景色，整行块仅 10% alpha、行内字符 28%、gutter 16%，并增加 55% alpha 的行内边框及 overview 色；目标是红绿更可辨而整体对比更低。`npm run compile` 与面板诊断通过。
- 已继续降低 Monaco diff 覆盖感：整行背景由 10% 降至 6.5%，字符级高亮由 28% 降至 22%，gutter 由 16% 降至 10%，边框由 55% 降至 42%；红绿基色保持 VS Code Git decoration 色。`npm run compile` 与面板诊断通过。
- 已改回更接近原生的配色来源：Monaco 直接读取 VS Code 当前主题的 `diffEditor` line/text/textBorder、`diffEditorGutter` 与 overview token；不再以 Git decoration 前景色手工混合背景，仅在 token 缺失时用低饱和 fallback。Monaco advanced 算法继续保证未变更字符不使用 `insertedText`/`removedText` 行内高亮。`npm run compile` 与诊断通过。
- 完成仅限 `SingleDiffPanel` / `MultiDiffPanel` 的重构：SingleDiffPanel 变为纯 `DiffPayload`→`SingleDiffData` 工厂，删除 HTML 拼接、转义和重复语言回退；MultiDiffPanel 定义 Host/Webview 联合消息、封装面板创建/发布/文件跳转，Webview 仅接收结构化 files 后以 DOM API 构建单文件容器并创建 Monaco models。保留多文件 Monaco diff、主题映射、标题选中、右侧行号跳转、ready 队列、模型正确销毁和 Store 生命周期。`npm run compile` 与两文件诊断通过。
- 用户明确后续重点为 SingleDiffPanel 的比对颜色渲染，而非仅数据/生命周期重构；应将 Monaco diff 的块底色、字符级 text 高亮、VS Code token 采样和未变更词不高亮的规则下沉到 SingleDiffPanel，MultiDiffPanel 只保留聚合与实例管理。
- 已完成差异颜色职责下沉：`SingleDiffPanel.monacoThemeScript()` 定义 VS Code token 采样、整行块/字符级 text/gutter/border/overview 配色与主题变更观察；MultiDiffPanel 仅注入脚本并调用 `configureSingleDiffTheme`、`observeSingleDiffTheme`，其编辑器字体配置也复用 `singleDiffColor`。`npm run compile` 与两文件诊断通过。
- MultiDiffPanel 的 Monaco `ignoreTrimWhitespace` 由 `false` 改为 `true`（与 VS Code 原生 diff 默认一致），消除行尾空白差异导致的字符级假高亮；解释了整行字符级高亮的三种来源（纯增删行无配对、advanced 行内 LCS 相似度低整行替换、空白差异）。
- 继续对齐 VS Code 原生 Diff Editor 默认值：`hideUnchangedRegions:{enabled:true,contextLineCount:3,minimumLineCount:3,revealContextCount:20}`（原生默认折叠未变更区域，Monaco 默认关闭）、`diffCodeLens` 改 `false`（原生默认）、`renderSideBySideInlineBreakpoint` 900→9000（原生默认，避免窄面板过早切内联）。`npm run compile` 与诊断通过。
- 再对齐原生 editor 默认值：`minimap:{enabled:true}`、`scrollBeyondLastLine:true`、新增 `renderWhitespace:'selection'`、`renderLineHighlight:'all'`、`guides:{indentation:true,highlightActiveIndentation:true,bracketPairs:false,highlightActiveBracketPair:true}`；SingleDiffPanel 主题脚本补 minimap/slider token 映射。刻意保留 `wordWrap:'off'`（用户要求）与 `readOnly:true`。`npm run compile` 与诊断通过。
- 按用户要求改为"并联+词级高亮"：`useInlineViewWhenSpaceIsLimited:false` 强制始终并排双栏；SingleDiffPanel 主题将 `diffEditor.insertedLineBackground`/`removedLineBackground` 及 `diffEditorGutter.*` 整行底色显式设为透明 `#00000000`（不定义会回落 vs-dark 基础主题默认色，必须显式覆盖），只保留 insertedText/removedText 词级高亮（fallback alpha 0x38→0x48 略加深）、textBorder 与 overview 色；`renderIndicators:true` 的 gutter 红绿指示条仍标识行级增删。`npm run compile` 与诊断通过。
- 修复编译错误：`multiPagePanel.ts`（未被引用的实验文件）引用了未安装的 `diff`/`diff2html`，已 `npm install diff diff2html @types/diff`；随后按用户要求移除 `diff2html` 依赖：重写 `multiPagePanel.ts`，改用 `diff` 包的 `diffLines` + `diffWordsWithSpace` 自渲染 side-by-side 表格（相等行配对、连续删/增块按 min 配对做词级 del/ins 高亮、剩余独立删除/新增行、CSS 类 row-del/row-ins/row-chg 映射 VS Code diff 主题变量），`npm uninstall diff2html` 后 `npx tsc --noEmit` 通过。
- 按用户要求彻底清除 diff2html 遗留：删除未被引用的 `multiPagePanel.ts`（diff2html 时代实验文件）并 `npm uninstall diff @types/diff`；`singleDiffPanel.ts` 主题脚本移除 GitHub fallback 色（#2ea043/#f85149 及手动加深 alpha）与原生没有的 `insertedTextBorder`/`removedTextBorder` 描边，新增 `applyThemeColor(colors, token, key)`：token 缺失时不定义 key，回落 Monaco vs/vs-dark 原生默认色；保留 `#00000000` 整行/gutter 透明（词级高亮需求）与主题变量映射。`npx tsc --noEmit` 与诊断通过。
- 按用户要求 SingleDiffPanel 改成最简 webview + Monaco diff 并删除 MultiDiffPanel：`singleDiffPanel.ts` 重写为独立 WebviewPanel 类（ensurePanel/renderCurrentDiff/最简 HTML），一次只渲染选中文件（files.find(selectedPath) ?? files[0]），单个 `createDiffEditor({automaticLayout,readOnly,originalEditable:false})` 全量填充视口，Monaco 原生 vs/vs-dark 基础主题（setTheme，无 defineTheme/主题脚本/标题栏/跳转/selectFile 消息），仅 3 种 Host 消息 loading/error/render + webview→host 仅 ready，语言用静态 `languageForPath` 扩展名小映射嵌入模板 toString；`gitkViewProvider.ts` 改用 `new SingleDiffPanel()`（onSelectFile 可选参数，删除 syncFileHighlightFromDiffPanel），字段 multiDiffPanel→singleDiffPanel 全替换，updateMultiDiffVisibility→updateDiffVisibility；删除 `multiDiffPanel.ts`。全 src 搜 multiDiffPanel/SingleDiffData 零匹配，诊断零错误。
- 最终形态：`multiDiffPanel.ts` 重建为最简包装类（~25 行，内部 `new SingleDiffPanel()` 纯委托 show/cancelPending/hide/dispose）；`singleDiffPanel.ts` constructor 去掉 onSelectFile 参数，createDiffEditor 加 `renderSideBySide:true`（双栏），require 回调改为最小词级主题 `gitk-word-diff`：cssVar 读取 editor.background/foreground/gutter/lineNumber 四个 CSS 变量（空值删 key），`diffEditor.insertedLineBackground`/`removedLineBackground` 及 `diffEditorGutter.*` 显式 `#00000000`（整行底色禁用），词级 insertedText/removedText 色不定义 → 继承 vs/vs-dark 基础主题原生默认；`gitkViewProvider.ts` 切回 import MultiDiffPanel + 字段 multiDiffPanel（this.singleDiffPanel 全替换回 this.multiDiffPanel），updateDiffVisibility 名称保留。诊断零错误。
- 修复编译错误（gitkViewProvider.ts L150 残留 `new SingleDiffPanel()` → `new MultiDiffPanel()`），并按用户要求 createDiffEditor 参数对齐 VS Code 原生 diff 默认：`renderSideBySide:true, ignoreTrimWhitespace:true, renderIndicators:true, renderOverviewRuler:true, diffAlgorithm:'advanced', diffWordWrap:'off', wordWrap:'off', renderWhitespace:'selection', renderLineHighlight:'line', scrollBeyondLastLine:true, minimap:{enabled:true}, folding:true, hideUnchangedRegions:{enabled:true,contextLineCount:3,minimumLineCount:3,revealContextCount:20}`（整行底色透明主题保留，词级高亮）。
- 排查"一直正在准备 Diff..."：根因是 webview 模板里嵌入 `(${SingleDiffPanel.languageForPath.toString()})(message.path)` —— 静态方法 toString 产物是方法简写 `languageForPath(path){...}`，作为表达式 `(languageForPath(path){...})(...)` 是 SyntaxError，整段内联脚本解析失败 → handle 与第二个 message 监听器从未注册，Host 的 render/loading 消息只被第一个监听器推进 gitkQueue 无人消费，overlay 停在初始"正在准备 Diff..."。tsc 不检查模板字符串内 JS。修复：改为 `private static readonly EXTENSION_LANGUAGES` Record + `JSON.stringify` 注入 `const LANG=...`，render 内用 `LANG[ext]||'plaintext'`。教训：方法 toString 只能嵌箭头函数；模板字符串内嵌函数必须用箭头函数或 JSON 数据。
