- 继续修复提交历史动态刷新：删除 `GitkViewProvider` 中重复的 `**/.git/refs/**` watcher、状态签名、300ms debounce 及 `refreshOnRepositoryStateChanged()` 链路，统一由 `SelectedRepoTotalBranchWatcher` 监听 `refs/heads/**`、`refs/remotes/**`、`packed-refs`，经 `GitBranchesController` 驱动 `GitCommitController.refresh()`；扩展自身 commit 成功仍直接调用 `forceRefreshCurrentSelection()`。恢复被误删的通用 `getRepoRootUri()` 方法并移除无用 `getGitRepositoryState` 导入。`npm run compile` 与 diagnostics 通过，未提交。
- 提交图虚拟行文字颜色统一：`gitkViewProvider.ts` 的 `.working-tree-label--staged`(原蓝 `--vscode-charts-blue`) 与 `.working-tree-label--changes`(原红 `--vscode-charts-red`) 均改为 `#ffffff` 白色。lint 通过。
- staged↔unstaged 切换仍显示"正在加载变更文件"根因: 二者文件清单共享同一 `WorkingTreeChanges`, 但 diff 内容不共享(staged=HEAD↔index, unstaged=index↔工作区, 见 `diffReader.readWorkingTreeDiffs` 的 `readsIndex`), 且旧复用靠 `this.files` 里的 `diffKey`(前缀 `staged:`/`unstaged:` 不同)必然 miss → 每次切换全量重读。
- 实现方案B(预读+持久缓存)于 `gitkViewProvider.ts`: 新增独立 `prewarmDiffReader`(不受主 `diffReader.stop()` 影响)、`workingTreeDiffCache`(key=diffKey, 容纳两套)、`workingTreeDiffCacheContext`(=仓库+HEAD, 变化即整体失效)、`prewarmGeneration`(预读门禁)。`selectWorkingTreeChanges` 复用来源从 `this.files` 换成缓存, 全命中(`filesToRead.length===0`)时 `filesLoading=false` 且不设 `pendingFilesRevealGeneration`(列表立即显示、不等 Diff 渲染信号); 当前视图就位后调 `prewarmWorkingTreeDiffs` 后台读另一套填缓存。失效: `invalidateWorkingTreeDiffCache(paths)` 删两套, 由 `selectWorkingTreeChanges`(affectedPaths)与 `refreshWorkingTreeDiffs` 入口调用(该通道不经 selectWorkingTreeChanges); `applyRefreshedDiffs` 重读结果同步写回缓存防过期。`cancelActiveRequests` 停预读+推进代次。缓存失效严格靠 affectedPaths 而非 equals(工作区文件无 objectId)。`npm run compile` 通过, 未提交。
- 已确认子模块提交变更识别现状：`getCommitFiles`/工作区 raw diff 解析已保留 `oldMode`、`newMode`、`oldObjectId`、`newObjectId`；`160000` 是 gitlink 的可靠根因判据，且同步流程的 `getGitlinkChanges()` 已用此判据。`CommitFile`/`DiffPayload` 和 Changed Files UI 尚未建立显式 `isSubmodule` 语义，`DiffReader` 还会将 gitlink 的 commit object 当作普通文件内容读取。实现应在 raw 解析统一建模 `isSubmodule`，跳过 `cat-file` 内容读取，并由 Changed Files/Multi-Diff 显示旧→新子模块 SHA；不可用仓库拓扑 `hasSubmodules` 替代单文件判定。
- 对子模块 gitlink：父仓库 raw diff 已能直接提供旧/新 SHA；提交描述目前不能直接读取，因为 `getCommitFiles` 仅在父仓库执行，描述必须在变更路径对应的子模块仓库内对 SHA 执行 `git show -s --format=...`。子模块仓库已由 `.gitmodules` 发现和拉取时的逐个 fetch 支持；当该 SHA 在子模块对象库可达时即可读取 subject/body，若缺失应报告对象未取得而不可伪造为空描述。
- 已实现子模块 gitlink Changed Files 展示：`parseRawStatus` 以 `160000` 写入 `isGitlink`；`RepoSubmoduleWatcher.findSubmoduleRepository` 由直接父仓库和相对路径精确映射已初始化子模块；`GitkViewProvider` 对旧/新 SHA 在子模块内批量 `git show -s` 取 short hash+subject。`DiffReader` 不再将 gitlink 送给 `cat-file`，改生成 `Submodule commit <short SHA> <subject>` 的两侧文本。树状/平铺列表与 Multi-Diff 标题显示 `Submodule` 标签，元信息显示旧、新提交。子模块对象缺失时仍显示父仓库 SHA、主题留空。`npm run compile` 通过（2026-08-28）。
- 子模块标签位置调整：Changed Files 树状/平铺记录与 Multi-Diff 标题均将 `Submodule` 标签置于行首，先于状态、展开箭头和路径（2026-08-28）。
- 子模块描述显示逻辑确认：Multi-Diff 元信息与正文均读取同一 `DiffPayload.oldGitlinkCommit/newGitlinkCommit.subject`；正文的 `createGitlinkText` 仅在 subject 存在时拼接。旧、新 SHA 在子模块内独立解析，任一对象未初始化、浅克隆缺失或提交 subject 为空时，该侧只显示 SHA；但同一侧的元信息与正文理论上应一致（2026-08-28）。
- 截图确认：`5671e5f` 实际已显示 subject，缺失的是 `3d407ec` 一侧。当前查询格式只读取 Git `%s`（subject），不读 `%B`（完整提交说明）；在命令整体成功而仅单侧无文字时，该 SHA 的 subject 为空（可能仅有正文）是当前代码能解释的直接根因。后续若需支持完整描述，应扩展 gitlink 模型并读取 `%B`，内容区展示完整 message、元信息用首个非空行摘要（2026-08-28）。
- 已改为子模块 gitlink 提交使用 `git show -s --format=%H%x1f%h%x1f%B%x1e` 读取完整 `%B` 消息；`GitlinkCommit` 新增 `message`，用首个非空行生成 `subject` 供元信息摘要显示，Diff 正文显示 SHA 后完整 message。因此空 `%s` 而正文存在的提交可正确展示；完全空消息或对象缺失仍只显示 SHA。`npm run compile` 通过（2026-08-28）。
- 当前子模块 gitlink Diff 仅查询并渲染 `oldObjectId/newObjectId` 两个端点提交；未执行 `git log <old>..<new>`，因此中间提交不会显示。若要完整提交链，须在子模块中按祖先范围读取所有可达提交并建模为列表；处理非快进或无祖先关系时需要明确展示两端不可线性展开，不能伪造范围（2026-08-28）。
- 已实现 gitlink 快进提交范围展示：先以 `merge-base --is-ancestor old new` 验证线性祖先关系，再以 `git log --reverse old..new` 读取从旧提交之后到新提交的每个提交（完整 `%B` 消息），写入 `gitlinkRangeCommits`；Diff 左侧保留旧端点，右侧顺序显示范围内所有提交（含新端点）。新增/删除及无范围/非线性历史仍只显示可确认的端点，避免伪造线性链。`npm run compile` 通过（2026-08-28）。
- 用户已澄清子模块 Diff 正文需求：不保留 `index <old>..<new> 160000` 元信息；单栏提交列表按新到旧排列，新的子模块提交显示在第一行，其余范围提交依次向下（2026-08-28）。
