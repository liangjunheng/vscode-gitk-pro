- 继续修复提交历史动态刷新：删除 `GitkViewProvider` 中重复的 `**/.git/refs/**` watcher、状态签名、300ms debounce 及 `refreshOnRepositoryStateChanged()` 链路，统一由 `SelectedRepoTotalBranchWatcher` 监听 `refs/heads/**`、`refs/remotes/**`、`packed-refs`，经 `GitBranchesController` 驱动 `GitCommitController.refresh()`；扩展自身 commit 成功仍直接调用 `forceRefreshCurrentSelection()`。恢复被误删的通用 `getRepoRootUri()` 方法并移除无用 `getGitRepositoryState` 导入。`npm run compile` 与 diagnostics 通过，未提交。
