# 2026-08-25

- 已完整撤销本轮 `Changed Files` 横向滚动、分区固定标题与文件操作固定实验，恢复会话开始时的原始分区 DOM 与 CSS；未回退本轮其它状态流、性能或诊断改动。`gitkViewProvider.ts` diagnostics 为 0。
- 只读排查首次初始化 Changed Files 偶现变慢：首要可证实根因是初始化 I/O 竞争与重复 status。`GitkViewProvider.refreshInternal()` 非等待地启动仓库扫描后又异步执行 `captureRepositoryStateSignature()`（含 `status --untracked-files=normal`）；HEAD 落地同时触发 `UncommittedFilesWatcher` 首次 `status --untracked-files=all`、`GitCommitController` 的 `git log` 和完整分支读取。初始 status 在途时 workspace/index 事件会令刷新槽 `needsRefresh=true`，首轮结束后再补读一次；Changed Files 的虚拟提交读取等待同一 slot completion，之后还同步等待全部 Diff 正文才提交 `files`，故慢 status/补读会直接延后首次列表。另有 `GitBranchesController.applyHeadBranchChanged()` 先 `fireSelected()` 启动提交 refresh，再发 current-head 令 `GitCommitController.forceRefresh()` 设置 `pendingForceRefresh`，特定初始事件顺序下会串行补跑一次 `git log`。未修改源码；现有 watcher 绝对时间日志足以验证 status/补读，但首次仓库状态签名、commit refresh 和初次 Diff 尚缺同一 trace。
- 首次 Changed Files 慢的根因修复方案已确定：一是收敛状态所有权，Provider 的 refs 刷新签名不再读取 working tree status，工作区唯一由 `UncommittedFilesWatcher` 读取；二是删除 `GitCommitController` 对 current-head 的重复 `forceRefresh` 链路，HEAD 更新统一由 selected-branches 事件触发一次提交读取；三是 watcher 保留“监听后读取”的无丢事件模型，但把读取期间的 workspace 路径事件和 index 事件改为增量对账，首轮全量 status 后只按受影响路径（index 使用前后 staged 路径并集）刷新，不再因任意事件补跑全量 status。先用同一 trace 验证三段耗时，再按此顺序实施；不采用 debounce、延时或限制重试轮数。
- 已完成首次 Changed Files 慢的根因修复：`getGitRepositoryState()` 的签名读取移除 working-tree status，Provider 签名只比较 HEAD/branch/refs；`GitCommitController` 删除 current-head `forceRefresh/pendingForceRefresh` 第二入口，提交列表只由 selected branches 变化触发；`UncommittedFilesWatcher.getUncommittedFilesByHeadBranch()` 在首轮运行中只等待既有 completion，不再误记补读，index watcher 通过 `getStagedPaths(diff --cached --name-only -z -M -C)` 将前后 staged 路径并集写入同一刷新槽，缓存存在时走路径级 status，首次或显式 full refresh 才全量 status。`npm run compile`、四文件 diagnostics、`git diff --check` 均通过。
- 本轮优化收益确认：首次初始化从可能的“Provider status + watcher 全量 status + watcher 误补读 + 第二轮 git log”收敛为“一次 watcher 全量 status + 一次 git log”；后续 index 变化从全仓库 `--untracked-files=all` 降为 staged 前后路径并集的路径级 status。收益主要体现在大仓库、未跟踪文件多和慢磁盘环境；首次唯一一次全量 status 与完整 Diff 正文读取仍是必要成本，需用运行日志验证实际耗时下降。
- 复核刚完成的性能代码发现一项中风险：index 对账仅用 `git diff --cached --name-only` 的 staged 路径前后并集，`git add -N/--intent-to-add` 只改变 index、但通常不进入 cached diff；若由外部 Git 执行且工作区文件本身未再产生事件，路径集合可能为空，watcher 会保留旧缓存。普通 stage/unstage、rename/copy、冲突及刷新槽并发未发现新增丢请求。删除 CommitController current-head `forceRefresh` 对 checkout 安全，因为 `GitBranchesController.applyHeadBranchChanged()` 会先发布 selected branches；但现有 refs watcher 只调用 Provider `refresh(false)`、不驱动 branches controller，普通 commit/fetch 后的 refs 更新链路仍有既有职责断点，需与本轮 intent-to-add 风险分别处理。空仓库无 current HEAD 因而不建立 uncommitted watcher 也是既有缺口。
- 已修复 index 对账风险：`getStagedPaths()` 替换为 `getIndexEntries()`，通过 `git ls-files --stage -z` 建立 `path -> mode/object/stage` 完整签名；`UncommittedFilesWatcher` 对前后 index 路径并集逐项比较签名，仅将新增、删除或签名变化路径加入路径级 status，覆盖 intent-to-add、unstage、冲突多 stage、mode/object 变化，同时避免所有已跟踪文件都被重复刷新。首次/显式全量刷新同步建立 index 基线，HEAD 切换清空旧基线。`npm run compile`、两文件 diagnostics、`git diff --check` 均通过。
- 随后复核性能确认 `git ls-files --stage -z` 存在大 tracked 仓库性能回退风险：每次 index 事件都输出全部已跟踪条目并在 TS 中重建/比较 Map，复杂度和内存为 O(N tracked)，首次全量 status 后还额外读取一次完整 index。更合适方案是恢复“变更路径集合”而非完整 index 快照，使用 `git diff --cached --ita-visible-in-index --name-only -z -M -C` 覆盖 intent-to-add，再与上一轮 staged 路径取并集；冲突路径也由 cached diff 输出。当前 ls-files 方案不能作为最终性能优化交付。
- 当前交付评估：`npm run compile` 和 diagnostics 通过，但不能交付。`UncommittedFilesWatcher.refreshSlot()` 在每次 index 事件调用 `getIndexEntries()`，对大仓库产生 O(N tracked) 的完整 index 扫描、字符串拆分和 Map 重建，与优化目标冲突；应先改回变更路径命令并重新做大仓库/连续 index 事件验证。`package.json` 已移除 `editor/content` proposed menu，相关警告的直接触发点已消失，但 `enabledApiProposals` 残留声明应单独确认是否还被其他贡献使用。
- 已处理交付阻断：删除 `getIndexEntries()` 完整 index 快照方案，改用 `git diff --cached --ita-visible-in-index --name-only -z -M -C` 获取相对 HEAD 的变更路径集合；watcher 保留前后路径并集和路径级 status，不再按 tracked 文件总数重建 Map。`npm run compile` 通过，相关 diagnostics 为 0，`git diff --check` 仅有 memory 文件既存换行提示。
- 已接入提交分页：`GitCommitController` 将 200 条改为每页大小，按仓库保存页数据与 offset，公开 `loadMoreCommits()` 及加载状态；首屏建立分页基线，后续调用 `getGitCommits(..., 200, offset)` 追加并重新构建已加载提交图；仓库/分支切换会取消在途分页并清理游标，搜索态禁用分页。`GitkViewProvider` 已把 `loadMoreCommits` intent 接到 Controller，并推送真实 `hasMoreCommits/isLoadingMoreCommits/commitPageError`。`npm run compile`、相关 diagnostics 和 `git diff --check` 通过。
- 按用户要求将 `COMMIT_PAGE_SIZE` 从 200 调整为 50；Webview `IntersectionObserver` 的底部预加载 rootMargin 从 800px 调整为 520px，在行高约 26px 的列表中约等于提前 20 行触发，即累计 50 条时约滑到第 30 条加载下一页，后续约第 80、130 条触发。编译、diagnostics、diff check 均通过。
- 已将分页触发从像素阈值改为 item 索引：Webview 根据 `commits.length - 20` 计算目标提交行，直接观察 `.commit-row[data-row="<index>"]`，移除 `rootMargin: 520px`，不再依赖行高、字体和窗口尺寸。`gitkViewProvider.ts` diagnostics 为 0，`npm run compile` 和 `git diff --check` 通过。
- 用户质疑分页修改是否成功；已通过源码和 `git diff HEAD` 核实：`COMMIT_PAGE_SIZE = 50`、`loadMoreCommits()` 已接入、Webview 使用 `triggerIndex = commits.length - 20` 并观察对应 `.commit-row`，旧 `rootMargin` 和 footer 观察逻辑已删除。分页相关文件确实存在工作区修改，其中 `gitCommitController.ts` 为 staged，`gitkViewProvider.ts` 同时有 staged/unstaged 差异。
- 排查“Changed Files 显示时是否重新排序导致慢”：当前代码没有对 Changed Files 调用 `sort()`；普通提交文件由 `getCommitFiles()` 的 `diff-tree --raw` 顺序直接传递，Webview `renderFiles()` 也直接渲染 `files`；工作区增量合并只过滤受影响路径并把新结果追加，可能造成局部顺序变化，但不是全量排序。现有高成本仍是 status、DiffReader.readDiffs 和 `files.map` 重建 DiffPayload；需要 trace 才能确认实际耗时。
- 用户要求修改“按当前提交列表倒数第 20 个 item 触发分页”：`gitkViewProvider.ts` 已保留 `triggerIndex = commits.length - 20`，并改为从 `#graph` 容器内查询 `.commit-row[data-row="<index>"]`，再由 `IntersectionObserver` 观察该 item；不再使用固定像素高度。`npm run compile` 通过，但当前诊断显示 Provider 引用了 Controller 分页 API 而类型检查未看到对应声明，需后续确认 staged/unstaged 混合状态。
- 复核当前差异的必要性：`gitLogProvider.ts` 移除状态签名中的 status、`gitkViewProvider.ts` 同步移除 state.status、`uncommittedFilesWatcher.ts` 的刷新槽/路径级 index 对账是本次首次 Changed Files 性能根因修复；`gitCommitController.ts` 删除 current-head forceRefresh 属于减少重复 git log 的必要改动，但需实测 fetch/外部 HEAD 变化，因 refs watcher 既有链路可能未显式驱动 branch controller；`package.json` 删除 editor/content proposed menu 与本次性能问题无关，应仅在用户确实要消除 proposed API 警告时保留，否则应恢复。
- 确认提交列表 200 条的根因：`gitCommitController.ts` 的 `COMMIT_LIMIT = 200` 传入 `getGitCommits(..., 200, ..., 0, ...)`，而 `getGitCommits` 虽支持 `skip`，Controller 始终传 0；Webview 的 `loadMoreCommits` 消息在 `gitkViewProvider.ts` 中明确标记“分页尚未纳入控制器职责”并直接 break，同时状态固定 `hasMoreCommits: false`。因此当前只是 UI 预留了分页元素，后端实际没有分页加载；与最近的性能改动无关。
- 2026-08-24 运行日志证实 `UncommittedFilesWatcher` 同仓库 slot 被 workspace/index watcher 的连续事件长期保持在 `running`：unstage 请求于 16:02:36/16:02:45 合并后直到 16:03:10 才发布，根因是 `drainRefresh()` 固定两轮读取且 `needsRefresh` 未按每轮消费，无法区分旧请求与补读期间新请求。应改为按轮清除 dirty 标志、读取后仅因本轮新请求继续的 drain-until-quiescent 状态机（可进一步用 revision）；另有初始化时 GitBranchesController 先发布 current branch、Uncommitted watcher 尚未 apply 对应 HEAD 的时序竞态，导致一次“该分支不是仓库当前 HEAD”日志。
- 2026-08-25 分析首次 `Uncommitted Changes` 初始化耗时波动：主耗时由首次 `git status --untracked-files=all` 决定，仓库未跟踪文件规模、Windows 文件系统/OneDrive 防病毒扫描会显著影响扫描；该命令位于 `UncommittedFilesWatcher.refreshSlot()` 首次无缓存路径。它与 `GitCommitController.refresh()` 的 `git log --max-count=200`、分支/HEAD 读取和首次 Changed Files 全量 Diff 读取并行竞争磁盘。初始化期间 workspace/index watcher 事件会合并为额外刷新，且 `selectWorkingTreeChanges()` 在 status 后还同步读取每个文件内容，因此最终显示时间取决于最慢链路。方案应先用 trace 分离 status、commit log、Diff 三段耗时，再从根因收敛初始化数据流：工作区 status 只由 watcher 读取一次并作为快照发布，提交列表/refs 不再重复读取工作区；初始化期间不让自身 Git 读操作触发第二轮全量 status；Changed Files 首屏先发布文件元数据，Diff 内容按文件/展开懒读而不是阻塞首屏。禁止用 debounce、延时或存在性兜底掩盖根因。
- 已修复操作后的刷新收敛：`UncommittedFilesWatcher.drainRefresh()` 改为每轮读取前消费 `needsRefresh`、仅在读取期间有新请求时再读，直到事件静止；`GitkViewProvider` 的 add/unstage/restore 操作完成后改用 `refreshUncommittedFilesByHeadBranch` 读取完整状态，不再将操作刷新混入路径级缓存合并。`npm run compile` 与相关 diagnostics 通过。
- 最新日志修正根因判断：主仓库 refresh 自 `16:13:15.691` 开始，首个 `status unchanged` 到 `16:13:49.825` 才返回，说明主要阻塞是单次 `git status --untracked-files=all` 约 34 秒，不是 slot 循环；大量 `refresh merged` 只是该 Git 进程执行期间堆积的文件事件。修复需优化 status 查询的未跟踪扫描策略/仓库范围并补充触发 URI 日志，不能靠限制刷新轮数掩盖。
- 已将 `UncommittedFilesWatcher.handleWorkspaceFileChanged` 改为把触发 URI 归一化为仓库相对路径并合并到 `pendingPaths`，再走 `getWorkingTreeStatusForPaths`；工作区文件变更不再触发父仓库 `git status --untracked-files=all` 全量扫描。操作路径刷新本就使用同一正确路径级语义；`.git/index` 事件仍保留全量刷新以覆盖索引中无法从工作区 URI 得知的变化。`npm run compile` 与 diagnostics 通过。
- 最新日志表明路径刷新已稳定为约 1–2 秒并可正常结束；剩余 `refresh retry` 由 index watcher 合入的无路径请求触发，下一轮 `paths: []` 全量 status 仍耗约 2–3 秒。Commit Editor 在单次 watcher 状态发布后的 metadata/Diff 两阶段各触发 `commitPanel.update`，且 Provider direct watcher 与 Changed Files 链路同时更新，出现 4 次 snapshot/render 投递；显示本身几毫秒但存在重复工作。
- 下一步根因方案：index watcher 不应把自身 Git 写入与外部 index 改动都退化为全量 `status --untracked-files=all`；应以“上轮缓存的 staged 路径 ∪ 当前 `git diff --cached --name-only -z` 路径”作为 index 影响集合，再走路径级 status，覆盖 stage/unstage 前后路径而不扫描无关未跟踪文件。Commit Editor 的唯一实时更新入口保持 Provider 直接 watcher 订阅，移除 `selectWorkingTreeChanges` 元数据和 Diff 两阶段对 Commit Panel 的重复 update。
- 已清理确认无效工作区代码：删除 `GitkViewProvider.searchAbortController` 及取消逻辑；删除 `GitCommitController` 工作区事件 accepted/ignored 高频日志；删除总分支/总提交事件中重复 `schedulePushState()`；将 `syncCommitRepositories()` 的同步 map 去掉无效 `Promise.all(async ...)`。未删除仍有业务职责的 `refreshWorkingTreeImmediately`、watcher 合并/重试状态机。`npm run compile` 和两文件 diagnostics 通过。
- 已输出 HEAD Commit Editor 风险解决方案：优先修复会话启动期仓库占位/规范化 key、统一 opened/completed 异常收敛、消除真实 `git commit -v -v` 模板生成副作用、对动态文件路径做 DOM/textContent 安全渲染；随后按状态 identity 缓存模板并抽取历史消息共享读取逻辑。
- 已按用户要求修改 `src/git/gitCommitService.ts`：`readCommitTemplate()` 通过 `resolveGitDir()` 后直接读取对应仓库 `.git/COMMIT_EDITMSG`，不再执行失败编辑器方式的 `git commit -v -v`，避免 hooks、工作区和共享提交消息文件副作用；`amend` 参数保留以兼容现有调用链。lints 与 `npm run compile` 通过。
- 已落实其余 HEAD Commit Editor 方案：`gitCommitEditMsgEditor.ts` 使用 repoPath (`rootUri.toString()`) 作为会话 Map 和启动占位 key，生命周期结束按 repoPath 释放；标签关闭/活动编辑器则在 session values 中按 document URI 定位；`gitActions.ts` 提前绑定 `session.completed` 错误处理并等待 `session.opened` 失败后的清理；`commitPanel.ts` 文件行改为 DOM/textContent 构建；`gitLogProvider.ts` 抽取共享 `readCommitHistoryMessages()`，编辑器和 Provider 共用。相关 diagnostics 无错误，`npm run compile` 通过。
- 重新评估 CommitPanel 模板来源：直接读取 `.git/COMMIT_EDITMSG` 可能是旧内容，不能代表当前 staged/unstaged 状态。用户真实目标是让用户知道最终提交信息；由于当前提交走 `git commit --cleanup=strip -F <temp>`，最终消息还可能被 `prepare-commit-msg`/`commit-msg` hooks 修改，提交前无法用纯只读方式保证最终字节一致。正确产品边界是输入区实时展示 cleanup 后的“将提交消息”，提交成功后用新 commit hash 读取 `%B` 展示/确认“实际提交消息”；若强求提交前绝对一致，只能执行真实 hooks/提交事务，会产生副作用。
- 明确 `git commit --cleanup=strip` 不是只读命令：它仍执行真实 commit，`--cleanup=strip` 仅规定提交消息清理模式（去注释、裁剪空白、折叠连续空行）；项目 `commitWithMessage()` 使用 `git commit [--amend] --cleanup=strip -F tempFile`。
- 更正 `git stripspace --strip-comments` 用法：不能作为裸命令运行，它会持续从 stdin 读取直到收到 EOF，终端直接执行看起来会“卡死”。用户最终指定预览直接读取 `.git/COMMIT_EDITMSG`，并只返回固定 scissors 行 `# ------------------------ >8 ------------------------` 之前的内容，不包含该分隔线及之后 diff；`readCommitTemplate()` 已实现该截取，lints 与 `npm run compile` 通过。
- 用户最终决定直接移除 Commit Editor 的“显示完整 Git Commit 内容”栏。已删除 `commitPanel.ts` 的模板 DOM/CSS/state/消息协议/compose 逻辑，删除 Provider 的 `onRequestTemplate`/`provideCommitTemplate` 和 import，删除 `gitCommitService.ts` 无调用方的 `readCommitTemplate` 与 scissors 常量。相关 diagnostics 无错误，`npm run compile` 通过。
- 排查“提交列表加载变慢”：刚移除完整 Git Commit 栏只删代码，不在列表初始化链路；当前未提交 diff 对提交读取仅新增按点击触发的 `readCommitHistoryMessages`。真正可由代码确认的慢点在现有控制器链：`GitBranchesController.applyHeadBranchChanged()` 先 `fireSelected()` 触发 `GitCommitController.selectBranches()->refresh(true)`，紧接 `currentHeadBranchEmitter` 又触发 `forceRefresh()`，加载中设置 `pendingForceRefresh`，第一轮结束后串行再跑一次完整 `git log`。
- Commit Panel 无任何 staged/unstaged 更改时，标题栏仓库名右侧的“无更改”标签改为绿色背景 `#2e7d32`、白色文字、圆角胶囊样式；空文本时隐藏。diagnostics 和 `npm run compile` 通过。
- Commit Panel 的 Unstaged Changes 改为默认展开且不再半透明；Staged/Unstaged 标题有全部操作按钮。最终修复全部按钮无效：不再用“缺失 path 表示全部”的隐式协议；Webview 从当前卡片快照显式发送 `paths[]` 与 `untrackedPaths[]`，宿主校验数组后转给 Provider 专用 `runCommitPanelWorkingTreeAction()`，直接入确认/队列，不再回查 `this.branches` 等不同域状态。单文件按钮也统一使用该协议。diagnostics 和 `npm run compile` 通过。
- Gitk 主 Webview 顶部已重组：repo 文案/下拉/未提交仓库徽章组成 Repo 组，Repo 左侧无竖线；branchs 文案/下拉组成 Branches 组，搜索框/提交数量/刷新按钮组成搜索组；相邻选择组之间各仅一条竖线。Header 组间 gap 已设为 0，Repo/Branches 的视觉间距只由各组内边距控制；搜索组自身不画边框；Repo 徽章通过更高优先级 `#header .uncommitted-repo-badge` 覆盖 Header 按钮通用 2px 圆角，现为 16px 正圆蓝底白字（相对原 20px 缩小 20%），hover 显示 `Git - N 个仓库有未提交文件`；刷新位于提交数量后。diagnostics 通过。
- Commit Editor 文件行的还原、暂存、取消暂存按钮改为常驻且完全不透明显示：移除 `.row-actions` 的透明隐藏及 hover 规则；将 Unstaged 的半透明样式从整个 section 下放到标题、状态、路径和空提示，避免父级 opacity 连带影响按钮；按钮组、按钮元素及 codicon 均显式 `opacity:1`，相关 diagnostics 无错误。
- 审查 HEAD `faa848c`（添加 Commit Editor）确认主要风险：同仓库编辑会话在异步启动完成前未占位，可能并发覆盖同一 `COMMIT_EDITMSG` session 并让 Git 永久等待；`readCommitTemplate()` 用真实 `git commit -v -v` + 失败编辑器取模板，会执行 hooks、污染共享 COMMIT_EDITMSG，且展开模板时每次 snapshot 都重新请求；CommitPanel 文件路径直接拼 innerHTML，特殊文件名可注入 Webview HTML；`session.opened` 的启动失败拒绝未在 void 调用链统一收敛。低风险冗余为两个历史提交消息读取/去重实现和仓库 URI key 未统一规范化。
- 已将 Gitk 视图标题的 `Refresh Gitk` 替换为 `$(git-commit)` CommitPanel 入口，命令 `vscode-gitk.openCommitPanel` 直接调用 Provider 的统一多仓库面板打开流程；Gitk Webview 内部刷新按钮保留。视图原生 badge 使用 `commitRepositories` 中 staged/unstaged 非空的仓库数，并由 `UncommittedFilesWatcher` 事件实时更新。相关 diagnostics 与 `npm run compile` 通过。
- 已将上述 CommitPanel 标题入口功能独立到 `src/webview/commitPanelEntry.ts`：该类独占命令注册、WebviewView 绑定/解绑及未提交仓库 badge 计算；Provider 仅提供打开回调并在 watcher/Store 状态流中传递快照，`extension.ts` 不再注册该命令。diagnostics 与 `npm run compile` 通过。
- 为明确职责，`commitPanelEntry.ts` 已重命名为 `commitPanelViewTitleController.ts`，类/字段同步改为 `CommitPanelViewTitleController` / `commitPanelViewTitleController`；旧文件已删除，diagnostics 与 `npm run compile` 通过。
- 已梳理 Changed Files 虚拟提交布局：提交图顶部仅有一条 `uncommitted` / `Uncommitted Changes` 虚拟行；选中后右侧 Changed Files 按 `Staged Changes`、`Unstaged Changes` 两个 section 渲染，布局、CSS 与状态接线均集中在 `src/webview/gitkViewProvider.ts`。
- 已从布局根因修复虚拟提交横向滚动：section header 仅负责纵向 sticky，标题/数量拆成左侧 sticky 岛，分组及逐文件 stage/unstage/discard 操作改成右侧 sticky 岛，并继承标题/行状态的不透明背景；无需滚动监听或 JS 补偿。`npm run compile` 与 diagnostics 通过。
- 虚拟提交的 staged/unstaged 分区现由统一 `.working-tree-content` 单列 intrinsic grid 包裹，列宽取 `max(面板宽度, 最宽子项 max-content)`；两个标题栏、所有文件子项及两个分区因此使用同一宽度，同时保留左右 sticky 标题/操作岛。编译、diagnostics、diff check 通过。
- 虚拟提交标题栏与文件子项的操作区现共用同一 CSS 几何规则：`right: 0`、相同 padding/gap/box-sizing，标题和子项的 stage/unstage/discard 图标按右侧列对齐；各自只保留层级和背景差异。验证通过。
- 已消除横向滚动最右端的操作图标微位移：根因是 sticky 元素到达自然位置时暴露父行右 padding/边框的不同右边界；标题与文件行右 padding 统一为 0，标题选中效果改用不占布局尺寸的 outline，sticky 和自然位置右边界一致。编译、diagnostics、diff check 通过。
- 虚拟提交选择高亮已从单文件提升到所属整个 staged/unstaged section；随后按用户要求调整为 `.has-selected` 仅显示 `vscode-focusBorder` 蓝色 outline，不改变标题、正文、子项或按钮组背景/前景。单文件 `.selected` 仍不显示独立色块。
- 截图确认仅给 sticky `.file-actions` 设置 background 仍会看到其下横向路径文字；现为标题/文件按钮组建立 `isolation:isolate + overflow:hidden` 独立绘制上下文，并用绝对定位 `::before` 实体背景层覆盖整个按钮组，按钮置于其上，彻底阻断底层文字。标题和两种文件渲染都通过 `workingTreeActionsHTML()` 在按钮后插入真实 spacer；第二张截图显示 11px spacer 使操作图标比顶部显示方式图标左偏约 3px，已校准为 8px。
- 第三张截图暴露 `.working-tree-content` 的 class 选择器被 `#filesList > * { min-width:max-content }` 高优先级覆盖，导致短内容未占满右侧面板；已改为高优先级 `#filesList > .working-tree-content`，并最终用 `display:inline-grid; grid-template-columns:1fr; width:max-content; min-width:100%` 明确表达容器宽度 = `max(父面板宽度, 最宽子项 intrinsic 宽度)`，标题与全部子项共享该列宽。虚拟提交文件子项恢复普通 commit 的选中态。
- 选中态按钮组透字根因是 VS Code 主题的 `list.activeSelectionBackground` 可能含 alpha；将其直接用于 sticky 遮罩会继续混合下方路径文字。现通过两层 background 在按钮组内预合成：状态色 linear-gradient 在上，实色 editor/editorWidget background 在下，视觉保持主题选中颜色但最终像素不透明。编译、diagnostics、diff check 通过。
- 已恢复 MultiDiffPanel 上下滚动联动时 Changed Files 选中项可见性：`revealSelectedFile()` 的上界排除 sticky section header；底部只显示一半的根因是 `getBoundingClientRect().bottom` 包含水平滚动条区域，现上下边界改由 `clientTop + clientHeight` 得到真实内容 viewport，选中项越上界贴到标题下、越下界完整贴在水平滚动条上方，仅调整 `scrollTop`。编译、diagnostics、diff check 通过。
- Changed Files 文件点击现统一走 `openDiff()`→`MultiDiffPanel.show()`，会先创建/激活编辑器标签再定位；Store 不再吞掉重复点击当前文件的命令副作用。Commit Panel staged/unstaged 文件行新增 `selectFile` 消息（操作按钮 stopPropagation），Provider 通过仓库/分支控制器切到对应仓库的 `uncommitted`，等待权威 Diff 快照后按 `staged:path`/`unstaged:path` 激活定位。该流程使用独立 AbortController，快速点击最后一次胜出，取消时释放分支/Store 监听；重复点击加载中的同一目标附着实际 generation。编译、diagnostics、diff check 通过。
- 只读分析 Gitk 面板 `pull` 慢：代码中的真实网络/合并阶段只是 `runGitSync()` 内同步执行的 `git pull`；其前后各有一次 `rev-parse HEAD`，HEAD 改变后再做 `git diff --raw` 检测 gitlink。若 Submodule 指针变化，会继续同步执行 `git submodule update --init --recursive`；随后 `onMutated()` 刷新，refs watcher 还会读取由 4 条 Git 命令组成的仓库签名并触发提交刷新。因此通知总时长不等于 `git pull` 网络耗时。`runGitCommand()` 隐藏终端且不传 stdin/交互策略、无超时和取消，认证或 SSH 等待时只表现为“正在拉取”。本轮未修改源码。
- 已定位 `editor/content` 警告根因：`package.json` 的 `menus.editor/content` 使用 VS Code proposed menu 标识，扩展虽声明 `contribEditorContentMenu`，但生产/普通启动并未启用 proposed API；已移除该菜单贡献，保留稳定的 `editor/title` 命令入口。`npm run compile` 与 `package.json` diagnostics 通过。
- Changed Files 与 Commit Editor 现共用 `vscode-gitk.changedFilesDisplayMode`：两者平铺均显示“文件名 + 空格 + 半透明目录”，并按目录后文件名排序；Commit Editor 新增可折叠树状模式及切换按钮，切换复用 Store intent/全局配置。工作区文件名颜色统一为 staged 绿色、untracked 红色、unstaged 蓝色，树状和平铺均生效。`npm run compile`、diagnostics、`git diff --check` 通过。
- 复核截图中的排序：红框 `InkScreenView.kt`、`MultiPageView.kt` 与中间的 `InkSurfaceScreenView.kt` 三者目录完全相同，当前比较器先比较完整父目录，再在同目录内按文件名 `localeCompare`；因此顺序为 `InkScreenView.kt` → `InkSurfaceScreenView.kt` → `MultiPageView.kt`，它们实际上已组成连续的同目录分组。
- 用户随后指定 Changed Files 与 Commit Editor 不再拆分目录/文件名作为排序键，统一直接按 `file.path.localeCompare()` 比较完整文件路径；平铺和树状均复用该规则。`npm run compile` 与 diagnostics 通过。
- 经 `git diff` 复核，改动前 Changed Files 的树状模式本来就是按完整 `file.path.localeCompare()` 排序后按父目录分组；普通提交与 staged/unstaged 虚拟提交一致。改动前 Commit Editor 不支持树状模式，文件保持输入顺序。
- 用户最终决定所有文件视图均不自行排序：Changed Files 的普通提交和 staged/unstaged，以及 Commit Editor 的平铺/树状渲染均直接消费上游 Git 数据数组顺序；树状模式仅按输入遍历顺序建立目录分组。已删除客户端比较器与 `.sort()`，`npm run compile` 和 diagnostics 通过。
- 截图中“保留 Git 输入顺序”和“完整路径排序”结果相同不是渲染故障：Git porcelain 输出本身为 `.../api/view/InkSurfaceScreenView.kt`、`.../api/view/MultiPageView.kt`、`.../model/CanvasFinishedType.kt`，完整路径比较也因目录段 `api/view` 先于 `model` 得到相同顺序；UI 虽把文件名显示在前，但排序键若是完整路径仍然先比较目录。另确认 watcher 的 `mergePathChanges()` 会把受影响文件追加到尾部，故 Git 输入顺序在增量刷新后并非稳定的全量 status 顺序。
- 用户澄清目标是“相同父目录文件必须连续”，而非字典排序或完全原样输入。Changed Files 与 Commit Editor 曾短暂加入稳定目录分组；随后用户要求撤销，现已删除 `groupFilesByFolder` 及全部调用，恢复所有视图直接使用 Git 输入数组。`npm run compile` 与 diagnostics 通过。
