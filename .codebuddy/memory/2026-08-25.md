# 2026-08-25

- 已完整撤销本轮 `Changed Files` 横向滚动、分区固定标题与文件操作固定实验，恢复会话开始时的原始分区 DOM 与 CSS；未回退本轮其它状态流、性能或诊断改动。`gitkViewProvider.ts` diagnostics 为 0。
- 2026-08-24 运行日志证实 `UncommittedFilesWatcher` 同仓库 slot 被 workspace/index watcher 的连续事件长期保持在 `running`：unstage 请求于 16:02:36/16:02:45 合并后直到 16:03:10 才发布，根因是 `drainRefresh()` 固定两轮读取且 `needsRefresh` 未按每轮消费，无法区分旧请求与补读期间新请求。应改为按轮清除 dirty 标志、读取后仅因本轮新请求继续的 drain-until-quiescent 状态机（可进一步用 revision）；另有初始化时 GitBranchesController 先发布 current branch、Uncommitted watcher 尚未 apply 对应 HEAD 的时序竞态，导致一次“该分支不是仓库当前 HEAD”日志。
- 已修复操作后的刷新收敛：`UncommittedFilesWatcher.drainRefresh()` 改为每轮读取前消费 `needsRefresh`、仅在读取期间有新请求时再读，直到事件静止；`GitkViewProvider` 的 add/unstage/restore 操作完成后改用 `refreshUncommittedFilesByHeadBranch` 读取完整状态，不再将操作刷新混入路径级缓存合并。`npm run compile` 与相关 diagnostics 通过。
- 最新日志修正根因判断：主仓库 refresh 自 `16:13:15.691` 开始，首个 `status unchanged` 到 `16:13:49.825` 才返回，说明主要阻塞是单次 `git status --untracked-files=all` 约 34 秒，不是 slot 循环；大量 `refresh merged` 只是该 Git 进程执行期间堆积的文件事件。修复需优化 status 查询的未跟踪扫描策略/仓库范围并补充触发 URI 日志，不能靠限制刷新轮数掩盖。
- 已将 `UncommittedFilesWatcher.handleWorkspaceFileChanged` 改为把触发 URI 归一化为仓库相对路径并合并到 `pendingPaths`，再走 `getWorkingTreeStatusForPaths`；工作区文件变更不再触发父仓库 `git status --untracked-files=all` 全量扫描。操作路径刷新本就使用同一正确路径级语义；`.git/index` 事件仍保留全量刷新以覆盖索引中无法从工作区 URI 得知的变化。`npm run compile` 与 diagnostics 通过。
- 最新日志表明路径刷新已稳定为约 1–2 秒并可正常结束；剩余 `refresh retry` 由 index watcher 合入的无路径请求触发，下一轮 `paths: []` 全量 status 仍耗约 2–3 秒。Commit Editor 在单次 watcher 状态发布后的 metadata/Diff 两阶段各触发 `commitPanel.update`，且 Provider direct watcher 与 Changed Files 链路同时更新，出现 4 次 snapshot/render 投递；显示本身几毫秒但存在重复工作。
- 下一步根因方案：index watcher 不应把自身 Git 写入与外部 index 改动都退化为全量 `status --untracked-files=all`；应以“上轮缓存的 staged 路径 ∪ 当前 `git diff --cached --name-only -z` 路径”作为 index 影响集合，再走路径级 status，覆盖 stage/unstage 前后路径而不扫描无关未跟踪文件。Commit Editor 的唯一实时更新入口保持 Provider 直接 watcher 订阅，移除 `selectWorkingTreeChanges` 元数据和 Diff 两阶段对 Commit Panel 的重复 update。
- 已清理确认无效工作区代码：删除 `GitkViewProvider.searchAbortController` 及取消逻辑；删除 `GitCommitController` 工作区事件 accepted/ignored 高频日志；删除总分支/总提交事件中重复 `schedulePushState()`；将 `syncCommitRepositories()` 的同步 map 去掉无效 `Promise.all(async ...)`。未删除仍有业务职责的 `refreshWorkingTreeImmediately`、watcher 合并/重试状态机。`npm run compile` 和两文件 diagnostics 通过。
- Commit Editor 文件行的还原、暂存、取消暂存按钮改为常驻且完全不透明显示：移除 `.row-actions` 的透明隐藏及 hover 规则；将 Unstaged 的半透明样式从整个 section 下放到标题、状态、路径和空提示，避免父级 opacity 连带影响按钮；按钮组、按钮元素及 codicon 均显式 `opacity:1`，相关 diagnostics 无错误。
