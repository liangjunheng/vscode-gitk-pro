# 2026-08-08

- 修复分页加载提交时的分支图断连：`buildGraph` 的可见哈希集合现包含已加载提交的父提交哈希，首批构图会预留后续页父提交车道；`loadMoreCommits` 增量追加时可复用该车道，避免父提交重新开线。已通过 `tsc --noEmit` 与 `gitLogProvider.ts` linter 验证。
- 优化连续滚动分页性能与资源生命周期：`GraphState` 缓存 `visibleHashes`，追加页仅处理新增提交；预取 `git log --no-walk` 支持 `AbortSignal`，仓库/分支刷新、视图隐藏或销毁时取消；重试定时器被统一清理。Diff 面板隐藏/销毁时终止 `git cat-file` 子进程，且旧进程退出不会覆盖新进程引用。已通过全量 `tsc --noEmit`、相关 linter 和 `git diff --check`。
- 修复切换仓库时加载异常：`selectRepositories` 现在先清空 `refreshInFlight` 再启动新刷新，避免旧刷新 Promise 阻塞新仓库加载；`refreshInternal` 入口捕获 `refreshGen`，在并行块和顺序路径的关键 await 后均检查 `refreshGen !== this.refreshGeneration`，旧刷新发现世代变化即提前返回，不再向 webview 投递过期数据。
- 修复加载进度文案与实际操作不匹配：首次并行加载的初始文案从 "加载提交..." 改为 "正在加载仓库、分支和提交..."；切换仓库时 `beginCommitReload` 文案改为 "正在切换仓库..."；切换分支时改为 "正在切换分支..."。
- 修复内存泄漏：(1) `resolveWebviewView` 的视图级订阅 (onDidReceiveMessage/onDidChangeVisibility/onDidDispose) 从 `context.subscriptions` 移至独立 `viewDisposables` 数组，`onDidDispose` 和扩展停用时统一释放，避免视图反复创建时订阅累积。(2) `customDiffPanel.ts` 的 `onDidDispose` 回调新增 `stopChildProcess()` 调用，确保用户直接关闭 Diff 面板时 `git cat-file` 子进程也被终止。
- 修复初始化时多次加载仓库：`refreshWithRetry` 的 `skipSelectors` 逻辑从 `!isFirstAttempt` 改为 `!isFirstAttempt && this.hasBranchSelection`，首轮失败（选择器未加载成功）时重试不跳过选择器；并行块内 `refreshSelectors` 失败时直接抛出触发重试，不再落入顺序路径重复调用 `refreshSelectors`。
- 修复加载进度文案与实际操作不一致：并行块移除初始 "正在加载仓库、分支和提交..." 文案（让 `refreshSelectors` 自行投递 "加载仓库..." / "加载分支..."），移除 `getGitCommits` 的 `onProgress` 回调避免文案交错；`beginCommitReload` 统一用 "正在加载..." 通用文案，由后续操作投递具体进度。
- 修复切换仓库/分支时提交个数和页脚残留旧文案：`showLoadingProgress` 新增隐藏 `commitFooter`、断开 `commitLoadObserver`、清空 `countLabel` 和 `commits` 数组，确保切换时不会残留 "已加载全部 N 条提交" / "继续滚动以加载更多提交" 等过期文案。
- 修改提交描述展开行为：默认不展开，双击展开/折叠，未展开时单击延迟 300ms 显示 "双击展开描述" 提示（双击时取消提示）；移除 `commits` 和 `selectedCommit` 消息处理中的自动展开逻辑；`render()` 调用 `hideExpandHint()` 清除提示。
- 修复加载仓库提示词重复：`getGitRepositories` 的 `onProgress` 回调新增 `message` 参数，区分 "加载仓库..." (API 阶段) / "解析工作区..." (工作区文件夹) / "扫描子模块..." (子模块) 三个阶段，避免进度条从 0→1 重置为 0→N 看似加载两次。
- 优化初始化速度与加载文案：首次加载并行块从 `refreshSelectors` (含全量仓库扫描+分支加载) 改为 `refreshBranchesOnly` (仅加载选中仓库分支, 快速), 提交显示后后台 `refreshRepositories` 补充仓库下拉; `getGitRepositories` 进度计数修复, 工作区文件夹解析改用 indeterminate (total=0), 不再将可能与 API 仓库重复的工作区文件夹计入 total, 避免单仓库显示 "加载多个"。
- 添加搜索功能：`gitLogProvider.ts` 新增 `searchCommits(rootUri, keywords, refs)` 使用 `git log --grep --all-match -i` 搜索全部提交 (含未加载), 支持多关键词空格分隔; `gitkViewProvider.ts` 新增 `performSearch`/`refreshSearchCleared` 方法, 搜索时备份原始提交列表, 清除时恢复; webview 新增搜索输入框 (回车触发, Esc 清除), `commits` 消息支持 `isSearchResult`/`searchMatchCount` 字段, 页脚和计数标签显示搜索匹配数; `showLoadingProgress` 非搜索阶段清除搜索输入和状态; `beginCommitReload` 清除搜索状态。
- 修复三个问题：(1) 仓库下拉加载异常 — `renderSelectors` 在 `repositories` 为空时跳过仓库下拉渲染 (保留 "加载仓库..." 状态), `refreshRepositories` 后台调用加 `.catch()` 错误处理。(2) change files 未选中 commit 时残留旧内容 — `showLoadingProgress` 新增清空 files/selectedPath/selectedCommitHash 并重置 filesList 为占位文案, 与 multi-diff 面板同步隐藏。(3) 搜索 UI 现代化 + 多字段 OR 匹配 — 搜索框增加放大镜图标和清除按钮, 圆角+聚焦发光样式; `searchCommits` 重写为全量获取后在 TS 端过滤, 任意关键词命中 hash/作者/邮箱/消息/日期任一字段即返回 (OR 逻辑), maxBuffer 提升至 64MB。
- 修复四个问题：(1) 仓库一直加载 — 回退 `refreshBranchesOnly`/`refreshRepositories` 拆分, 恢复 `refreshSelectors` 在并行块中 (先仓库再分支再提交), 删除两个临时方法。(2) 提交个数改为位置/总数 — `commits` 消息新增 `totalCommits` 字段, webview 新增 `updateCountLabel()` 显示 `选中位置/总数`, 点击行时本地更新 `selectedCommitHash` 即时刷新。(3) 状态栏和 panel 仅在有 git 仓库时显示 — `statusBar.ts` 重写为检查 Git API 仓库列表后决定 show/hide, 监听 `onDidOpenRepository`/`onDidCloseRepository` 动态更新; `extension.ts` 添加 `onDidChangeWorkspaceFolders` 监听; `refreshInternal` 在仓库列表为空时显示 "当前工作区未找到 Git 仓库"。(4) 搜索改为自动搜索+防抖 — 搜索输入从 `keydown` (Enter) 改为 `input` 事件 + 300ms 防抖 (`searchDebounceTimer`), Esc 清除保留; provider 端新增关键词去重检查 (`keywords.join('\0') === this.searchKeywords.join('\0')` 跳过相同搜索)。
- 修复三个问题：(1) commit 偶现两个高亮 — webview `commits` 消息处理新增 `selectedCommitHash` 有效性校验: 若当前选中不在新列表中则默认选第一个, 防止 `commits` 与 `selectedCommit` 消息间出现旧选中残留。(2) commit 区域必须始终有一个选中 — `performSearch` 和 `refreshSearchCleared` 发送 `commits` 后新增 `selectCommit` 调用选中首条; `refreshBranchCommits` 的 `selectedCommit` 查找补 `?? this.commits[0]` 兜底; webview `commits` 处理在无有效选中时本地默认选第一个 (不发消息, 由 provider 的 `selectedCommit` 确认)。(3) 选中节点默认展开描述 — `commits` 和 `selectedCommit` 消息处理均新增 `expandedCommits.clear()` + `expandedCommits.add(选中 key)`, 确保选中行始终展开描述, 非选中行始终折叠。
- 修复三个问题：(1) 首次加载速度变慢 — `getCommitHashes` (git rev-list --max-count=10000) 从 `Promise.allSettled`/`Promise.all` 关键路径移出, 改为提交显示后后台异步执行, 完成后通过 `updateTotalCommits` 消息更新计数; 涉及 `refreshInternal` 首次加载路径/普通路径和 `refreshBranchCommits` 三处。(2) 搜索删除按钮未垂直居中 — `#searchClear` 从 `top: 50%; transform: translateY(-50%)` 改为 `top: 0; bottom: 0; margin: auto 0`, font-size 从 15px 降为 14px, line-height 改为 16px。(3) 刷新按钮移到仓库选择器前面 — `#refreshBtn` 从 `#toolbarActions` 移出, 放到 `#header` 第一个位置。
- Changed Files 标题栏的树状/平铺文字按钮改为图标按钮: 使用 `.toolbar-icon` 样式, 树状模式显示阶梯形路径图标, 平铺模式显示三条横线图标; `renderFiles()` 根据模式切换 `<path d>` 和 `title` (显示方式（当前：树状/平铺）); `#filesHeader button` CSS 改为 `#filesHeader .toolbar-icon`。
- 调整提交描述展开状态：删除遗留的 `hideExpandHint()` 调用；修正 `selectCommit` 的回传消息会立即清空 `expandedCommits`、使刚展开描述收起的问题。现在仅当 `commits` 消息的提交列表实际变化（仓库/分支/搜索切换）时收起描述；同一列表的选择回传保留用户展开状态。已通过 `tsc --noEmit`、`git diff --check`；`gitkViewProvider.ts` 仅有既存未使用 `context` 提示。
- 修复首次子模块加载重复/总数递增：`getGitRepositories` 增加 `repositoriesInFlight` 单飞 Promise，缓存落地前的并发刷新复用同一扫描；新建 `collectSubmoduleRepositories` 先递归收集全部嵌套子模块，再以固定总数报告“加载仓库”进度，避免扫描中途动态增加 total 而出现多次加载观感。
- Changed Files 标题栏新增截图中的十个提交操作图标，按提交操作、分支操作、复制操作分组并置于显示方式前；图标使用自绘 16px 线框 SVG，标题栏增加 VS Code 风格焦点与组间距。Webview 的 `commitAction` 消息交由 VS Code Git 命令处理，复制 hash 使用剪贴板。显示方式图标改为简洁的列表/层级线条。已通过 `tsc --noEmit` 与 `git diff --check`。
- 修复加载进度交替闪烁后卡在"加载仓库"：`refreshSelectors` 新增 `silent` 参数，并行块内调用 `refreshSelectors(true)` 抑制仓库/分支进度消息；并行块前投递初始"加载提交..."消息。这样并行块内仅显示提交加载进度，不再与仓库/分支消息交替。
- 修复初始化一直显示"加载提交"：并行块内 `getGitCommits` 的 `onProgress` 回调投递 `1/1`（满格）后 `refreshSelectors` 仍在静默扫描，进度条满格不动看似卡住；移除并行块内 `getGitCommits` 的 `onProgress` 回调，进度条保持 indeterminate 动画直到 `Promise.allSettled` 完成。
- 恢复"加载仓库"进度显示：并行块内 `refreshSelectors` 改回非静默（正常投递仓库/分支进度），`getGitCommits` 保持无 onProgress（静默）。仅一个消息源投递进度，不会交替。
- 子模块发现优化：`collectSubmoduleRepositories` 由串行扫描改为 BFS 并行——同层仓库并行读 `.gitmodules`，同层子模块并行 `resolveRepositoryRoot`，显著减少串行 git 命令数。进度文案 "发现子模块 (N)..." 改为 "扫描子模块 (N)..."。
- 初始化流程改为严格顺序: 1.初始化环境(getFirstRepoPath) → 2.初始化仓库(getGitRepositories API+workspace) → 3.加载子仓库(collectSubmoduleRepositories BFS并行) → 4.加载分支(getGitBranches) → 5.加载提交(getGitCommits+getWorkingTreeChanges) → 6.刷新内容(发送commits+selectCommit)。移除并行块(原 refreshSelectors+getGitCommits 并行)，移除 refreshSelectors 的 silent 参数。getGitRepositoriesInternal 文案统一: "加载仓库"→"初始化仓库", "扫描子模块"→"加载子仓库", 移除发现后即时的"加载仓库"进度循环(Map赋值瞬间完成无需进度)。
- 恢复子模块发现进度：`collectSubmoduleRepositories` 新增 `onDiscovered` 回调，每发现一个子模块即调用；`getGitRepositoriesInternal` 传入回调，进度消息显示"发现子模块 (N)..."。
- Changed Files 全部图标重新设计为扁平化风格：更简洁的 SVG 路径，统一 1.4 描边宽度，15px 图标尺寸，组间增加 1px 竖线分隔符；树状/平铺切换图标路径同步更新。
- 三项改动：(1) "加载子仓库"文案全部改为"加载子模块"（gitLogProvider.ts 3处 + gitkViewProvider.ts 2处注释）。(2) 初始化流程审查通过，严格顺序无问题：初始化环境→初始化仓库→加载子模块→加载分支→加载提交→刷新内容。(3) 仓库图标按"是否有子模块"区分：GitRepositoryOption 新增 hasSubmodules 字段，getGitRepositoriesInternal 遍历 allRepositories 的 parentPath 构建 parentPaths 集合判定；webview repositoryIcon 参数从 kind(repo/subrepo) 改为 hasSubmodules(boolean)，有子模块的仓库显示文件夹+内嵌矩形图标(.has-submodules 绿色)，无子模块显示纯文件夹图标；CSS .repository-root 改为 .has-submodules。
- 重绘 Changed Files 标题栏全部操作图标：Add Tag、分支创建/合并、Checkout、Cherry-pick、Revert、Drop、Rebase、Reset、复制 Hash 与树状/平铺切换均统一为 16px、1.5px 圆角线框 SVG；标题栏按钮统一为 24px，并补充 VS Code 风格 hover、active、focus 状态和分组间距。`npm run compile` 通过。
- 修复同一 commit 折叠/展开会重刷 Changed Files：Webview `setupRow` 仅在 `!wasSelected`（选择新提交）时发送 `selectCommit` 消息；当前已选 commit 的展开/收起只更新 `expandedCommits` 并本地 `render()`，不再触发宿主 `setCommitFiles`、Git diff-tree 查询和文件列表 loading/重绘。`npm run compile` 通过。
- Changed Files 标题布局调整：将 8 位简短 commit id（`filesCommitHash`）和 Copy Hash 按钮移动到 "Changed Files" 文案后，组成左侧紧凑分组；其余提交/分支操作仍右对齐。新增 `updateFilesCommitHash()`，在选择提交、点击提交行、加载清空时同步显示/清空，工作区 changes/staged 不显示 hash。`npm run compile` 通过。
- 优化“加载更多提交”Webview 端性能：新增 `columnWidthChars` 缓存和 `updateColumnWidths(items, startIndex)`；全量 render 时重置后扫描一次，appendRows 时仅扫描新增批次，而不再对全部 commits 执行三次 `map` + `columnWidth`。仍在新增 lane 或 refs 导致图宽扩张时安全回退全量 render。`npm run compile` 通过。
- Changed Files 提交操作完成接线：Add Tag、Create Branch、Checkout、Cherry-pick、Revert、Merge、Rebase、Reset、Copy Hash 均由 `runCommitAction` 通过受控 `runGitCommand` 调用 Git；标签/分支要求输入名称，Reset 要求确认模式，Drop 保持不显示/不自动改写历史。`updateFilesCommitHash()` 在 `changes`/`staged` 虚拟提交时隐藏全部 `.commit-action` 与操作组。`runGitSync(..., 'pull')` 改为 `git pull --recurse-submodules` 后执行 `git submodule update --init --recursive`，覆盖未初始化及嵌套子模块。`npm run compile` 通过。
- 修复仓库切换与 Checkout 后提交区卡在“正在加载”：旧实现会直接清空 `refreshInFlight` 并并发启动新刷新，使旧请求与新请求交错，且旧 Promise 的 finally 可错误解除新请求的单飞锁。`refreshWithRetry()` 现等待旧刷新收敛后只执行最新 generation；`refresh()` 仅在自身 Promise 仍是当前请求时才清空 `refreshInFlight`；`selectRepositories()` 不再伪取消旧请求，并立即更新仓库状态订阅。`npm run compile` 通过。
- 优化仓库切换速度：仓库下拉选择调用 `refreshWithRetry(true)`，复用已加载仓库列表，跳过全量仓库/嵌套子模块扫描；刷新单飞锁新增 `refreshInFlightGeneration`，只复用同 generation 请求，新仓库切换可立即启动目标仓库加载，旧请求通过既有 generation 检查自动失效且不会释放新锁。`npm run compile` 通过。
