# 工作记录 - 2026-08-06

- 自定义 Diff 的新增/删除行、文本与 gutter 现优先使用 VS Code 主题令牌：`--vscode-diffEditor-insertedLineBackground` / `insertedTextBackground` / `diffEditorGutter-insertedLineBackground` 及对应 removed 令牌；Webview 未提供令牌时分别回退绿色与红色半透明色。联网核对 Microsoft VS Code Diff Editor 颜色注册后实施；`npm run compile` 通过，`customDiffPanel.ts` 无诊断。
- 自定义 Diff 并排模式使用 `repeat(2, minmax(0, 1fr))` 固定左右两栏等宽；将横向溢出滚动移入各自 pane，避免超长内容撑宽其中一侧。`npm run compile` 通过，`customDiffPanel.ts` 无诊断。
- pinned 文件栏改为由主滚动视口中首个可见 `.diff` 条目驱动：滚动使用 `requestAnimationFrame` 节流后更新 pinned，并经 `selectFile` 回传同步 Changed Files 高亮；初始无定向文件时默认选首项。`npm run compile` 通过，`customDiffPanel.ts` 无诊断。
- Multi-Diff 显示现受 Gitk 状态门禁：`GitkViewProvider` 维护 Gitk 加载完成与 Webview 焦点状态；选择提交、点击变更文件时仅在 `!isLoading && isFocused` 时打开 Diff，Gitk 重新加载时会关闭现有 Diff。为避免 Diff 自身获得编辑器焦点后立刻关闭，失焦只阻止后续打开而不主动关闭既有面板。涉及 `gitkViewProvider.ts`、`commitFilesViewProvider.ts`、`customDiffPanel.ts`、`extension.ts`；`npm run compile` 通过，相关文件无诊断。
- 已进行首轮加载性能优化：`getGitCommits` 预建 `refsByCommit`，移除每个提交对全部 refs 的重复扫描；`GitkViewProvider` 合并并发刷新、用 generation 取消旧重试、复用已渲染提交且节流 resize 重渲染；`CustomDiffPanel` 为同一提交的文件内容读取缓存 Promise，合并重复/并发 Git 文档请求，切换提交清缓存。`npm run compile` 通过，涉及文件无诊断。
- 统一自定义 Diff 并排视觉规格：`customDiffPanel.ts` 的 `.split` 固定为两个相同的 `minmax(0, 1fr)` 网格栏；`.pane` 禁用独立滚动；标题固定为 25px，代码行固定使用编辑器主题行高且按相同盒模型渲染，超长内容裁剪而不影响栏宽。`npm run compile` 通过，文件无诊断。
- 对齐并排 Diff 的语义色：成对删除/新增行标为灰色修改区；仅删除行呈红色，对侧呈斜线灰色占位；仅新增行呈绿色，对侧呈斜线灰色占位。内联模式的修改仍显示为红色删除加绿色新增。优先使用 VS Code 主题令牌并提供 Webview 回退色；`npm run compile` 通过，文件无诊断。
- 联网直接核对 VS Code `diffEditorDecorations.ts` 和 `style.css` 后修正语义：VS Code 对“修改”并无中性灰色行样式，而是原始侧使用删除（红色）背景与字符高亮，修改侧使用新增（绿色）背景与字符高亮；纯增删的空侧使用 `--vscode-diffEditor-diagonalFill` 的 8px `-45deg` 斜线图案。`customDiffPanel.ts` 已采用该规则，并补齐文本边框主题令牌；`npm run compile` 通过且无诊断。
- 已诊断自定义 Diff 内容加载失败和速度慢的根因：提交/范围异步请求缺少 hash 或 generation 校验，旧请求能覆盖新提交；范围与文件内版本读取均以 `Promise.all` 无并发上限执行；Webview 主线程用最多 160000 单元的 LCS 计算完整文本，折叠发生在计算之后；`readText` 将 Git 读取异常伪装成正文。后续修复应优先加请求代次校验、2 文件并发队列、降低 LCS 阈值并结构化显示读取错误。
- 修复点击 Changed Files 后 Multi-Diff 消失：`openDiff(hash, filePath)` 之前统一调用 `canShowMultiDiff()`，而文件列表点击会使 Gitk Webview 失焦，导致该条件失败并隐藏面板。现仅在无指定文件的自动打开路径要求 Gitk 聚焦；指定 `filePath` 的用户点击可继续显示/定位 Diff，但 Gitk 加载时仍关闭。`npm run compile` 通过且无诊断。
- Gitk 底部容器已改为一个不可单独折叠的 Webview View：`package.json` 移除 `vscode-gitk.commitFilesView`；`GitkViewProvider` 内嵌固定高度比例的 Changed Files 区，直接加载所选提交文件、切换树状/平铺并打开/同步自定义 Diff；`extension.ts` 不再注册 `CommitFilesViewProvider`。文件列表异步结果以 hash 校验防止旧提交覆盖。
- Gitk 主 Webview 现为左右固定分栏：`#workspace` 横向布局，提交图在左并可伸缩，Changed Files 在右侧（280px，最小 180px），仍是同一不可单独折叠的 View。`CustomDiffPanel` 增加提交 generation 校验、最多两个 Git 文档读取并发、预读范围从 900px 缩至 360px/两文件批次，并将读取失败作为结构化错误展示，避免错误文本进入 LCS Diff。`npm run compile` 通过，相关文件无诊断。
- Gitk 左右分栏调整为默认等宽：`#workspace` 使用两个 `minmax(180px, 1fr)` 区域与中间 5px 分隔条；鼠标拖动分隔条可实时改变提交图和 Changed Files 宽度，并保持两侧至少 180px。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 提交列表的 `date` 列默认宽度从动态计算结果扩大为两倍，以完整显示时间；`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- `CustomDiffPanel` 改为完整加载模式：重置后以最多 6 个 Git 文本读取并发准备全部文件，逐文件报告 `progress`，只在全部 Diff 内容就绪后显示列表。Webview 增加主题化加载进度条，移除 IntersectionObserver/分段 `loadRange` 请求，且保留 generation 防止旧提交的结果显示。联网核对 VS Code Git API 后确认公开 `Repository.diffBetween()` 仅提供变更文件元数据，无法提供 patch/hunk；当前不直接调用 Git CLI 的架构下，全量读取仍是可用数据源。`npm run compile` 通过，`customDiffPanel.ts` 无诊断。
- 提交表格调整：图列目标宽度缩小一半，Commit ID 默认宽度扩大 25%，作者列扩大约 33%；描述列不再截断，以 50ch 宽度自动换行，提交图 SVG 会随增高的行同步绘制；所有标题单元格文字居中。`npm run compile` 和 `git diff --check` 通过，`gitkViewProvider.ts` 无诊断。
- 修复全量 Diff 在“准备中”停留：文件内容读取完成后，Webview 的 LCS/DOM 渲染原在单个 message 回调内同步执行，阻塞事件循环且无法及时更新加载页；现 `renderAllDiffs` 使用 `requestAnimationFrame` 每帧渲染一个文件，动态显示“正在生成 Diff”进度，全部完成后显式隐藏加载区并显示列表。提交图列重构为自适应宽度：按实际 lane 数和最长 ref 名计算，图 SVG 右侧内嵌 HEAD/分支/远端/tag 标签，移除独立 Refs 列，长标签缩略并保留 title。`npm run compile`、`git diff --check` 通过，`customDiffPanel.ts` 与 `gitkViewProvider.ts` 无诊断。
- Diff 准备期间文件区点击现在可生效：同一提交的 `CustomDiffPanel.show()` 原本已会发送 `selectFile`，但 Webview 完整渲染结束时使用 reset 时的旧 `revealPath` 覆盖用户的新选择；现在 `selectFile` 实时更新 `revealPath`，`renderAllDiffs` 完成后读取该最新值定位并同步高亮。`npm run compile`、`git diff --check` 通过，`customDiffPanel.ts` 无诊断。
- Changed Files 平铺模式现在保留完整路径：文件夹前缀以 `.file-folder` 半透明显示，文件名保持正常前景色；重命名箭头和其后的旧路径已从文件区移除（仍通过 title 显示完整当前路径）。树状模式不受影响。`npm run compile`、`git diff --check` 通过，`gitkViewProvider.ts` 无诊断。
- Changed Files 的 Tree 模式已重构为层级文件树：按路径创建文件夹节点、显示展开/折叠箭头及缩进；点击文件夹切换 `collapsedFolders`，被折叠目录的所有后代节点均隐藏，文件点击和选择高亮保持原行为。平铺模式保持完整路径半透明前缀。`npm run compile`、`git diff --check` 通过，`gitkViewProvider.ts` 无诊断。
- Changed Files 的 Tree 模式现按完整父目录路径分组，例如 `src/components` 仅显示为一个目录行；不再生成 `src`、`components` 等多层可折叠节点。目录展开后显示其直属变更文件，根目录文件保持直接展示。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 修复部分历史 Diff 无法读取：`gitLogProvider.ts` 的 `statusFromChange` 已按 VS Code Git API 当前 `Status` 数值重写，新增类状态映射为 `A`、删除类状态映射为 `D`，使 `CustomDiffPanel` 不再为新增文件读取不存在的 `${hash}^` 版本；重命名变更改从 `originalUri` 得到父提交旧路径，`uri` 保持新路径。`npm run compile` 和 `git diff --check` 通过，`gitLogProvider.ts` 无诊断。
- 文件区 `#filesList` 现作为弹性受限高度滚动容器；每次高亮同步重绘后会检测选中项是否越出容器，上越界即滚至首个可见位置、下越界即滚至最后可见位置。自定义 Diff 的文件定位同样仅在目标 `.diff` 越出窗口（固定工具栏和 pinned 文件栏以下）时滚动到可见边界，避免每次选择都强制置顶。`npm run compile` 与 `git diff --check` 通过，相关文件无诊断。
- 第二轮 Diff 加载优化：`CustomDiffPanel` 的 Git 文档读取并发由 6 提升至 8，并将 `loadAll` 从“每个文件单独 async map + 每完成一个都 postMessage”改为固定数量 worker 拉取索引队列；保持结果原始顺序，进度每累计 4 个文件或最终完成时才发送，减少大量文件时 Promise/IPC 调度开销。公开 Git API 仍仅提供变更元数据而不提供 patch，因此保持 git scheme 文本读取架构。`npm run compile` 与 `git diff --check` 通过，`customDiffPanel.ts` 无诊断。
- 第三轮 Diff 加载优化：按用户允许变更方案，`CustomDiffPanel` 改用本地 `git cat-file --batch`，一次子进程批量读取当前提交与父提交所需的 Git blob，替代每个文件两次 `git:` TextDocument IPC。输出按对象大小解析回现有 `DiffPayload`，并保留新增/删除/重命名路径规则与 generation 竞态保护。此桌面方案通常比逐文件读取快得多，但不再兼容 Web Extension Host。`npm run compile` 与 `git diff --check` 通过，`customDiffPanel.ts` 无诊断。
- 修复“无法加载变更文件：Cannot read properties of undefined (reading 'clear')”：异常来自 VS Code Git 扩展的 `Repository.diffBetween` 内部缓存路径；`getCommitFiles` 改用本地 `git diff-tree --root --name-status -r -M -C <hash>` 获取文件状态，并新增解析函数保留新增/删除/类型变更/重命名/复制路径。`npm run compile`、`git diff --check` 通过，`gitLogProvider.ts` 无诊断。
- 提交区工具栏现提供仓库/子仓库和分支两个下拉框：仓库来源为 Git 扩展已识别的 `api.repositories`，分支来源为当前仓库 `state.refs`（跳过 tag）；切换任一项会清空旧提交、变更文件及 Diff 面板后重新加载，提交历史通过 `repo.log({ hash: selectedBranch })` 限定到选择分支。`npm run compile`、`git diff --check` 通过，`gitkViewProvider.ts` 与 `gitLogProvider.ts` 无诊断。
- 提交区刷新控件由文字按钮改为 `↻` 图标按钮，带 `title` 与 `aria-label`，通过 `margin-left: auto` 放置于工具栏最右侧。`npm run compile`、`git diff --check` 通过，`gitkViewProvider.ts` 无诊断。
- 提交区选择器进一步对齐 VS Code Git Graph 的信息范围：仓库菜单按 Git API 全部 `repositories` 显示相对路径，并根据嵌套根路径标记子仓库；分支菜单包含当前仓库 `state.refs` 的全部本地和远程分支（tag 排除），以 optgroup 分组，切换分支仍使用原始 ref 名查询日志。`npm run compile`、`git diff --check` 通过，`gitLogProvider.ts` 与 `gitkViewProvider.ts` 无诊断。
- 已联网核对 VS Code `scmHistoryViewPane.ts` 与 `scmRepositoriesViewPane.ts`：Graph 仓库选择是 Quick Pick，包含 `Auto`（跟随 active repository）与全部 `ISCMViewService.repositories`，名称用 `provider.name`、描述用 `provider.rootUri.fsPath`；子仓库层级由 `provider.parentId` 而非文件路径推断。Graph 引用筛选是多选 Quick Pick，状态为 `all` / `auto` / ref ID 数组，引用由历史 provider 的 `provideHistoryItemRefs()` 提供并按 `category` 分组；此前项目的普通 select、路径推断与单分支筛选并不等同于内置实现，后续应按该模型重构。
- 工具栏调整：仓库选择仅显示根目录名并加 `repo` 标识；分支改用本地 Git `for-each-ref --format=%(refname:short)%09%(refname) refs/heads refs/remotes` 读取，解决 Git API `state.refs` 为空造成的分支缺失，并分组本地/远程分支。新增 fetch/pull/push 操作，调用 Git 扩展 `Repository` API；安装 `@vscode/codicons@0.0.36`，Webview 加载其官方字体并使用 cloud-download/repo-pull/repo-push/refresh 图标。选择器宽度按内容自适应并设最大占比。`npm run compile`、`git diff --check` 通过，相关文件无诊断。
- 仓库选择器现会基于 Git 扩展已识别仓库递归解析每个已初始化子模块的 `.gitmodules`，经 `git rev-parse --show-toplevel` 去重验证后加入列表，根仓库标记 `repo`、子仓库标记 `subrepo`。分支与提交图标签统一改从 `git for-each-ref` 读取：UI 显示 `%(refname:short)` 的分支名，但筛选继续传完整 `refs/heads/*` / `refs/remotes/*` 给 `repo.log`，解决分支名称缺失和同名歧义。子模块配置使用 NUL 分隔解析。`npm run compile` 与 `git diff --check` 通过，`gitLogProvider.ts` 无诊断。
- 提交表格的 graph/hash/message/author/date 五列现由当前提交的最长内容动态确定宽度，并统一额外保留六个字符空间；图列以实际字体测得的六字符像素宽度补白。移除日期列 19ch 最小宽度。Fetch/Pull/Push/Refresh 由外部 Codicon 字体替换为内联自生成 SVG 线条图标。`npm run compile` 与 `git diff --check` 通过，`gitkViewProvider.ts` 无诊断。
- 分支选择器刷新时会通过 `git symbolic-ref --quiet --short HEAD` 获取当前本地分支，并在没有用户已选有效分支时默认选择其完整 ref（`refs/heads/<name>`）；因此按钮显示当前分支名而不是“所有分支”，分支读取与当前分支查询并行。游离 HEAD 或分支不存在时保留“所有分支”。`npm run compile`、`git diff --check` 通过，两个改动文件无诊断。
- 修复仓库选择后“未找到 Git 仓库”：`getGitRepositories()` 会把每个 workspace folder 用 `git rev-parse --show-toplevel` 验证并补入列表，不再完全依赖 Git 扩展异步填充 `api.repositories`；`getGitCommits()`、`runGitSync()` 使用标准化路径的 `findRepository()` 兜底匹配，避免 URI 表示差异使 `api.getRepository()` 返回空。`npm run compile`、`git diff --check` 通过，`gitLogProvider.ts` 无诊断。
- 提交表头原已居中；提交行的 graph/hash/message/author/date 单元格现统一 `text-align:center`，并把 Graph 内 SVG 与引用标签组设为 flex 居中，保证所有列内容和标题居中。`npm run compile`、`git diff --check` 通过，`gitkViewProvider.ts` 无诊断。
- 分支下拉框初始与切换仓库期间显示禁用的“加载分支...”，收到 selector 数据后才启用；移除了分支菜单中的“所有分支”，无分支时显示“未找到分支”。仓库 option 移除子仓库箭头/通用前缀，只有 `description === 'repo'` 的根仓库前添加生成的 `◆` 仓库标识。`npm run compile`、`git diff --check` 通过，`gitkViewProvider.ts` 无诊断。
- Multi-Diff 的文件标题栏样式放大两倍（70px 高度、双倍字体/间距/图标槽）；移除独立 pinned 复制标题，改为每个 `.file-header` 在工具栏下 `position: sticky`。滚动时后续文件标题会自然将前一个标题顶出，避免两个相同标题并存；选中同步仍取顶部可见文件。定位边界同步改为 105px。`npm run compile`、`git diff --check` 通过，`customDiffPanel.ts` 无诊断。
- 仓库选择器在初始加载、手动刷新和切换仓库期间会和分支选择器同时置灰，并显示“加载仓库...”/“加载分支...”，返回 selectors 后按是否有仓库启用。Multi-Diff 从 Files 区点击文件时，按目标 `.file-header` 的顶端精确滚动到 105px 的首个可见位置；程序滚动后 200ms 内禁止视口自动同步，且以 `elementFromPoint` 判定顶部可见 diff，修复 sticky 标题和滚动事件导致跳回相邻文件的问题。`npm run compile`、`git diff --check` 通过，相关文件无诊断。
- 提交列表的表头与 graph/hash/message/author/date 内容现统一左对齐，Grid 仍以 `align-items: center` 保持垂直居中；仅描述列使用 `-webkit-line-clamp: 2` 限制两行并隐藏溢出内容。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 用户明确不恢复独立 pinned 文件栏；Multi-Diff 继续由各 `.file-header` 自身 sticky 顶推。其视觉改为浅色 VS Code widget 背景、细边框、10px 圆角、12px 横向留白与轻阴影的现代卡片，同时不新增复制标题。`npm run compile` 通过，`customDiffPanel.ts` 无诊断。
- 联网查看 `mhutchie/vscode-git-graph` 的公开源码后，将项目提交表格改为独立的“分支图”和“引用”相邻列：SVG 仅渲染在 `col-graph`，分支/HEAD/tag 标签改渲染到 `col-refs`；六列表头、Grid 模板、列宽与拖拽手柄同步更新。图列宽度仅按实际 lane 轨道计算，引用列继续按最长引用内容另加六字符留白。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 用户要求 Multi-Diff 的 sticky 文件标题卡片缩小至原现代化样式约三分之一：高度 24px、字号约 0.67 倍、间距/边距/圆角/阴影同步收紧；仍不恢复独立 pinned 栏。`npm run compile` 通过，`customDiffPanel.ts` 无诊断。
- 主提交列表加载完成后，若此前选择的 `currentHash` 仍在当前日志中则保持它；否则自动调用 `selectCommit(this.commits[0].hash)` 选中第一条提交、同步高亮并加载其 Changed Files/Multi-Diff。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- Multi-Diff sticky 文件标题卡片在 24px 紧凑版基础上放大三分之一：最小高度 32px、字号约 0.89 倍、间距/图标槽/圆角/阴影同步增大，仍不使用独立 pinned 栏。向下箭头 `.chevron` 改为伸展高度的 flex 容器，以 `align-items`/`justify-content: center` 和 `line-height: 1` 修正其字体基线垂直偏移。标题卡片现使用 `width: 100%` 与 `margin: 4px 0` 横向铺满 Multi-Diff 视图。`npm run compile` 通过，`customDiffPanel.ts` 无诊断。
- 提交列表现在从 `git status --porcelain=v1 -z --untracked-files=all` 读取工作区状态；当存在文件时，真实提交顶部按顺序显示虚拟 `Changes`（未暂存）和 `Staged Changes`（暂存）行。选中后复用 Changed Files 与自定义 Multi-Diff：暂存比较 `HEAD` 对 index，未暂存比较 index 对工作区文件。重命名旧路径按 index/working-tree 状态分别保存，刷新时尽量保留当前虚拟行选择。`npm run compile` 通过，相关三个 TypeScript 文件无诊断。
- Multi-Diff 工具栏新增上/下修改点按钮。并排模式以右侧新增/修改后行作为每处变更唯一目标，内联模式以新增/删除行为目标；按当前位置向前/后平滑滚动、到边界循环，并同步选中的 Changed File。`npm run compile` 与 `git diff --check -- src/customDiffPanel.ts` 通过，`customDiffPanel.ts` 无诊断。
- Multi-Diff sticky 文件标题移除 `:hover` 背景覆盖，避免 pinned 状态悬停变透明；标题点击只调用现有 `revealDiffTarget(holder)` 将该文件标题滚回工具栏下方，不再调用 `selectFile` 或向扩展回传 `selectFile`，因此不会改变 Changed Files 高亮。`npm run compile` 与 `git diff --check -- src/customDiffPanel.ts` 通过，`customDiffPanel.ts` 无诊断。
- Multi-Diff pinned 标题定位改为按 `.diff` 容器文档坐标减去 toolbar 实际高度的绝对滚动，保证文件顶部完整露出。修改点导航改为借鉴 VS Code 的 hunk 概念：`calculateDiff` 为每个连续变更块分配 `change`，渲染为 `data-change`，上下按钮仅跳到每个 hunk 的第一行并在首尾循环；不再按每一行或左右列重复跳转。成对的替换行 (`modified-removed`/`modified-added`) 使用 VS Code 主题的 unchanged-region 中性灰底，单侧新增/删除继续保留绿/红语义。`npm run compile`、`git diff --check` 通过，文件无诊断。
- 修复 Multi-Diff 修改点识别：并排模式不再只查询右侧行（纯删除块右侧是 placeholder 会被遗漏），而是按每个 `.diff` 内首个非 placeholder 的 `data-change` 行收集每个 hunk，并按实际页面纵坐标排序；内联模式沿用同一归并方式。`npm run compile` 与 `git diff --check -- src/customDiffPanel.ts` 通过，文件无诊断。
- 双侧替换 Diff 行现使用 VS Code `diffEditor-unchangedCodeBackground`（缺失时 `rgba(86, 156, 214, .14)`）淡蓝底。修改点识别重构为按当前布局遍历每个 `.diff`，用 `Map<change, firstLine>` 聚合首个非占位行，再按页面纵坐标排序，适用新增、删除和替换 hunk。提交日志新增 `body` 保留标题后的原始正文；提交行点击可展开/收起正文，展开状态按 hash 保持，图形 SVG 固定首行高度避免节点位移，描述文本点击不触发收起。`npm run compile` 和 `git diff --check` 通过，三个改动文件无诊断。
- Multi-Diff WebviewPanel 的 `show()` 现在在它原有 editor group 中调用 `reveal(..., false)`，允许 Multi-Diff 获得编辑器焦点。焦点语义明确为 Gitk 范围包含 Multi-Diff，因此 Gitk Webview 触发 `blur`（因 Multi-Diff 获焦）不会关闭 Diff；Gitk Webview 重新获得焦点时，`setFocused(true)` 仍会在已有提交、变更文件且加载结束后重开/恢复 Multi-Diff。VS Code 公共 API 没有 WebviewPanel 的非销毁 `hide()`。`npm run compile`、`git diff --check -- src/customDiffPanel.ts` 通过，文件无诊断。
- 提交描述展开改为互斥：任意提交行或已展开正文被点击时，先收起全部描述；若目标此前未展开则只展开该目标，因此任一时刻最多一条正文展开。提交仍会同步原有选中与 `selectCommit` 行为。`npm run compile`、`git diff --check -- src/gitkViewProvider.ts` 通过，文件无诊断。
- Gitk Webview 的 `setFocused(false)` 现在会调用 `customDiffPanel.hide()` 关闭 Multi-Diff；此前没有任何 blur 到 hide 的调用，所以 Gitk 失焦后 Diff 会继续存在。不可读取 Git 对象不再将整个文件替换为单行错误：仍渲染普通 Diff 的文件头、左右（及内联）面板、状态和路径，仅在内容区域显示读取错误。`npm run compile` 与 `git diff --check -- src/gitkViewProvider.ts src/customDiffPanel.ts` 通过，两个文件无诊断。
- 点击 Commit 的加载流程不再以 `canShowMultiDiff()`（取决于 Webview 瞬时焦点）决定是否打开 Diff；只要该 Commit 有变更文件即调用 `openDiff()`，确保每次 Commit 选择都刷新到对应 Multi-Diff。`npm run compile`、`git diff --check -- src/gitkViewProvider.ts` 通过，文件无诊断。
- Multi-Diff 显示策略改为基于 Gitk `WebviewView.visible`：`canShowMultiDiff()` 要求未加载中、Gitk 视图可见且已有当前选择；`onDidChangeVisibility` 统一处理，视图隐藏即关闭 Diff，可见即恢复当前 Diff。DOM focus/blur 仅保留现有可用性事件，不再决定 Diff 的显示或关闭。`npm run compile`、`git diff --check -- src/gitkViewProvider.ts` 通过，文件无诊断。
- 仓库选择器由原生 `select` 改为自定义 `.repository-picker`：菜单绝对定位在触发按钮上方（`bottom: calc(100% + 4px)`），从而稳定向上展开；保留当前仓库文字、根仓库 `◆` 标记、标题提示、加载禁用、选中高亮及点击外部收起行为。分支仍使用原生下拉框。`npm run compile`、`git diff --check -- src/gitkViewProvider.ts` 通过，文件无诊断。
- 按用户要求，仓库控件已恢复为与分支完全一致的原生 `<select>`：共用 `#header select` 样式、`<option>` 数据渲染及 `change` 事件流程，移除了自定义 picker/menu 结构、样式和点击外部收起逻辑。原生菜单的实际展开方向由 Webview/Chromium 决定。`npm run compile` 与 `git diff --check -- src/gitkViewProvider.ts` 通过，`gitkViewProvider.ts` 无诊断。
- Multi-Diff 并排模式的左右 `.pane` 现允许独立横向滚动；行内容使用 `max-content` 保留超长代码宽度。通过捕获式 `scroll` 事件，当前栏 `scrollLeft` 会同步到同一文件的另一栏，并使用锁防止互相触发循环。内联模式不参与同步。`npm run compile` 与 `git diff --check -- src/customDiffPanel.ts` 通过，`customDiffPanel.ts` 无诊断。
- 仓库与分支选择器曾升级为统一的自定义 VS Code 风格菜单，但其 `position: absolute` 向上弹层受 Webview 视口及 `body { overflow: hidden; }` 裁剪，无法像原生下拉层一样绘制在顶栏外。按用户要求恢复原生 `<select>`，保留现代化 26px 圆角、hover/focus 样式；Chromium 的系统菜单可在可用空间不足时显示于触发器上方，仓库和分支遵循同一机制。`npm run compile`、`git diff --check -- src/gitkViewProvider.ts` 通过，文件无诊断。
- Multi-Diff 对 Git 对象读取失败的内容区提示已统一为两行：`文件：<完整路径>` 和 `无法读取原因：该文件无法进行内容对比`；`DiffPayload.fullPath` 通过 `path.join(rootUri.fsPath, file.path)` 在提交和工作区 Diff 两条数据流中生成。不再显示底层 Git 对象路径/哈希错误，普通文件头及左右/内联栏框架仍保留。`npm run compile` 与 `git diff --check -- src/customDiffPanel.ts` 通过，文件无诊断。
- 仓库与分支选择器恢复为此前 Webview 内原生 `<select>` 弹窗形式：仓库选项保留根仓库 `◆` 标记，分支以本地/远程 `optgroup` 分组。已移除 `showQuickPick()` 与自定义按钮触发流程；`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 参照 `C:\Users\JUNHENG.LIANG\Desktop\vscode-git-graph-develop` 的 `web/dropdown.ts` 与 `web/styles/dropdown.css`，Gitk 的仓库和分支选择器改为 Webview 自定义 DOM 下拉：当前值按钮、可筛选的菜单、选项滚动、点击外部/Escape 关闭和 VS Code menu/input 主题变量。仓库保留根仓库 `◆` 与完整路径 title；分支保留本地/远程分组。菜单相对顶栏向下定位，避免顶部向上弹层被 Webview 视口裁剪。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 参照 `vscode-git-graph-develop` 的 `web/main.ts` 与 `web/graph.ts`，Gitk 提交列表已将独立“引用”列合并进“分支图 / 引用”列：SVG 图与 ref 标签同格横向显示；列结构从六列改为五列。点击有正文的提交仍互斥展开，但会重新渲染，展开行的 graph cell 跨两行且 SVG 高度拉伸、支线路径延续到详情底部，避免展开后图线断裂；引用标签改为紧凑且基于 VS Code 主题色的样式。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 仓库和分支自定义下拉菜单现会在打开时将 `max-height` 设置为当前 Webview 面板高度的一半，并在面板 resize 后更新已展开菜单；菜单改为纵向 flex 布局，筛选框固定，选项区域自动占用剩余高度且可滚动。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 提交表与 Changed Files 标题栏已采用 Multi-Diff pinned 卡片风格（32px、主题化背景、细边框、5px 圆角及轻阴影）；Commit ID 列恢复在作者之后。提交作者解析兼容对象/字符串并回退提交者或邮箱，行展示避免重复邮箱；所有提交均可展开，详情显示完整 Commit ID 与作者。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- Multi-Diff 上/下修改点跳转现为目标行添加 `change-reveal` 主题化 `outline` 与 selection 背景动画；并排模式依据共同的 `data-change` 使同一修改块左右栏同步高亮。导航垂直定位额外扣除当前文件 sticky header 高度，确保目标行位于 pinned 文件标题下方；提示一秒后消失。`customDiffPanel.ts` 无诊断。
- 默认提交视图现在直接在 `#graph` 渲染固定 `#commitHeader`，加载状态也可见；标题使用 Multi-Diff `.file-header` 对应的 32px 高度、4px 垂直外边距、7px 内边距、主题背景与边框、5px 圆角及轻阴影。提交列表内重复标题已删除，动态列宽同步至 `#graph` 以对齐固定标题栏。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- Multi-Diff 差异背景改为以连续的同一 `data-change` 修改块为单位渲染：并排左右栏分别应用新增、删除或修改块背景，内联模式将同一变更块合并为统一背景；单行仍保留更细粒度的文本差异底色。`customDiffPanel.ts` 无诊断。
- 修复 Multi-Diff 修改点上下跳转：候选目标改以视口中 pinned 标题下方的固定位置判断，不再将滚动坐标与页面坐标混用，避免重复选中当前块导致无法上下跳转。`customDiffPanel.ts` 无诊断。
- Multi-Diff 跳转反馈的 `change-reveal` 已从 `.line` 改为 `.change-block`：通过块内共同 `data-change` 找到相关块，使提示背景和框选覆盖完整连续修改区域（并排两栏同步），1 秒后自动移除。新增/删除也匹配空白占位侧，确保并排两栏完整变更块同步高亮。`customDiffPanel.ts` 无诊断。
- Multi-Diff 跳转现在在目标实际代码行的文本开头绘制固定的编辑器插入竖线光标（`change-cursor`）；新增/删除命中占位侧时自动转至实际代码行，后续跳转会移除旧光标并迁移至新行。保留鼠标悬停行的主题选中背景。`customDiffPanel.ts` 无诊断。
- 已按用户反馈撤回最近一次提交标题、Commit ID 与作者相关的 UI/数据改动，恢复已知稳定的加载实现。为避免仓库/分支读取异常导致界面永久加载，`refreshInternal()` 现隔离 `refreshSelectors()` 异常、退出加载状态并显示错误；列宽变量同时设置在持续存在的 `#graph` 容器，保证提前显示的表头不与提交列错位。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- Commit 与 Changed Files 标题栏现在采用同一全宽样式：贴合对应区域顶部和左右边缘，保留 Changed Files 原有 30px 高度，使用统一主题背景、前景色和底边框；移除了 pinned 卡片的外边距、圆角和阴影。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- Multi-Diff 修改点导航现在将目标代码行垂直居中于面板视口，使用目标行高度计算中心偏移并即时滚动；不再按工具栏或 sticky 文件标题的顶部偏移定位。`customDiffPanel.ts` 无诊断。
- Multi-Diff 的编辑器插入光标现支持点击定位：点击非占位代码行时移除旧 `change-cursor`，按鼠标相对代码文本横坐标设置 `--cursor-x`，在点击位置显示并以 1 秒间隔闪烁的竖线；跳转时仍定位至目标行文本起点。`customDiffPanel.ts` 无诊断。
- 已撤回“修改点定位在 pinned 文件标题下方”的导航规则；该规则此前已被目标代码垂直居中的实现替代，当前 `navigateChange()` 继续按面板居中定位。`customDiffPanel.ts` 无诊断。
- 分支选择器现在支持复选、多选状态与“全部分支/已选 N 个分支”摘要；勾选时菜单保持打开且仅本地更新勾选状态，不重新渲染选择器。扩展端按所选分支并行读取提交历史后按 hash 去重，使用刷新代次丢弃过期请求，刷新期间显示“正在刷新...”但保留既有提交列表。`npm run compile` 通过，`gitkViewProvider.ts` 与 `gitLogProvider.ts` 无诊断。
- Multi-Diff 移除 `.line:hover` 行高亮。点击插入光标改用 `caretRangeFromPoint()` 得到字符边界，防止竖线落在字母中间；修改点提示恢复为直接添加 `change-reveal`，不再通过强制回流重启动画。`customDiffPanel.ts` 无诊断。
- 修复 Multi-Diff 点击插入光标横向错位：默认位置与空字符位置均对齐 `.line-code` 的 8px 左内边距；点击后以字符范围的实际 `getBoundingClientRect().right` 相对代码单元左边界定位，替代未包含内边距的范围宽度计算。`customDiffPanel.ts` 无诊断。
- Multi-Diff 修改点导航取消循环：到首个修改点禁用“上一个”，到末个修改点禁用“下一个”；新 Diff 数据加载完成、重置或切换内联/并排布局时同步重置并重新计算按钮状态。`npm run compile` 通过，`customDiffPanel.ts` 无诊断。
- 修复大文件 Diff 回退遗漏变更：当 LCS 单元格超出阈值时，改为保留相同前缀/后缀并把中间区域渲染为带共同 `data-change` 的修改、纯新增或纯删除块，确保差异可见且可被修改点导航。导航居中计算现排除工具栏和当前 sticky 文件标题占用的区域，目标代码在剩余可视区居中，不会视觉停在文件名；`npm run compile` 和 `git diff --check -- src/customDiffPanel.ts` 通过，`customDiffPanel.ts` 无诊断。
- 已确认修改点目标始终来自 `.line[data-change]:not(.placeholder)`，不会选取文件标题；跳转时仍看到文件栏，是 `.file-header { position: sticky; top: 35px; }` 的设计结果。若要跳转后不显示该栏，需要在导航过程中暂时取消其 sticky 行为或改动标题呈现规则。
- Multi-Diff 点击 `.line-code` 右侧空白时，光标现在以完整文本 Range 的右边界定位到本行行末；点击文本范围内仍保持按浏览器 caret 字符边界定位。大文件 Diff 改为耐心排序（唯一行锚点 + LIS）递归分段，每个阈值内片段用精确 LCS，缺少锚点时保守回退，避免先前仅以前后缀将分散差异合并成单一错误块。`npm run compile`、`git diff --check -- src/customDiffPanel.ts` 通过，`customDiffPanel.ts` 无诊断。
- 点击光标事件已从仅监听 `.line-code` 改为监听整条 `.line`：点击行号栏、行容器或代码文本右侧空白时，均会把光标定位到该行最后一个字符后；代码文本内点击仍按字符边界定位。`npm run compile` 和 `git diff --check -- src/customDiffPanel.ts` 通过，`customDiffPanel.ts` 无诊断。
- Diff 行的代码网格列改为填满剩余可视宽度（`minmax(max-content, 1fr)`），且非占位 `.line-code` 使用 `cursor: text`；因此鼠标移到行末空白区域也显示编辑态 I-beam 光标。`npm run compile` 和 `git diff --check -- src/customDiffPanel.ts` 通过，`customDiffPanel.ts` 无诊断。
- Diff 语义与配色调整：仅删除/新增数量完全相同才显示为修改（两侧蓝底）；数量不一致的变化保持新增或删除，并在另一侧使用既有 `.placeholder` 斜线填充。随后按用户要求恢复 VS Code 原有 Diff 主题颜色：使用 `--vscode-diffEditor-*` 变量和原回退值，不再强制信号灯红绿及 50% 蓝底。`npm run compile` 与 `git diff --check -- src/customDiffPanel.ts` 通过，`customDiffPanel.ts` 无诊断。
- 曾将修改点高亮和光标改为持续显示；用户反馈该状态影响跳转后，已撤销：`.change-reveal` 恢复 1 秒渐退动画，`highlightChangeTarget` 恢复计时器清理高亮块，光标仍按原逻辑在新跳转时切换。随后修复跳转状态：以 `diff.dataset.index:data-change` 作为稳定键，而非仅依赖重绘后会失效的 DOM 引用；重置和布局切换会清空该键，防止重复、跳错或跳过修改点。再修复并排模式漏项：原选择器将逗号后的 `.right-pane` 只匹配在 `.diff` 祖先下，导致其右侧 `.line` 选择范围错误；现在左右栏各自完整匹配 `.line[data-change]:not(.placeholder)`，所有新增、删除与修改块均纳入导航。`npm run compile` 与 `git diff --check -- src/customDiffPanel.ts` 通过，`customDiffPanel.ts` 无诊断。
- 仓库选择器现支持复选、全选和清空。扩展端为每个选中仓库独立读取并构建提交图，提交行携带 `repositoryPath`，因此同 hash 的提交仍能从所属仓库读取 Changed Files 与 Multi-Diff。多仓库模式下隐藏分支筛选并清空工作区虚拟变更行；分支菜单同步加入全选和清空。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 优化仓库与分支筛选器：全选/清空即时同步复选框与摘要，不等待 selector 重绘；仓库清空不会再恢复默认选择，提交列表同步清空。摘要改为选项名用逗号拼接、固定前 20 个字符加 `...`，悬停显示完整文本。仓库与分支下拉框固定为 `20ch` 宽，弹出菜单仍保留内容自适应宽度。`repo:` 与 `branchs:` 固定显示于各自下拉框外。分支默认不勾选，空选择表示读取全部分支。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 仓库或分支全选时，选择器摘要分别固定显示为 `全部仓库`、`全部分支`，避免名称拼接被截断；悬停标题仍保留完整选中名称列表。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 切换仓库或分支筛选时，`GitkViewProvider` 立即清除旧 Commit、Changed Files、工作区状态与 Multi-Diff，并向 Webview 发送“正在加载提交...”加载态；重新获取历史后自动选择新列表第一条 Commit，加载其 Changed Files 和 Multi-Diff。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 多仓库全选时不再隐藏 `branchs` 控件，而是保留为禁用的“未找到分支”状态；单仓库首次加载和切换仓库后，分支默认选中全部且下拉菜单同步全部勾选。全选分支会改为一次默认 `repo.log()`，不再对每个分支分别读历史；相同的仓库/分支选择消息直接跳过，减少无效重载。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 修复默认全选后的单分支筛选：取消任一分支勾选时，Webview 改为只保留该分支并立即发送单一 ref，使 Commit 列表重新按该分支加载。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 修复 `Changes` / `Staged Changes` 虚拟提交没有文件的问题：虚拟行不携带 `repositoryPath`，而前端原逻辑要求该字段才发送 `selectCommit`，导致扩展端不会执行 `selectChanges()` / `selectStagedChanges()`。现在虚拟 hash 会单独发送选择消息。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 分支筛选对齐 Git Graph：空分支集合表示 `Show All` / “全部分支”，由默认日志读取全部可见历史；只在勾选具体分支时才传 refs 过滤。移除“默认全选所有分支”和“从全选取消一项自动变为单选”的自定义行为；菜单“清空”改为“显示全部”。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 根据用户后续定义恢复分支空选择为“未选择分支”，按钮名称恢复“清空”。修复筛选状态判断：仅在 `hasBranchSelection` 为真时传入 refs，不再按已选数量与总分支数比较而丢弃全选的过滤条件。`getGitCommits()` 继续使用 VS Code Git API 支持的 `hash` 参数，以所选 ref 作为日志起点；尝试使用不存在的 `range` 参数后已立即撤销。`npm run compile` 通过，`gitkViewProvider.ts` 与 `gitLogProvider.ts` 无诊断。
- 用户确认分支默认应使用全部分支：单仓库首次加载或仓库切换后，若用户尚未作分支选择，自动选中所有分支并将 `hasBranchSelection` 设为真，保证列表和菜单均是全选状态。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 修复 Commit 与自定义 Multi-Diff 的高亮同步：Webview 保存扩展端 `selectedCommit` 消息中的 hash/仓库路径，并在每次重绘时据此恢复选中行。`Changes`、`Staged Changes` 虚拟提交仅在其 hash 被当前选中时高亮，普通 Commit 同样可由 Multi-Diff 状态更新反向同步。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 为 `tsconfig.json` 显式配置 `types: ["node", "vscode"]`，确保 `customDiffPanel.ts` 的 `child_process`、`fs/promises`、`path` 和 `Buffer` 解析到已声明的 `@types/node`；`npm run compile` 通过，相关 Node 类型错误消失（保留一个既有未使用 `context` 提示）。
- 移除 `gitkViewProvider.ts` 中虚拟 `Changes` / `Staged Changes` 提交行 `working-tree` 的默认背景色，仍保留悬停和实际选中时的高亮；编译和该文件诊断均通过。
