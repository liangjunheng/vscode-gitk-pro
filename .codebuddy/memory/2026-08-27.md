- Commit editor 与 Repo 弹窗列表不同步（2026-08-27，已修复）：根因是 Repo 弹窗直接消费 `GitRepoController.totalRepoList`，但 Commit editor 的 `commitRepositories` 仅从 `UncommittedFilesWatcher.listCurrentHeadBranches()` 构建；新仓库需异步完成 git-dir、HEAD、分支及未提交 watcher 链路后才出现。现 `GitkViewProvider.syncCommitRepositories()` 直接遍历 `repoController.totalRepoList` 建卡，HEAD/未提交数据仅作为卡片内容补充；仓库拓扑发布时若 Commit editor 已打开，立即同步并更新卡片。同步函数去掉伪 async，所有打开/刷新调用同步执行。`npm run compile` 和 diagnostics 通过。已提交：`8b9fe34 修复提交编辑器子模块仓库同步`。
- 已按“可视 Diff 文件变化时立即重新检查”的最小方案修改并二次优化（2026-08-27）：复用 `UncommittedFilesWatcher` 现有仓库 `**/*` watcher，不新建每文件 watcher。`MultiDiffPanel.updateVirtualization()` 在 mounted 集合稳定后上报当前可视的 unstaged/untracked 路径，集合相同不重复发；面板关闭清空。Watcher 保存当前仓库可视集合，命中路径时在进入原 status 队列的同时立即发 `onVisibleDiffFileContentChanged`。初版即时链路虽使用独立 `visibleDiffReader`，但仍启动 `git cat-file` 重读不变左侧基准，可能继续与慢 Git status/磁盘竞争；现已去掉该 reader，可视 unstaged/untracked 更新只通过 `vscode.workspace.fs.readFile` 读取变化的右侧正文，复用当前 `DiffPayload.original`，保持独立 generation 门禁，彻底不启动 Git。新增 `[Gitk][VisibleDiffPerformance] filesystem event/content ready` 日志验证是否命中及 event-to-store。延迟内容事件过滤可视路径防重复读，切换提交清空旧集合。随后按用户确认进一步修正：命中可视路径的 `**/*` 事件不再加入 `pendingWorkspaceContentPaths`，只保留 `pendingPaths` 给状态队列确认文件结构，避免同一文件再次进入延迟内容通道。最终再按事件语义修正：仓库 watcher 分别注册 create/change/delete；可视文件的普通 `change` 事件发出即时内容事件后直接 return，不再进入 `refreshSlot`，而 create/delete 仍进入状态队列处理临时文件替换、删除和结构变化。`tsc --noEmit`、`git diff --check`、lint 均通过，尚未提交。
- 用户随后要求撤销刚添加的诊断日志；已删除 `uncommittedFilesWatcher.ts` 的 watcher 命中/可视事件日志、`gitkViewProvider.ts` 的可视内容完成日志及相关 `observedAt` 参数，保留可视路径 Set、即时内容刷新及 create/change/delete 事件分流实现；lint 与 `tsc --noEmit` 通过。
- 按用户新要求，在 `UncommittedFilesWatcher.handleWorkspaceFileChanged()` 事件入口增加 `**/*` 命中日志，输出 create/change/delete 事件类型及 `uri.fsPath` 完整文件系统路径。该文件 diagnostics 无错误；全量编译被 `gitkViewProvider.ts:327` 现有参数数量错误阻断，与本次单行日志改动无关。
- 为定位 workspace file change 到 MultiDiffPanel 可视卡片更新的耗时，仅在关键边界增加 `[Gitk][VisibleDiffPerformance][traceId]` 日志：可视 watcher 命中、工作区文件读取完成、Store 更新、Diff 快照发送、Webview 卡片更新。`traceId` 与 `observedAt` 沿 watcher event → Provider → snapshot 传递，所有阶段用 `Date.now() - observedAt` 输出累计毫秒，便于直接比较阶段差值。3 个修改文件 diagnostics 与 `npx tsc --noEmit -p ./` 均通过。
- 分析性能日志发现追踪实现存在归因错误：`visibleDiffPerformanceTrace` 写入后从未消费/清空，而 `MultiDiffPanel` 订阅 `files/diffLoading/diffError/diffProgress`，后续任意 Store 发布都会通过 getter 重复携带最后一次 trace，因而出现同一 trace 在 65s、153s、227s 后反复输出；这些不是同一次文件更新耗时。当前 `card updated` 也位于 `updateVirtualization()` 调用后、`Promise.all(mounts)` 完成前，只代表卡片外壳同步重建，不代表 Monaco diff 更新完成。
- 说明 `workspace file change` 与 `workspace read complete` 并非必然成对：前者位于 `handleWorkspaceFileChanged()` 所有过滤之前，对 `**/*` 每次命中都打印；只有路径命中 `visibleDiffPathsByRepository` 才发可视内容事件，Provider 还会再次按 `visibleDiffPaths` 和当前 `DiffPayload` 过滤，任一集合未命中都会在执行 `workspace.fs.readFile()` 前返回，因此不打印 read complete。
- 进一步判断可视集合可能未及时发布：Webview 的 `publishVisibleDiffPaths()` 只在 `updateVirtualization()` 的 `Promise.all(mounts)` 完成后调用，而 `mountEntry()` 要等待 Monaco `onDidUpdateDiff`；在此之前 watcher 仍持有旧集合。另外 `updateVirtualization()` 在 `cards.length === 0` 时直接 return，不会发布空集合，存在保留旧路径的确定缺口。是否属于当前现象可用有无 `watcher visible file` 区分。
- 已从根因修复 MultiDiff 可视路径发布：集合改由 `isCardVisible(entry)` 的视口相交状态计算，不再依赖 `entry.mounted`；`updateVirtualization()` 算出范围后立即发布，不再等待 Monaco `Promise.all(mounts)`；空卡片快照及折叠操作立即发布更新，消除旧路径残留。`multiDiffPanel.ts` diagnostics 与 `npx tsc --noEmit -p ./` 通过。
- 用户要求撤销上一项可视路径发布时序修改；已精确恢复为按 `entry.mounted` 收集路径、等待 `Promise.all(mounts)` 后发布，移除空列表/空快照/折叠时的即时发布。性能追踪日志等更早修改保留。撤销后 diagnostics 与 `npx tsc --noEmit -p ./` 通过。
- 定位 `VisibleDiffPerformance` 后续完全不打印的确定根因：`selectCommit()` 在 Extension Host 调 `updateVisibleDiffPaths([])` 清空 watcher 集合，但 Webview 的 `lastVisibleDiffPaths` 未同步清空；新快照若可视路径与上次相同，`publishVisibleDiffPaths()` 因字符串去重直接 return，不再上报，导致 Host 永久保持空集合，文件事件只打印普通 workspace change，无法进入性能链路。
- 已从根因修复该状态副本分裂：`multiDiffPanel.ts` 的 `dispose()` 在销毁全部卡片时同步 `lastVisibleDiffPaths=''`，使去重键生命周期与卡片集合一致；卡片重建后即便算出相同路径也会重新上报，Host 集合不再停留空态。修复前另加了三处诊断日志（Provider `updateVisibleDiffPaths`、Webview `publishVisibleDiffPaths` 含 deduped、Watcher `setVisibleDiffPaths`）用于实证。`npx tsc --noEmit -p ./` 与 diagnostics 通过。注意 `setVisibleDiffPaths` 日志里的 `repositoryPath` 是仓库根路径（第一参数），真正的可视文件在 `paths`（第二参数），`paths: Array(0)` 表示清空而非监听文件夹。
- 用户实测日志给出决定性证据（2026-08-27）：`dispose()` 重置去重键并未解决问题，说明那不是根因。诊断链路显示 uncommitted 视图下 `publishVisibleDiffPaths` 的 `cards=37` 且 `kinds=staged|staged|...`（全部 staged），过滤条件 `workingTreeKind==='unstaged'||'untracked'` 天然得空数组 → `paths=[]` → `setVisibleDiffPaths` 恒空。即当前打开的虚拟提交面板里只有 staged 卡片、没有 unstaged/untracked，可视即时刷新链路按设计只服务工作区右侧可编辑的 unstaged/untracked，对 staged（比较 index↔HEAD，编辑工作区不改变其 diff）本就不纳入。根因是"面板内容全是 staged"而非状态副本或文件监听问题；`workingTreeKind` 从 `selectWorkingTreeChanges`→`DiffReader.createPayloads/readWorkingTreeDiffs`→`publish` 全程保留，未丢失。待用户确认后需清理本轮所有诊断日志。
- 用户决定：所有可视卡片（含 staged）都要即时刷新，流程统一为 publishVisibleDiffPaths(不限类型)→setVisibleDiffPaths→**/*命中→onVisibleDiffFileContentChanged→重读diff内容→渲染。按"移除冗余逻辑"落地（2026-08-27）：`publishVisibleDiffPaths` 去掉 unstaged/untracked 过滤（仅 mounted&&!collapsed）；`updateVisibleDiffPaths` 去掉 currentHash/staged/files.some 过滤，路径直接透传；核心是 `refreshVisibleWorkingTreeDiffs` 删除自造的 `fs.readFile`+`!=='staged'` 过滤，改用既有 `this.diffReader.readDiffs(rootUri,'uncommitted',files,'uncommitted')`——它内部已按 workingTreeKind 分流（staged 读 index、unstaged 读工作区、untracked 左空），保证 staged 右侧是 index 内容而非工作区正文，语义正确。连带删除失去引用的死代码 `containsNul`。代价：改用 DiffReader 会启动 git 子进程，比纯 fs.readFile 慢，但这是正确读取 staged index 的必要手段，非冗余。`npx tsc --noEmit -p ./` 与三文件 lint 通过。诊断日志（VisibleDiffPerformance 各阶段、publish/setVisibleDiffPaths/received 等）仍保留，待用户确认效果后统一清理。
- 澄清"setVisibleDiffPaths 有值但 staged 卡片编辑工作区不刷新"：这是正确语义而非 bug。staged 卡片右侧 = index 内容（`readsIndex` 对 `workingTreeKind==='staged'` 返回 true，`modified` 取 `:path`），编辑工作区不改 index，重读结果与旧值一致，故视觉无变化；staged 真正刷新时机是 git add/reset 引起的 .git/index 变化，由 indexWatcher 走全量 status 通道。验证法：改一个 unstaged 文件应能即时刷新。
- 已按用户要求在四处补防回归注释（2026-08-27）：`updateVisibleDiffPaths`（勿重加类型过滤）、`refreshVisibleWorkingTreeDiffs`（必须用 DiffReader、staged 看不到变化是对的、严禁改读工作区正文）、`publishVisibleDiffPaths`（不按类型过滤 + 去重键须随 dispose 清空）、watcher 可视分支（staged 内容不变符合语义、勿误判链路断裂）。三文件 lint 通过。
- staged 卡片即时刷新方案定案并从根因实现（2026-08-27）：正确刷新源是 .git/index 变化（git add/reset），非工作区变化。复用既有 indexWatcher 通道，补两处数据流缺口：(1) `uncommittedFilesWatcher.refreshSlot` 里 `contentChangedPaths` 原只含 workspaceContentPaths，现 `reconcileIndex` 时并入 `slot.indexChangedPaths`(旧)+`currentIndexChangedPaths`(新)，解决 git add 已暂存文件时清单不变(`previousChanges.equals`)导致漏发内容事件；(2) `gitkViewProvider.onWorkingTreeFileContentChanged` 原用 `nonVisiblePaths` 排除可视路径(理由是走工作区快通道)，但 index 变化不经快通道，会漏刷可视 staged，现改为对全部 affectedPaths 调 `refreshWorkingTreeDiffs`(DiffReader 按类型正确读，staged 读 index，diffGeneration 门禁防重复覆盖)。最终分工：unstaged/untracked 由工作区变化+visibleDiffContentEmitter 快通道；staged 由 index 变化+contentChangesEmitter。`npx tsc --noEmit` 与两文件 lint 通过，未提交。诊断日志仍待确认后清理。
- 将单个 "Uncommitted Changes" 虚拟提交行拆成两个虚拟提交行 "Changes"(未暂存/未跟踪) 与 "Staged Changes"(已暂存)，底层共用同一份 WorkingTreeChanges（2026-08-27）。设计：新增两个虚拟 hash `'changes'`/`'staged'` 取代旧的单一 `'uncommitted'`。types/git.ts 加 `WORKING_TREE_HASHES`/`WorkingTreeHash`/`isWorkingTreeHash(unknown)` helper。改动点：state.ts workingTreeRows 类型 hash 改 `'changes'|'staged'`；pushStateToWebview 产出两行(各自 enabled 只看对应分组非空)，selectedCommit.kind 用 hash；gitCommitController.selectedCommit getter 用 isWorkingTreeHash；onSelectedCommitChanged 的 currentChangeSet=hash；selectWorkingTreeChanges 按 selectedHash 只取 staged 或 changes 子集(`files = selectedHash==='staged' ? staged : unstaged`)；所有 `=== 'uncommitted'` 守卫(内容刷新/可视刷新/onWorkingTreeChangesChanged/effect selectCommit)改 isWorkingTreeHash；openCommitPanelWorkingTreeDiff 按 section 映射 targetHash(staged→'staged', unstaged→'changes')，waitForWorkingTreeSelection 加 targetHash 参数；saveWorkspaceFile 仅 'changes' 放行(staged 右侧是 index 不可回写)。内联 webview JS：加 `workingTreeRowsState` + `isWorkingTreeHash()`，updateWorkingTreeRows/render 循环渲染两行(afterbegin 需倒序、innerHTML 正序)，renderFiles 按选中行只显示对应 section，各 `=== 'uncommitted'` 判断改 helper。注意 readDiffs 的 changeSetMode 仍传 'uncommitted'(靠每文件 workingTreeKind 分流，正确)。`npx tsc --noEmit -p ./` 与四文件 lint 全过，未提交。
- 两虚拟行点击无反应根因(2026-08-27)：`workingTreeRowHTML` 的 `disabled` 来自 `enabled`，我把 enabled 改成看懒加载明细 `workingTree.staged/changes.length`，但提交图初次渲染时明细未填充(由轻量 presence `hasUncommittedChanges` 单独维护)，两行都 disabled → click 在 `if(row.classList.contains('disabled'))return` 被拦。已在 pushStateToWebview 加 `[Gitk][WorkingTreeRows]` 诊断日志(输出 hasUncommittedChanges/stagedCount/changesCount)待用户实测确认，再决定是否把 enabled 改回 presence 驱动/拆 per-group presence。此日志尚未清理。
- 虚拟行泳道节点绘制(2026-08-27)：虚拟行不在 commits 数组、原 `.graph-svg` 为空 span 无节点。新增 webview `workingTreeGraphSvg(hash)`：cx=LANE_W/2+5=11、cy=ROW_H/2、r=DOT_R 画空心圆(fill=editor-background, stroke=descriptionForeground)；'staged' 行额外画一条从圆底到行底的竖直虚线(stroke-dasharray='2 2')连接下方节点，'changes' 行只画空心圆不连接。注入 `workingTreeRowHTML` 的 `.graph-svg`。并在 `setupRow` 加 `isWorkingTree` 判断跳过 drawSvg(否则用无效 data-row 调 drawSvg 会擦掉自绘 SVG)；render 的 expanded 重绘循环已用 `commits[index]` 守卫，安全。lint 通过，未提交。
- 虚拟行标签配色 + 动态显隐 + 重命名(2026-08-27)：`workingTreeRowHTML` 标签加 class `working-tree-label--{hash}`；CSS `--staged` 蓝(gitDecoration-stageModifiedResourceForeground→charts-blue→#3794ff)、`--changes` 红(gitDecoration-modifiedResourceForeground→charts-red→#f14c4c)。'Changes' 重命名为 'Unstaged Changes'。关键改动：`workingTreeRows` 从"始终两行+enabled 置灰"改为"按分组非空动态产出行"(未暂存在上/已暂存在下)，彻底移除置灰——这也顺带解决了之前"用懒加载明细判 enabled 导致初次渲染两行都 disabled 点不动"的问题(明细由 syncWorkingTreeForCurrentBranch 分支选择时 eager 填充，稳态可靠；空分组直接不产出)。`[Gitk][WorkingTreeRows]` 诊断日志已移除。连接线：staged 行 SVG 从圆底 cy+r 向下贯穿虚线到 ROW_H 连下方 HEAD 节点；unstaged 只画空心圆不向下连。lint 通过，未提交。遗留：切分支后首次 sync 完成前的极短时间窗行可能未出现(明细异步)，如需严格无缺失可改 per-group presence 占位，待用户确认。
- 点击虚拟行无反应根因修复(2026-08-27)：`GitCommitController.selectCommit`(第253行)有 identity 去重——已选 identity==目标 hash 时 return false 不 fire。若该虚拟行文件此前被门禁 bail 过没加载出来，去重会把重试永久挡死→点击无反应。修复：effect `selectCommit` 分支对工作区虚拟 hash，当 `selectCommit` 返回 false 但 `currentHash===effect.hash && currentRepositoryPath===branch.repo` 时，显式 `this.selectWorkingTreeChanges().then(()=>refreshCommitPanel())` 强制重载。`npx tsc --noEmit` 与 lint 通过。`[Gitk][WorkingTreeClick]` 四处诊断日志(webview click / effect / selectWorkingTreeChanges enter/guard/loaded)仍在，待用户确认点击生效后清理。unstaged 红色 CSS(`--changes`)已就位，若仍非红需再查 disabled 覆盖或主题变量。
- MultiDiffPanel 空态保留(2026-08-27)：面板已打开时最后一个 diff 文件被移除(stage/unstage/discard), 原 `selectWorkingTreeChanges` 的 `diffs.length===0` 分支直接 `multiDiffPanel.hide()` → `panel.dispose()` 强制销毁面板。改为：面板已打开(`isOpen()`)时调 `show()` 推送空快照(store.files=[]→publish→snapshot.diffs.length===0→webview 显示"暂无变更文件"), 面板未开则不弹出(直接 return)。新增 `MultiDiffPanel.isOpen(): boolean { return !!this.panel; }` 查询方法。webview 空态文案从"没有可显示的 Diff 内容"改为"暂无变更文件"统一。`npx tsc --noEmit` 与 lint 通过。
- 虚拟行空分组显示"暂无变更文件"(2026-08-27)：`renderFiles` 的 `isWorkingTreeHash(selectedCommitHash)` 分支原直接渲染 section(staged 空返回空串, unstaged 空渲染空 section), 视觉上右侧空白。改为先取 `sectionFiles = selectedCommitHash==='staged' ? stagedFiles : unstagedFiles`, 若 `!sectionFiles.length` 则 `list.innerHTML='<div id="filesEmpty">暂无变更文件</div>'` 提前 return, 与普通提交空文件表现一致。`npx tsc --noEmit` 与 lint 通过。
- 清理本轮技术债(2026-08-27)：删除全部本轮诊断日志与性能追踪管道。(1) `uncommittedFilesWatcher.ts`：删 `visibleDiffTraceSequence` 模块变量、`VisibleDiffFileContentChangedEvent` 的 `traceId/observedAt` 字段、`setVisibleDiffPaths`/`handleWorkspaceFileChanged`/`requestRefresh`/`drainRefresh`/`refreshSlot` 的全部 `[Gitk][UncommittedWatcher]`/`[Gitk][VisibleDiffPerformance]` console.log(merged/started/retry/finished/discarded/status changed/status unchanged/workspace file/watcher visible file)。(2) `gitkViewProvider.ts`：删 `visibleDiffPerformanceTrace` 字段、MultiDiffPanel 构造第8参 `()=>this.visibleDiffPerformanceTrace`、onVisibleDiffFileContentChanged 传的 traceId/observedAt、`refreshVisibleWorkingTreeDiffs` 的 traceId/observedAt 形参与两处 diff read/store updated 日志及 trace 赋值、`selectWorkingTreeChanges`/`updateVisibleDiffPaths`/effect selectCommit 的 `[Gitk][WorkingTreeClick]`/`[Gitk][VisibleDiffPerformance]` 日志、`onSelectedRepoListChanged` 三处 `[GitkViewProvider]` 日志、webview `updateSelectedRepoDisplay` 的 `[Webview]` 日志。(3) `multiDiffPanel.ts`：删 DiffSnapshot.performanceTrace 字段、构造第7参 getPerformanceTrace、received visibleDiffPaths 日志、publish 日志与 snapshot posted 日志、webview 内联 publishVisibleDiffPaths 两处 `[Gitk]` log 与 render 分支的 `card updated` performanceTrace 死代码。保留：非本轮的 `render #`/`receive #` 生命周期 log 及 error/log 运行时错误通道。全程用编辑工具非 PowerShell。`npx tsc --noEmit` 与三文件 lint 全过。功能实现(staged 双通道刷新/虚拟行拆分/置灰/搜索过滤/虚线连接)均保留未动。
- 虚拟行改回置灰 + 搜索过滤(2026-08-27)：按用户要求把"动态显隐"改回"置灰"。pushStateToWebview 两个虚拟行(Unstaged/Staged Changes)始终产出，`enabled = count>0`(空分组置灰而非隐藏)。CSS 补 `.commit-row.working-tree.disabled { opacity:.5; cursor:default }` + `:hover 透明`；仍不用 `pointer-events:none`(曾致点击完全无响应无法排查)，点击拦截由 JS 已有的 `if(row.classList.contains('disabled'))return` 负责。搜索过滤：虚拟行非真实 commit 不经 searchCommits，新增 `matchesSearch(label)` 用 `commitController.searchKeywords`(已暴露 getter) 判断 label 是否含任一关键词(不区分大小写)，非空搜索且未命中的虚拟行直接 `.filter` 掉不产出。`npx tsc --noEmit` 与 lint 通过。
- 虚拟行配色与 staged 虚线连接修复(2026-08-27)：(1) 配色——`--staged` 原用 `--vscode-gitDecoration-stageModifiedResourceForeground` 在部分主题解析为绿色(用户截图所见)，改为固定 `--vscode-charts-blue,#3794ff`(蓝)；`--changes` 同理改固定 `--vscode-charts-red,#f14c4c`(红)，不再依赖 gitDecoration 变量。(2) 虚线断裂——SVG 逐行独立绘制，staged 行虚线只到本行底部 `ROW_H`，而下方 HEAD commit 行在 `drawSvg` 里仅 `inputIndex>=0` 时才画"行顶→节点"线；HEAD 作为最新提交 `inputIndex<0` 故节点上方不画线 → 断开。根因修复：`drawSvg` 的 `inputIndex<0` 分支补 else，当 `hasStagedRow`(workingTreeRowsState 含 'staged') 且当前是 HEAD commit 且属当前分支时，补画一段从行顶到节点 `cy` 的虚线(dasharray '2 2', descriptionForeground)衔接虚拟行泳道。条件用 hasStagedRow 而非 length>0，因只有 staged 行向下连线(unstaged 只画空心圆)。`npx tsc --noEmit` 与 lint 通过。
  - 后续修正(2026-08-27)：上面用 `c.refs.some(r=>r==='HEAD')` 判定 HEAD 首行有 bug——部分仓库首行 refs 只有 `origin/HEAD`/分支名, 无字面量 'HEAD'(截图节点是普通红点非带环 HEAD), 条件永不命中, 补线画不出。改为 `idx===0`(虚拟行 afterbegin 插在最前, 正下方就是 commits[0], 布局上紧邻)判定, 保留 `inputIndex<0`(首行无输入泳道时才需补顶部线)与 hasStagedRow。虚线 x 对齐: 虚拟行 cx=LANE_W/2+5=11, commits[0] circleIndex=0 时 cx=11, 一致。
- 点击虚拟行(changes/staged)右侧 Changed Files 空白的确定根因(2026-08-27)：`src/state/store.ts` 的 `selectCommit` reducer 守卫仍写死 `const isWorkingTree = intent.hash === 'uncommitted';`。虚拟行拆分后 webview 点击只发 `{type:'selectCommit', hash:'changes'|'staged'}`(不带 repositoryPath)，于是 `isWorkingTree=false`(hash 非'uncommitted')、`isCommit=false`(无 repositoryPath)，命中 `if(!isWorkingTree&&!isCommit) break`，**不派发任何 effect** → `onSelectedCommitChanged` 不触发 → 文件区空白。之前只改了下游 `runEffect` 的 `isWorkingTreeHash(effect.hash)`，漏改上游 store reducer 守卫，effect 根本走不到那里。修复：`store.ts` 导入 `isWorkingTreeHash`，reducer 改 `const isWorkingTree = isWorkingTreeHash(intent.hash);`。`diffReader` 的 `changeSetMode==='uncommitted'` 与 `multiDiffPanel` 的 `currentChangeSet==='uncommitted'` 是有意保留的兼容分支(前者靠 workingTreeKind 分流, 后者虚拟行走 'changes'/'staged' 已被覆盖)，无需改。`npx tsc --noEmit` 与 lint 通过。
- "初始化卡在'正在加载历史提交列表'"的确定根因(2026-08-27)：webview 内联脚本里 `stateUpdate` 处理器(约2302行)与虚拟行点击(约3098行)调用了 `log('[Gitk][WorkingTreeRows]...')` / `log('[Gitk][WorkingTreeClick] virtual row clicked...')`，但 webview 作用域从未定义 `log`(其余诊断都用 `console.log`)。每次收到 stateUpdate 时 `log(...)` 抛 `ReferenceError: log is not defined`，处理器在到达"隐藏 loading 遮罩、显示 commitList"分支前中断，故永远停在 `showLoadingProgress`。虚拟行点击同样因该错误中断。根因修复：删除这两处未定义的 `log(...)` 诊断调用(非兜底，是删除错误代码)。`npx tsc --noEmit` 与 lint 通过。
- 点击虚拟行"完全无 WorkingTreeClick 日志"的确定根因(2026-08-27)：CSS `.commit-row.working-tree.disabled { pointer-events: none; }` 使带 disabled 类的虚拟行完全不接收鼠标事件，click listener 都进不去→无任何日志。动态显隐改造前空分组行会被置灰加 disabled→死活点不动。已移除该 pointer-events:none 及置灰遗留规则；配合"行只在分组非空时产出且 enabled:true"，逻辑自洽。另在 webview stateUpdate 处加 `[Gitk][WorkingTreeRows] stateUpdate rows=` 日志确认后端下发的行。lint 通过，未提交。诊断日志待用户确认点击生效后统一清理。
- 清理提交 a6e5871 遗留技术债(2026-08-27)：先核实 a6e5871("diffpanel可视范围的unstaged文件要支持实时更新")引入的性能追踪调试代码(`visibleDiffTraceSequence`/`traceId`/`observedAt`/`performanceTrace`/`getPerformanceTrace`/全部 `[Gitk][VisibleDiffPerformance]` 与 workspace file 日志)已被后续提交 7f0891f 全部清除，当前工作区 grep 零命中。真正残留技术债是 `gitkViewProvider.ts` 中 `refreshWorkingTreeDiffs`(全量内容事件链路)与 `refreshVisibleWorkingTreeDiffs`(可视快通道链路)尾部写回段逐字重复(refreshedByKey→nextFiles→store.setState)。抽出私有辅助 `applyRefreshedDiffs(refreshed)` 统一写回，两方法各自的门禁差异(前者 commitFilesGeneration+stop()+bump diffGeneration，后者独立 visibleDiffGeneration 不 stop 低延迟)保持不变。`npm run compile` 与 lint 通过。