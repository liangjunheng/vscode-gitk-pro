# 2026-08-07

## vscode-gitk: 加载进度条
- 在 gitkViewProvider.ts 中为仓库/分支/提交加载流程添加可视化进度条
- 扩展端: refreshSelectors 发送 step 1 (仓库) / step 2 (分支), refreshInternal 发送 step 3 (提交), refreshBranchCommits 发送 step 1/1, beginCommitReload 发送 step 0/3
- webview 端: `#loading` 区域从纯文本改为 loadingText + progressBar (CSS 过渡动画) + progressStep; 新增 showLoadingProgress() JS 函数; 更新 loading/error/refreshing/render/refresh按钮 等所有引用

## vscode-gitk: resolveCommitRefs 批量化
- 问题: 进度条显示 217 (216 分支逐个 git rev-parse + 1 git log), 但实际只展示 100 条提交
- 修复: resolveCommitRefs 从逐个 Promise.all 改为单次 `git rev-parse ref1^{commit} ref2^{commit} ...` 批量调用
- 部分失败处理: catch 块从 error.stdout 提取有效 40 位 hex 哈希 (正则过滤)
- getGitCommits totalOps 从 `refs.length + 1` 改为 `2` (1 resolve + 1 log)
- resolveCommitRefs 移除 onProgress 参数 (单次调用无需逐个报进度)

## vscode-gitk: 加载速度优化
- getGitRefs 加 5s TTL 缓存, 避免 refreshSelectors + getGitCommits 重复调用 git for-each-ref
- getGitRepositories 工作区文件夹解析改 Promise.all 并行 (原串行 for...of await)
- getGitRepositories 子模块扫描改 Promise.all 并行, 嵌套子模块按层批次处理
- buildGraph 增量构建: 新增 GraphState 接口 (activeLanes + nextColor), 支持 startIndex 参数跳过已处理提交
- gitkViewProvider loadMoreCommits 改用增量构建: 保存 graphState, 只处理新追加的提交 (prevCount 起始), O(N²)→O(N)
- refreshInternal / refreshBranchCommits 全量重载时重置 graphState = { activeLanes: [], nextColor: 0 }

## vscode-gitk: 首次加载 + 加载更多速度优化
- 跳过 resolveCommitRefs: refs 直接传给 git log (省 1 次 git rev-parse 调用)
- 提取 parseLogOutput + buildRefsByCommit 公共函数, 消除 getGitCommits 和 getGitCommitsByHashes 间重复代码
- 新增 getCommitHashes(rootUri, refs, limit=10000): git rev-list --topo-order 预取全量 hash (仅 hash 无格式化, 快)
- 新增 getGitCommitsByHashes(rootUri, hashes): git log --no-walk 按 hash 直接取数据, O(101) 无遍历
- getGitCommitsByHashes 输出按输入 hash 顺序排序 (--no-walk 不保证顺序)
- refreshInternal: 首次加载时 git log + getCommitHashes + getWorkingTreeChanges 三路并行
- refreshBranchCommits: git log + getCommitHashes 两路并行
- loadMoreCommits: 优先用 allCommitHashes.slice(skip, skip+101) + getGitCommitsByHashes (O(101)); allCommitHashes 耗尽时回退 git log --skip (O(N), 适用于 >10000 提交的仓库)
- hasMoreCommits: allCommitHashes.length > rawCommits.length || page.length > COMMIT_PAGE_SIZE
- gitkViewProvider 新增 allCommitHashes 字段, refreshInternal/refreshBranchCommits/early-return 均重置

## vscode-gitk: webview 增量渲染
- loadMoreCommits 改发 appendCommits 消息 (只含 newCommits) 替代全量 commits 消息
- webview 新增 appendRows(startIndex): maxLane 或 graphW 增大时回退全量 render, 否则 insertAdjacentHTML 追加新行
- 提取 rowRefX/buildCommitRowHTML/setupRow/calcMaxLane/calcWidestRefRow 公共函数
- render() 重构使用提取的函数, 重置 currentMaxLane/currentGraphW
- 编译验证通过 (npx tsc --noEmit 零错误)

## vscode-gitk: 描述展开方向修复
- 问题: commit-description 有 grid-column:2/-1 但无 grid-row, CSS Grid 自动布局把 "只明确列" 的元素先于 "都不明确" 的元素放置, 导致 description 跑到第 1 行, 主内容被挤到第 2 行 (往上展开)
- 修复: .commit-description 加 grid-row:2, 使其行列都明确, 固定到第 2 行 (往下展开)
