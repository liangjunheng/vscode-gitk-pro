# 工作记录

- 提交图数据源改为单次 `git log --topo-order`（完整 hash、完整父列表、所选 refs 可达提交并集），不再使用第一父链或多个 `repo.log()` 的非全局拓扑顺序。`buildGraph()` 改为逐行完整车道转换：过滤不可见父节点、共同父去重、为所有存活车道生成位置迁移边，解决 merge、分叉和截断造成的重复/悬空/错连。SVG 曲线统一按行顶部到下一行底部绘制；引用标签路径增加背景色横向避让通道，垂直线不会与标签重叠。图宽按车道与标签需求计算。`npm run compile` 通过，相关文件无诊断。
- Branchs 首次加载默认选中 `getCurrentGitBranch()` 返回的本地当前分支。引用标签统一定位在最右侧车道之后，提交节点通过横线连接到该标签列，不再覆盖任意车道。`buildGraph()` 对八爪合并仅保留前两个可见父提交，以二叉连接表达单行分叉。`npm run compile` 通过，相关文件无诊断。

- Multi-Diff 文件名栏原有 Unicode `⌄` 已替换为内嵌 SVG chevron，并升级 `.chevron` 为 18px 圆角图标容器，包含主题自适应描边和文件标题悬停态。正常 Diff 和读取失败分支同步更新；`npm run compile` 与 `git diff --check -- src/customDiffPanel.ts` 通过，`customDiffPanel.ts` 仅保留既有未使用 `context` 提示。
- 用户要求还原工具栏修改点导航的原始文字箭头，已将 SVG 图标和 `.change-navigation` 容器恢复为独立的 `↑` / `↓` 按钮及原 `.toolbar-button` 样式；文件名栏 SVG 下箭头保持不变。`npm run compile` 和 `git diff --check -- src/customDiffPanel.ts` 通过。
- 提交图 SVG 改为：延续车道先从行顶连到提交节点，当前提交的父边从节点中心继续至行底；跨车道曲线以节点为起点，节点最后绘制覆盖接合点，解决弧线脱离节点与车道断续。新起车道不绘制节点顶部入线，避免最新提交出现额外上行支路；引用标签仍始终经横线连接到节点，避免标签悬空。`npm run compile` 通过，`gitLogProvider.ts`、`gitkViewProvider.ts` 无诊断。
- 分支图列宽按所有车道的总宽度 `naturalGraphW` 自适应，且通过 `max(30ch, naturalGraphW)` 保证最少显示 30 个字符宽；引用标签宽度不参与列宽计算。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 提交列表每一行的分支图容器在该提交存在引用时新增原生悬停提示，使用 `c.refs` 的全部分支/引用名称按逗号分隔显示。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 已完成提交加载性能只读评估：优先减少 Webview 500 行提交图的重复整表/SVG 重绘，其次延后首次 `selectCommit()` 引发的 `diff-tree` 和全量 Diff 打开；Git 读取路径当前是 `git log --topo-order --max-count=500` 并行 `for-each-ref`，完整刷新还并行读取工作区状态。可选优化包括：虚拟列表、选中状态局部更新、批量绘图、事件委托、按需加载 Diff、分页/较低初始提交数、缓存 refs/选择器、工作区状态异步化、取消过期请求、增量刷新、减少 Git log 字段与 refs 范围、仓库并发限流及性能埋点。
- 描述列展开模板已改为 `Commit`、`Parents`、`Author`、`Committer`、`Date`、空行、提交主题、完整提交正文的固定顺序。`getGitCommits()` 的单次 `git log` 格式加入 `%cn` / `%ce`，并为 `GitCommit` 提供提交者名和邮箱；缺失时回退作者信息，未增加额外 Git 请求。`npm run compile` 通过，相关文件无诊断。
- 分支图标签位置改为按当前行全部车道的最右位置计算，而非按全局最大车道；标签起点与该行最右车道相距三个字符（21px），因此标签尽量贴近对应车道且不会覆盖任何该行车道。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 用户已撤回“取消真实的旧 Diff 请求与 Webview 渲染队列”优化需求；不继续保留或推进该方案的实现。
- 针对“100 条分页、距末尾 50 条继续加载、底部状态”完成只读优化审查：现有源码仍一次读取 500 条并全量渲染，尚无分页/滚动加载。实施前应优先设计统一分页会话与过期结果保护、101 条探测 `hasMore`、累计重建图以维持车道连续；高收益后续项为 Webview 虚拟列表/事件委托、refs 缓存、提交正文懒加载。用户当前仅要求方案，未修改代码。
- 已向用户说明低风险分页辅助优化仍有边界风险：refs 缓存涉及失效时机，滚动节流影响预取时机，101 条探测依赖偏移分页正确性，footer 可能与全量重绘竞争，累计重构图会随着页数增加产生 CPU/内存压力；未实施代码改动。
- 用户要求长期性能最优且不发生数据或渲染异常。建议后续采用“Git 游标式分页 + 单调提交唯一性校验 + 可续接图布局状态 + 虚拟列表/固定节点池 + 版本化追加协议 + 端到端不变量测试”的正确性优先架构；需明确零异常无法承诺，只能通过事务式状态机、回退和自动化覆盖尽量保证。
- 用户要求给出最优且风险最低的单一方案：优先实施保守分页第一阶段——100 条页、额外读取 1 条确定 hasMore、会话版本防旧结果、累计原始提交后调用既有 `buildGraph()` 整体重构、独立 footer；明确暂不采用 cursor、增量布局与虚拟列表，以最小改动换取提交图与数据语义稳定。
- 用户再次强调长期最优且列表不能出现异常；结论是不能真实承诺零异常，长期正确性优先方案应以冻结 Git 历史快照、游标分页、事务式数据/图状态提交、版本化 Webview 协议、虚拟列表和不变量/端到端测试共同保证；任何快照或渲染校验失败均不追加并提示刷新。
- 用户希望简化方案：推荐回归到小范围、正确性优先的分页实现——每页 101 取前 100、`--skip` 作为偏移、会话 generation 丢弃旧结果、累计原始提交后继续复用全量 `buildGraph()`、独立底部状态；暂不引入游标、增量布局和虚拟列表。
- 用户要求澄清“长期稳定、性能最优、列表百分百准确”的方案：应采用冻结查询快照下的游标分页、每页数据校验与原子提交、可续接图布局状态、虚拟列表；数据源发生变化或校验失败时禁止追加并提示刷新。强调绝对零异常无法承诺，但该策略保证不将检测到的错误数据渲染到列表。
- 仓库选择器仅调整 Webview UI：仓库选项由 checkbox 改为同名 radio，移除仓库“全选/清空”按钮；仍向既有 `selectRepositories` 协议发送单元素 `paths` 数组，未改扩展端逻辑。仓库名称前原有的 `◆ ` 标记已替换为自定义内嵌 SVG：根仓库现为接近 VS Code SCM 的 Git 分支图标并使用 Git 装饰色，子仓库为资源管理器风格文件夹图标；当前选中项与下拉选项均显示。仓库弹窗选项中的 radio 圆点已隐藏，选中状态改由行高亮表达。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- Multi-Diff 未变更内容折叠控件改为 VS Code Diff 风格：双侧细分隔线、chevron SVG、居中“显示 N 行未更改内容”、主题化 hover/focus 状态；保留原有点击后展开整个上下文块的行为。`npm run compile` 通过，`customDiffPanel.ts` 无诊断。
- 对“首批 100、距末尾 50 条预取下一页、底部加载状态”再次只读核验：`getGitCommits()` 仅支持 `--max-count`，`GitkViewProvider` 固定取 500 并整批 `buildGraph()` 后发送 Webview；`#graph` 没有滚动监听/observer，`render()` 全量重建行与 SVG，当前 `#loading` 不适用于追加加载。推荐 101 条探测 + `--skip` 偏移、query generation 防过期结果、累计原始提交后整体重建图、独立 footer；不应逐页独立布局，否则跨页 lane/父边会断裂。未修改代码。
- 用户确认长期稳定方向：分页实施以正确性优先，采用单仓库分页会话、101 条探测、查询 generation、原始提交去重及原子合并、累计 `buildGraph()` 重建和底部状态；超过性能阈值时再独立引入虚拟列表，避免首次实现同时改变数据与渲染模型。
- 用户要求重设计仓库选择器中 workspace 主仓库（`description === 'repo'`）与普通仓库前置图标，视觉风格参考 VS Code；当前只需以表格展示修改方案，尚未修改代码。
- 用户澄清其“表格形式的修改方案”指提交历史分页与性能优化方案，而非仓库图标重设计；后续性能方案优先用表格简洁呈现。用户进一步要求一次性完整方案，并需同时说明采用原因与缺点；在过度复杂的一步到位设计后，用户要求回归为简单的正确性优先方案。进一步结论：不存在同时比该方案更快、更简单且更稳定的替代；采用 `IntersectionObserver` 触发、101 条探测、`--skip` 分页、累计整体建图和底部状态，深历史性能问题留待实际测量后专项处理。用户要求将这一改进正式融入后续的统一方案。
- Multi-Diff 未更改区域改为依据 VS Code 官方 `hideUnchangedRegionsFeature.ts`：24px 高度的折叠区、上下 4px 可拖拽式展开边缘、中心下拉按钮与“隐藏行”计数、未更改区域主题变量及阴影。上/下边缘每次各显示 10 行，中心按钮或双击计数展开全部；`npm run compile` 通过，`customDiffPanel.ts` 无诊断。
- Multi-Diff 文件标题栏已移除左侧 SVG chevron/logo，同时删除 `.chevron` 专用样式；保留状态标记、文件路径与原点击定位逻辑。`npm run compile` 通过，`customDiffPanel.ts` 无诊断。
- 修复 Multi-Diff Webview 持续“准备中”：`renderRows()` 在折叠上下文渲染分支中引用了不存在的 `expanded` 变量，触发运行时 `ReferenceError` 并中断初始渲染；改为依据 `expansion.all`。同时移除读取失败分支遗留的 chevron。`npm run compile` 通过，`customDiffPanel.ts` 无诊断。
- 切换仓库时清空旧分支后立即向 Webview 发 `branchesLoading`，禁用分支下拉并显示“加载分支...”，直到 `selectors` 消息重新填充选项。分支默认状态统一为当前仓库的全部分支，仓库切换后也不再预选当前检出分支；移除不再使用的 `getCurrentGitBranch` 依赖。`npm run compile` 通过，`gitkViewProvider.ts` 无诊断。
- 为提升 Multi-Diff 隐藏代码展开区的可辨识度，基于 VS Code `hideUnchangedRegionsFeature.ts` 的完整模式控件强化边界与可操作元素：24px 区域使用未更改区域主题边框/背景，中心 `⌄` 作为可见展开按钮，隐藏行计数使用链接色与加粗/悬停下划线；上下 4px 渐进展开手柄保持不变。`npm run compile` 通过，`customDiffPanel.ts` 无诊断。
- 用户撤回隐藏代码展开区的强化视觉：`customDiffPanel.ts` 改为整体 50% 不透明度；悬停/焦点时恢复完全不透明，隐藏行标签悬停只增加下划线且不改变颜色。`npm run compile` 通过，文件无诊断。
- Multi-Diff 的未更改代码折叠展开行（`.context-fold`）新增 `user-select: none`，禁止选择该控件文字，保留点击、双击及拖拽展开操作。`npm run compile` 通过，文件无诊断。
- 用户决定撤销提交历史分页与增量加载讨论中所有拟议优化：当前不实施 100/101 条分页、`--skip`、`IntersectionObserver`/sentinel、footer 加载状态、累计重建图、滚动位置恢复、失败重试，以及所有后续 Worker、虚拟列表、hash cursor、generation 状态机方案；维持现有固定 500 条全量加载与渲染逻辑，不修改代码。
- 用户重新提出仓库、分支与 commit 加载缓慢，需要重新制定方案；只读核验发现：仓库发现对工作区与递归子模块串行执行 `rev-parse`/`.gitmodules` 查询；分支 refs 在 `refreshSelectors()` 和 `getGitCommits()` 中重复执行 `for-each-ref`；默认全选全部 local/remote branches，随后逐个 `rev-parse` 并以所有头部运行 500 条 `git log`；首次提交加载还并行执行完整工作区 `status --untracked-files=all`，并自动 `selectCommit()` 触发 `diff-tree`。应优先按阶段削减重复 Git 进程与自动附带工作，不先恢复提交分页方案。未修改代码。
- 用户要求先查看提交分页加载的实现方式；只读结论：现有 `getGitCommits()` 固定 `--max-count`，`GitkViewProvider` 有刷新和分支刷新 generation 但无分页状态/消息，Webview `#graph` 是滚动容器、`render()` 每次全量重建行和 SVG。若实施，最小正确方案为 100 条正式页 + 第 101 条探测、`--skip=<acceptedCount>`、单仓库分页会话（loaded/hasMore/isLoading/generation）、Webview 底部 sentinel 的 `IntersectionObserver`、累计原始提交后整体 `buildGraph()` 并发送完整结果、独立 footer 与失败重试；不得逐页独立建图或将 probe 条目混入渲染。未修改代码。
- 用户明确取消“必须显示固定 500 条提交”的约束；后续提交加载方案可将首次展示量改为分页首批 100 条，按需继续加载，不受原固定 500 条逻辑限制。
- 已实现提交历史分页：`getGitCommits()` 增加 `skip` 并传给 `git log --skip`；`GitkViewProvider` 首次/分支变更读取 101 条、展示 100 条、保留 raw commits 与 hasMore/loading/generation 状态；Webview 底部 `IntersectionObserver`（800px 预取）触发 `loadMoreCommits`，有加载中/全部/失败重试状态。新页按 hash 去重后与累计 raw commits 整体 `buildGraph()`，并恢复滚动位置。多仓库继续使用首次加载结果但不启用“加载更多”。`npm run compile`、两处编辑文件诊断和 `git diff --check` 均通过。
