# 2026-08-21

- 完成提交模型重构：新增 `CommitMetadata`、`GitCommitOption`，`GitCommitOption` 持有 `CommitMetadata`、`CommitFile[]`、`DiffPayload[]`，三类对象提供 `equals()`；`RepositoryCommit`/`GitCommit` 已移除，提交控制器和文档改用 `CommitMetadata`。Webview 状态改为由 Provider 预计算 `key`、工作区行和选中提交 ViewModel，Webview 只负责显示和发送事件。`src/types/git.ts` 的全部 interface 已改为 class 并提供 `equals()`；仓库、分支、提交、Diff、工作区状态、GraphLane 和 MultiDiffLoadEvent 的构造均已迁移到 class 实例。Webview 不再调用 equals 或负责业务去重；`npm run compile`、lint 与 `git diff --check` 通过。
- 继续完成 class/equals 迁移收尾：统一仓库 URI 表示为 `GitRepositoryOption.path` 的 URI 字符串，修复当前分支归属可能为 undefined；修复 `resolveBranch()` 的重复 equals 条件和无条件 name 回退；仓库、分支、提交控制器改用实例 `equals()`，提交选择仍按仓库路径+hash 语义。`equalsRepositoryOption`/`equalsBranchOption` 生产调用已移除，相关 lint 与 `npm run compile` 通过。
- 已移除 `copyRepositoryOption()`、`copyBranchOption()`、`equalsRepositoryOption()`、`equalsBranchOption()` 的实现和生产引用；控制器直接通过 class 构造新实例及调用 `instance.equals()`。相关文档已将旧函数描述改为 class/equals 语义，编译通过。
- `GitCommitController.selectBranches` 并发策略调整：当分支名集合或所属仓库 path 集合与上次数据源不一致时，先 abort 当前提交列表读取，再按新 refs/仓库启动读取；同名分支但仓库不同也视为变化。每轮内部创建 AbortController 与 generation，信号透传 `getGitCommits` / `searchCommits`，每个 await 后及 finally 均校验代次，旧结果不落地且不能关闭新流程 loading。搜索与 `forceRefresh` 仍保留原门禁语义，dispose 会取消列表读取。`doc/gitcommitcontroller.md` 已同步；`npm run compile`、`git diff --check` 和控制器诊断通过。
- 补齐 BranchController/CommitController 取消重启后的 loading 关联：提交列表 loading 聚合 `selectionLoadingPhase || branchesController.isLoading || commitController.isLoading`。优先级为分支控制器 loading/branch phase → 提交控制器 loading → repository phase；原因是 BranchController 当前分支快路径会提前发布 selectedBranches 并触发可取消的提交预读，在全量分支尚未完成时仍应显示“正在获取当前分支...”。两个控制器旧流程 finally 均以 generation 门禁不发布 false，因此新任务接管时无 loading 空窗。`doc/gitcommitcontroller.md` 已同步；编译和格式检查通过，仅有既存 `searchKeywords` 未使用 hint。
- 修复“正在获取当前仓库...”看不到而分支提示可见：根因是 Webview 的 radio change 在同一浏览器任务中先调用 `showLoadingProgress(repository)`，随即 `postMessage`；扩展端很快把阶段推进为 branch，浏览器在首次 paint 前已收到覆盖状态，因此 repository DOM 从未真正绘制。现用 `requestAnimationFrame` 在仓库提示提交一帧后再发送 `selectRepositories`，不增加固定延时，随后仍由真实 BranchController loading 接管。编译和格式检查通过，仅有既存 `searchKeywords` hint。
- 弹窗总列表改为严格单一事件源：仓库总列表只在 `GitRepoController.ontotalRepoListChanged` 回调更新 Provider 快照并发送 `totalRepoListChanged`；分支事件重命名为 `GitBranchesController.onTotalBranchesListChanged`，只在该回调更新快照并发送 `totalBranchesListChanged`。通用 `stateUpdate` 不再携带 repositories，分支提交状态与弹窗 `totalBranches` 已分离；选择/loading 只基于既有总列表重绘勾选和 spinner，不赋值总列表。`initialSelection` 不再覆盖仓库弹窗 DOM，`branchesReset` 不再清空分支总列表。Webview 通过 ready 握手重放仅由对应回调维护的快照。`doc/gitrepocontroller.md`、`doc/gitbranchscontroller.md`、`doc/gitcommitcontroller.md` 已同步；编译、格式检查及相关 TS 诊断通过，仅有既存 `searchKeywords` hint。
- 仓库/分支当前显示栏也改为严格单一选择事件源：仓库显示快照只在 `onSelectedRepoListChanged` 回调更新并发送 `selectedRepoDisplayChanged`；分支显示快照只在 `onSelectedBranchesChanged` 回调更新并发送 `selectedBranchDisplayChanged`。Webview 中 label/title/disabled 的运行期写入已收敛到 `updateSelectedRepoDisplay` / `updateSelectedBranchDisplay` 两个专用处理器。总列表渲染、loading、通用 stateUpdate、仓库 radio 临时勾选、分支 checkbox 草稿、initialSelection、branchesReset 均不再修改显示栏；仓库选择期间分支显示保持上次确认值，直到新的 onSelectedBranchesChanged 到达。ready 重放的显示快照同样只由对应选择回调维护。相关控制器文档与 `repository-switching-sequence.md` 已同步；编译和格式检查通过，仅有既存 `searchKeywords` hint。
- 重新定位首次初始化状态问题：当前工作树的提交区仍错误地以 `branchesController.isLoading` 为最高优先级，导致完整分支列表读取覆盖提交阶段。现修正为：仅 `repoController.scanning && !hasRepositorySelection` 显示首次“正在获取当前仓库...”；仓库一旦自动选择，`onSelectedRepoListChanged` 的 branch phase 与 CommitController loading 接管，扫描子模块不会覆盖后续阶段。扫描结束仍无仓库时清除 repository phase。`gitkViewProvider.ts` 无新增诊断错误；本次 `npm run compile` 被既有 `src/statusBar.ts is not a module`（`extension.ts` 导入失败）阻塞，`git diff --check` 无格式错误。
- 进一步收紧仓库/分支 dropdown loading 单一事件源：Provider 的 `repoController.onScanningChanged` 和 `branchesController.onBranchesLoadingChanged` 都在 true/false 时发送专用 `repoLoadingChanged` / `branchLoadingChanged`。Webview 的 `updateDropdownLoading()` 成为唯一 spinner/弹窗进度节点写入点；通用 `stateUpdate` 不再携带 reposLoading/branchesLoading，`loadingProgress`、提交进度、选择、总列表、临时 checkbox 也不再影响 dropdown loading。仓库和分支显示内容的 label/title/disabled 仍分别只由选择回调维护。`doc/gitrepocontroller.md`、`doc/gitbranchscontroller.md` 已同步；`gitkViewProvider.ts` 无新增诊断，构建仍仅受既有 `statusBar.ts is not a module` 阻塞。
- `GitBranchesController` 现在构造时接收 `GitRepoController` 并内部订阅 `onSelectedRepoListChanged`；仓库选择处理改为 private，Provider 不再直接调用。该内部入口以 `sameRepositories` 做顺序无关值去重，内容变化时才取消旧读取并启动新流程。`selectBranches` 保留候选分支去重，并将 `sameSelected` 改为仓库 path + 分支 name 的顺序无关集合比较，防止相同多选但输入顺序变化引发重复发布。`doc/gitbranchscontroller.md` 已同步；分支控制器无诊断，构建仍仅受既有 `statusBar.ts is not a module` 阻塞。
- 明确仓库选择状态边界：`GitRepoController.selectRepositories` 是唯一公开修改入口，Provider 仅将 Webview 选择意图转为候选项后调用；BranchController、CommitController 与 Webview 状态消息只能订阅/读取 `onSelectedRepoListChanged`，不得写入 `selectedRepoList`。控制器内部初始化默认选择和扫描收敛仍通过 private `applySelected` 执行。`doc/gitrepocontroller.md` 已同步且无诊断。
- `GitRepoController.selectRepositories` 去重显式化：先按 `path` 将输入候选项映射到 `totalRepoList`，通过 Map 去除重复项；任一候选不存在则整体拒绝。再以 `path + equalsRepositoryOption` 对规范化结果与当前选择做顺序无关集合比较，相同则不触发事件或下游加载。`doc/gitrepocontroller.md` 已同步；`gitRepoController.ts` 无诊断。构建仍仅受既有 `src/statusBar.ts is not a module` 阻塞。
- `GitCommitController` 现在构造时接收 RepoController 与 BranchController，内部监听 `onSelectedRepoListChanged` 和 `onSelectedBranchesChanged`；`selectRepositories` / `selectBranches` 均已私有化，Provider 不再直接传仓库或分支。仓库选择变化会取消旧提交读取、更新内部仓库快照并推进 repository generation；后续分支回调即使分支名不变也会按新仓库重新读取。`search` 只接收关键字。`selectCommit` 的去重以 `repositoryPath + hash` 作为业务键，对象重建或图形字段变化不会触发重复选择。`doc/gitcommitcontroller.md` 已同步；控制器无诊断，构建仍仅受既有 `statusBar.ts is not a module` 阻塞。
- 虚拟提交读取性能分析：`GitkViewProvider.selectWorkingTreeChanges(mode)` 当前每次点击都调用 `getWorkingTreeChanges()`；该方法并行执行 `git status --porcelain`、`git diff --cached --raw -z -M -C`、`git diff --raw -z -M -C` 三个命令，然后仅消费当前 mode 的一个数组，后续 `DiffReader` 还会按文件读取 HEAD/Index/工作区内容。首要优化是新增按 mode 的定向元数据读取 API，点击 staged 只跑 cached raw diff，点击 changes 只跑普通 raw diff，直接用 raw status 解析 `CommitFile[]`，不跑另一侧 diff/status；保留现有完整 API 给同时计算两条虚拟行状态的场景。
- 已确认 `GitRepoController` 中两类比较不是重复：`sameOptionList` 用于 `applyTotal` / `applySelected` 的有序状态快照发布，顺序变化需要发布；`sameSelectedRepositories` 只用于用户 `selectRepositories` 输入的无序集合幂等比较，输入重复或顺序变化不应触发选择事件。`dedupeSelectedRepositories` 则负责按 path 规范化并剔除重复输入。
- 虚拟提交文件清单读取改为按 mode 定向：新增 `getWorkingTreeChangeFiles(rootUri, mode, signal)`；`staged` 仅执行 `git diff --cached --raw -z -M -C`，`changes` 仅执行普通 raw diff 并以 `git ls-files --others --exclude-standard -z` 补未跟踪文件。`GitkViewProvider.selectWorkingTreeChanges` 与 `GitCommitController.getVirtualCommitContent` 均接入该接口，仍每次从本地读取，Provider 的 AbortController/generation 门禁不变。`doc/gitcommitcontroller.md` 已同步。`git diff --check` 通过；`npm run compile` 仍只受既有 `src/statusBar.ts is not a module` 阻塞。
- 编译异常根因已修复：工作区的 `src/statusBar.ts` 被意外清空，导致 `src/extension.ts` 无法导入 `GitkStatusBar` 并报 TS2306。已按当前 `HEAD` 恢复该模块，`npm run compile` 与 `git diff --check` 均通过。
- `GitRepoController` 的仓库加载状态已从对外 `scanning` 改为 `isLoading`，新增 `onReposLoadingChanged: Event<boolean>`；Provider 只监听该事件并发送 `repoLoadingChanged`。Webview 的 `updateDropdownLoading` 现在同时控制仓库弹窗顶部进度条和仓库显示控件后的圆形 spinner，且重建显示栏时保留 loading 状态。相关类型注释和 `doc/gitrepocontroller.md` 已同步，编译与格式检查通过。
- `GitCommitController` 的加载事件已从 `onLoadingChanged` 重命名为 `onCommitsLoadingChanged`，Provider 和 `doc/gitcommitcontroller.md` 的引用已同步；旧名称在项目源码和文档中无残留，编译与格式检查通过。
- 审计仓库切换链路后修复两处根因：`GitRepoController.selectRepositories()` 返回是否真正接受，Provider 对无效/重复选择不再停留在仓库 loading 阶段；`GitBranchesController.refresh()` 切换仓库时先裁剪内部旧选择但不立即发布空 `onSelectedBranchesChanged`，避免提交控制器因中间空快照触发无效提交读取，改由新仓库当前分支落地时发布有效选择。`npm run compile` 和 `git diff --check` 通过。
- 提交列表 loading 已收敛为 `GitCommitController.onCommitsLoadingChanged` 单一事件源：Provider 不再聚合 `repoController.isLoading`、分支 loading 或 `selectionLoadingPhase`，Webview 仓库/分支选择也不再直接发送提交区阶段提示；提交区只显示“正在加载提交历史”。仓库/分支 dropdown 自身 loading 事件链保持不变。编译与格式检查通过。
- 重新定位提交区 loading 根因：`showLoadingProgress()`、`stateUpdate`、`render()`、`updateCountLabel()`、`renderCommitFooter()` 多个入口可独立改写 DOM，导致 loading 时滚动仍能看到列表或 footer 数量。新增 Webview `isCommitLoading` 快照，并在所有上述渲染入口加门禁；loading 时隐藏 `commitList`、`commitFooter`、`countLabel`，禁止其渲染，结束后统一恢复。编译与格式检查通过。
- 分支显示控件置灰规则已改为只依赖完整 `totalBranches.length === 0`；不再因当前选中分支为空或显示快照暂时为空而禁用。
- `GitBranchOption` 已新增必填 `repoPath`，所有分支构造（普通分支、detached HEAD、当前分支）均写入所属仓库路径；已移除 `virtualCommits` 字段及相关类型、比较逻辑和 Webview 合成渲染，Changes/Staged Changes 改由提交区按选择独立生成。`doc/gitbranchscontroller.md` 和 `doc/gitcommitcontroller.md` 已同步，编译与 lint 通过。
- 用户要求修复编译问题；重新执行 `npm run compile`、全量 `src` lint 和 `git diff --check`，当前均通过，无需新增代码修复。
- 补回 `virtualCommits` 移除后的实际展示逻辑：提交区根据当前选中的 `current` 分支及其 `hasChangeFiles` / `hasStagedChangeFiles` 生成 Changes、Staged Changes 行，并在两个标志变化时原地更新；Provider 选中 HEAD 判断同时使用 `repoPath + name`。编译、lint 和格式检查通过。
- 全面检查 GitRepositoryOption/GitBranchOption/GitCommit 相关对象比较：`GitBranchOption.repoPath` 已由引用 `===` 改为 `equalsRepositoryOption`；提交选择改为 `equalsBranchOption + hash`。字符串、枚举、布尔值和数字比较仍保留 `===`，这些不是对象比较。编译和相关 lint 通过。
- `GitCommitController` 新增 `onSelectedCommitChanged`，`selectCommit` 接受选择后立即 fire；首次默认选择也复用该入口。Store 不再直接写提交区域选中态，Provider 通过回调统一更新；虚拟 `changes`/`staged` 行先构造 `CommitMetadata` 再调用 `selectCommit`，普通与虚拟提交都遵循同一选择事件源。`doc/gitcommitcontroller.md` 已同步；编译、lint 和 `git diff --check` 通过。
- 三个控制器的公开选择入口统一为“过滤/去重/幂等校验后，loading=true → 写入选择字段 → 立即 fire 选择回调 → loading=false”：`GitRepoController.selectRepositories`、`GitBranchesController.selectBranches`、`GitCommitController.selectCommit`。对应三份控制器文档已同步；编译、lint 和 `git diff --check` 通过。
- 分支显示栏与仓库弹窗的 loading 已统一由 `GitBranchesController.onBranchesLoadingChanged` 独占驱动：true 时两者同时显示 spinner/进度条，false 时同时移除。Provider 已移除 `onReposLoadingChanged -> repoLoadingChanged` UI 链路，Webview 只处理 `branchLoadingChanged`，重建时的 loading 快照也只由分支 loading 回调维护。文档、编译、lint 和格式检查通过。
- 三个 controller 的字段与回调按文档对齐：保留内部字段 `total`/`selected`/`loading`，公开 getter 使用 `totalRepoList`、`selectedRepoList`、`selectedBranches`、`selectedCommit`、`isLoading`；回调统一为文档名称，仓库总列表保持既有 `ontotalRepoListChanged` 拼写兼容。编译、lint 和 `git diff --check` 通过。
- 修正此前命名回滚错误：三个 controller 现在以文档名称命名内部字段（`_totalRepoList`、`_selectedRepoList`、`_selectedBranches`、`_selectedCommit`、`_isLoading`），getter 和回调保持文档名称；修复了字段与 getter 同名导致的重复标识符，并通过 lint 与编译。
- `GitRepoController` 已移除 `currentRepo` 字段；初始化默认仓库使用局部 `defaultRepo`，收敛阶段基于 `selectedRepoList` 重映射。`selectRepositories` 不再调用 `dedupeSelectedRepositories` 或 `sameSelectedRepositories`，直接发布传入的 `selectedRepoList`。
- 仓库扫描已移除增量追加流程：`scanSubmodules` 只返回完整扫描结果，`refreshRepositories` 等待扫描结束后由 `converge` 一次性替换全部仓库列表；已删除未再使用的 `mergeIntoTotal`。编译、lint 和格式检查通过。
- `GitRepoController` 已移除 `RepoRecord` 与 `GitRepositoryOptionType`，工作区解析、子模块扫描、收敛流程统一使用 `GitRepositoryOption`；扫描路径与 URI 路径转换和 option 构造已直接内联，随后移除了 `toOptionPath`、`toOptions`、`sortOptions`（以及此前的辅助构造转换方法）。源码通过 lint、编译和格式检查。
- `GitRepoController.refreshRepositories` 已移除 `converge`：等待完整扫描后直接排序并通过 `applyTotal` 替换整个仓库列表；不再根据旧 `selectedRepoList` 做重映射、剔除或回退，初始化时仅从完整列表选择第一项。
- 根据当前简化后的 `GitRepoController` 实现更新 `doc/gitrepocontroller.md`：扫描等待完整结果后一次性替换 `totalRepoList`，不处理 `selectedRepoList`；删除旧的增量扫描、收敛和选择剔除规则说明，补充当前 loading、扫描、选择和事件职责。
- 用户反馈编译异常；实际错误是 `gitBranchesController.ts` 和 `gitCommitController.ts` 中两个无 await 的 `async ...: void` 方法触发 TS1064。已改为保留 `async` 并显式返回 `Promise<void>`，`npm run compile` 恢复通过。
- `GitBranchesController` 已移除 `SelectedBranch`，内部 selected 状态统一使用 `GitBranchOption[]`；`GitBranchOption.repoPath` 全局重命名为 `repoOption`，同步更新类型定义、分支/提交控制器、日志提供器、Webview 和分支文档。编译和 lint 通过。
- 为排查仓库选择到 Webview 显示的时序，已在 `GitRepoController.selectRepositories/applySelected`、分支和提交 `onSelectedRepoListChanged` 监听器、`GitkViewProvider.onSelectedRepoListChanged/postMessage` 以及 Webview `updateSelectedRepoDisplay` 加入 `performance.now()` 日志；编译和 lint 通过。
- 按用户要求临时验证事件阻塞：`GitBranchesController` 与 `GitCommitController` 的 `onSelectedRepoListChanged` 监听改为 `queueMicrotask` 调度 `selectRepositories`，并保留微任务完成日志；编译、lint 和格式检查通过。
- 将临时方案收敛为正式实现：分支/提交仓库选择监听均合并同一轮微任务中的最新仓库列表，增加 `disposed` 门禁与 Promise `.catch`；分支 `selectRepositories` 使用 `await this.refresh(...)` 保留 async 语义，dispose 时清理 pending 状态。编译、lint 和格式检查通过。
- 用户要求撤销复杂微任务方案，改用订阅顺序：`GitkViewProvider` 在构造阶段先订阅仓库选择回调，再创建 `GitBranchesController`/`GitCommitController`，因此 Provider 先于下游收到 `onSelectedRepoListChanged`；两个 Controller 恢复直接 `void selectRepositories(repositories)`。编译、lint 和格式检查通过。
- 在 `GitkViewProvider` 的订阅顺序关键位置增加中文保护注释，明确仓库显示订阅必须先于分支/提交 Controller 创建，且 `selectedRepoSubscription` 不应移到后续 Controller 创建之后。编译、lint 和格式检查通过。
- 调整 `onSelectedBranchesChanged` 订阅顺序：`GitBranchesController` 创建后立即由 Provider 注册 `selectedBranchesSubscription`，再创建 `GitCommitController`；Provider 的分支显示回调先于提交刷新监听器执行，并添加顺序保护注释。
- 明确 loading 职责并实现 UI 分离：`onReposLoadingChanged` → `repoLoadingChanged`，只驱动仓库显示栏和仓库弹窗；`onBranchesLoadingChanged` → `branchLoadingChanged`，只驱动分支显示栏和分支弹窗。Provider 维护两个独立快照，Webview 分别更新两个 dropdown；相关文档已同步，编译、lint 和格式检查通过。
- 撤回 `GitCommitController` loading 所有权改动：移除 `refreshGeneration` 及其相关判断/赋值，恢复 `selectCommit` 始终在选中提交后发送 loading=false，`forceRefresh` 恢复使用 `_isLoading` 判断。
- 修复提交选择体验：`selectCommit()` 不再把普通选择伪装成提交列表 loading，Webview 通过提交列表模型 key 跳过无关的整列表 `render()`；虚拟提交行使用当前分支所属仓库路径，与 `selectedCommit` 身份一致，支持保持 selected。`npm run compile` 和相关 lint 通过。
- 修复仓库切换时分支 loading 不可见：`updateSelectedBranchDisplay()` 改为重建 label 时保留 spinner，`updateDropdownLoading()` 在 spinner 被删除时自动补回；仓库选择的下一帧先调用 `updateBranchLoading(true)`，覆盖扩展端消息往返前的 UI 空窗。编译和 lint 通过。
- Diff 两侧均支持 Ctrl/Cmd+hover 下划线与左键跳转：`multiDiffPanel.ts` 为 original/modified editor 绑定 Monaco 鼠标事件和行内下划线 decoration，消息新增 side；Provider 按侧选择 `path`/`oldPath` 与 original/modified 内容，右侧保留工作区一致性校验。hover 与点击已改用 Monaco `getWordAtPosition`，只装饰鼠标所在单词，空白区域不触发跳转，并以单词起始列定位。编译与相关 lint 通过。