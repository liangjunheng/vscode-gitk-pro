# 2026-08-24- Commit 面板空仓库卡片的折叠状态不再保留用户展开态：`updateCard()` 每次刷新都按 `stagedFiles.length===0 && unstagedFiles.length===0` 直接设置 `cardCollapsed`，空仓库始终折叠；有变更仓库始终展开。`commitPanel.ts` diagnostics 无错误。
- 修复性能提交的 rename/copy 局部刷新缓存身份问题：`UncommittedFilesWatcher.refreshUncommittedFilesForPaths()` 现在以 `CommitFile.path` 和 `oldPath` 双键淘汰受影响缓存，避免按新路径刷新时遗留旧路径条目；`npm run compile`、IDE diagnostics 与 `git diff --check` 通过。
- 修复 `Uncommitted Changes` 虚拟提交始终置灰：根因是 `UncommittedFilesWatcher` 可能在 `GitCommitController` 接收 selected branch 前完成初始发布，而提交控制器只订阅后续事件，导致 `workingTree` 一直为空。现在 `GitCommitController.selectBranches()` 落地 current branch 后直接通过 `UncommittedFilesWatcher.getUncommittedFilesByHeadBranch()` 同步快照，并校验 repoPath/hash 后更新 presence；不再由 Provider 重新读取 Git 状态。`npm run compile` 与 IDE diagnostics 通过。
- 修复虚拟提交持续显示“正在加载变更文件”：`GitkViewProvider.selectWorkingTreeChanges()` 首次调用没有传入事件参数，却在写入 store 时错误访问可选参数 `changes.staged/changes.changes`，抛出 TypeError，导致 `filesLoading` 未被清除。改为始终使用已解析的 `workingTreeChanges`；`npm run compile` 与 diagnostics 通过。
- 已撤销将 stage/unstage/discard 与 `UncommittedFilesWatcher` 绑定的共享 Git 队列方案；该方案只序列化本扩展的一部分操作，不能消除外部 Git 进程或已遗留 `.git/index.lock`，且扩大 watcher 职责。最终无锁 watcher 方案：`UncommittedFilesWatcher` 文件事件只作为刷新信号，排除 `.git/**` 后不再运行 `git check-ignore`；Git status 的结果天然不含 ignore 文件。`getWorkingTreeChanges()` 的 `status`、`diff --cached`、`diff` 三条只读 CLI 均显式带 `--no-optional-locks`，防止 watcher 读取路径创建 optional Git locks。写操作仍按 Git 原子锁语义运行；`npm run compile` 与 diagnostics 通过。
- `UncommittedFilesWatcher` 已补充每仓库真实 git-dir 的 `index` 文件 watcher：通过无 optional locks 的 `git rev-parse --absolute-git-dir` 定位 git-dir，工作区 watcher 继续排除 `.git/**`，index watcher 独立触发同一仓库 `running + needsRefresh` 刷新槽，确保外部 stage/unstage 等只变 index 的操作也刷新 staged changes。index watcher 创建本身按 repoPath 单飞去重，create/change/delete 三种文件事件都仅进入该刷新槽：在途时只置一次 `needsRefresh`，后续重复事件忽略，完成后至多补读一次。两类 watcher 均在 current HEAD 移除和 dispose 时释放；`npm run compile` 与 diagnostics 通过。
- 修复 `restore --staged` pathspec 不匹配的状态根因：Provider 之前在 Git 写入前乐观把 UI 文件从 staged 移到 unstaged；当真实写入失败或 watcher 快照尚未回填时，后续“全部 Unstage”会把非暂存的路径传给 `git restore --staged`，报 pathspec 未被 Git 追踪。已移除乐观分区更新，Changed Files 仅由 `UncommittedFilesWatcher` 的真实 Git 快照更新；单次操作失败后立即请求 watcher 回填真实状态。`npm run compile` 与 diagnostics 通过。
- 已删除 `GitkViewProvider` 中重复且过宽的全局 `**/.git/index` watcher；它与 `UncommittedFilesWatcher` 的真实 git-dir/index 精确监听职责重叠，并可能诱发 VS Code 对普通文件派生 `<file>.git` 的 ENOENT lstat。Provider 不再监听全局 index；`npm run compile` 与 diagnostics 通过。
- 修复虚拟提交中 Unstaged 文件执行 `git add` 后无变化：真实 index watcher 只负责外部 Git 写入信号，不能作为本扩展写操作完成的唯一确认路径。`GitkViewProvider` 的 stage/unstage/discard 成功后现在校验该操作仍属于当前 selected current HEAD，并直接调用 `UncommittedFilesWatcher.refreshUncommittedFilesByHeadBranch()`；刷新仍使用 watcher 的 `running + needsRefresh` 合并和无 optional locks 的只读查询。`npm run compile` 与 diagnostics 通过。
- 修复 stage 成功后 `workingTreeActionLoading` 已结束但 Changed Files 仍显示或闪现“正在加载变更”：`onWorkingTreeChangesChanged` 复用首次选中虚拟提交的 `selectWorkingTreeChanges()`，每次 watcher 快照都重新设置 `filesLoading/diffLoading` 并等待 Multi-Diff 渲染回执。现拆分首次选择与 watcher 状态更新：事件更新直接写入 staged/unstaged 文件快照、清除 loading，不重载 Diff 或创建渲染等待；首次选择仍走完整 Diff 加载。`npm run compile` 与 diagnostics 通过。
- 已确认后续将未提交 watcher 的三条查询收敛为 porcelain v2 单命令时，不会降低 `Uncommitted Changes` 的 Change Files/Diff 实时性：状态快照需保留 staged/unstaged、status、path、oldPath（rename/copy）；现有 `DiffReader.readWorkingTreeDiffs()` 对 staged 以 `HEAD:path`→`:path`、对 unstaged 以 `:path`→工作区文件构建内容，不依赖 watcher 每次读取的 raw-diff object id。仅标题展示的 raw object/mode 元数据需后续按需补齐。
- 已按用户要求撤回 porcelain v2 单命令实验；`getWorkingTreeChanges()` 恢复并行 `status --porcelain=v1`、`diff --cached --raw`、`diff --raw` 三条查询及完整 metadata 合并，`npm run compile` 与 diagnostics 通过。
- `GitCommitController` 现直接订阅 `GitBranchesController.onEachRepoCurrentHeadBranchChanged`：仅当前已选仓库且有 HEAD 时调用已有 `forceRefresh()`，复用 `_isLoading + pendingForceRefresh` 的一次补刷新去重。Provider 移除了重复的 `onBranchHeadCommitChanged → forceRefresh` 订阅，提交刷新责任收敛到控制器；`npm run compile` 与 diagnostics 通过。
- 提交列表重绘前会以首个可视 `.commit-row` 的 `repositoryPath + hash + 相对视口 top` 建立锚点；重绘后查找同一提交并按偏移恢复 `graph.scrollTop`。因此 HEAD 刷新导致列表顶部插入提交时，当前可视提交位置保持不变；锚点提交不再加载结果中时回退原 scrollTop。`npm run compile` 与 diagnostics 通过。
- 虚拟 `Uncommitted Changes` 的 Staged/Unstaged 分区现支持 workspace `filesMode` 的树状/平铺切换：树状按分区独立目录折叠 key 聚合，并保留单文件 stage/unstage/discard 操作。修复操作后“正在加载变更文件”重现的根因：Diff 后台刷新会设置 `diffLoading`，Webview 将其与 `filesLoading` 等同处理，覆盖当前分组文件列表；虚拟提交现在仅在 `filesLoading` 时显示占位，Diff 加载继续后台执行且不遮挡 Change Files。`npm run compile` 与 diagnostics 通过。
- `changedFilesDisplayMode` 持久化已从 `vscode.ConfigurationTarget.Workspace` 改为 `Global`，树状/平铺选择仅写入用户级 VS Code Settings，不再写入项目 `.vscode/settings.json`；`npm run compile` 与 diagnostics 通过。
- 性能优化已实施：保留原有 `getWorkingTreeChanges()` 的 status + 两条 raw diff 完整 metadata 接口，同时新增 `getWorkingTreeStatus()` 仅运行 `git --no-optional-locks status --porcelain=v1 -z --untracked-files=all`。`UncommittedFilesWatcher` 全部实时刷新改用轻量 status 快照，stage/unstage/discard 不再等待全仓库 raw diff 扫描；现有 `DiffReader` 仍按当前文件的 staged/index/工作区 URI 实时读取 Diff 内容。`npm run compile` 与 diagnostics 通过。
- 已完成第二轮全命令调用链复核：仓库扫描/HEAD/index watcher、分支、提交、工作区、Diff、同步及写操作均已逐项检查。未再改动 `git log` 输出格式：commit body 是当前 TS 全字段搜索与 `CommitMetadata` 契约的一部分，去掉 `%b` 会改变搜索结果；`diff-tree -M -C` 的 copy 检测、sync fetch/pull/push/submodule update 与各写操作也因当前功能语义保留。确认可安全的性能收益仍是已落地的 watcher status-only 快照与全只读命令 `--no-optional-locks`；`npm run compile` 与 diagnostics 通过。
- 修复工作区操作成功后仍长时间 loading 且切换 commit 被阻塞：每项 `git add/restore` 已等待 `UncommittedFilesWatcher` status 刷新，但队列轮次结束后 Provider 又重复 `requestUncommittedPresenceCheck + refreshWorkingTreeImmediately`，造成第二次 status 等待；已删除该重复读取。另将 `GitCommitController.onCommitsLoadingChanged` 从 Provider 全局 `isLoading`/Diff 可用性门禁中解耦，只推送提交列表 loading 状态，避免后台 HEAD 提交刷新禁止用户切换 commit 或加载 Diff。`npm run compile` 与 diagnostics 通过。
- 初始化性能根因与修复：`GitRepoController.runScan()` 原先等待 `scanSubmodules()` 完成后才发布根仓库并默认选择，导致当前仓库、HEAD watcher、当前分支都被递归 `.gitmodules`/`rev-parse` 扫描阻塞；现在先 `applyTotal(roots)` 与默认选择，再后台完成子模块扫描并补全列表。`GitBranchesController.refresh()` 原先先 await 当前 HEAD 快路径、再启动完整 `getGitBranches()`，产生串行等待；现在两个 Promise 同时启动，当前 HEAD 完成即发布默认选择，完整列表随后独立落地。`npm run compile` 与 diagnostics 通过。
- 修复 commit 点击后 Multi-Diff 不显示：提交 `14987c9` 将 `GitCommitController.onCommitsLoadingChanged` 改为只推送列表状态，遗留的 Store `isLoading` 初始值为 `true` 且不再会被写为 `false`；`canShowMultiDiff()` 和 `openDiff()` 均以它为门禁，导致每次选择提交都不创建 Diff 面板。已恢复该事件向 `setLoading()` 同步，同时仍保留列表状态推送。
- 审查最新性能提交 `8475de3`：关键改动为只读 Git 调用统一加 `--no-optional-locks`、仓库/分支初始化并发、未提交文件 watcher 改为 `status` 轻量快照及 stage/unstage/discard 后仅按受影响路径合并刷新。已确认一次明确回归是提交 loading 与 Provider 的 Diff 门禁脱节，已修复；仍需重点回归验证带改名/复制和部分路径操作的未提交文件元数据与缓存一致性。
- 优化已确认的 Diff 回归修复：不再通过恢复 `isLoading` 同步把提交列表加载重新耦合为 Diff 前置条件。`canShowMultiDiff()` 与 `openDiff()` 现仅以已选 commit hash（及既有仓库根目录解析）判断可用性，因此后台提交刷新不会阻断点击 commit 的 Diff。
- 已将虚拟 `Uncommitted Changes` 的 add/store(discard/unstage) 操作进度从 Changed Files 顶部 loading 条迁移到 VS Code Notification：每个文件逐一执行 Git 命令并通过 `progress.report` 展示 `当前/总数 + 文件路径`；删除旧 `workingTreeActionLoading` 状态、顶部 DOM/CSS 及 Webview 驱动，`npm run compile` 与 diagnostics 通过。
- 通知栏工作区操作已改为不确定进度：移除 `progress.report` 的 `increment`，只保留 `当前/总数 + 文件路径` 消息，让 VS Code 使用 Notification 的滚动进度状态；`npm run compile` 与 diagnostics 通过。
- 当前 `workingTreeActionLoading` 已从 Store 与 Webview 推送路径一致移除；复跑 `npm run compile` 通过，先前报告的 TS2339 构建阻塞不再存在。
- 通知栏文案按 Git 操作语义细化：`stage` 对未跟踪文件 (`isUntracked`) 显示“添加文件”，对已跟踪文件显示“更新文件”；`unstage` 显示“取消暂存文件”；`discard` 显示“还原文件”。未跟踪路径集合在 `runWorkingTreeAction` 调用时刻从 `store.getState().unstagedFiles` 快照读取并随队列项 `untrackedPaths` 传递，避免执行阶段异步读取过期 Store 状态；`npm run compile` 与 diagnostics 通过。
- 对虚拟提交 add/restore 的三个现象完成根因定位，暂未修改源码：还原 untracked 文件仍执行 `git restore --worktree`（Git 对未跟踪文件无效）；显式路径刷新与 watcher 全量刷新可并发写同一 `changesByRepository` 缓存，旧快照可能覆盖新快照；每次 watcher 事件都调用 `selectWorkingTreeChanges(changes, false)`，其中会停止 Diff、取消 pending、重置 Store 并重新 `openDiff/loadDiffData`，导致 Change Files 闪烁；通知栏持续 loading 需继续沿同一并发/队列调用链确认具体卡点，不能用表层延时或存在性判断修复。
- 进一步确认当前实现的竞态：`processWorkingTreeActionQueue` 对每个操作打开一个 `withProgress`，操作完成后才调用路径刷新；但 Git index/workspace watcher 会同时触发 `requestRefresh` 全量读取并发出变化事件，Provider 又对事件执行 `selectWorkingTreeChanges(changes, false)`，与操作队列及路径刷新交错，造成通知任务与 UI 数据更新脱钩、旧快照覆盖新状态、文件区重置闪烁。根因是工作区变更存在多个刷新入口且没有统一提交后的状态发布顺序，不应通过延时或存在性判断修复。
- 已定位 Multi-Diff 后台更新 loading/位置跳动的设计方向：以稳定的 `currentHash`（虚拟提交固定为 `uncommitted`）作为前台切换与同一提交刷新边界；同一 identity 刷新不应重置 `filesLoading`/`diffLoading`，也不应走 Webview 全量 dispose + reveal。后续需把此 identity 判定贯穿 Changed Files 和 Multi-Diff，并对同结构 diff 做内容级更新以保持 scroll 位置。
- 进一步审查确认仅加 identity 短路仍不完整：`AppState.files` 同时承载 Changed Files 元数据与 DiffPayload，虚拟提交刷新先写入无正文的 `CommitFile[]`，会让 `MultiDiffPanel.publish()` 过滤出空 diffs；应拆分文件清单和 Diff 正文状态，并分别记录 loaded identity。Multi-Diff snapshot 还需携带 identity，同身份按 `diffKey` 做 keyed reconcile（结构变化也保留可视锚点），并合并 Store 多 selector 引发的重复 publish；公开 `provider.selectCommit()` 命令路径也需纳入统一选择身份流程。
- `Uncommitted Changes` 的 discard 已按 VS Code Git 语义重做：确认弹窗按已跟踪、未跟踪、混合文件分别使用 VS Code 简体中文文案和按钮；混合选择支持仅放弃 tracked 或放弃全部。tracked 文件执行 `git restore --worktree`，untracked 文件不再错误执行 restore，而是依据 `git.discardUntrackedChangesToTrash` 通过 `workspace.fs.delete` 移入回收站或永久删除；`npm run compile` 与 diagnostics 通过。
- 方案约束修正：用户明确要求 Changed Files 清单与 Diff 正文必须共用同一份 `AppState.files`，不得拆分。最小方案为：普通提交和虚拟提交统一使用 `repositoryPath + currentHash` 身份（虚拟 id 固定 `uncommitted`）；同身份刷新保留旧的完整 `DiffPayload[]`，在本地完成清单与正文读取后原子替换；仅身份切换显示 loading。Multi-Diff 同身份更新不显示加载页，以首个可见文件及视口偏移为锚点完成重建后位置恢复。
- 已针对虚拟提交 add/restore 的三个现象修改：操作后的路径刷新不再直接调用 `getWorkingTreeStatusForPaths`，而是统一进入 watcher 的 `requestRefresh` 串行队列，消除定向刷新与 index/workspace watcher 全量刷新互相覆盖；工作区状态事件更新文件列表时，仅在首次加载或当前选中文件消失时重建 Diff，避免每次事件停止并重新加载导致 Change Files 闪烁；`npm run compile` 与 diagnostics 通过。
- 按用户要求回滚本轮除“Change Files 防闪烁”之外的修改：恢复 `refreshUncommittedFilesForPaths` 原有定向刷新/合并逻辑及 `getWorkingTreeStatusForPaths` 导入；保留 `selectWorkingTreeChanges` 仅在选中文件变化时停止并重载 Diff 的改动。`npm run compile` 与 diagnostics 通过。
- 修复 Changed Files 水平滚动条缺失：`#filesList` 明确使用 `overflow-x:auto; overflow-y:auto`，并让直接子内容使用 `min-width:max-content`，避免 flex 子项收缩导致长路径被省略而不产生水平溢出；`npm run compile` 与 diagnostics 通过。
- 已落实提交刷新方案：`UncommittedFilesWatcher` 所有文件事件统一进入每仓库 `running + needsRefresh` 槽，严格执行一次当前读取和至多一次补读，补读期间事件不再产生第三轮；删除并发写缓存的路径级刷新旁路。普通提交及虚拟提交都先在局部完成文件清单和 Diff 正文读取，再将完整 `DiffPayload[]` 原子写入同一 `AppState.files`；同 identity 刷新不进入 Changed Files/Multi-Diff loading。Multi-Diff snapshot 增加 `repositoryPath + hash` identity，同 identity 重建以首个可见文件和视口偏移恢复位置，并合并同轮重复 snapshot；`npm run compile` 与 diagnostics 通过。
- COMMIT_MSG_EDITOR 按钮改为编辑器内右下角悬浮（对齐 VS Code COMMIT_EDITMSG 效果）：查内置 Git 扩展 `resources/app/extensions/git/package.json` 确认其用的是 `menus."editor/content"`，并在 `enabledApiProposals` 中声明 `contribEditorContentMenu`。本项目照此实现：`package.json` 加 `"enabledApiProposals": ["contribEditorContentMenu"]`，新增 `editor/content` 菜单（complete=`navigation@1`、cancel=`navigation@2` 渲染为悬浮主按钮；history=`secondary@1` 进溢出菜单），`editor/title` 的三项保留作为安全 API 回退。**重要约束**：`editor/content` 是 proposed API，不能发布到 Marketplace，且仅在带 `--enable-proposed-api=<publisher>.<name>` 启动时生效；实际 ID 为 `liangjunheng.vscode-gitk-pro`（`name` 是 `vscode-gitk-pro` 而非 `vscode-gitk`）。已同步给 `run.bat` 与 `.vscode/launch.json` 加该参数；`run.bat` 原先只跑 `npx tsc` 会跳过 `copy-monaco.cjs` 资源同步，已改为 `npm run compile`。`npm run compile`、`vsce ls`、diagnostics 均通过。
- 提交编辑重构为独立 Commit 面板（webview，编辑器区，形式类 MultiDiff）：新增 `src/git/gitCommitService.ts`（`readCommitTemplate` 用一个必定失败的 `GIT_EDITOR=node -e process.exit(1)` 触发 `git commit [--amend]`，git 中止提交但留下 `.git/COMMIT_EDITMSG` 模板，读取原文；`commitWithMessage` 写临时文件后 `git commit --cleanup=strip -F <file>`，amend 加 `--amend`），`src/webview/commitPanel.ts`（顶部 textarea 提交信息框 + 可展开/隐藏 git 注释模板 + staged/unstaged 列表，unstaged 半透明置灰，支持 add/restore，底部 Commit/Amend 切换按钮，Ctrl+Enter 提交）。`gitkViewProvider` 的 `runOpenCommitEditor` 改为读模板并 `commitPanel.show()`，新增 `buildCommitSnapshot/refreshCommitPanel/toggleCommitAmend/runCommit`；放弃了原 `gitCommitEditMsgEditor` 的 GIT_EDITOR 命名管道桥接（该文件暂保留未删）。**待确认**：amend 时未把上一条提交信息预填进信息框。
- Commit 面板与 Changed Files 区共用同一份工作区数据与操作（用户要求）：`buildCommitSnapshot` 改为直接读 Store 的 `stagedFiles`/`unstagedFiles`（即 `selectWorkingTreeChanges` 在 372-381 行写入、Changed Files 也消费的那份），不再单独调 `getUncommittedFilesByHeadBranch`；`runWorkingTreeAction` 统一以 `uncommittedRepositoryPath` 为目标仓库并只用 Store 清单派生 paths/untrackedPaths，删除了 Commit 面板独立向 watcher 取数的分支。`onWorkingTreeChangesChanged` 在 `selectWorkingTreeChanges().then()` 后调 `refreshCommitPanel()`，使两处随同一数据源同步刷新。add/restore 走同一 `workingTreeActionQueue`，队列完成后同时刷新两者。前提约束：Commit 按钮只在选中虚拟提交行时出现，其仓库与 `uncommittedRepositoryPath` 一致，故 Store 清单必然已填充。UI 微调：Commit 按钮靠右，其左侧加 `codicon-history` 历史按钮（QuickPick 选历史提交信息填入，`git log --all --format=%h%x1f%s%x1f%B%x1e` 去重取 10 条），Unstaged Changes 默认折叠。`npm run compile` 与 diagnostics 通过。
- 展开区从"Git 默认注释"改为"完整 Git Commit 内容"（用户要求含 unstaged/staged/提交内容变化）：`readCommitTemplate` 的探测命令由 `git commit` 改为 `git commit -v -v`，`-v -v` 使 COMMIT_EDITMSG 在 scissors 行后附带 staged 与 unstaged 两份完整 diff，正好覆盖三部分。面板 toggle 文案改为"显示/隐藏完整 Git Commit 内容"，`#templateBody` 由 `white-space:pre-wrap;max-height:280px` 改为 `white-space:pre;max-height:60vh` 便于查看长 diff。安全性：commit 时只发送 messageInput 本文，template 为展示专用不参与提交，故 `--cleanup=strip`（不识别 scissors）不会把 diff 混入提交信息。`npm run compile` 与 diagnostics 通过。
- Commit 面板重构为多仓库卡片 + DOM 复用（避免 N 个仓库批量 spawn git 卡顿）：关键认知——MultiDiff 池化的是 Monaco 实例，而 Commit 卡片无 Monaco，真正的重开销是每仓库 spawn 一次 `git commit -v -v` 读模板。因此虚拟化=模板懒加载：卡片外壳（信息框+文件列表）是廉价 DOM 全渲染，`buildCommitSnapshot` 遍历所有 `kind==='current'` 分支生成 `CommitCard[]`（不含 template），只有用户展开某卡片"完整 Git Commit 内容"时才 `requestTemplate`→`provideCommitTemplate`→`readCommitTemplate` 取该仓库模板。webview 端 `cardEls` Map 按 repositoryPath keyed 复用 DOM，保留用户已输入的提交信息；`render()` 增删/排序复用卡片。per-repo 状态：`commitAmendByRepo`(Map)/`commitCommittingByRepo`(Set) 取代原单例 `commitPanelAmend`/`commitPanelCommitting`，各卡片独立 amend/commit。数据来源：当前选中仓库仍复用 Store 共用清单，其余卡片向 watcher 取各自 `getUncommittedFilesByHeadBranch`（watcher 构造时已为所有 current 分支建 slot，多仓库数据可得）。`runWorkingTreeAction` 加可选 `repositoryPath` 参数，Changed Files 不传用虚拟提交仓库，Commit 卡片显式传其仓库。`runOpenCommitEditor` 打开时展示所有卡片并 `show(snapshot, focusRepo)` 定位到触发仓库；点提交图某仓库 uncommitted 只需 focus 到对应卡片。amend 切换 bug 根因已修：webview 只发纯 `toggleAmend` 信号（不带 `!state.amend` 过期值），宿主翻转权威 `commitAmendByRepo`。`npm run compile` 与 diagnostics 通过。
- UncommittedFilesWatcher 改为全部仓库监听（用户要求）：原构造依赖 `GitBranchesController`，其 `getBranches('current')`/`onEachRepoCurrentHeadBranchChanged` 只覆盖**已选仓库**（`this.repositories` 由 `onSelectedRepoListChanged` 驱动）。改为依赖 `RepoHeadBranchWatcher`（订阅 `ontotalRepoListChanged`、用 `totalRepoList`，是全部仓库 HEAD 的唯一监听者），订阅其 `onEachRepoHeadBranchChanged`（负载 `{repositoryPath, headBranch}`）驱动 `applyCurrentHeadBranch`，删除原构造里对已选仓库的 seeding 循环（head watcher 首次读取 HEAD 时会 emit，新仓库自然进入）。provider 构造处 `new UncommittedFilesWatcher(this.repoHeadBranchWatcher)`（该 watcher 在其前创建）。commitController 仍通过 watcher 的事件与 `getUncommittedFilesByHeadBranch` 取数，对选中仓库当前分支过滤是 Changed Files 单仓库视图的正确行为，无需改。`npm run compile` 与 diagnostics 通过。
- uncommitted 数据变化改由 watcher 事件驱动多仓库 Store：provider 直接订阅 `uncommittedFilesWatcher.onEachHeadBranchUncommittedFileChanged`（负载 `{branch, changes:WorkingTreeChanges}`），新增 `onRepositoryUncommittedFilesChanged` 按 repositoryPath 增量更新 `AppState.commitRepositories`（命中则替换该条，否则追加），面板可见时立即 `commitPanel.update(buildCommitSnapshot())`。此前多仓库 Store 只在打开面板/amend/commit/操作队列完成时主动 `syncCommitRepositories` 拉取，其他仓库的工作区变化不会驱动更新；现在任一仓库变化都经 watcher 事件推送，不依赖仓库选择。补充 import：`WorkingTreeChanges`（从 `../types` 具名导入，值非类型）。`npm run compile` 与 diagnostics 通过。
- Commit 面板显示所有仓库卡片：`syncCommitRepositories` 原遍历 `this.branches`（已选仓库）只出已选仓库卡片；新增 `UncommittedFilesWatcher.listCurrentHeadBranches()`（遍历内部 `slots` 返回全部仓库当前 HEAD 分支），改为用它枚举，覆盖全部仓库。
- Commit 卡片交互（用户要求）：①无任何 staged 时 commit 按钮置灰（`disabled=committing||staged.length===0`）；②内间距/卡片间距本就与 MultiDiff 一致（`#app{padding:8px;gap:14px}` vs `#list{padding:8px}`+`.diff{margin:0 0 14px}`），未改；③无任何 uncommitted files（staged+unstaged 都空）的仓库卡片首次自动折叠（`card-body` hidden）且可点标题展开，头部显示"无更改"标签与 chevron；有 uncommitted files 的仓库强制展开且不可折叠（chevron 隐藏、`_collapsible=false`）。折叠状态用 `_emptyApplied` 标记确保只首次自动折叠，用户展开后不被后续 update 强制收起，直到该仓库重新变空。`npm run compile` 与 diagnostics 通过。
- Commit 面板展开的"完整 Git Commit 内容"拼上编辑框提交信息：git 的 COMMIT_EDITMSG 模板正文区本就留空给消息，故在 webview 端合成——`state.rawTemplate` 存 git 原文，`composeTemplate()` = 编辑框当前 message + '\n' + rawTemplate，messageInput input 事件、templateResult 到达、setMessage（历史填入）后都调用它实时刷新。`--cleanup=strip` 提交仍只用 messageInput 本文，展开区为展示专用不影响提交。
- 遗留待办：用户反馈"显示完整 git commit 内容很慢"，根因是 `readCommitTemplate` 用 `git commit -v -v` 生成 staged+unstaged 两份完整 diff，大仓库耗时，尚未优化（可考虑去掉一个 -v 或按需/缓存）。
- add/restore 性能最终回归原路径级刷新：恢复 `refreshUncommittedFilesForPaths()`，Git 操作后只读取 `operation.paths` 并合并缓存；移除 `workingTreeFilesUpdate` 和对全仓库 watcher 读取、全部 Diff 正文落地的同步等待。操作通知只等待受影响路径的状态刷新，完整 Diff 界面由事件异步更新，避免大仓库操作显著变慢。`npm run compile` 与 diagnostics 通过。
- 修复同 identity Diff 刷新时高亮和位置变化：Provider 优先保留原 `diffKey`，stage/unstage 导致 key 改变时按真实文件路径映射新卡片；Multi-Diff 刷新前记录当前高亮卡片及其相对视口偏移，重建后分别恢复高亮和该文件位置，不再使用首个可见卡片作为锚点。`npm run compile` 与 diagnostics 通过。
- 置顶高亮与 Changed Files 的相互影响根因及修复：①`MultiDiffPanel.revealPath` 是与 Store `selectedPath` 并行的第二份选中态，`selectFile` 消息会把旧 `diffKey` 写进去，刷新后快照仍按过期 key 定位；已删除该字段，`show/revealFile` 只透传参数，快照 `revealPath` 统一取 `state.selectedPath`。②Webview `dispose()` 会清空 `activePath`/`clickedPath`，重建后首个 `scroll` 事件触发 `syncActiveFromViewport()` → `topVisibleCard()` → `setActive(path,true)`，把高亮改成首个可见卡片并回写 Changed Files；`restoreRefreshState()` 现在恢复时同时写 `clickedPath`，命中 `clickedEntry&&isCardVisible` 短路。③恢复目标顺序为原 `diffKey` → 原真实路径 → `snapshot.revealPath` → 原序号邻近卡片；仅当仍是同一文件才按原偏移还原滚动，`notify` 仅在与 Store 值不一致时发送，避免多余 `selectFile` 往返。`npm run compile` 与 diagnostics 通过。
- add/restore 后位置错乱的真正根因：卡片重建时 `createCardShell()` 只用 `estimateBodyHeight()`（按行数 `lines*20+12`）估算高度，`restoreRefreshState()` 在 `updateVirtualization()` 之前就按这些估算值滚动一次；真实高度要等各 `mountEntry()` 异步完成后由 `fit()` 测量 Monaco 内容才确定。原 `fit()` 只补偿完全位于视口上方（`rect.bottom<=0`）的卡片，而 add/restore 会让文件在 `staged:`/`unstaged:` 分区间迁移，锚点上方卡片集合整体改变，累积估算误差无人纠正。修复改为锚点持续对齐：新增 `pinnedAnchor` 记录选中卡片 key 与原视口偏移，`applyPinnedAnchor()` 在每次 `fit()` 高度落定后重新对齐（替代原逐卡片补偿分支）；`selfScroll` 标记区分锚点自身写入与用户滚动，用户滚动、`reveal()` 显式定位、`dispose()` 均清除锚点。`npm run compile` 与 diagnostics 通过。
- 本次方案复核结论修正：`pendingFilesRevealGeneration`/`handleDiffRendered`/Multi-Diff `onRendered` 与 `rendered` 消息**不可删除**，它们控制 identity 切换首次加载时 Changed Files 的放行时机及面板销毁回退，删除会改变两区显示顺序。已执行的无风险清理只有：删除只写不读的 `UncommittedFilesWatcher.branchesByRepository`（含 `dispose()` 清理与两处写入）、`needsRefresh` 直接赋值。同时保留 `schedulePublish()` 合并、`reveal=anchor` 日志文案、Changed Files 仅看 `filesLoading` 的判定；`cancelPending()` 职责拆分按用户要求不做。`npm run compile` 与 diagnostics 通过。
- 修复打包后虚拟提交 add/restore 图标不可见（点击仍有效）：图标是 codicon 字体图标，`.vscodeignore` 排除 `node_modules/**`，而 `gitkViewProvider.getHtml()` 与 `localResourceRoots` 都指向 `node_modules/@vscode/codicons/dist`，安装版该路径不存在导致 `codicon.css` 404；按钮 DOM 与事件委托不依赖 CSS，故点击正常。`multiDiffPanel` 更是从未加载 codicon 样式，开发期只是蹭到 Monaco `editor.main.css` 自带的 `@font-face codicon`（隐式依赖）。修复统一走"随包分发的 media 资源"：`scripts/copy-monaco.cjs` 改为资源清单式，额外把 `codicon.css`/`codicon.ttf` 复制到 `media/codicons/`（`compile` 与 `watch` 都会执行，保证开发版与打包版同源）；两个 webview 均改引用 `media/codicons/codicon.css` 并把该目录加入 `localResourceRoots`，Diff 面板 HTML 显式 `<link>` 该样式表。`media/**` 已被 `.vscodeignore` 显式包含且不在 `.gitignore` 中。`vsce ls` 已确认包含 `media/codicons/codicon.css` 与 `codicon.ttf`；`npm run compile` 与 diagnostics 通过。
- 修复显示方式切换偶现闪一下又弹回的根因：`displayMode` 存在 Store 与 VS Code 配置两个所有者且形成回环。`toggleFilesMode` 先在 Store 翻转并 `notify()` → `schedulePushState()`（Webview 已按新模式渲染），随后副作用异步 `update(..., ConfigurationTarget.Global)`；配置变更事件再回灌 `this.displayMode = get(...)`。问题在于配置声明 `"scope": "window"` 而写入用 Global，若更高优先级层级（工作区/文件夹）存在同名键，`get()` 的生效值不等于刚写入值，回灌即把显示方式设回旧值。偶现是因为 `update()` 异步且与 `store.subscribe` 推送顺序不固定，且仅当高优先级层级确实覆盖时回灌值才不同。修复：`package.json` 中该配置 `scope` 由 `window` 改为 `application`，使生效值只来自 Global、与写入目标一致，回读值必然等于刚写入值，回环自然消解（未使用来源标记等判别式补丁）。`npm run compile` 与 diagnostics 通过。
- 虚拟提交文件夹无法折叠的根因：`renderFiles()` 中 `selectedCommitHash==='uncommitted'` 分支在 `bindWorkingTreeActions`+`bindFileItems` 后直接 `return`，从未走到函数末尾那段 `.folder-item` 的 pointerup 绑定（该绑定原本内联在普通提交路径尾部）。`workingTreeSectionFilesHTML()` 其实已渲染 folder-item 行并读写 `collapsedFolders`，只是没有事件。修复：把绑定提取为 `bindFolderItems(list)`，普通提交与虚拟提交两个分支都调用，折叠状态仍共用 `collapsedFolders`（虚拟提交 key 形如 `working-tree:<section>:<folder>`，与普通提交天然隔离）。
- 文件夹图标未垂直居中的根因是 CSS 特异性冲突：`codicon.css` 的 `.codicon[class*='codicon-']`（0,2,0）带 `display:inline-block` 与 `font:...16px/1`，压过单类选择器 `.tree-chevron`/`.tree-folder-icon`（0,1,0），导致 `inline-grid`+`place-items:center` 从未生效，16px 行盒顶对齐在 24px 行内。修复改为两级选择器 `.folder-item .tree-chevron, .folder-item .tree-folder-icon`（0,2,0）并用 flex 居中、`height:100%` 跟随行高。`#commitToggleBtn` 自身已是 `inline-flex`+居中，无需调整。
- 实施方案二（自有语言 ID，与方案一配置迁移无关，方案一未做）：新增 `contributes.languages` 声明 `COMMIT_MSG_EDITOR` + `contributes.grammars`，语法文件 `syntaxes/commit-msg-editor.tmLanguage.json`（scope `text.commit-msg-editor`，含注释区/scissors/subject 长度分级/fixup!/trailer 规则）与 `syntaxes/commit-msg-editor.language-configuration.json`（lineComment `#`）。**刻意不写 `filenames`**，避免与内置 Git 的 `git-commit` 争抢 `COMMIT_EDITMSG`；改由 `openEditorSession()` 中 `vscode.languages.setTextDocumentLanguage(opened,'COMMIT_MSG_EDITOR')` 显式指定，返回的新 document 用于后续 `showTextDocument`。`vsce ls` 已确认两个 syntaxes 文件入包。注：`syntaxes/diff.tmLanguage.json` 仍是死资产（未被任何 grammars 引用）。
- 更正上一条误判（2026-08-24）：`fillHistoryMessage()`/`readHistoryMessages()` **从未丢失**，原实现一直在 `gitCommitEditMsgEditor.ts` 中且更完善（处理 `core.commentChar`、`--all --max-count=200` 后按完整消息去重、`getMessageRange()` 精确定位消息区、光标落在消息末尾）。我误判为方法丢失并重复添加了一份简化版，导致 TS2393 Duplicate function implementation；已删除多加的版本，保留原实现。教训：报 TS2339 时先全文件搜索确认符号是否真的缺失，不要凭局部片段断定并补写。
- 实施方案一（配置所有权迁移，与方案二语法高亮无关）：新增 `vscode-gitk.discardUntrackedChangesToTrash`（boolean，默认 `true`，`scope: application` 与 `changedFilesDisplayMode` 一致，避免层级覆盖回环），`gitkViewProvider.ts:899` 由 `getConfiguration('git')` 改为 `getConfiguration('vscode-gitk')`。全库已无对 `git` 命名空间配置的读取，至此对内置 Git 的两处软依赖全部消除。`npm run compile` 与 diagnostics 通过。
- 全库排查对内置 `vscode.git` 的依赖，结论是**已无硬依赖**，可直接禁用内置 Git 扩展：无 `getExtension('vscode.git')`/`getAPI()`，无 `executeCommand('git.*')`，无 `vscode.scm`/TimelineProvider/FileDecorationProvider，无 `git:` scheme（自有 `gitk-empty`，`uncommittedFilesWatcher` 还显式过滤非 `file:`），`when` 子句不含 `gitOpenRepositoryCount`/`scmProvider`/`git.state`，无 `extensionDependencies`，仓库发现与内容读取全走自建 `execFile/spawn('git')` 与 `git cat-file --batch`。仅剩两处软依赖：①`gitkViewProvider.ts:899` 读 `git.discardUntrackedChangesToTrash`，禁用后 schema 消失但 `get()` 回落默认 `true`，行为不变（如需彻底自持应迁到 `vscode-gitk` 命名空间）；②`COMMIT_EDITMSG` 的 `git-commit` 语法由 vscode.git 贡献，禁用后以 plaintext 打开，仅失去高亮，提交流程不受影响（可自建 `contributes.languages`+grammar 补回）。另发现 `syntaxes/diff.tmLanguage.json` 是死资产（`package.json` 未声明 `contributes.grammars`/`languages`）；所有 git 调用假定 `git` 在 PATH，不读 `git.path`（既有行为，非禁用导致）。
- 修复关闭 Git 功能后 COMMIT_EDITMSG 的 commit/x 图标消失：两个 `editor/title` 菜单的 `when` 原为 `editorLangId == git-commit && resourceFilename == COMMIT_EDITMSG && gitk:commitEditMsgActive`，其中 `git-commit` 语言 ID 由内置 `vscode.git` 贡献（本项目无 `contributes.languages`，`syntaxes/` 仅有 `diff.tmLanguage.json`）。禁用 Git 扩展后该语言注册消失，文件回退为 plaintext，条件恒假故按钮隐藏；而 `Ctrl+Enter`/`Esc` 的 keybinding 只用 `gitk:commitEditMsgActive`，所以仍可用。该提交流程自身不依赖 `vscode.git`（自行 spawn `git commit` + 命名管道桥接 `GIT_EDITOR`），故根因是 UI 可见性错误依赖外部扩展贡献的语言 ID。修复：两个 `when` 简化为仅 `gitk:commitEditMsgActive`，与 keybinding 一致。另两个条件本就冗余——非本扩展打开的 COMMIT_EDITMSG 无 session，`getActiveSession()` 返回 undefined 时 context 即为 false；`finishSession()` 内已调用 `updateActiveContext()`，tab 关闭路径也经由它，context 不会残留。`npm run compile` 与 diagnostics 通过。
- 图标尺寸按用户要求调小：文件夹 chevron 与文件夹图标由 16px 盒/16px 字号改为 14px 盒/13px 字号，`#commitToggleBtn .codicon` 由 16px 改为 13px 并补 `line-height:1`。注意这两处的字号声明必须留在能压过 `codicon.css` 的 `.codicon[class*='codicon-']`（0,2,0）的选择器上，否则会被其 `16px/1` 覆盖。
- Changed Files 图标统一为 codicon 风格：文件夹折叠指示由 `⌄`/`›` 文本字符改为 `codicon-chevron-down`/`codicon-chevron-right`，文件夹图标由 `▰` 改为 `codicon-folder-opened`/`codicon-folder`（展开态与 VS Code 资源管理器一致，两处树状渲染分支同步）；`.tree-chevron`/`.tree-folder-icon` 改为 16px inline-grid 图标盒、行高 24px 对齐，`.folder-item` 取消 8px gap 改由 `.file-path` 的 4px 左边距控制间距，与 VS Code 树行紧凑排布一致。Commit 按钮右侧的 `commitToggleBtn` 由自绘 chevron SVG 改为 `codicon-arrow-swap` 切换图标，同时删除已失效的 `svg` 旋转规则（原 `.amend svg{rotate(180deg)}` 选择器不再匹配），amend 态改用半透明背景标识。`npm run compile` 与 diagnostics 通过。
- COMMIT_EDITMSG 标题栏在 commit 图标左侧新增“历史提交信息”按钮：命令 `vscode-gitk.commitEditMsg.history`（`$(history)`，`editor/title` 的 `navigation@0`，`when` 与其余两个按钮一致仅用 `gitk:commitEditMsgActive`）。`GitCommitEditMsgEditor.fillHistoryMessage()` 用 `git log --max-count=200 --all --format=%h%x1f%s%x1f%B%x1e` 读历史并按完整 message 去重后只取最新 10 条（`--max-count=50` 是去重余量，重复消息不占用 10 条名额），QuickPick 选中后**替换**消息区。消息区边界由 `getMessageRange()` 按注释字符定位（文档开头到首个注释行），注释字符经 `git config --default '#' --get core.commentChar` 读取，`auto` 按 Git 首选值 `#` 处理；已实测空仓库下 `log --all` 与该 `config --default` 退出码均为 0，无需额外兜底分支。`npm run compile` 与 diagnostics 通过。
- 虚拟提交文件面板的 `Staged Changes`/`Unstaged Changes` 标题增加 `position: sticky; top: 0` 吸顶；`workingTreeSectionHTML()` 按 `section:path` 判断选中文件所属分区，添加 `has-selected`，标题及选中文件行使用 VS Code focusBorder 蓝框高亮；`npm run compile`、diagnostics 通过。
- 修复仓库列表与已选仓库图标不一致：`GitRepoController` 的选中态改为仅保存规范化路径，`selectedRepoList` 始终从 `_totalRepoList` 派生，仓库元数据只存在于总列表。最终子模块扫描更新 total 后，Webview 的 total-list 监听器用派生的 selected 列表刷新顶部显示；不再把 `hasSubmodules` 等元数据更新广播为选择变更，因而不会触发分支/提交控制器重载。`npm run compile` 与 diagnostics 通过。
- 虚拟提交文件行的 add/restore/unstage 操作按钮改为 `position: sticky; right: 0`，并设置面板背景与左侧阴影，水平滚动长路径时操作按钮保持在 Changed Files 面板右侧；`gitkViewProvider.ts` diagnostics 无错误。
- 提交选中态改为身份派生：`GitCommitController` 用 `{repositoryPath, hash}` 保存选择，普通提交优先从完整 `total` 列表派生、在新一轮读取尚未提交 total 时从 `searched` 派生，虚拟 `uncommitted` 从当前分支派生。该优先级保持搜索过滤稳定，并避免首次默认选择在 total 尚未更新时成为 undefined、把提交文件读取推迟到 total 事件后而串行化。总提交列表刷新后 Provider 以派生提交重绘文件/选中显示，但不重发提交选择事件；因此图形、提交信息、分支对象等列表元数据更新不会触发重复选择。`npm run compile` 与 diagnostics 通过。
- 优化工作区 add/restore/unstage：同一操作将 tracked 路径合并为单条 `git add` 或 `git restore` 命令，未跟踪 discard 保持逐个文件系统删除；显式操作后 watcher 的路径级状态更新携带受影响路径，`selectWorkingTreeChanges()` 复用元数据未变化的 `DiffPayload`，仅重读受影响、状态迁移或新出现文件的 Diff。外部 watcher 的全量语义未改；`npm run compile` 与 diagnostics 通过。
- 修复 add/restore 后列表不刷新：根因是 `refreshUncommittedFilesForPaths()` 绕过 `UncommittedFilesWatcher` 的仓库刷新槽，与 index/workspace watcher 的全量读取并发写入 `changesByRepository`，旧全量快照可能覆盖路径刷新结果且抑制事件。路径刷新现写入 slot 的 `pendingPaths` 并经同一 `requestRefresh()` 队列执行；`refreshSlot()` 在队列内消费路径集做增量合并，所有缓存和事件按单一顺序提交。`npm run compile` 与 diagnostics 通过。
- 虚拟提交所有文件子项设置 `min-width:max-content; width:100%`，确保每项占满最长内容宽度；右侧操作区使用不透明 `background-color: var(--vscode-editor-background)` 与阴影，hover/selected 状态分别同步对应背景色；编译和 diagnostics 通过。

- 继续验证 Changed Files 横向滚动条布局：`npm run compile` 成功，`gitkViewProvider.ts` diagnostics 无错误。
- Commit Editor 仓库标题后新增三种蓝底白字状态徽章：`Untracked <n>`、`Unstaged <n>`、`Staged <n>`；未跟踪文件从 `unstagedFiles` 中单独计数，未暂存数排除未跟踪文件，避免重复统计；零数量徽章隐藏。`npm run compile` 与 diagnostics 通过。
- 定位 add/restore 同步卡顿：`refreshUncommittedFilesForPaths()` 进入刷新槽时，若 index/workspace watcher 已启动全量刷新，路径刷新只能等待该全量 `git status` 完成后再补读；随后 `selectWorkingTreeChanges()` 在受影响文件 Diff 读取完成前不提交 `store`，Changed Files 与 Commit Editor 的统一快照因此共同等待。共享数据不等于共享快速更新协议；后续应将操作路径刷新设为队列的合并输入并先提交文件元数据，Diff 作为后续独立更新。
- 修复工作区状态同步被 Diff 阻塞：`selectWorkingTreeChanges()` 现于读取到 `WorkingTreeChanges` 后立即在同一次 store 提交中更新 `stagedFiles`、`unstagedFiles` 与当前仓库 `commitRepositories`，并立刻发布 Commit Editor；后续仅异步读取和提交 `files`/`DiffPayload`。保持单一工作区数据源，同时让 Changed Files 与 Commit Editor 不再等待 Diff 内容。`npm run compile` 与 diagnostics 通过。
- 优化 Commit Editor 初始化：`syncCommitRepositories()` 对非当前仓库改为仅读取 `UncommittedFilesWatcher` 已发布的缓存快照，不再 await 每个仓库的 `getUncommittedFilesByHeadBranch()`，避免最慢子模块阻塞面板打开；`onEachHeadBranchUncommittedFileChanged` 既有订阅会在各仓库状态完成后增量更新其卡片。`npm run compile` 与 diagnostics 通过。
- 修复 add/restore/unstage 成功后列表不变：操作队列此前从 `selectedCommit` 取当前分支；当主图选择普通历史提交而非虚拟 `uncommitted` 时，路径级刷新被跳过，进度通知消失但工作区快照仍旧。现在按操作目标仓库从 `branches` 查找其 `current` HEAD 分支并刷新路径状态，操作与当前提交选择解耦。`npm run compile` 与 diagnostics 通过。
- 工作区操作完成后异步通知：操作队列按目标仓库在 `UncommittedFilesWatcher.listCurrentHeadBranches()` 中的真实 HEAD 触发非阻塞路径刷新；watcher 多仓库事件更新可见 Commit Editor，当前展示 `uncommitted` 时 CommitController 事件更新 Changed Files。
- 重新收敛 add/restore/unstage 调用链：操作层仅执行 Git 命令和异步请求 `UncommittedFilesWatcher`；所有 UI 更新均由 watcher 事件驱动。Provider 按仓库记录显式操作的受影响路径，CommitController 的当前仓库 watcher 事件消费对应路径以增量更新 Changed Files，Provider 的同一 watcher 事件更新对应 Commit Editor 卡片；不再在 Changed Files 更新完成后额外重刷 Commit Editor。`npm run compile` 与 diagnostics 通过。
- 已核实 `UncommittedFilesWatcher.onEachHeadBranchUncommittedFileChanged` 仅有两个直接订阅者：`GitCommitController`（仅匹配当前选中仓库与 HEAD hash 后更新单仓库工作区快照）和 `GitkViewProvider`（按 repositoryPath 增量更新所有仓库的 Commit 面板状态）；事件由 watcher 在 status 快照实质变化后发布。
- `UncommittedFilesWatcher` 的主动刷新公开入口为全量 `refreshUncommittedFilesByHeadBranch` 与路径级 `refreshUncommittedFilesForPaths`；二者均经每仓库 `requestRefresh → drainRefresh` 槽串行处理，在读取中合并为最多一次补读，路径级刷新仅把路径写入 `pendingPaths`，由同一槽读取并合并缓存，避免与全量刷新并发提交。
- UI 监听边界：Commit Editor 由 Provider 直接监听 `UncommittedFilesWatcher.onEachHeadBranchUncommittedFileChanged`，更新任意仓库的 `commitRepositories` 后刷新可见卡片；Changed Files 监听 `GitCommitController.onWorkingTreeChangesChanged`，控制器先用当前选中仓库/HEAD 过滤 watcher 事件，且 Provider 仅在当前身份是该仓库 `uncommitted` 虚拟提交时重建文件/Diff。
- 已确认刷新槽并发模型：`slots` 以 repositoryPath 为键，`requestRefresh()` 只检查该仓库 slot 的 `running`，因此不同仓库可并发执行状态读取；同一仓库复用 completion 并将重入请求合并为 `needsRefresh` 后至多补读一次。
- 为定位 add/restore 卡顿，已加入 `[Gitk][WorkingTreeAction]` 与 `[Gitk][UncommittedWatcher]` 分段耗时日志：覆盖 Git 写入、未跟踪文件删除、路径刷新请求/完成、slot 合并/补读、`git status` 路径或全量读取、状态未变/变更发布及总耗时；`gitkViewProvider.ts`、`uncommittedFilesWatcher.ts` diagnostics 无错误。
- 已补齐 UI 显示耗时观测：Commit Editor 的 `[Gitk][CommitPanel]` 记录 snapshot post 与 Webview 卡片 `render` 耗时/卡片数；Changed Files 的 `[Gitk][ChangedFiles]` 记录主 Webview `renderFiles` 从 DOM 构建、事件绑定到定位选中文件的耗时/文件数。渲染耗时通过受类型校验的 `renderTiming` intent 回传；相关 diagnostics 无错误。
- add/restore 性能日志已扩展为端到端 trace：工作区操作请求、准备/确认、队列等待、Git 写入、路径刷新请求/slot 合并/读取/发布、Provider watcher 接收、Commit Editor snapshot 构建/投递/Webview render、CommitController 事件过滤、Changed Files 元数据写入、Diff 准备、完整快照写入与 Webview render 均记录。显式操作使用 `traceId` 和起始时间按仓库关联，可直接从总耗时定位阶段；相关 diagnostics 无错误。
- 日志协议已按用户要求改为只记录绝对 ISO `timestamp` 与 `traceId` 的阶段事件，不再计算或输出 `elapsedMs`/`totalElapsedMs`；Commit Editor 与 Changed Files Webview 均以 `rendered` 完成事件回传显示时刻，相关 diagnostics 无错误。
- 清理 Changed Files 工作区分区布局：此前多次试验留下同一选择器的重复 Flex/Grid/sticky 规则，后写规则覆盖前写规则导致布局混乱。现只保留单一 Flex 结构：标题与其操作区固定在分区外层，文件 body 独立横向滚动以显示完整路径；`npm run compile` 与 diagnostics 通过。
- Changed Files 改为仅一条底部横向滚动条：`#filesList` 是唯一 x/y 滚动容器，工作区分区 body 不再创建滚动条；文件路径以 `max-content` 撑开公共内容宽度，分区 header 用 `position:sticky; left:0` 固定标题和右侧操作。补齐 `GitkIntent` 的 `renderTiming` 联合类型，`npm run compile` 与 diagnostics 通过。
- 修复 Changed Files 横向滚动布局：分区标题及右侧操作区在外层固定，只有 `.working-tree-section-body` 为独立横向滚动容器；文件行和路径使用 `max-content`，完整路径可通过该分区滚动查看，外层 `#filesList` 仅纵向滚动。清除了多次试验遗留的 Flex/sticky/截断覆盖规则。`npm run compile` 与 diagnostics 通过。
