# 2026-08-23

- Multi-Diff 导航高亮浮起样式最终使用 focusBorder 主题蓝色 30% 透明背景、2px 蓝框、下投影和上移 1px；编译与 lint 通过。
- 修复导航高亮多一行及增删块高亮整份代码：不再让 `EndLineNumber=0` 的空侧锚点参与 Overlay 几何合并；只按有实际变更行的一侧计算顶部和高度，两侧都有内容时高度取最大。代码块底部使用下一行顶部而非 `endLine + lineHeight`，避免折行/View Zone 多算一行；外层跳转也只用有内容侧定位。删除旧 `lineChangeRange()` 空侧映射。编译、lint、diff check 通过。
- 修复超屏差异分页把不可见区域计入的问题：使用当前 `pinnedGroup` 实际高度和窗口底部减 14px 计算真实可视带。跳转目标不再贴顶，在该可视带上方预留五分之一，目标代码定位到 `navigationTop`；页高同步改为 `visibleBottom - navigationTop`，避免预留区导致屏外内容又被计入当前页。超屏差异仍逐页推进，走完后进入相邻差异/文件。编译、lint、diff check 通过。
- 修复纯 Rename 导航卡死：Rename 路径已变化但内容相同，Monaco `getLineChanges()` 返回空，导航层生成文件级合成差异；纯 Rename 也按真实可视区域分页高亮，分页走完后进入相邻差异/文件。未修改 DiffReader 或伪造 Git 行差异。
- 用户要求完全移除 Multi-Diff 软换行：已删除 package.json 开启/关闭命令与 editor/title 菜单、extension Context Key/命令注册、Provider globalState 读写、MultiDiffPanel 构造状态/快照字段/消息、Webview wordWrap 状态与 applyWordWrap，以及 monacoDiffConfig 中 `wordWrap/diffWordWrap` 字段。项目源码与清单已无 WordWrap/wordWrap/diffWordWrap/multiDiffWordWrap 命中，保留底部水平滚动条。清单解析、编译、lint、diff check 通过。
- Changed Files 显示方式（tree/flat）原已通过 Store effect 持久化，但使用 `workspaceState`，按工作区隔离。现启动读取和切换写入统一迁移到 `context.globalState['gitk.filesDisplayMode']`，作为用户级 UI 偏好，插件重启及切换工作区后保持。编译、lint、diff check 通过。
- 当前状态核查：`master` 与 `origin/master` 同步，3 个修改均已暂存且无未暂存变化：版本号从 1.0.5 回退为 1.0.4、Changed Files 显示模式由 `workspaceState` 改为 `globalState`、以及本日工作记忆更新。版本号回退表明存在撤回修改，后续不得默认沿用原方案。
- 再次核查后 `package.json` 版本号回退已被用户撤回；当前只剩 `src/webview/gitkViewProvider.ts` 的 Changed Files 显示模式全局持久化，以及工作记忆更新，均已暂存，无未暂存修改。
- 用户指出 Changed Files 显示方式应可通过 Settings 配置，因此撤销隐藏 `globalState` 方案并改为正式配置 `vscode-gitk.changedFilesDisplayMode`（`tree`/`flat`，默认 `flat`，window scope）。Provider 启动时读取配置、监听外部配置变化，工具栏切换回写 Workspace Settings，Settings 成为唯一持久化数据源。清单 JSON、TypeScript 编译、IDE diagnostics、diff check 通过；项目 eslint 命令因环境找不到 eslint 可执行文件而未实际运行。
- 调整 commit 展开描述的内容顺序：提交标题与正文置顶，作者信息移至 `Commit` 元数据之前；根因修改限定在 `buildCommitRowHTML()` 的描述字符串构造，未改数据解析、布局或展开交互。TypeScript 编译与 IDE diagnostics 通过。
- commit 描述列的原生 hover 提示由仅显示标题改为“标题 + 正文 + 分割线 + 作者 + 时间”，复用 `buildCommitRowHTML()` 已解析的提交字段构造 tooltip；TypeScript 编译与 IDE diagnostics 通过。
- 修正 commit 展开布局：展开时原摘要子项固定保留在 26px 第一行且对齐方式不变，详细描述作为带顶部分隔线的独立第二行追加；分支图单独跨两行延伸。TypeScript 编译与 IDE diagnostics 通过。
- 展开详情与 hover tooltip 共用规范化后的标题正文文本：去除首尾空白，并把三个及以上连续换行（含空白字符行）压缩为两个换行，即最多保留一个空行。TypeScript 编译与 IDE diagnostics 通过。
- 展开详情的正文与元数据之间不再使用空行，改为与 hover tooltip 相同的 `────────────────` 分隔符；其余元数据顺序保持不变。TypeScript 编译与 IDE diagnostics 通过。
- 展开详情元数据顺序调整为 `Author` 后紧接 `Date`，之后依次为 `Commit`、`Parents`、`Committer`；hover 已是作者后接时间，无需修改。TypeScript 编译与 IDE diagnostics 通过。
- Changed Files 选中 `Uncommitted Changes` 时，在显示方式左侧显示蓝色圆角 Commit 分割按钮；右侧子按钮切换 `Commit`/`Commit (Amend)`，主按钮通过内置 `vscode.git` API 定位准确 Repository 后调用 `git.commit`/`git.commitAmend` 打开 VS Code `COMMIT_EDITMSG` 标签页。意图经 Store effect 接通；TypeScript 编译、IDE diagnostics、diff check 通过。
- 修复 Commit 按钮误报 `There are no changes to commit`：Gitk 的 Stage 用 CLI 直接改真实 index，而内置 `git.commit` 命令先读可能尚未同步的 SCM `indexGroup` 并提前判空。改为对目标公开 Repository 执行 `status()` 同步后，直接调用 `repository.commit('', { amend, useEditor: true })`，由真实 index 启动 Git/VS Code 原生 `COMMIT_EDITMSG`，不再经过 `smartCommit` 缓存预检。编译、IDE diagnostics、diff check 通过。
- Commit 打开流程按目标 Repository 的真实状态分流：`status()` 后若 `indexChanges` 非空，直接打开 `COMMIT_EDITMSG` 且不暂存任何 unstaged；若 staged 为空但存在 `workingTreeChanges`/`untrackedChanges`，弹出“是否暂存全部未暂存更改”，仅选择“是”才通过 `repository.add()` 暂存这些路径后打开编辑器，选择“否”或关闭均取消；两类都为空则提示没有可提交更改。编译、IDE diagnostics、diff check 通过。
- 修复 Commit 工具组分隔线：此前 Commit 组不属于 `.action-group`，右侧无法命中相邻组分隔规则；同时历史操作只隐藏子按钮，空父组伪元素形成错误左线。现 Commit 组加入 `.action-group`，历史提交父组在 uncommitted 模式整体隐藏，正确显示 Commit 与显示方式之间的右侧分隔线。Commit 流程加入 Notification 不定进度，按真实 `status`、等待确认、`add`、再次 `status`、打开 `COMMIT_EDITMSG` 阶段报告。编译、IDE diagnostics、diff check 通过。
- Commit 状态检查轻量化：移除最多两次会触发 VS Code SCM 全量扫描的 `repository.status()`，改为单次 `git --no-optional-locks status --porcelain=v1 -z --untracked-files=normal --ignore-submodules=dirty --no-renames`，只解析 staged/unstaged 两个布尔值；确认暂存全部后执行目标仓库 `git add -A -- .`，无需再次刷新即从真实 index 打开编辑器。通知首阶段改为“正在检查更改...”。编译、IDE diagnostics、diff check 通过。
- 修复 Commit Editor 已打开但 Notification 一直不结束：公开 `repository.commit('', { useEditor: true })` 的 Promise 生命周期覆盖整个编辑会话，会等待用户保存/关闭编辑器并完成提交，不能作为“打开标签页”进度的完成条件。现启动该 Promise 后立即结束 `withProgress`，并为后台 Promise 保留失败提示；通知在编辑器启动后消失。编译、IDE diagnostics、diff check 通过。
- Commit 通知最终交互调整：保持单一进度流程，标题精简为普通提交“创建提交”、Amend“修改提交”，等待阶段文案为“COMMIT_EDITMSG：等待完成或取消提交...”。失败仍为带 `$(warning)` 图标且 3 秒自动移除的状态栏消息。编译、IDE diagnostics、diff check 通过。
- 已确认公开 `vscode.git` API 只能通过 `repository.commit(..., { useEditor: true })` 发起编辑器提交，并返回整个 Commit 完成 Promise；没有本次操作专属的 editor-open 事件、URI 或 token。通用 tab/editor 事件只能观察同名文件，无法可靠归因到本次调用，因此不用于宣称编辑器已真正打开。
- Commit 防重复交互：Store 新增 `commitEditorLoading` 并在接收 `openCommitEditor` 意图时原子置 true、busy 时拒绝后续请求；Provider 通过 `runOpenCommitEditor()` 在真实 Promise 的 finally 中复位。Webview 点击时同步禁用主按钮与切换子按钮以封住消息往返窗口，Store 快照持续驱动 disabled/aria-busy/loading；主按钮显示旋转 loading，完成、取消或失败后恢复。编译、IDE diagnostics、diff check 通过。
