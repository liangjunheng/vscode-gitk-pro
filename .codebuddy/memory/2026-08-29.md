# 2026-08-29

- 子模块 Diff 正文已改为单栏静态提交列表：`MultiDiffPanel` 对 `isGitlink` 卡片跳过 Monaco `DiffEditor`，直接渲染 `gitlinkRangeCommits`，每个提交一行含短 SHA 与完整消息；范围为空时显示新端点。子模块范围查询移除 `--reverse`，按 Git 默认新到旧排序，最新提交位于第一行。保留 index 元信息行且移除其重复的 `Submodule commit` 行。`npm run compile` 通过。
- 已创建中文提交 `0fbf62c`：`支持子模块提交变更展示`，包含子模块 gitlink 识别、提交范围读取及单栏展示改动（2026-08-29）。
- 已将 MultiDiffPanel 的 `Submodule` 标签移动到标题折叠箭头之后、状态图标之前；相关文件无诊断错误（2026-08-29）。
- 子模块 index 上方元信息缺失原因确认：此前按用户“移除 index 栏的 submodule commit”要求，已删除 `metaHtml()` 中 gitlink 的两条 `Submodule commit <SHA>: <subject>` 元信息渲染；`index <old>..<new> 160000` 本身仍由通用逻辑保留。并非单栏正文改造导致丢失（2026-08-29）。
- 已恢复 `metaHtml()` 的 gitlink 元信息：在 index 行上方重新显示旧、新端点的 `Submodule commit <SHA>: <subject>` 两行；新增或删除条目只显示存在的一侧（2026-08-29）。
- 子模块第二端点描述缺失的当前判定：`metaHtml()` 只显示 `GitlinkCommit.subject`，该值由子模块内 `git show -s --format=%B` 的完整消息取首个非空行；该字段为空时仍展示 SHA 而不加冒号描述。若同一命令已成功返回另一端点描述，则不能归因于整体子模块未初始化或命令失败，需检查该 SHA 的实际 `%B` 消息是否为空（2026-08-29）。
- 新端点描述不应额外发起 Git：快进变更已经运行 `git log old..new` 并把新端点包含在 `gitlinkRangeCommits`（新到旧时首项）。应复用该列表按 `newObjectId` 回填 `newGitlinkCommit`，无需再为新 SHA 单独运行 `git show`；旧端点仍由现有批量 `git show` 读取（2026-08-29）。
- 已将 CommitEditor 卡片路径字号缩小至默认编辑器字号的 90%；移除 Commit 右侧切换按钮，改为 Submodule、Amend 复选框与 Commit 按钮同组。Submodule 依据真实变更文件的 `160000` gitlink 状态可选，无子模块变更时强制取消并置灰；Amend 始终可勾选，勾选后按钮显示 `Amend`；Commit 保持高亮且仅在提交进行中禁用。空变更点击提交时显示“变更文件为空，无需提交”，未执行提交。`npm run compile` 通过（2026-08-29）。
- 已移除 CommitEditor 的 Submodule 复选框、快照字段、消息参数和 `--recurse-submodules` 提交参数；在 Commit 右侧新增 Push 按钮，复用现有 `gitSync: push` 链路。`npm run compile` 通过且相关文件无诊断错误（2026-08-29）。
- 已创建带中文正文的提交 `ae8ec61`：`完善子模块提交展示`；正文说明范围数据复用、端点描述一致性、DiffPanel 标签位置及端点摘要恢复（2026-08-29）。
- CommitEditor 底部操作区已优化为两组：`Amend + Commit` 为提交组，`Submodule selector + Push` 为子模块推送组；增加边框、背景、间距和自动换行，改善窄宽度下布局。`npm run compile` 及 `commitPanel.ts` lint 通过（2026-08-29）。
- 已修正 CommitEditor 三栏：顺序为 `Unstaged Changes`、`Staged Changes`、`Committed Changes`；三栏标题均支持独立折叠，默认展开，折叠图标随状态同步。`npm run compile` 和 `commitPanel.ts` lint 通过（2026-08-29）。
- 已实现 Push 依赖顺序：CommitPanel 将当前仓库加入 Push 路径集合；Provider 使用 `getRepositoryAncestry()` 的深度去重排序，按最深子模块→父模块→当前仓库顺序串行执行现有 `syncRepository('push')`。相关 lint 无诊断，`npm run compile` 通过（2026-08-29）。
- CommitPanel 因 changes 排序/子模块选择状态重构再次空白：`render()` 仍引用已删除的 `selectedSubmodules`，但当前状态已改为 `selectedSubmodulesByParent: Map`，首次渲染触发 `ReferenceError`。已改为遍历 Map 中各父仓库的 Set 清理失效选择项；`commitPanel.ts` lint 通过，`npm run compile` 通过（2026-08-29）。
- `MultiDiffPanel` 的 gitlink 正文已具备 `gitlinkScanPending` 分支显示“正在扫描子模块提交…”，并在扫描完成后由 Provider 刷新。当前这条需求要求进一步增加可视 loading 状态（例如转圈/进度视觉），不能只依赖静态文案（2026-08-29）。
- 子模块下拉弹窗导致 CommitPanel 偶现空白的根因是弹窗改造遗留 `dialogList` 未定义，动态脚本在有 staged 子模块时抛 `ReferenceError`；已改为 `dropdownList`，并清理失效 backdrop 选择器，补充中文注释说明 DOM 结构、变量唯一名称、currentCards 依赖和 detached 卡片挂载顺序。`npm run compile` 和 `commitPanel.ts` lint 通过（2026-08-29）。
- DiffPanel 扫描期间显示“暂无变更文件”的根因：`MultiDiffPanel.publish()` 的 `loading` 只绑定 Store 的 `diffLoading`，没有绑定 `RepoSubmoduleWatcher.isLoading`；子模块扫描是独立异步流程。扫描时 Provider 可能发布 `diffs=[]` 且 `loading=false` 的快照，Webview `render()` 在第 850 行优先处理空列表并直接显示“暂无变更文件”，不会进入后续 `snapshot.loading` 分支。`gitlinkScanPending` 只存在于已构造的 gitlink payload，无法覆盖空快照阶段。正确修复应把子模块扫描状态纳入 DiffPanel 快照/发布状态，并让扫描结束后的状态变化触发重发布，不能靠延时或空列表兜底（2026-08-29）。
- CommitPanel 执行暂存/还原/取消暂存后跳到或停留在 DiffPanel 的根因：stage/unstage/discard 本身只执行 Git 操作并刷新共享工作区状态，不直接调用 `openDiff`；但 CommitPanel 文件点击链 `selectFile -> openCommitPanelWorkingTreeDiff -> openDiff` 会先打开 DiffPanel，后续刷新只更新 DiffPanel 数据，没有 `CommitPanel.show/focus` 或活动视图恢复逻辑。因此操作后仍停留 DiffPanel 是当前焦点所有权设计导致，并非操作消息误调用 DiffPanel（2026-08-29）。
- 无子模块仓库时，CommitPanel 现在隐藏整个 `.submodule-group`，因此 `Submodule` 选择器和 `Push` 按钮都不显示；有子模块仓库时保留选择器及无变更提示。`commitPanel.ts` lint 通过，`npm run compile` 通过（2026-08-29）。
- 子模块按钮偶现不显示已从根因优化：`buildCommitSnapshot()` 现在按路径从 `repoController.totalRepoList` 读取最新权威仓库拓扑，再使用 changes Store 的 staged/unstaged 数据；不再直接信任可能过期的 `repo.repository`。这样异步扫描更新后，`repositoryHasSubmodules` 与父子关系不会被旧 changes 快照覆盖。`gitkViewProvider.ts` lint 与 `npm run compile` 通过（2026-08-29）。
- Submodule 选择器已改为弹窗：Push 左侧只显示 `Submodule` 按钮，点击打开弹窗；弹窗提供全选、单选、半选状态、完成关闭和“暂无子模块变更”提示。选择状态继续按父仓库隔离，保留嵌套子模块关联和提交信息同步；无子模块仓库继续隐藏整个操作组。`npm run compile` 及 `commitPanel.ts` lint 通过（2026-08-29）。
- 偶现 Submodule 按钮不显示的根因：`buildCommitSnapshot()` 直接读取 `store.commitRepositories` 中可能过期的 `repo.repository.hasSubmodules`，而子模块扫描/未提交文件刷新会并发写入中间仓库快照；按钮由 `repositoryHasSubmodules` 直接控制，旧快照覆盖新拓扑后被隐藏。关闭重开重新同步仓库列表所以恢复。修复应使用仓库控制器当前拓扑按路径统一重建/合并仓库对象，避免异步快照覆盖权威 `hasSubmodules`（2026-08-29）。
- 子模块选择器已按分支选择器 UI 改为锚定下拉菜单：VS Code menu/dropdown 主题、Chevron、筛选输入、全选/半选、确定/取消、外部点击和 Escape 回滚；选择仅在确定后写入已确认集合，Push 只读取已确认选择，并保留嵌套父子关联和提交信息同步。`npm run compile` 与 `commitPanel.ts` lint 通过（2026-08-29）。
- Submodule 下拉菜单显示异常修复：下拉菜单被 `.card` 和 `.submodule-group` 的 `overflow:hidden` 裁剪，导致视觉上像编辑框；已改为 `overflow:visible`，并修正按钮仅更新内部标签以保留下拉箭头。补充了重建前释放 document 级监听器的清理逻辑，避免每次快照重复注册。`npm run compile` 和 `commitPanel.ts` lint 通过（2026-08-29）。
- Submodule 选择器前显示“附带子模块：”，未选择摘要显示“无”，选中后显示对应子模块名称；按钮设置 `min-width: 7em`。Commit 与 Push/Submodule 两组之间增加垂直分割线和间距；History 图标按钮使用与 Commit 按钮相同字体基准和计算高度；`commitPanel.ts` lint 无诊断（2026-08-29）。
- CommitPanel staged/unstaged 不同步的根因修复：主列表工作区行直接读取 `UncommittedFilesWatcher` 的 HEAD 缓存，而 `syncCommitRepositories()` 曾对当前仓库改读 `store.stagedFiles/unstagedFiles`（仅当前选中 changes/staged 虚拟行的 UI 投影，选普通提交时为空），从而覆盖正确快照。现统一所有仓库直接消费 watcher 缓存；打开前仍先 sync。`gitkViewProvider.ts` lint 和 `npm run compile` 通过（2026-08-29）。
- CommitPanel 又空白的根因是内嵌 Webview 脚本位于外层 TypeScript 模板字符串中，`updateSummary()` 的 `join('\\n')` 被提前解释成真实换行，生成非法 JavaScript，导致 `ready` 不发送、宿主不发布 snapshot；已改成保留反斜杠 `join('\\\\n')`，并加注释防止回归。`commitPanel.ts` lint 及 `npm run compile` 通过（2026-08-29）。
- 已创建中文提交 `d14ba8d`：`优化提交面板子模块交互`，包含 CommitPanel 工作区数据源统一、子模块下拉选择、嵌套关联、推送顺序和布局优化（2026-08-29）。
- CommitPanel 撤回父仓库 gitlink 时，若检测到嵌套子模块会显示模态选择：“递归撤销”或“仅撤销直接子模块”；取消则不操作。范围确定后先恢复父仓库 gitlink，再按选定仓库逐个执行 `git restore --staged --worktree -- .`，避免 `--recurse-submodules` 越过用户选择；最后刷新父仓库及实际受影响子模块。未跟踪文件仍保留。`npm run compile` 和相关 lint 通过（2026-08-29）。
- Commit 与 Push 两套 Submodule 功能已彻底独立：Commit 消息携带 `selectedCommitSubmodulesByParent` 路径集合，Provider 按深度从内到外串行提交子模块与当前仓库；每个子模块提交后，在已选择的直接父仓库执行 `git add <gitlink>`，父仓库最后提交，只有当前仓库使用 Amend。Push 仍使用 `selectedPushSubmodulesByParent` 和原有深度排序，仅负责推送。Commit 候选来自 staged/unstaged，Push 候选来自最新 HEAD gitlink。Commit/Push 按钮通过 `margin-left:auto` 固定在各自行右侧；Push 更新逻辑限定查询 `.submodule-group .submodule-selector`，不再错误覆盖 Commit selector，Push 子模块控件恢复显示。`npm run compile` 和相关 lint 通过（2026-08-29）。
- CommitPanel 再次空白根因是新增 `pushedDescendants()` 的路径正则位于外层 HTML 模板字符串中，`/^[/\\]+/` 输出到 Webview 后成为非法正则；已改为双重转义 `^[/\\\\]+` 并加中文注释。脚本可正常发送 ready，`npm run compile` 和 lint 通过（2026-08-29）。
- 已移除卡片左下角“变更文件为空，无需提交”提示；空状态文案改到对应列表中：Staged 显示“没有已暂存的更改”、Unstaged 显示“没有未暂存的更改”、Committed 显示“没有已提交的更改”。无 staged/unstaged 的仓库卡片首次为空或由非空转为空时默认折叠；持续为空时保留用户手动展开状态，有变更时自动展开。`npm run compile` 和 lint 通过（2026-08-29）。
- CommitEditor 布局已调整：Commit 按钮位于左侧，Amend 紧随其右，History 移入提交信息输入框右下角且通过右侧内边距避免遮挡；textarea 使用 `scrollHeight` 在输入和程序回填时自动增高/缩小，禁用手动 resize。`commitPanel.ts` lint 与 `npm run compile` 通过（2026-08-29）。
- Commit/Push 子模块控件已改为各自操作组内联的圆角线框多行复选框：Commit 标题“自动同步当前提交信息”，Push 标题“附带推送子模块”；两者分别使用独立选择状态，Commit 实时同步提交信息，Push 仅控制推送。无候选时显示“暂无子模块变更”，并保留嵌套子模块路径展示。`npm run compile` 和 `commitPanel.ts` lint 通过（2026-08-29）。
- 用户确认继续采用上述 Commit/Push 内联子模块控件方案；当前工作区修改已完成编译验证，相关文件无 lint 诊断（2026-08-29）。
- 根据用户截图定位到 UI 未符合需求的根因：操作组仍保留“附带子模块：”旧前缀，且 `.action-groups` 采用纵向布局，导致选择器未按要求占据按钮右侧空间。已删除旧前缀并调整操作组内联布局、控件伸展和垂直对齐；`npm run compile` 通过。（2026-08-29）
- 用户反馈修改后 Submodule 控件直接消失。代码链路表明显隐由 `repositoryHasSubmodules` 严格控制：Commit 选择器设置 `hidden`，Push 组直接设置 `display:none`；该字段来自扫描器发现的子仓库拓扑，而不是 staged gitlink/子模块变更本身。另有父仓库无自身改动时卡片自动折叠、隐藏整个 card body 的独立路径。需优先拆分控件显隐与拓扑扫描状态，避免把扫描中间态/父仓库空状态误判为无子模块。（2026-08-29）
- 提交编辑框已调整为默认至少三行（`rows=3` 与 `min-height`），History 按钮已从操作组移入 `.message-box` 内部右下角，并通过底部内边距预留内容空间，避免覆盖提交文字；编译及 lint 均通过。（2026-08-29）
- Commit 子模块候选已改为仅使用 staged changes，排除 unstaged changes；Push 候选改为同时满足：子仓库当前 HEAD 相对 upstream 存在 ahead 提交，且父仓库当前 HEAD 的树中实际包含对应 gitlink，嵌套路径递归展示为“子模块/子模块的子模块”。新增 `getGitAheadCount` 与 `getGitlinkPathsInCommit`，编译及 lint 通过。（2026-08-29）
- 针对截图反馈，修复三项根因：工作区状态读取改用完整 raw diff 元数据，使 staged/unstaged gitlink 保留 mode 与 SHA；CommitPanel 不再以 `repositoryHasSubmodules` 隐藏控件，而是始终渲染内联选择框；文件行在状态与路径之间显示 `Submodule` 标签；工作区 staged/unstaged Diff 加载前补充 gitlink SHA、描述及范围提交历史。`npm run compile` 与相关 lint 通过。（2026-08-29）
- 再次根据截图核查发现，源码中的内联选择器虽会渲染，却被错误放在操作组并因 flex `order:3` 与按钮 `margin-left:auto` 的组合挤到不可见区域。已保留需求指定的“Commit/Amend 左侧、Submodule 右侧”和“Push 左侧、Submodule 右侧”DOM，修复 Push 查询路径并重新编译；`commitPanel.ts` lint 通过。（2026-08-29）