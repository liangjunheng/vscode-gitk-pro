
- 恢复 Change Files 工具栏布局：移除隐藏 `.commit-history-action-group` 与 Hash 复制按钮的 CSS，恢复 `#filesActions` 的 flex 排版；此前移除的文件路径右键菜单功能仍保持移除，因为用户要求还原的是布局而非已明确移除的功能。
- 提交列表右键菜单的“展开描述/关闭描述”新增动态箭头图标：根据提交行 `.expanded` 状态切换箭头方向；右键菜单其余 6 个操作图标保持不变。`gitkViewProvider.ts` lint 通过，`npm run compile` 通过。
- 提交列表右键菜单中的 `创建 branch` 改为 `create branch`，`创建 tag` 改为 `create tag`；功能行为和图标不变。
- 提交列表右键菜单定位：菜单节点在脚本初始化时 `document.body.appendChild` 提升为 body 直接子节点，避免因面板布局层级影响 `position: fixed` 的包含块。定位改为智能翻转：右/下方空间不足时分别向左/向上翻转，使菜单完整落在视口内（`top` 不再固定用 clientY）；并给菜单加 `max-height: calc(100vh - 8px)` 与 `overflow-y:auto`，防止面板极矮时仍被截断。`gitkViewProvider.ts` lint 通过，`npm run compile` 通过。
- 采用 C 方案（原生 popover）根治右键菜单被父容器裁剪：`engines.vscode` 与 `@types/vscode` 从 `^1.80.0` 抬到 `^1.94.0`（popover 需 Chromium 114+，VS Code 1.94 起 Electron 30/Chromium 124 稳定支持）。`#commitContextMenu` 加 `popover="manual"`，CSS 用 `:not(:popover-open){display:none}` 且清零 UA margin；显隐改用 `showPopover()/hidePopover()`，外部点击与 Esc 关闭；contextmenu 里先 `showPopover()` 再按真实尺寸做视口翻转定位。popover 进入浏览器 top-layer，天然不受面板 overflow 裁剪。移除了之前的 body.appendChild。`npm run compile`、lint 通过。
