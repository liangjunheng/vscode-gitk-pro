# 2026-08-30

- CommitPanel 的 Push 组新增默认勾选的 Pull 选项；点击 Push 后使用 VS Code 原生 QuickPick 选择本地、已配置 upstream 的目标分支。分支候选读取工作区状态、upstream 和最近未推送提交，固定为三次并行只读 Git 查询，避免按分支循环运行 Git/reflog。候选按当前分支、最近未推送提交时间、分支名排序。确认后按子模块深度从深到浅执行；勾选 Pull 时先 `git pull`，根仓库最后向所选 upstream 显式推送目标分支。`npm run compile` 与相关 lint 通过（2026-08-30）。
- 用户再次反馈 Commit/Push 子模块控件不可见。根因是 `.commit-btn,.push-btn` 的 `margin-left:auto` 在同一 flex 行抢占了右侧剩余空间，而选择器又允许压缩，最终控件宽度为零。已移除自动外边距，控件设为最小 180px、可伸展的右侧 flex 项，并明确 Commit/Amend/Push/Pull 的顺序；编译与 lint 通过（2026-08-30）。
- 用户截图仍显示控件为空，进一步定位真实根因：`updateInlineSubmoduleSelector` 创建 `.submodule-inline-box` 后没有执行 `selector.appendChild(box)`，因此标题和复选框从未进入 DOM，布局调整无法解决。已补上挂载语句；`npm run compile` 与 `commitPanel.ts` lint 通过（2026-08-30）。
- 子模块内联控件视觉重排：去除填充背景，改为圆角虚线轮廓；标题与选项改为同行分栏并可换行，标题文案分别为“同时提交子模块”和“同时推送子模块”。编译与 lint 通过（2026-08-30）。
- CommitEditor 的 gitlink `Submodule` 标签已移到文件行最前端（状态标识之前），并复用 DiffPanel 的边距、边框、圆角、字号与 VS Code badge 主题变量，保持两处视觉一致；编译与 lint 通过（2026-08-30）。
- CommitEditor 的 gitlink 标签文案改为 `Repo`，视觉改为绿色半透明背景、绿色描边和胶囊圆角，突出其仓库指针语义；编译与 lint 通过（2026-08-30）。
- Commit 子模块候选的根因修复：此前错误要求“子模块自身仍有 staged 文件”，但用户已将子模块 commit 指针 staged 在父仓库时，子模块工作区应当干净。现改为从每层父仓库的 staged gitlink 路径匹配直接子模块，并递归匹配嵌套 gitlink；选择状态同步使用同一候选来源。编译与 lint 通过（2026-08-30）。
- 修复 Commit 无响应与状态加载慢：`runCommit` 原先从异步 Store 读取 staged/unstaged，CommitPanel 已显示变更但 Store 快照滞后时会直接跳过所有仓库并仅提示“无需提交”；现改用 CommitPanel 同源的 `buildCommitSnapshot()` 判断。性能回归来自将 watcher 的 `getWorkingTreeStatus` 改为全量 `status + diff --cached --raw + diff --raw` 三命令；已恢复 watcher 使用轻量 status 查询，完整 raw 元数据仅按需用于 Diff。编译与 lint 通过（2026-08-30）。
- 用户要求必须根因修复后进一步确认：`buildCommitSnapshot()` 也不是提交状态的权威源，它会受 CommitPanel 可见性和异步 UI 更新影响。`runCommit` 已改为直接 await `UncommittedFilesWatcher.getUncommittedFilesByHeadBranch()`，由 watcher 缓存/刷新链路作为唯一状态源，再决定每个仓库是否提交。编译与 lint 通过（2026-08-30）。
- CommitPanel 再次空白的根因：内嵌 Webview 模板 `stagedGitlinkDescendants` 的路径正则只使用两层反斜杠，模板求值后生成 `/^[/\]+/` 的无效正则，脚本解析阶段中断，`ready` 未发出，宿主不会发布 snapshot。已改为四层反斜杠；`npm run compile` 和 `commitPanel.ts` lint 通过（2026-08-30）。
- 仓库切换变慢的根因：每次 `refreshCommitPanel()` 都遍历全部仓库，并为每个仓库无条件异步运行 `getGitAheadCount` 与 `getGitlinkPathsInCommit`；每个异步完成又递归触发 refresh，导致同一 HEAD 的 Git 查询呈倍增。现仅对有子模块拓扑的仓库读取 Push 数据，并用 `repositoryPath\0HEAD` 作为 loaded/loading 键去重缓存；完整最新提交文件不再在打开/刷新 CommitPanel 时预读。编译与 lint 通过（2026-08-30）。
- 当前仓库 staged/unstaged 延迟显示的根因：`UncommittedFilesWatcher` 在首次 cache 刷新时，先串行运行无旧快照可比较的 `git diff --cached --name-only`，再执行实际产出列表的 `git status`，导致 CommitPanel 只能先显示空快照。现首次刷新直接执行 `git status`；仅已有 previous cache 的 index/full refresh 才读取 index diff。编译与 lint 通过（2026-08-30）。
- 技术债清理：`getPushBranches` 以前将 `%(upstream:short)`（如 `origin/main`）错误传入 `git push` 的 remote 参数，并丢弃含 `/` 的本地分支。现使用 Git 格式字段分别读取 `upstream:remotename` 与 `upstream:remoteref`，保留 feature 分支并用正确的 remote/refspec 推送；Push 成功后按仓库使 ahead/gitlink 状态缓存失效。编译与 lint 通过（2026-08-30）。
- Amend 的 Committed Changes 加载慢根因：一次勾选会先全量刷新 CommitPanel，再在 `loadAmendCommittedFiles` 标记 loading 后再次全量刷新；实际 `getCommitFiles` 还带 `-C` copy detection。已合并进入 Amend 的两次刷新、删除无调用方的最新提交文件预读/缓存，并移除提交文件清单不需要的 `-C`（保留 rename `-M`）；编译与 lint 通过（2026-08-30）。
- 撤销/暂存/取消暂存变慢根因：一次 Git mutation 同时触发显式路径刷新、index watcher、`**/*` workspace watcher；在途刷新会被 `abort` 后重启，导致 `git status`/`git diff --cached` 重复执行。已在 `UncommittedFilesWatcher` 增加 begin/end 工作区操作事务：事务期间 watcher 只合并事件路径、不刷新，事务结束后根仓库路径刷新一次，递归撤销的子模块全量刷新一次。编译与 lint 通过（2026-08-30）。
- 按用户要求移除非必要工作区性能优化：删除 `GitkViewProvider` 的 staged/unstaged 跨视图 Diff 缓存、独立 `prewarmDiffReader` 后台预读及相关失效/代次逻辑；删除 `MultiDiffPanel`↔`UncommittedFilesWatcher` 的可视卡片路径白名单与专用 Diff 快通道。工作区 Diff 现只按用户当前选择读取，所有文件内容变化统一进入 watcher 的状态/内容刷新链路。保留刷新队列、事务合并、generation 门禁、index/工作区监听等正确性机制。编译与 lint 通过（2026-08-30）。
- 工作区代码整理：删除 CommitPanel 的未消费 `repositoryIsSubmodule` 字段、`hasRepository`、无发送方的 close 回调链；删除 watcher 未使用的文件事件类型参数；删除 `gitLogProvider` 中无调用的 `awaitWithAbort`、`checkWorkingTreePresence`/`spawn` 及已被轻量 watcher 读取替代的全量 working-tree raw-diff 元数据链。保留子模块、Push/Pull 和事务刷新功能；编译与 lint 通过（2026-08-30）。
- Commit 组 staged gitlink 未显示的根因：Webview 以前从 URI 字符串截取父子仓库相对路径，再同 raw diff 的 Git 相对路径比较；仓库路径编码/分隔符与 Git 路径不属于同一规范化层，匹配失败即丢失候选。现由宿主 `RepoSubmoduleWatcher.findSubmoduleRepository` 使用 fsPath 标准化与父子拓扑解析 staged gitlink，快照传递标准子仓库路径，Webview 仅按路径集合筛选。编译与 lint 通过（2026-08-30）。
- 普通仓库的 CommitPanel 不再显示子模块控件：Commit selector 与 Push group 均由 `repositoryHasSubmodules` 的已扫描拓扑控制；无子模块仓库不显示空选择框或 Push 行，有子模块仓库才渲染对应控件。编译与 lint 通过（2026-08-30）。
- 所有 gitlink 展示标签统一为 `Repo`：DiffPanel、Changed Files 树状视图及平铺视图均替换原 `Submodule` 文案，避免同一对象跨面板语义不一致；编译与 lint 通过（2026-08-30）。
- 虚拟提交（staged/unstaged）gitlink 显示“无法读取此文件”的根因：整理时只保留 `git status --porcelain`，其不含 mode `160000` 与 old/new object id，导致 gitlink 被当作普通目录传给 DiffReader。现工作区状态并行读取 porcelain、`HEAD→index` raw diff、`index→worktree` raw diff，并按路径合并 mode/SHA/isGitlink；现有 gitlink 提交历史读取和静态 Diff 分支可正确生效。编译与 lint 通过（2026-08-30）。
- CommitEditor 的 Repo 标签右侧外边距从 6px 调整为 3px，缩小一半；编译与 lint 通过（2026-08-30）。
- 移除可视 Diff 快通道时遗漏虚拟化渲染函数中的 `Promise.all(mounts).then(publishVisibleDiffPaths)` 调用；函数定义已删除，导致 Diff 渲染抛出 `publishVisibleDiffPaths is not defined`。已改为仅等待挂载 Promise，重新编译产物确认无残留调用，lint 通过（2026-08-30）。
- Commit 组的 Amend 复选框现与 Push 组的 Pull 一致，位于对应操作按钮之前；通过操作组选项顺序实现，编译与 lint 通过（2026-08-30）。
- 子模块控件现只在存在实际候选时显示：Commit 依据 staged gitlink 解析出的子模块候选，Push 依据当前提交包含且未推送的子模块候选；没有候选时隐藏控件，不显示“暂无子模块变更”空框。编译与 lint 通过（2026-08-30）。
- Commit 子模块控件只在普通 Commit 模式显示；勾选 Amend 后即使 staged gitlink 存在也隐藏，避免将普通提交时的子模块同步操作错误暴露到 Amend 流程。编译与 lint 通过（2026-08-30）。
- Commit/Push 子模块选择项现显示其相对嵌套路径以及 `Staged N，Unstaged N` 数量，便于直接识别子模块的工作区状态；编译与 lint 通过（2026-08-30）。
- Submodule 控件中每个子模块选项改为独立小卡片：圆角边框、轻量背景、hover/选中状态和紧凑内边距，保持多行换行布局；编译与 lint 通过（2026-08-30）。
- CommitEditor 的 Repo 标签右侧外边距从 3px 再缩小一半至 1.5px；编译与 lint 通过（2026-08-30）。
- Commit 子模块控件候选改为：父仓库 staged 或 unstaged 任一侧存在 gitlink 变更，且目标子模块自身 staged 和 unstaged 文件数量均大于 0。宿主合并两侧 gitlink 并解析标准子仓库路径，Webview 使用同一候选规则渲染和清理选择状态；编译与 lint 通过（2026-08-30）。
- 用户更正 Commit 子模块数量条件：子模块 staged 与 unstaged 数量仅在两者都为 0 时隐藏；任一侧有变更即显示。编译与 lint 通过（2026-08-30）。
- Commit 组布局调整为 Commit 按钮在前、Amend 复选框位于其右侧，子模块控件仍占据后续剩余空间；编译与 lint 通过（2026-08-30）。
- 虚拟提交 gitlink 列表不一致的根因修复：unstaged `git diff --raw` 的新端 OID 可能是全零工作区占位，导致提交 subject/range 被过滤，DiffPanel 无可显示列表；现由 `readGitlinkCommitSubjects` 在 unstaged 且新端无真实 OID 时读取子模块 `HEAD` 回填，再以真实两端 SHA 读取 `old..new` 提交范围。范围查询也改为只接受非零 OID；编译与 lint 通过（2026-08-30）。
- CommitPanel 卡片交互状态数据驱动化：提交消息、Commit/Push 子模块勾选和 Pull 选项从 Webview 本地 Map/DOM 迁入 `GitkViewProvider` 的每仓库状态，并随 `CommitCard` 快照下发；Webview 仅回传状态补丁并按快照重渲染，提交/推送也读取卡片快照，避免刷新或重建时 UI 业务状态漂移。`npm run compile` 与相关 lint 通过（2026-08-30）。
- Commit 子模块选项中的 `Staged`/`Unstaged` 数量改为复用卡片标题的 `repository-status-badge` 视觉；名称不再内嵌数量，并改成链接按钮，点击后经 `focusRepository` 定位并聚焦对应仓库卡片。`npm run compile` 与 `commitPanel.ts` lint 通过（2026-08-30）。
- Push 按钮右侧现由 `CommitCard.pushTargetLabel` 显示将要推送的 upstream（如 `origin/main`）。CommitPanel 打开时仅对具子模块拓扑、会显示 Push 组的仓库读取 `getPushBranches()` 首项作为默认最近目标；点击 Push 后仍弹出原生 QuickPick，选定分支写回 Provider 权威状态并立即更新按钮，再按同一分支执行推送。`npm run compile` 与相关 lint 通过（2026-08-30）。
- Push 子模块选项的右侧新增由 `pushTargetLabel` 驱动的远程分支按钮，左侧以边框分隔；点击该分支仅为对应子模块打开原生 upstream 分支选择器。选择写入同一 `pushBranchByRepository` 权威状态，子模块实际 Push 显式使用所选 remote/refspec，避免显示与执行不一致。`npm run compile` 与相关 lint 通过（2026-08-30）。
- 根仓库 Push 按钮内的远程分支标签同样增加左侧分割线与内边距，视觉上明确区分 `Push` 动作和目标 upstream；`npm run compile` 与 `commitPanel.ts` lint 通过（2026-08-30）。
- Commit 按钮改用 VS Code `button-secondary` 背景/前景/hover 主题色，Push 保持主按钮主题色，明确区分两类操作层级；`npm run compile` 与 `commitPanel.ts` lint 通过（2026-08-30）。
- Commit 按钮背景丢失的根因是部分 VS Code 主题未定义 `button-secondary*` CSS 变量，使用无 fallback 的 `var()` 会令整条背景声明无效。现次要按钮背景/前景/hover 分别回退到 editorWidget/editor foreground/list hover 主题色，保持与 Push 主按钮的差异且不透明；`npm run compile` 与 `commitPanel.ts` lint 通过（2026-08-30）。
- 用户确认 Commit 应保留原蓝色圆角主按钮背景。现 `Commit` 使用 `button` 主色，`Push` 使用具回退值的 `button-secondary` 次要色，以同时保留 Commit 视觉和两者差异；`npm run compile` 与 `commitPanel.ts` lint 通过（2026-08-30）。
- Push 按钮右侧目标改为“最近成功 Push”的远程分支，而非最近加载或仅已选择目标：每批 Push 全部成功后按实际 root/submodule remote/refspec 更新 `lastPushedBranchByRepository`，写入 `workspaceState.lastPushedBranches` 并随 `CommitCard.pushTargetLabel` 渲染；重启后恢复记录。未有成功记录时才回退当前默认分支。`npm run compile` 与 `gitkViewProvider.ts` lint 通过（2026-08-30）。
- Push 按钮蓝色背景丢失的根因是为区分 Commit/Push 而新增的 `.push-btn` 次要按钮声明覆盖了 `.commit-btn,.push-btn` 的主按钮背景。现 Push 也显式使用 `button` 主色与 hover 主色，恢复蓝色圆角矩形；`npm run compile` 与 `commitPanel.ts` lint 通过（2026-08-30）。
- CommitPanel Push 组子模块控件新增 `unpushedCommitCount` 快照字段，数据源直接是 `getGitAheadCount()` 的原始 ahead 数量；Push 组选项仅显示 `Commits <数量>` 和远程目标，不显示工作区 Staged/Unstaged。Commit 组保留原有 Staged/Unstaged 徽标逻辑，不复用该 Push 数据。`npm run compile` 与相关 lint 通过（2026-08-30）。
- 原生 Push 远程分支选择器仅在候选存在未推送提交时显示 `未推送：<subject>`；无未推送提交时不再渲染“没有待推送提交”的无关空状态描述。`npm run compile` 与 `gitkViewProvider.ts` lint 通过（2026-08-30）。
- Push 远程分支选择器的 detail 改为 `未推送提交：<数量>`，不再显示单条最近提交标题；数量来自 `getPushBranches()` 已读取的 `recentUnpushedCommits` 列表长度，无数量时不显示 detail。`npm run compile` 与 `gitkViewProvider.ts` lint 通过（2026-08-30）。
- Push 远程分支选择器现始终显示 `未推送提交：<数量>`，包括 `0`，以统一候选项信息结构；`npm run compile` 与 `gitkViewProvider.ts` lint 通过（2026-08-30）。
- 已创建提交 `a02fa6d`（`Improve commit panel submodule workflow`），包含本轮 CommitPanel/Push 子模块交互、gitlink 撤销和相关状态流调整；提交前已通过 `git diff --check` 和 `npm run compile`（2026-08-30）。
- Push 分支选择列表为空的根因是 `getPushBranches()` 原来只读取 `refs/heads` 且强制过滤无 upstream 的本地分支；未设置 upstream 的仓库因此不产生任何候选。现同时读取 `refs/remotes`，以每个本地分支生成已配置 upstream（优先）和其他远程跟踪分支候选，去重后按当前分支、最近未推送提交、远程分支排序；`npm run compile` 与 `gitLogProvider.ts` lint 通过（2026-08-30）。
- 撤销 gitlink 变更此前只执行父仓库 `restore --worktree` 与子模块 `restore --staged --worktree .`，后者不会移动子模块 `HEAD`，导致父 gitlink 已恢复但子模块仍停在错误提交。现撤销前按父仓库索引 `rev-parse :<gitlinkPath>` 读取每层目标 SHA，恢复后对子模块执行 `checkout --detach <SHA>`；普通 Changed Files 与 CommitPanel 两个入口都传递 gitlink/子模块范围，嵌套仓库按父到子的依赖顺序恢复。`npm run compile` 与 `gitkViewProvider.ts` lint 通过（2026-08-30）。
- gitlink 撤销目标读取报 `rev-parse :` 的根因是 `getRepositoryAncestry()` 返回“根仓库到当前子模块”完整链，恢复代码误取 `at(-1)` 作为父仓库，实际得到子模块自身，计算相对路径为空。现取 `at(-2)` 作为直接父仓库，因此 `rev-parse :<gitlinkPath>` 可解析父索引的目标 SHA；`npm run compile` 与 `gitkViewProvider.ts` lint 通过（2026-08-30）。
- 原生 Push 分支选择器以前将本地分支作为所有选项的 `label`，在同一分支可推送多个远程目标时造成开头重复；现两处选择器均以 `upstreamName` 作为主文案，仅展示远程分支名，保留 detail 说明状态。`npm run compile` 与 `gitkViewProvider.ts` lint 通过（2026-08-30）。
- Push 远程分支选择项不再展示仓库级“工作区有未提交更改”（会对同仓库所有候选重复且与目标无关）；detail 仅保留该本地分支的最近未推送提交或“没有待推送提交”。`npm run compile` 与 `gitkViewProvider.ts` lint 通过（2026-08-30）。
