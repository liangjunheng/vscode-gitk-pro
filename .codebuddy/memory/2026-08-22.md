# 2026-08-22

- 确认当前 Diff 正文读取策略：`DiffReader.prepare()` 以 128 个文件为一批；文件数不超过 128 时单批读取，超过时所有批次通过 `Promise.all` 并发。每批完成只更新 `diffProgress`，全部批次完成后按 index 排序并一次性替换 `store.files`。
- 用户观察到 Diff 面板渲染进度为 1、9、17；该数值来自 `MultiDiffPanel.render()` 中所有文件卡片并发 `createCard()` 后按完成回调累加 `ready++`，不是 `DiffReader` 的 128 文件读取批次。多个 Promise 在浏览器一次绘制前连续完成，界面可能只看见间隔约 8 的抽样进度。
- 评估 Monaco 卡片按 128 个分批渲染：技术上可将 `snapshot.diffs` 每 128 个切片并批间串行，但少于 128 个文件时与当前全并发完全相同；每张 Monaco DiffEditor 较重，单批 128 仍可能阻塞 Webview。用户最终要求保持按传递的 `snapshot.diffs` 一次性全部创建；当前 `snapshot.diffs.map(createCard)` + `Promise.all(tasks)` 已符合，无需修改。
- 最终定位“1、9、17、25、33”根因：该序列精确对应 Git 历史 `fc34ecf^` 的旧 `DiffReader.prepare()`——`batchSize=8`，首批 `size=start===0?1:batchSize`，每批更新共享 `diffProgress`。提交 `fc34ecf` 曾改为 128，但用户运行中的 Extension Host/已安装扩展明显仍加载旧构建；当前 workspace 的 `src/git/diffReader.ts` 和 `out/git/diffReader.js` 均为 128，且 ≤128 时只会 0→N。Changed Files 与 MultiDiff 都订阅同一 `diffProgress`，所以旧运行代码会在两侧同步显示相同 1+8n 序列。另有 MultiDiff 多 selector 导致重复完整 publish 的独立问题，但它不能产生精确的 1+8n 读取进度。
- 按用户要求将当前 `DiffReader.prepare()` 改为每批 16 个串行读取。每批开始和 await 返回后均检查 reader/store generation；`stop()` 增加代次并 kill 当前 `git cat-file` 子进程，因此切换提交可打断当前批次且不会启动后续批次。全部完成后仍一次性写入 `store.files`。编译与 lint 通过。
- 16 批次变慢的直接原因：实现同时从“128 批次并发”改成了“16 批次串行”。每批 `readDiffs()` 都新建并等待一个 `git cat-file --batch` 子进程，N 个文件需 ceil(N/16) 次进程启动/仓库读取/输出解析；后批必须等待前批完成。每批更新 diffProgress 还会触发 Store/Webview 发布开销。
- 已按方案一重构当前 `DiffReader`：普通 commit 整轮只启动一个 `git cat-file --batch`，一次写入全部唯一对象请求，stdout 到达时按 header/size 流式解析；按连续完成的每16个文件更新 `diffProgress`，最终一次写入 `store.files`。`stop()` 仍通过 generation + kill 唯一子进程取消。公共 `readDiffs()` 复用同一流式对象读取器但不写 Store。工作树磁盘文件仍由现有 API 并发读取。`npm run compile` 与 lint 通过。
- MultiDiff 虚拟化最终改为接近 VS Code 原生：逻辑项外壳保存 diff/value/collapsed/bodyHeight/左右 selections；只为 viewport 相交项借用 Monaco DiffEditor 模板，离屏解绑 model 并归还对象池，空闲模板最多6个，文件 models/listeners 按项释放。动态 `contentHeight` 写回 bodyHeight，视口上方项高度变化时补偿 window.scrollY；reveal 自动展开并按前序逻辑项缓存高度累计定位。不再固定保留上下各5张。编译与 lint 通过。
- 修复虚拟化后一直显示“正在准备 Diff”：`estimateBodyHeight()` 位于 TypeScript 模板字符串内，源码 `split('\n')` 在生成 HTML 时变成包含真实换行的 JS 字符串字面量，导致整个内联脚本语法错误、`webviewReady` 不发送。改为模板层双重转义 `split('\\n')`，编译与 lint 通过。
- 按用户要求撤销 Changed Files 定位的保活、强制挂载、真实高度后二次对齐、revealToken/mountVersion catch 门禁和重复点击重试改动；当前只保留 `reveal()` 在定位前自动清除目标卡片 collapsed 状态，其他滚动与虚拟化行为恢复原实现。编译与相关 lint 通过。
- 调研 VS Code 原生 MultiDiff：核心在 `src/vs/editor/browser/widget/multiDiffEditor/`。它不是上下各N张 overscan；`render()` 只为与 viewport 相交的 item 借用 `DiffEditorItemTemplate`，其余 hide 并归还池。逻辑 item 保存 `lastTemplateData.contentHeight`（初始500）和 selections，实际高度来自 `onDidContentSizeChange`；模板用 ObjectPool 复用重型 DiffEditor，空闲最多缓存约6个。动态高度通过 contentHeight observable 重算总高度；内部 editor 滚动用 delta 回调修正外层 scrollTop，避免跳动。reveal 按前项累计 contentHeight 定位，导航前自动展开。
- 按用户要求完整撤销零增删 Diff 自动折叠替代方案：删除 updateLineStats 的折叠副作用、zeroDiff 字段，并将 Monaco 配置恢复到用户误还原后的 `revealContextCount` 状态；当前只显示 +0/-0，不做隐藏。编译与 lint 通过。下一步必须先由用户选择方案再修改。
- 按用户要求完整撤销 Monaco 风格零增删全文控件方案及其展开显示修复：删除全部 unchanged widget CSS/DOM/函数、zeroDiff/unchangedExpanded 状态、对象池控件保留、fit 和虚拟化分支；恢复所有可见未折叠项正常挂载 Monaco。当前 +0/-0 不做额外隐藏。编译与 lint 通过。
- 零增删 Diff 后续候选方案：A 调用 Monaco 0.52.2 私有 `collapseAllUnchangedRegions/showAllUnchangedRegions`（视觉最原生但升级风险）；B 自建 Monaco 风格 view-zone 占位控件（稳定可控但状态较多）；C 复用整卡片 collapsed（最小改动但隐藏元信息）；D +0/-0 仅自动缩短为摘要行、点击标题展开（折中）。用户要求先选方案再修改。
- 对用户详细分析了零增删 Diff 的方案一（独立代码区折叠）和方案二（摘要行）：两者都只需修改 `multiDiffPanel.ts`；方案一与现有对象池、动态高度和卡片结构匹配度更高，建议优先实现；方案二空间压缩更好，但首次准确判断、摘要状态和滚动高度变化更复杂。当前仍未实施任何方案。
- 检查当前 `multiDiffPanel.ts` 相对 HEAD (`d2e0a7f`)：仅该文件有 139 行新增、71 行删除，核心从“为全部文件并发创建并永久持有 Monaco”改为“全部轻量卡片外壳 + viewport 相交挂载 + 最多 6 个空闲 DiffEditor 对象池”；并加入 model/监听/选区/编辑值释放恢复、动态正文高度与上方高度变化滚动补偿、基于缓存高度的 reveal 定位。渲染完成通知也从等待全部 Monaco 差异计算改为外壳和首屏挂载启动后立即发送。`git diff --check` 通过，未修改代码。
- 已按用户要求用表格归纳当前 MultiDiff 相对 HEAD 的优缺点；结论保持不变：虚拟化显著降低大量文件时的 Monaco 和内存成本，但需优先处理编辑防抖保存被离屏释放取消、首次远距离定位误差、临时 Diff 监听释放和 `rendered` 语义提前等风险。
- 说明 `MAX_IDLE_EDITORS=6` 只限制离屏后的空闲 DiffEditor 模板，不限制 viewport 内同时挂载数量；6 是参考 VS Code 原生对象池的保守复用上限，不是 Monaco 限制。可以动态调整，推荐按最近可视卡片数量计算并设置上下限（如 2~12），而不是按总文件数增长。
- 用户曾要求将 `multiDiffPanel.ts` 的 `MAX_IDLE_EDITORS` 从 6 改为 12，随后又要求恢复为 6；当前最终值为 6，只限制离屏空闲 DiffEditor 对象池，不限制 viewport 内挂载数量。恢复后 `npm run compile` 和该文件 lint 均通过。
- 按用户要求修复 MultiDiff 主要问题 2、4：每个卡片新增 `cancelMountReady`，离屏/折叠/销毁时显式 dispose 首次 `onDidUpdateDiff` 监听并 resolve 等待，防止监听残留；新增 `mountPromise`，`rendered` 改为等待首屏可视卡片 Diff 就绪或降级完成后发送，不再仅创建外壳即通知。`npm run compile`、该文件 lint 和 `git diff --check` 均通过。编辑保存的 400ms timer 是输入防抖，避免每次击键都向扩展侧发送整文件并写磁盘；问题 1 尚未修改。
- 核对 VS Code main 源码：原生 `multiDiffEditorWidget.ts`、`textFileEditor.ts` 和 `textResourceEditorInput.ts` 都没有固定 400ms 内容保存 timer；原生 MultiDiff 直接编辑工作台文本模型，由统一 dirty/save/Auto Save 服务管理。`textFileEditorModel.ts` 中存在的是 Undo/Redo 后自动保存参与者的 500ms 节流，不是通用保存延迟。因此项目里的 400ms 是 Webview 跨消息回写的自定义防抖值，不是照搬 VS Code 原生。
- 修复 MultiDiff 问题 1：新增 `flushPendingSave(entry)`；`disposeEntry()` 先从 modified model 读取最新内容，再在离屏、折叠、快照替换或面板销毁前清除 400ms timer 并立即发送最后一次 `saveFile`，避免防抖保存被释放流程取消。没有待保存 timer 时不重复写回。`npm run compile`、该文件 lint 和 `git diff --check` 均通过。
- 按用户要求将 `DiffReader` 的进度更新粒度从每 16 个文件改为每 32 个文件：commit 流式解析的取模进度点和工作树进度循环均改为 32，并同步注释。底层仍是一轮单个 `git cat-file --batch`，取消机制和最终一次性写回不变。`npm run compile`、该文件 lint 和 `git diff --check` 均通过。
