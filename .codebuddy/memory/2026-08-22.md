- Multi-Diff commit 标题接入完成：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`；已通过编译、lint 和 diff check。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。

- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 确认当前 Diff 正文读取策略：`DiffReader.prepare()` 以 128 个文件为一批；文件数不超过 128 时单批读取，超过时所有批次通过 `Promise.all` 并发。每批完成只更新 `diffProgress`，全部批次完成后按 index 排序并一次性替换 `store.files`。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 用户观察到 Diff 面板渲染进度为 1、9、17；该数值来自 `MultiDiffPanel.render()` 中所有文件卡片并发 `createCard()` 后按完成回调累加 `ready++`，不是 `DiffReader` 的 128 文件读取批次。多个 Promise 在浏览器一次绘制前连续完成，界面可能只看见间隔约 8 的抽样进度。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 评估 Monaco 卡片按 128 个分批渲染：技术上可将 `snapshot.diffs` 每 128 个切片并批间串行，但少于 128 个文件时与当前全并发完全相同；每张 Monaco DiffEditor 较重，单批 128 仍可能阻塞 Webview。用户最终要求保持按传递的 `snapshot.diffs` 一次性全部创建；当前 `snapshot.diffs.map(createCard)` + `Promise.all(tasks)` 已符合，无需修改。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 最终定位“1、9、17、25、33”根因：该序列精确对应 Git 历史 `fc34ecf^` 的旧 `DiffReader.prepare()`——`batchSize=8`，首批 `size=start===0?1:batchSize`，每批更新共享 `diffProgress`。提交 `fc34ecf` 曾改为 128，但用户运行中的 Extension Host/已安装扩展明显仍加载旧构建；当前 workspace 的 `src/git/diffReader.ts` 和 `out/git/diffReader.js` 均为 128，且 ≤128 时只会 0→N。Changed Files 与 MultiDiff 都订阅同一 `diffProgress`，所以旧运行代码会在两侧同步显示相同 1+8n 序列。另有 MultiDiff 多 selector 导致重复完整 publish 的独立问题，但它不能产生精确的 1+8n 读取进度。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 按用户要求将当前 `DiffReader.prepare()` 改为每批 16 个串行读取。每批开始和 await 返回后均检查 reader/store generation；`stop()` 增加代次并 kill 当前 `git cat-file` 子进程，因此切换提交可打断当前批次且不会启动后续批次。全部完成后仍一次性写入 `store.files`。编译与 lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 16 批次变慢的直接原因：实现同时从“128 批次并发”改成了“16 批次串行”。每批 `readDiffs()` 都新建并等待一个 `git cat-file --batch` 子进程，N 个文件需 ceil(N/16) 次进程启动/仓库读取/输出解析；后批必须等待前批完成。每批更新 diffProgress 还会触发 Store/Webview 发布开销。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 已按方案一重构当前 `DiffReader`：普通 commit 整轮只启动一个 `git cat-file --batch`，一次写入全部唯一对象请求，stdout 到达时按 header/size 流式解析；按连续完成的每16个文件更新 `diffProgress`，最终一次写入 `store.files`。`stop()` 仍通过 generation + kill 唯一子进程取消。公共 `readDiffs()` 复用同一流式对象读取器但不写 Store。工作树磁盘文件仍由现有 API 并发读取。`npm run compile` 与 lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- MultiDiff 虚拟化最终改为接近 VS Code 原生：逻辑项外壳保存 diff/value/collapsed/bodyHeight/左右 selections；只为 viewport 相交项借用 Monaco DiffEditor 模板，离屏解绑 model 并归还对象池，空闲模板最多6个，文件 models/listeners 按项释放。动态 `contentHeight` 写回 bodyHeight，视口上方项高度变化时补偿 window.scrollY；reveal 自动展开并按前序逻辑项缓存高度累计定位。不再固定保留上下各5张。编译与 lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 修复虚拟化后一直显示“正在准备 Diff”：`estimateBodyHeight()` 位于 TypeScript 模板字符串内，源码 `split('\n')` 在生成 HTML 时变成包含真实换行的 JS 字符串字面量，导致整个内联脚本语法错误、`webviewReady` 不发送。改为模板层双重转义 `split('\\n')`，编译与 lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 按用户要求撤销 Changed Files 定位的保活、强制挂载、真实高度后二次对齐、revealToken/mountVersion catch 门禁和重复点击重试改动；当前只保留 `reveal()` 在定位前自动清除目标卡片 collapsed 状态，其他滚动与虚拟化行为恢复原实现。编译与相关 lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 调研 VS Code 原生 MultiDiff：核心在 `src/vs/editor/browser/widget/multiDiffEditor/`。它不是上下各N张 overscan；`render()` 只为与 viewport 相交的 item 借用 `DiffEditorItemTemplate`，其余 hide 并归还池。逻辑 item 保存 `lastTemplateData.contentHeight`（初始500）和 selections，实际高度来自 `onDidContentSizeChange`；模板用 ObjectPool 复用重型 DiffEditor，空闲最多缓存约6个。动态高度通过 contentHeight observable 重算总高度；内部 editor 滚动用 delta 回调修正外层 scrollTop，避免跳动。reveal 按前项累计 contentHeight 定位，导航前自动展开。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 按用户要求完整撤销零增删 Diff 自动折叠替代方案：删除 updateLineStats 的折叠副作用、zeroDiff 字段，并将 Monaco 配置恢复到用户误还原后的 `revealContextCount` 状态；当前只显示 +0/-0，不做隐藏。编译与 lint 通过。下一步必须先由用户选择方案再修改。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 按用户要求完整撤销 Monaco 风格零增删全文控件方案及其展开显示修复：删除全部 unchanged widget CSS/DOM/函数、zeroDiff/unchangedExpanded 状态、对象池控件保留、fit 和虚拟化分支；恢复所有可见未折叠项正常挂载 Monaco。当前 +0/-0 不做额外隐藏。编译与 lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 零增删 Diff 后续候选方案：A 调用 Monaco 0.52.2 私有 `collapseAllUnchangedRegions/showAllUnchangedRegions`（视觉最原生但升级风险）；B 自建 Monaco 风格 view-zone 占位控件（稳定可控但状态较多）；C 复用整卡片 collapsed（最小改动但隐藏元信息）；D +0/-0 仅自动缩短为摘要行、点击标题展开（折中）。用户要求先选方案再修改。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- MultiDiff 标题栏已改为“增删行 + 状态标识 + 文件名 + 路径”：文件名比编辑器字号大 2px，随后一个空格显示半透明目录并垂直居中。Rename 使用旧路径/新路径左右双标题，右标题起点按完整卡片中线对齐右侧正文；旧侧 R 标识、文件名、空格和路径整体连续删除线。Delete 的文件名、空格、路径同样整体连续删除线。`npm run compile` 通过；IDE lint 无诊断，CLI lint 因当前环境找不到 eslint 命令未运行。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 工作区虚拟提交重构为单个 `Uncommitted Changes`：删除 `GitBranchOption.hasChangeFiles/hasStagedChangeFiles` 及 BranchController 的工作区 watcher/presence 逻辑；CommitController 接管 `.git/index` 与仓库文档变化监听，防抖后单次 `git status` 获取完整 staged/unstaged 快照，仅当 HEAD 在已选分支中时暴露虚拟提交。Changed Files 显示两个可折叠分组及数量，支持整组/单文件 stage、unstage、discard（discard 有模态确认），空组置灰且隐藏操作。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- `Staged Changes`/`Unstaged Changes` 的整个非空标题行均可点击折叠或展开；还原、加号、减号等 Git 操作按钮通过事件目标排除，不触发折叠。空分组保持不可点击。编译、diff check 和 IDE lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 文件项右侧按钮的多层高亮根因是按钮继承了按钮组/文件行背景，hover 时边界连成块。现统一标题与文件行按钮为 VS Code Toolbar Action 样式：默认透明、按钮间隔 2px、hover 使用 `toolbar-hoverBackground`、active 使用 `toolbar-activeBackground`、5px 圆角，键盘 `focus-visible` 使用 `focusBorder`。文件项按钮改为常驻显示，按钮组不再设置任何背景或 sticky 遮挡层。编译、diff check 和 IDE lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- `Uncommitted Changes` 现打开 Multi-Diff，新增 `uncommitted` 模式直接比较 HEAD 与工作区并对 staged/unstaged 同路径去重，工作区变化后自动重载 Diff；右侧仍可编辑回写。`CommitFile.isUntracked` 从 porcelain `??` 精确标记，Unstaged 中仅真实未跟踪文件显示红色。Staged/Unstaged 折叠状态、箭头、样式和事件均删除，两个分组始终展开。文件右键菜单在左击或右击其他文件时先关闭旧菜单。编译、diff check 和 IDE lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 工作区文件类别图标继续重绘为流程语义：Untracked 为红色虚线文件+新增徽标，Unstaged 为正常前景色文件修改线+等待时钟，Staged 为绿色向下箭头进入暂存托盘；18px SVG、无底板，置于 A/M/D/R 前，Changed Files 与 Uncommitted Multi-Diff 标题共用同款视觉。Unstaged 文件名使用正常 `vscode-foreground`，仅 porcelain `??` 的真实 Untracked 文件状态符和文件名为红色。编译、diff check 和 IDE lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- Uncommitted 子项/整组执行 stage、unstage、discard 时，确认后立即设置独立 `workingTreeActionLoading`；Changed Files 标题下、列表顶部显示“正在更新未提交文件...”轻量 loading 条，旧列表保持可见。Git 命令完成后同步等待 `CommitController.refreshWorkingTreeImmediately()` 的最新 status 快照，事件会立即重载列表和 Diff，再结束 loading；同时阻止操作期间重复点击。编译、diff check 和 IDE lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- Uncommitted presence 最小纯 Git 方案已实施：新增流式 `checkWorkingTreePresence()`，运行 `git --no-optional-locks -C <repo> -c status.renames=true status --porcelain=v1 -z --untracked-files=normal`，stdout 首个数据块即 dirty 并 kill，空输出正常退出即 clean；AbortController + generation 防旧结果覆盖。CommitController 独立维护 `hasUncommittedChanges`，Provider/Webview 直接用该值控制虚拟提交置灰，不再从 Details 数量推断。文档编辑同步 dirty、保存/index/仓库/分支/HEAD 变化真实校准，stage/unstage 成功直接 dirty，discard 后真实校准。完整 `getWorkingTreeChanges()` 只在选择虚拟提交或当前查看时的 Git 操作后执行，移除了 Presence 的 200ms 防抖及三命令详情查询。编译、diff check 和 IDE lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 已完成 `Uncommitted Changes` 三个状态图标的只读设计评估，未修改产品代码。推荐以 Git 数据流为视觉隐喻：Untracked=文件轮廓+加号徽标，Unstaged=文件轮廓+向右变化箭头，Staged=文件轮廓+向下进入托盘；保留 18px/20px 槽位、VS Code 主题 token、形状与颜色双重编码，并要求 Changed Files 与 MultiDiff 两处保持一致。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 用户认为流程型图标方案偏抽象，随后也否定了问号/圆点/对勾状态灯方案。后续设计不要用抽象流程或通用状态灯，应从现有信息结构出发：Changed Files 已通过 Staged/Unstaged 分组表达两态，状态图标应避免重复信息；优先考虑只突出 Untracked，或使用直白文字标签。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 用户要求完全重新设计三种状态图标；新的候选方向是“文件对象三态”：Untracked=单页文件+加号，Unstaged=单页文件+铅笔，Staged=多页叠放文件。用物体状态区分，不使用圆形底板、问号、感叹号、对勾或箭头；三者统一为 18px 描边 SVG，并在 Changed Files 与 MultiDiff 同步。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 用户继续要求换方案；后续候选转为 VS Code 熟悉的“操作结果”图标：Untracked=新建文件、Unstaged=编辑铅笔、Staged=收纳箱/归档盒。避免文件轮廓加徽标、叠页及流程式表达；先用低保真视觉稿确认方向，再改代码。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 用户继续否定“操作结果”方向；后续不再使用文件、新建、铅笔、归档盒等具象物件。下一候选应以 Git 的三种存储层为核心：未跟踪=空心分支节点，未暂存=工作区波纹节点，已暂存=实心索引节点，并提供更接近 SVG 的几何稿。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 用户要求图标要与 Git 有直接关联且更具象；放弃抽象分支节点。下一候选采用 Git 三层实物隐喻：Untracked=仓库外的散页文件，Unstaged=工作区文件叠加红绿 diff 条，Staged=带 “INDEX” 页签的索引卡文件；需在 18px 中保持清晰。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 用户继续要求换方案；避免仓库边界、Index 字母页签和散页文件。下一候选采用 Git patch 工作流：Untracked=带星标的空白补丁页（新文件），Unstaged=红绿双 hunk 补丁页（工作区 diff），Staged=被 Git 暂存夹固定的补丁页（Index patch）。三者均为具体补丁对象，不使用抽象状态符号。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 用户继续否定补丁工作流方案；后续不再用补丁页、星标或夹条。下一候选采用 Git 命令语义的“对象变换”：Untracked=工作区文件旁的 `git add` 加号，Unstaged=文件到索引之间的未完成虚线，Staged=文件已进入索引的实体标签；若继续否定，应先要求用户给出喜欢的参考风格，避免无边界枚举。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 用户要求尽可能简单易懂；后续优先采用 Git 原生 porcelain 状态文本作为视觉：Untracked=`??`、Unstaged=`·M`、Staged=`M·`（点仅用于显化 porcelain 的空格），以固定两格小标识替代具象 SVG。需要说明此方案与当前额外的 A/M/D 状态字母重复，实施时宜二选一而非并列。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 用户明确不要文字状态码，必须保留三个独立图标；后续不再建议删除图标或用文本替换。下一候选采用常见 Git 操作符号的最小图标：Untracked=Git 分支节点外加独立小菱形（未接入），Unstaged=Git diff 的两条并列差异线，Staged=Git commit 的实心节点加短横线；均只保留一个核心形状，避免文件、补丁、托盘和复杂组合。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 用户仍要求继续换且强调简单、通俗、易懂。后续应避免 Git 图节点、差异线等需解释的抽象符号，优先测试常见办公语义的单物体图标：Untracked=新文件页、Unstaged=编辑铅笔、Staged=书签；每个图标只使用一个主体形状，并保留 Git 状态 tooltip 兜底。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 用户指定后续方案必须沿用“Untracked / Unstaged / Staged + 单个图标 + 简短白话说明”的横向对照格式，不需要展开实现细节；继续寻找简单、通俗、易懂且保留三个独立图标的组合。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 用户进一步追问 `? / ! / ✓` 是否最通俗；结论是视觉统一但语义不完全准确：`!` 易被理解为错误，`✓` 易被理解为已提交。若仍保留独立小图标，优先采用同一圆框内 `+ / 铅笔 / ✓`：Untracked=新增文件、Unstaged=正在修改、Staged=已暂存待提交；tooltip 应明确 Staged 尚未提交。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- Uncommitted 同一路径同时存在 Staged/Unstaged 时现保留两份独立 Diff：`CommitFile.diffKey` 使用 `staged:<path>` / `unstaged:<path>` 贯穿 Store 选择、Changed Files 行、Provider 定位和 MultiDiff 卡片 Map。DiffReader 对 Staged 读取 HEAD→Index，对 Unstaged 读取 Index→Working Tree；不再按 path Map 去重。Staged 卡片只读，Unstaged/Untracked 卡片可编辑回写。编译、diff check 和相关文件 IDE lint 通过。最初选择显式 `diffKey` 是因为当时优先保证全链路身份唯一、最小化遗漏风险和快速落地验证：每一层都直接读取同一个 key，易搜索/调试/序列化；代价是 identity 属于 UI 关注点却进入 Git 模型，形成 `workingTreeKind/path/diffKey` 重复状态并扩大透传改动。复盘后推荐不持久化 `diffKey`，只在 Provider 和 MultiDiff 边界以 `workingTreeKind + path` 派生 identity，Store 仅保存/转发字符串；该方案模型更纯、改动更少，但两处派生公式必须严格一致，调试时 identity 不直接存在于载荷。尚未切换。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 工作区操作性能优化按用户要求只实施前 1–4 项：stage/unstage/discard 点击后先乐观更新 Staged/Unstaged 分组，操作进入串行队列不再因 loading 丢弃；队列持续执行，空闲点才合并运行一次完整 Details/Diff 校准，校准期间新操作会继续下一轮；乐观更新不覆盖现有 DiffPayload，旧 Diff 保持可见。Changed Files 标题下的文字/转圈已替换为与顶部加载相同的 4px、30% 蓝色往返滚动进度条；该进度条 DOM 在首次 Commit 初始化、切换 Commit、Diff 加载和工作区操作时统一保留/显示，文件列表 DOM 不再被加载文案覆盖。Staged 分组为空时完全隐藏；非空 Staged 和 Unstaged 分组左侧分别有绿色/修改色 1px 子项车道，车道左右间距均为 7px，不覆盖标题。编译、diff check 和 IDE lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- Uncommitted Multi-Diff 每张卡片标题栏右侧已增加工作区操作：Staged 显示减号取消暂存；Unstaged/Untracked 显示还原和加号暂存；普通 commit 不显示。按钮组底部黑边根因是组级半透明背景+2px padding 暴露在 sticky 标题遮罩边缘；现移除组背景/padding，改为每个按钮独立 24×24、5px 圆角半透明背景，仍保留 2px 间距。还原/暂存/取消暂存改用与 Changed Files 完全相同的 `codicon-discard/add/remove`，打开文件改为 `codicon-go-to-file`，统一 16px；所有按钮保留完整 title/aria-label 和 VS Code hover/active/focus。编译、diff check 和 IDE lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- Multi-Diff 增加整个 Webview 布局最底部的单一固定水平滚动条，不放在卡片底部；它只控制当前高亮 Diff，并同步 original/modified 两侧。高亮随卡片点击、Changed Files reveal 和纵向视口顶部卡片变化；每张卡片保存独立 `horizontalLeft`，虚拟化卸载/重挂后恢复。滚动范围取两侧 `getScrollWidth() - layout.contentWidth` 最大值，无横向溢出、折叠或未挂载时隐藏；Monaco 内部横滚、底栏、窗口 resize 和内容尺寸变化双向同步并用帧合并/循环保护。编译、diff check 和 IDE lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- Changed Files 工作区交互支持未聚焦时首击响应：stage/unstage/discard 按钮、文件项和正常 commit 的文件夹折叠均在 `pointerdown` 阶段仅阻止默认焦点切换，实际 Git 操作、打开 Diff 和折叠/展开在 `pointerup` 触发；兼顾未聚焦首击响应、按下视觉反馈及按住后取消操作的常规语义。子项与分组标题的全部还原、暂存、取消暂存按钮均补充完整 title/aria-label，明确当前/全部文件、移动目标及还原不可撤销。编译、diff check 和 IDE lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 工作区 stage/unstage/discard 慢的根因分析：操作命令后 `runWorkingTreeAction()` 同步等待 `refreshWorkingTreeImmediately()`；其每次全仓运行 status + staged raw diff + unstaged raw diff，快照事件又触发整组 DiffReader 和 MultiDiffPanel 重发/Monaco 重建。`.git/index` watcher 还额外触发 presence 与 Provider 的 300ms 仓库签名检查。`workingTreeActionLoading` 在全部详情/Diff链路结束前保持 true，连续点击会被直接 return 丢弃而非排队，因此体感更慢。推荐操作完成后立即乐观更新列表并释放 action loading，details/diff 在后台合并刷新，去重 watcher 引发的重复查询，并为连续操作加串行队列。尚未实施。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- Multi-Diff 标题栏右侧按钮背景最终按正确所有权处理：`.diff-actions` 整体铺满标题行高度并使用 `var(--header-surface)`，右侧 4px 间距改为组内 padding；各按钮默认透明，仅 hover/active/focus 着色。此前只给单按钮设置标题栏色，按钮间 gap 仍透出下层背景，视觉上仍是分离深色块。`npm run compile` 与 IDE lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 用户要求撤销 Multi-Diff 整卡回顶的 `lastOuterScrollAt`/120ms 静止判断方案；相关三处代码已完整恢复为原有无条件高度补偿，之前尝试的 `overflow-anchor:none` 也未保留。按钮组背景修改不受影响。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- 已按当前 HEAD 实际 diff 精确撤销“光标编辑态整卡高亮”：删除 `createCardShell()` 中卡片 `pointerdown -> setActive()` 事件。点击 Monaco 编辑区不再立即切换 `.selected`；Changed Files 定位、标题折叠和纵向滚动顶部卡片仍会更新 `activePath`，因此滚动自动高亮与底部全局水平滚动条不受影响。最终源码 diff 仅删除一行；编译、diff check 与 IDE lint 通过。用户记得的“改动很多”也属实：同一 HEAD 为全局水平滚动条扩展了 `activePath/setActive/updateGlobalHScroll`、每卡 `horizontalLeft`、双侧横滚同步、挂载/卸载/resize 更新等多处逻辑；其中只有新增的卡片 `pointerdown` 专属于点击 Diff 后高亮，其余是水平滚动条或早于 HEAD 已存在的定位/滚动选中逻辑。进一步核对确认旧代码从未通过 `onDidFocusEditor`/`onDidChangeCursorPosition` 判断“是否含编辑光标”；该语义由卡片 `pointerdown` 冒泡近似实现。`originalSelections/modifiedSelections` 仅用于虚拟化卸载/重挂后恢复 Monaco 光标和选区，早于 HEAD 已存在且不驱动 `.selected`，不应撤销。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- `outerScrollVersion` 方案已证明无法覆盖问题并已移除：滚动事件后的 RAF 才调用 `updateVirtualization()` 并记录版本，用户若已停下，异步 `fit()` 返回时版本仍相同，仍会补偿回推。现改为确定的因果边界且不使用版本号：`updateVirtualization(fromScroll)` 将滚动来源传入 `mountEntry`，滚动触发的挂载固定 `allowScrollCompensation=false`，其 Monaco 异步高度回写永不调用 `window.scrollTo`；初始渲染/非滚动挂载仍保留上方高度补偿。修复同时清除了删除版本号后残留的未定义引用。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- Multi-Diff 初始化保护修复：全局导航按钮事件绑定增加 DOM 存在性判断，避免导航初始化异常阻断脚本发送 `ready`，导致页面一直显示“正在准备 Diff...”。`npm run compile`、lint 与 diff check 通过。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
- Multi-Diff 初始化最终修复：上一轮全局导航相关 DOM/CSS/状态/事件已完整回退；“正在准备 Diff...”的实际根因是 `estimateBodyHeight()` 位于 HTML 模板字符串内，`split('\\n')` 转义层级错误导致生成脚本出现真实换行并触发语法错误，`ready` 无法发送。当前已确认源码使用模板层双重转义，编译、lint、diff check 通过。
- Multi-Diff 点击与滚动高亮冲突已优化：增加 `clickedPath`。点击卡片记录路径并高亮；滚动时若该卡片仍有有效可见区域（`rect.bottom > 36` 且未超出视口）则不覆盖高亮，完全离开后清除锁定并由首个可见 Diff 接管。重置快照时同步清除 `clickedPath`。`npm run compile`、diff check 与 IDE lint 通过。
- Multi-Diff 导航已改为顶部全局上下按钮：移除卡片内导航按钮；只遍历实际存在 `getLineChanges()` 的卡片；当前卡片内按差异块上下移动，边界跳到相邻有差异卡片，最顶/最底禁用按钮且不循环；跳转时对两侧设置对应 `Selection`、居中显示并 `setActive()` 同步高亮 Diff 文件。`npm run compile`、diff check 与 IDE lint 通过。
- 继续定位“准备 Diff...”确认上一轮回退误删 `diffKey(diff)` 基础函数，渲染阶段因此会抛 `ReferenceError`。已恢复原函数，不添加防御性补丁；编译、lint、diff check 通过。
- Multi-Diff 文件标题现显示当前提交标题与短哈希：`MultiDiffPanel.show()` 接收 `commitTitle`，由 `gitkViewProvider.openDiff()` 传入 `commitController.selectedCommit?.message`，标题格式为 `${commitTitle || 'Gitk Diff'} (${hash.slice(0, 8)})`。`npm run compile`、两个文件 lint 与 diff check 通过。
- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。

- Multi-Diff 初始化根因最终确认：上一轮全局导航回退不完整，`changeSelection/focusChange/navigateChange` 仍残留，`focusChange()` 调用不存在的 `updateGlobalDiffNav()`；首次渲染/导航相关路径触发 ReferenceError。已整体删除未完成导航函数及 `changeIndex` 字段，保留原有 Diff 渲染和 commit 标题改动。编译、lint、diff check 通过。


- Multi-Diff 初始化最终根因确认：全局导航已回退但 `updateGlobalHScroll()` 仍残留 `updateGlobalDiffNav()` 调用；首次活动 Diff 没有横向滚动内容时触发 `ReferenceError`，渲染链中断并持续显示“正在准备 Diff...”。已删除该残留调用；不是换行问题。编译、lint、diff check 通过。

- Multi-Diff 文件标题接入当前 commit 标题：面板标题由 `commitController.selectedCommit?.message` 传入，显示为 `commitTitle (短哈希)`，无标题时回退 `Gitk Diff (短哈希)`。已通过编译、lint 和 diff check。
