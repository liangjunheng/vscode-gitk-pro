# 2026-08-22

- 确认当前 Diff 正文读取策略：`DiffReader.prepare()` 以 128 个文件为一批；文件数不超过 128 时单批读取，超过时所有批次通过 `Promise.all` 并发。每批完成只更新 `diffProgress`，全部批次完成后按 index 排序并一次性替换 `store.files`。
- 用户观察到 Diff 面板渲染进度为 1、9、17；该数值来自 `MultiDiffPanel.render()` 中所有文件卡片并发 `createCard()` 后按完成回调累加 `ready++`，不是 `DiffReader` 的 128 文件读取批次。多个 Promise 在浏览器一次绘制前连续完成，界面可能只看见间隔约 8 的抽样进度。
- 评估 Monaco 卡片按 128 个分批渲染：技术上可将 `snapshot.diffs` 每 128 个切片并批间串行，但少于 128 个文件时与当前全并发完全相同；每张 Monaco DiffEditor 较重，单批 128 仍可能阻塞 Webview。用户最终要求保持按传递的 `snapshot.diffs` 一次性全部创建；当前 `snapshot.diffs.map(createCard)` + `Promise.all(tasks)` 已符合，无需修改。
- 最终定位“1、9、17、25、33”根因：该序列精确对应 Git 历史 `fc34ecf^` 的旧 `DiffReader.prepare()`——`batchSize=8`，首批 `size=start===0?1:batchSize`，每批更新共享 `diffProgress`。提交 `fc34ecf` 曾改为 128，但用户运行中的 Extension Host/已安装扩展明显仍加载旧构建；当前 workspace 的 `src/git/diffReader.ts` 和 `out/git/diffReader.js` 均为 128，且 ≤128 时只会 0→N。Changed Files 与 MultiDiff 都订阅同一 `diffProgress`，所以旧运行代码会在两侧同步显示相同 1+8n 序列。另有 MultiDiff 多 selector 导致重复完整 publish 的独立问题，但它不能产生精确的 1+8n 读取进度。
- 按用户要求将当前 `DiffReader.prepare()` 改为每批 16 个串行读取。每批开始和 await 返回后均检查 reader/store generation；`stop()` 增加代次并 kill 当前 `git cat-file` 子进程，因此切换提交可打断当前批次且不会启动后续批次。全部完成后仍一次性写入 `store.files`。编译与 lint 通过。
- 16 批次变慢的直接原因：实现同时从“128 批次并发”改成了“16 批次串行”。每批 `readDiffs()` 都新建并等待一个 `git cat-file --batch` 子进程，N 个文件需 ceil(N/16) 次进程启动/仓库读取/输出解析；后批必须等待前批完成。每批更新 diffProgress 还会触发 Store/Webview 发布开销。
- 已按方案一重构当前 `DiffReader`：普通 commit 整轮只启动一个 `git cat-file --batch`，一次写入全部唯一对象请求，stdout 到达时按 header/size 流式解析；按连续完成的每16个文件更新 `diffProgress`，最终一次写入 `store.files`。`stop()` 仍通过 generation + kill 唯一子进程取消。公共 `readDiffs()` 复用同一流式对象读取器但不写 Store。工作树磁盘文件仍由现有 API 并发读取。`npm run compile` 与 lint 通过。
