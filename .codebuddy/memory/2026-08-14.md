# 工作记录

- 分析 VS Code 原生 Diff 与当前 Custom Diff：VS Code 对整个 `LineRangeMapping` 做跨行字符 refine，并以 outer line decorations + inner range decorations 分层；不等高区域用 View Zone（`diagonal-fill`）按实际像素高度补齐。当前实现改为行级脚本后按 slot 配对，并用 placeholder 行模拟，固定行高场景近似但在跨行 innerChanges、自动换行/视图区对齐和 gutter 色层上存在差异。
- 调整 replacement 图层以接近 VS Code：`wholeLineWordDiff` 为不等长 replacement 的无配对尾行生成完整文本级 `innerChanges` 与深色 word 背景，同时保留浅色 `modified-*` 行背景；在中性 gutter 规则之后重设 `modified-*` 行号栏红绿背景。`npm run compile`、`git diff --check` 与文件诊断均通过。
- 针对截图中的 `s_cleaner.register`、`if (active.get())`、`cleanup.run()` 核实原生逻辑：外层 replacement 的两侧整行 decoration 始终着色；只有 `innerChanges` 命中的字符范围叠加深色 text decoration。`removeShortMatches`、词/子词扩张、`removeVeryShortMatchingTextBetweenLongDiffs` 可吞并短相等片段，但不能仅因外层 replacement 就断言相同片段必然被深色标记；最终取决于跨行字符 refine 输出的 RangeMapping。侧边对齐由按像素高度的 `diagonal-fill` View Zone 完成。
- 制定落实方案：以连续 replacement 块而非 slot 行对为字符级 Diff 单位；建立带换行位置的 grapheme 序列，执行跨行 DP/Myers、官方同类短匹配和词/子词扩展，再按原/新行投影 `innerChanges`。移除 replacement 尾行的无条件整行深色逻辑；保留浅色行级、独立 gutter 与 placeholder 布局。先覆盖截图场景、等长替换、纯增删、空行与大块 fallback，再集成渲染。
- 已实施跨行 replacement 细化：新增 `replacementWordDiff`，对完整旧/新区间展平为含换行锚点的 grapheme 序列，执行 LCS 字符级 Diff（250000 cells 预算 fallback），再按行投影、扩展到词/子词边界后传给 `codeHtml`。`appendChanged` 不再逐 slot 调用 `tokenWordDiff` 或强制尾行整行深色。`npm run compile`、`git diff --check` 与文件诊断均通过。
- 复审跨行 Diff 后发现语义回归风险：块级 LCS 可跨逻辑行匹配，而 DOM 仍按同 slot 并排，交换/合并/拆分行可能使当前显示的两行均无深色 inner change；换行 token 又未投影，因此拆合行变化会丢失词级标记。未发现直接的未定义引用；后续应在 slot 两侧文本不同且投影均为空时回退逐行 `tokenWordDiff`，并处理换行锚点。
- 曾尝试修复 Custom Diff 长期停留在“正在准备 Diff...”，加入了更低 LCS 预算和双 rAF；用户确认问题不在该处，已完整撤销这两项改动。
- 定位到跨行 replacement 引入的实际 Webview 初始化异常：`getHtml()` 是 TypeScript 模板字符串，`replacementWordDiff` 内写入 `value: '\n'` 会在生成 HTML 时变成单引号中的真实换行，导致内嵌脚本语法错误并停留在加载页。已改为 `value: '\\n'`，使生成的 Webview 脚本保留合法的 `\\n` 转义；`npm run compile`、`git diff --check` 与文件诊断通过。
- 再次踩同一模板转义坑：新增 `optimizeOffsetDiffs` 系列时，`boundaryCategory` 内 `'\n'`/`'\r'`/`'\t'` 写成单反斜杠，模板求值后脚本出现字符串内真实换行 → 整个 `<script>` 解析失败 → webview 停在"正在准备 Diff..."。已全部改为 `'\\n'`/`'\\r'`/`'\\t'`。新增防护验证法：用临时脚本把源码 `<script>` 段按模板字符串语义 eval 求值后 `new Function` 语法检查 + 检测 `'[\n\r]'` 原始换行残留（注意：此前用正则提取源码文本做 `new Function` 验证无法暴露此问题，因为提取上下文中 `'\n'` 是合法转义）。教训：凡在 customDiffPanel.ts 模板内新增含 `\n` 字面量的代码，必须写 `'\\n'` 并用上述求值法验证。
- 修复上轮官方 `translateOffset` 语义引入的两个回归（用户报告 26/27 间多出空行、部分行行首缩进无深绿）：1) start 落在行首 token（缩进后第一 token）时返回 `column:12` 而非 0 → 行首缩进段无填充；2) end 落在行首 token 时返回 `(下一行,0)`，`computeAlignmentSegments` 的 `end.column < maxColumn` 判断必真 → 强制切分对齐段 → placeholder 多余行。修复：`position()` 两端点吸附行边界——start 遇前 token 为 `\n` 时归 `(line,0)`（恢复行首深绿）；end 遇前 token 为 `\n` 时回退到 `\n` token 自身位置 `(line-1,行尾)`（消除多余行，且此时换行必然已被 diff 覆盖，与 coveredLineBreaks 一致）。保留了 trim+合并数学（行尾填充根因修复）。验证脚本改用"模板求值 + 提取花括号配平的单行函数"法，9 项断言全过（end 不落行首、无多余行、fillTextToEnd 保留、跨行行从列 0 起、单行词级无回归），编译与 lint 通过。
- 用户反馈上述方案仍不行（26/27 间多余行依旧），要求撤回并重新结合 VS Code 源码梳理。撤回 `git checkout -- src/webview/customDiffPanel.ts` 到暂存区版本后重拉官方源码（linesSliceCharSequence.ts / defaultLinesDiffComputer.ts / diffEditorDecorations.ts / style.css）得出决定性结论：1) 官方渲染层 `char-insert/char-delete` 装饰 range 原样使用，CSS 无任何行尾延伸规则 → 原生行尾深绿的唯一来源是数据层 range 的 end 恰好落在行长处（`translateOffset` 指向 `\n` token 时返回 `(本行, 行长)`）；2) 行尾深绿不需要"覆盖换行符"，更不需要裁剪行首空白——之前 trim+optimizeOffsetDiffs 全是错误前提下的过度工程。最终最小修复（保留暂存区管线不动）：a) `position()` 删掉 right 时跳过 lineBreak 的循环（旧行为把 `\n` 映射到下一行行首，丢失行尾信息）；b) 新增 `normalizeEnd()` 把 `(line+1,0)` 归一化为 `(line, 行长)`（消除 26/27 幻影行数据源）；c) `projectRanges` 规则改为 `end === 行长 → fillTextToEnd:true`（锚点同样适用）；d) `codeHtml` 检测 fill 后追加 `<span class="text-fill-to-end-tail word-*">`，CSS 用 `display:inline-block; width:calc(100vw - 100% - 36px)` 经典 diff 高亮延伸技巧（% 相对 max-content 的 line-code，自动填满到视口右缘，无需改 grid）。端到端验证（模板求值 harness）22 项断言全过：三条跨行行全部 end-at-EOL==fill 且渲染 tail、无幻影行（rows=5）、公共 8 空格前缀正确匹配（diff 从列 8 起，即"以前的方案"行为）、1:1 单行词级无回归；`npm run compile` 与 lint 通过。教训：verify harness 的花括号配平扫描会被 escapeHtml 内正则 `/"/g` 的引号干扰误过滤，需按函数名单独抓取。
- 截图复现确认 `private val`/`val` 选词错误：当前跨行 `refineReplacement()` 对完整 replacement 块做全局字符 LCS，存在重复 token `val` 时会为了最大公共子序列跨行错误配对，导致旧侧 line2 range=`[8,24)`（把 `private val val` 整段标成删除），新侧仅 `[8,12)`（`val`），与截图一致；因此是数据层 RangeMapping 错误，不是 CSS/word span 渲染问题。官方 `refineDiff` 先按 lineRangeMapping 取范围，并做 optimize→word/subword 扩展→short-match 处理；下一步应通过稳定行锚点/分段约束避免跨行重复 token 参与同一全局 LCS，而不要继续调选词 CSS。
- 修复用户报告的 `cleanup.run()`、`CleanerHolder.s_cleaner.register(target) {` 行尾短竖杆：行尾零宽 range 同时具有 `fillTextToEnd:true`，`codeHtml()` 先画 `word-anchor` 再画 tail，导致多余短竖杆。改为仅在 `end===start && !range.fillTextToEnd` 时输出 `word-anchor`；行尾 range 只输出 `text-fill-to-end-tail`，普通行内零宽插入仍保留锚点。模板求值后用 `new Function` 语法检查，断言 EOL 只含 tail、行内空 range 含 anchor，通过；`npm run compile` 与 lint 通过。
- 完成 `private val`/`val` 选词错配修复：先尝试等长多行 replacement 逐行递归 `refineReplacement`，但单行字符 LCS 仍会把 `private val` / `val` 内的 `va` 拆成公共片段，且 `extendOffsetDiffs` 把相等 `val` 扩入变更范围。最终对 `originalIndexes.length===modifiedIndexes.length>1` 启用稳定逐行前后缀切分（公共前缀 + 最大公共后缀，禁止跨行/字符级 LCS 和词扩展）：截图行旧侧精确 `[8,16)` = `private `，新侧 `[8,8)` = 零宽插入锚点，`val s_cleaner...` 完整保持未选中；原先整块跨行算法与词扩展继续用于行数不等的拆分/合并块。模板求值 harness 验证 4 项全过（private 精确范围、new val 仅锚点、首行不误 fill、不等长路径保留），`npm run compile` 与 lint 通过。
- 修复截图中 `// 局部化，避免 lambda 捕 outer this` 行尾误延伸高亮：此前 `projectRanges()` 以 `end === lineLength` 推断 `fillTextToEnd`，任何恰好到行尾的普通文本 range 都被追加 tail，造成尾部高亮块。依据官方 `diffEditorDecorations.ts` 直接消费 `innerChanges` range 的模型，改为在 `refineReplacement()` 从 offset diff 扫描实际覆盖的 `lineBreak` token，返回 `originalLineBreaks`/`modifiedLineBreaks`；`projectRanges()` 仅对该集合中的行设 fill。模板求值测试覆盖：普通行尾 range（无换行 token）不填充/不渲染 tail，跨行 range（覆盖换行 token）仍填充/渲染 tail，全部通过；此前 `npm run compile` 和诊断通过。
- 修复 `companion object` → `private object CleanerHolder` 中 `object` 被高亮：上轮等长多行的前后缀切分只能保住尾部，无法保住中间公共 token。等长多行 `refineReplacement()` 改为每对对应行执行完整 `splitDiffTokens` 序列的 token-LCS，并保留空白 token 作为稳定锚点（不能 filter whitespace，否则缩进被错误高亮）；按匹配 token 对切出多个 RangeMapping。结果：old `companion` range `[4,13)` / new `private` range `[4,11)`；中间 `object` 完全不高亮；`CleanerHolder` 为独立插入 range；`private val` 精确 `[8,16)` 保持。单行 replacement 保持原字符级 refine，验证通过；`npm run compile` 与 lint 通过。
- 用户确认跨行 replacement 块级 LCS 投影方案未达到预期，已完全撤销该方案及其模板字符串换行修复；恢复 `appendChanged` 的逐 slot `tokenWordDiff`，并恢复不等长 replacement 尾行使用 `wholeLineWordDiff`。保留此前已有的 `wordDiff` 兼容别名与 replacement gutter 图层样式；编译、格式检查和文件诊断通过。
- 继续核实 VS Code 源码后确定正确方向：不能仅将跨行 `RangeMapping` 投影到既有固定 slot 行。原生先保留跨行 RangeMapping，再由 `computeRangeAlignment` 按 `innerChanges` 的前后稳定文本将 hunk 分为多个对齐区间，并把每段高度差作为 View Zone 补在较短侧末尾。项目应先引入范围映射与 hunk 内对齐段模型，再在 DOM 中为各段输出行和按段 placeholder；完成该几何层后才渲染词级范围，避免再次出现跨行锚点与固定 slot 冲突。
- 补全落地细节：范围使用半开 `Position`（行、grapheme 列）并遵循 VS Code 边界规则——双侧结束都在行首则排除终点行，双侧起点都在 EOL 则排除起点行，文档尾端空侧锚定前一行 EOL。每个 replacement 以跨行 RangeMapping 的稳定间隙切为 alignment segments；DOM 按段输出行，并在较短侧段尾插入 placeholder。词级 range 要跨首/中/末行拆分，零长度 range 不生成 span、仅参与对齐；复杂度超过预算时回退单段外层行色且不伪造 innerChanges。
- 复核官方边界链路：`getLineRangeMapping` 只有在两侧 end 都为行首且 mapping 不会空时移除终点行，只有两侧 start 都到达各自行 EOL 且 mapping 不会空时移除起点行；跨行字符 `translateRange` 以 start=right/end=left 映射，并在终点早于起点时归一为空 range。`computeRangeAlignment` 仅在两侧 startColumn>1 时按前置稳定文本切段，且仅以 original endColumn 小于原行 maxColumn 判断后置稳定文本；范围倒退、零长双侧段和会重对齐的空侧中间段都被跳过，hunk 末端以 forceAlignment 强制收口。项目实现必须保留这些不对称与防护条件。
- 用户要求后续实现严格以 VS Code 源码为准，移除项目自创且官方实现未使用的边界规则；特别不要额外引入“任一侧边界收缩”、自定义空 range 标记策略、固定预算语义、CRLF 分支或对称的后缀切分。实现范围仅保留 `rangeMapping.ts`、`LinesSliceCharSequence`、`computeRangeAlignment` 和 `DiffEditorDecorations` 中可直接证实的条件。
- 已实现 Custom Diff 的 replacement 跨行范围与 hunk 内分段对齐：`replacementMappings` 在完整 replacement 内生成跨行位置范围，`rangesByLine` 仅将非空区间作为词级颜色层，`replacementSegments` 按 VS Code `computeRangeAlignment` 的双侧前缀/原侧后缀条件、无效段跳过和末端强制收口切分。`appendChanged` 基于各段输出行和段尾 placeholder，纯增删保持原路径。模板内换行转义已验证为合法 `\\n`；编译、格式检查和文件诊断通过。
- 修复跨行范围渲染回归：`rangesByLine` 现将非空字符范围扩展到已有 `subwordRange`，避免深色 span 在单词内部断词；保留零宽 mapping 并标记反侧颜色 anchor。`codeHtml` 在空行或零宽范围时输出已有 `.word-anchor` 短竖杆，而非提前返回 `&nbsp;` 或丢弃范围。编译、格式检查和文件诊断通过。
- 用户确认跨行 RangeMapping、hunk 分段 placeholder、断词/anchor 修复的整套方案效果不佳，已将 `customDiffPanel.ts` 工作树恢复至会话开始时的暂存基线；`npm run compile`、`git diff --check` 通过。后续仅继续官方源码研究，不基于该方案再扩展实现。
- 官方主链路补全：`DefaultLinesDiffComputer` 小文件行级（总行数<1700）用带连续匹配权重的 DP，否则 Myers；行级结果依次 `optimizeSequenceDiffs`、`removeVeryShortMatchingLinesBetweenDiffs`，每个块经 `refineDiff`：跨行 `LinesSliceCharSequence`、长度<500 用 DP 否则 Myers、`optimizeSequenceDiffs`、整词扩展、可选子词扩展、`removeShortMatches`、`removeVeryShortMatchingTextBetweenLongDiffs`，再 `translateRange` 并由 `lineRangeMappingFromRangeMappings` 合并相接行范围。渲染层先加外层行 decoration，replacement 再加 inner range decoration；side-by-side `computeRangeAlignment` 以实际行高/换行/外部 View Zone 计算每段高度差，用 `diagonal-fill` View Zone 补齐短侧。
- 再次实施精简版官方链路：replacement 改为先生成跨行字符 `RangeMapping`（包含 `\\n` 行界），仅将非空范围投影为文本 decoration；再按 `computeRangeAlignment` 的双侧前缀、original 后缀、无效段跳过与末端强制收口输出分段 placeholder。纯增删保留原路径；未恢复此前非官方的强制整行尾部词色、额外词扩张或空 range anchor。`npm run compile`、`git diff --check` 和文件诊断通过。
- 根据截图修复第二次精简实现的两个回归：跨行字符 mapping 先在投影时扩展到现有 `subwordRange`，避免词级背景从标识符中间断开；零宽 range 不再过滤，改为携带反侧颜色并由 `codeHtml` 输出既有 `.word-anchor`。空行亦可承载该 decoration。编译、格式检查和 TypeScript 诊断通过。
- 用户确认上述跨行子词扩展和零宽 anchor 改动造成效果衰退，已精确撤销这两处，保留此前的跨行 mapping/分段对齐基础。替代方案调研确认：公开稳定 API 可用 `vscode.diff` 打开单文件原生 Diff，`vscode.changes` 打开原生 Multi Diff；原生 `DiffEditorWidget` 不能嵌入 Webview。若要求像素级复刻，应把自定义面板作为控制层并将文本比对委托给原生 Diff，而非继续在 Webview 重写算法。撤销后编译、格式检查和文件诊断通过。
- 对比现状/目标截图再次排查颜色层级：当前 `refineReplacement` 仅做原始跨行 LCS，遗漏官方 `optimizeSequenceDiffs`、整词/可选子词扩展、`removeShortMatches` 与长差异间短匹配合并；`projectRanges` 既不归并重叠范围又丢弃零宽范围，`codeHtml` 无法显示 empty decoration。CSS 还把纯 `.added` 整行直接赋为 `insertedTextBackground`（文本层色），与 replacement 的 `insertedLineBackground`（行层色）混用。应先使数据层复刻 refineDiff 后处理，再分离行色/词色，最后按官方 empty decoration 条件处理锚点；不要先在 DOM 投影层强行扩词。
- 按推荐顺序实施颜色层修复：纯 `.added`/`.removed` 块改用 `insertedLineBackground`/`removedLineBackground`；`refineReplacement` 将原始 LCS 先保留为 offset diff，执行官方同类的短匹配合并和整词扩展后才转换为 RangeMapping；`projectRanges` 归并重叠 span 并保留真实零宽范围，`codeHtml` 以 `.word-anchor` 输出 empty decoration（包括空行）。`npm run compile`、`git diff --check` 和 TypeScript 诊断通过。
- 三图复查结论：当前图比目标多一条横贯 pane 的斜纹 placeholder，导致后半块下移一行；上次方案无该额外行但词级层仍不准。根因是 `computeAlignmentSegments` 将官方 `emitAlignment(i.originalRange.endLineNumber, i.modifiedRange.endLineNumber)`（1-based exclusive）错误移植为 0-based数组的 `end.line + 1`，而 `Position.line` 已是目标行索引，正确数组 exclusive 边界应直接使用 `end.line`。官方后缀切分仅检查 original endColumn，不能增加 modified 对称条件。此为几何/行数问题，和颜色 CSS 无关；尚未修改，待用户确认。
- 已修复目标截图中第 27、28 行之间多余 placeholder：`computeAlignmentSegments` 的后缀 `emit` 由 `end.line + 1` 改为 `end.line`，对齐 VS Code `Range.endLineNumber` 作为 exclusive 边界传入 `emitAlignment` 的语义。未调整颜色、词级范围或其它段切分。`npm run compile`、`git diff --check` 与文件诊断通过。
- 修复目标图第 3 行尾部缺少深绿色文本层：官方 `removeVeryShortMatchingTextBetweenLongDiffs` 会对总长度大于 100 的 diff，将同行、有效字符不超过 3 的相等后缀并入 diff。当前 `extendOffsetDiffs` 只处理短间隔，未覆盖该规则；新增 `absorbShortLineSuffixes`，在 RangeMapping 转换前按相同条件将该后缀合并到两侧 offset range。编译、格式检查和文件诊断通过。
- 复核用户指出的 `CleanerHolder.s_cleaner.register(target) {`、`if (active.get()) {`、`cleanup.run()` 三行行尾颜色：目标中连续延伸至行尾的区域是 VS Code `diffLineAddDecorationBackground` 行级层，非 `innerChanges` 文本层或 empty-decoration 短竖杆。当前 Webview 的 `refineReplacement` / `projectRanges` / `codeHtml` 只能影响文本 span/anchor，不能补该行尾层；应在 replacement 行容器层确保 `.modified-added` 背景覆盖完整可用代码列，而不再扩张字符 range。
- 修复 replacement 行级背景未延伸至右侧：`.line` 的代码列由 `max-content` 改为 `minmax(max-content, 1fr)`，保持超长代码的最小宽度并拉伸短行至 pane 宽度，使现有 `.modified-added` / `.modified-removed` 行层覆盖从文本末尾到右边界。未调整文本级 RangeMapping。`npm run compile`、`git diff --check` 与文件诊断通过。
- 用户反馈上一布局修复未生效，进一步定位并调整：`.line` 原有 `width:max-content; min-width:100%` 使 grid 先按内容宽度计算，`1fr` 无可分配宽度；渲染后脚本还将 `pane.scrollWidth`（最长行滚动宽度）强制设为所有 `.line/.change-block` 的 minWidth。现改为 `width:max(100%, max-content); min-width:0`，并移除该 `scrollWidth` 内联宽度循环，使行背景基于 pane 可视宽度而非最长行宽度。官方 `DiffEditorEditors` 也以可视 `getContentWidth()` 而非最长文本宽度协调子编辑器。编译、格式检查与文件诊断通过。
- 用户要求撤回最近两轮行级背景布局修改，已恢复 `.line` 的 `width:max-content; min-width:100%`、代码列 `max-content` 和 `pane.scrollWidth` 同步逻辑；编译和格式检查通过。重新从 VS Code `registrations.contribution.ts` 定位真正问题：`diffAddDecoration` 是 `char-insert` 且设 `shouldFillLineOnLineBreak:true`。目标中 CleanerHolder/if/cleanup 行文本后的深绿色来自跨行 `innerChanges` 在每个被覆盖行尾的文本 decoration 填充，而不是 `line-insert` 行级浅绿背景。当前 `projectRanges` 将跨行 RangeMapping 拆为纯文本 span，未保留“首行/中间行在换行处填充至行尾”的标记；后续应为跨行 range 的首行和中间行单独输出 fill-to-line-end decoration，不再调整 `.line` 宽度。
- 已实现跨行文本 decoration 的行尾填充：`projectRanges` 对跨行 RangeMapping 的首行和中间行设置 `fillLineEnd`，同一行合并 range 时保留该标记；`codeHtml` 添加 `word-fill-line-end`，其伪元素以 `word-added/removed` 的文本层背景从 span 末尾延伸到右侧。Range 结束行不填充，保持 `shouldFillLineOnLineBreak` 语义。`npm run compile`、`git diff --check` 与文件诊断通过。
- 用户确认 `word-fill-line-end` 的伪元素方案不正确，已精确撤回其 CSS、`fillLineEnd` 数据传播与 HTML class。新的替代方向：不以 `100vw` 伪元素模拟 Monaco；保留真实跨行 RangeMapping，在行数据中记录由换行覆盖产生的 `fillTextToEnd`，并由 `lineHtml`/`splitLineHtml` 在 `.line-code` 内输出受 pane 宽度约束的专用填充节点。该节点使用文本级颜色，行容器继续只负责浅色 `line-insert`/`line-delete` 层。实施前应先确认代码列的可伸缩包装方式，避免把深色文本层误扩展到所有普通 replacement 行。
- 已落实替代行尾填充方案：跨行 `projectRanges` 的首行/中间行记录 `fillTextToEnd`；仅这些行添加 `.text-fill-to-end`，将代码列改为可伸展 grid/flex。`codeHtml` 用 `.line-text` 包住实际文本，并追加 `.text-fill-to-end-tail` 节点，节点以 inserted/removed text 背景填充同一 `.line-code` 的剩余空间；普通 replacement 行不改变宽度或深色范围。编译、格式检查和 TypeScript 诊断通过。
- 用户反馈仅 `if (active.get())` 有行尾深绿：此前将 CleanerHolder/cleanup 缺失归因于跨行 mapping 终点 EOL 边界，并扩展了 `fillTextToEnd` 条件；后续复核确认该归因尚未由运行时 RangeMapping 证明，不能视为确定根因。仍可能由 `refineReplacement.position()` 的换行左右偏好、`projectRanges` 对零宽/反向范围的过滤、或 `appendChanged` 按 alignment segment 输出时未取得对应行的 range 导致。后续需先输出目标 replacement 的实际 `innerMappings` 和 `modifiedRanges`（每行 start/end/fill 状态）再修改逻辑。
- 用户要求不以 if/行号/EOL 特例处理行尾填充。通用替代模型已确定：`slice()` 的 lineBreak token 是事实来源；在最终 refined offset diff 的半开区间中记录每侧被覆盖的 lineBreak token（`coveredLineBreaks`），并在 `projectRanges` 将其投影为仅驱动行尾 tail 的 `lineBreakOnly + fillTextToEnd` marker。`codeHtml` 跳过该非文本 marker，真实零宽文本 range 仍渲染 anchor。删除当前从 Position start/end、行号和 EOL 形状推断 fill 的条件；该模型严格由 token 区间相交决定。
- 用户询问能否从算法入手；已依据 `LinesSliceCharSequence` 确认这是正确层级。当前 `refineReplacement` 的 `slice()` 具有 lineBreak token，但 `position()` 进行 offset→Position 后丢失 token 覆盖信息，`projectRanges` 被迫以行号/EOL猜测。正确改造为在最终 offset diff 后、`position()` 前计算每侧 `[start,end)` 覆盖的 `lineBreak` offsets，将其作为 enriched RangeMapping 数据传递给投影/布局；不依赖具体行或条件文本。
- 已实施算法层换行覆盖模型：`refineReplacement` 在最终 `refined` offset diff 中扫描 `lineBreak` token，输出每侧 `originalLineBreaks`/`modifiedLineBreaks`；`projectRanges` 改为只在当前行存在于对应 `coveredLineBreaks` 集合时设置 `fillTextToEnd`，移除原先按 `line < end.line` 或 `end === length` 推断的条件。`npm run compile`、`git diff --check` 和文件诊断通过。
- 复核后确认上项结构接入未覆盖“仅换行 token”场景：`position(..., right)` 跳过 `\\n`，使其所属行不在 `range.start.line..end.line` 的投影循环中；即使 `coveredLineBreaks` 已正确记录也不会渲染。`projectRanges` 现显式补入每个未投影的 covered lineBreak 所属行，作为 `lineBreakOnly + fillTextToEnd` 零宽标记；`codeHtml` 用其驱动尾部填充但不画 anchor。编译、格式检查和文件诊断通过。
- 调整提交图 merge 布局：`buildGraph` 对 merge 的第一父提交强制复用 merge 当前 lane 与颜色；即使第一父已在其他活动 lane，也先清理旧槽位。第二父仍使用已有 lane 或新增空 lane，因此正常分支保持直线，只有待合入支路形成额外曲线。`npm run compile`、`git diff --check` 与 `gitLogProvider.ts` 诊断通过。
- 定位并修复 27/28 行行尾无深绿的根因（对照官方源码核实）：`LinesSliceCharSequence` 默认（ignoreTrimWhitespace=true）裁剪行首空白，因此两 diff 间的相等串只剩 1 个 `\n`，`removeShortMatches`（任一侧间隔≤2，无词字符条件）得以合并跨行 diff，换行 token 落入 diff 内部产生 `shouldFillLineOnLineBreak` 填充；此前 `slice()` 保留缩进 token，相等串=`\n`+12 空格导致永不合并。已实施三项对齐：1) `slice()` 跳过行首空格/Tab；2) `position()` 改为官方 `translateOffset` 语义（指向 `\n` 落本行行尾、end-left 在行首 token 归列 0、start-right 在行首 token 落缩进后）；3) 新增 `optimizeOffsetDiffs`（`joinOffsetDiffsByShifting`×2 + `shiftSequenceDiffs`，含 `getBoundaryScore` 换行后 150 分、纯插入/删除平移、maxShiftLimit 100）插入官方管线位置（compute 后、词扩展前）。`projectRanges` 按官方单 decoration 语义重写零宽处理：整段零宽才画短竖杆 anchor，跨行段行尾零宽输出 lineBreakOnly 填充 marker，end 行列 0 零宽不渲染。Node 提取脚本验证复现场景：CleanerHolder/if/cleanup 三行均获 `fillTextToEnd:true` 且渲染 `text-fill-to-end-tail`，1:1 单行替换词级高亮无回归；`npm run compile` 与诊断通过。
- 梳理 VS Code 官方 SCM History Graph：核心在 `src/vs/workbench/contrib/scm/browser/scmHistory.ts`。每行用前一行 `outputSwimlanes` 深拷贝为下一行 `inputSwimlanes`；当前提交命中输入 lane 时，仅以 `parentIds[0]` 原位替换该 lane（继承标签色/旧色），其他父节点追加到输出 lane。SVG 单独为 `parentIds[1..]` 绘制汇入当前圆点的曲线；因此第一父是直通主线，其他父为支路。内置 Git `historyProvider.ts` 仅提供 `parentIds: commit.parents`，不计算布局；`git.ts` 在 ref 查询时用 `git log --topo-order`。官方实现完整保留多父 merge，而项目当前仍用 `visibleParents.slice(0, 2)` 截断 octopus merge。
- 官方图不把名为 `main`/`master` 的分支永久固定到某条 lane：轨道由当前可见提交序列与父关系动态推导。rebase 在提交 DAG 中没有特殊节点，重写后是一串单父的新提交，故按普通线性主线渲染；merge 才因多个 `parentIds` 显示额外支线。当前项目同样按 `parents` 与活动 lane 布局，但应移除二父限制以对齐官方多父处理。
- 已将项目 `buildGraph` 的 `visibleParents.slice(0, 2)` 改为遍历完整 `visibleParents`：第一父仍在当前 lane 直通，第二及后续父均分配既有或空闲/新增 lane，支持 octopus merge。`npm run compile`、`git diff --check` 与 TypeScript 诊断通过。
- 用户反馈此前效果不对，已按 VS Code `scmHistory.ts` 改造完整链路：`buildGraph` 使用 `inputSwimlanes → outputSwimlanes`，命中当前提交时仅用第一父原位替换、其余父追加；未命中时所有父追加，且不再过滤分页外父节点。`GitCommit` 新增输入/输出泳道数据；Webview 改为基于两组泳道顺序匹配绘制延续/横移线，第一父向下直连，多父从输出泳道弯曲汇入。`npm run compile`、`git diff --check` 和三文件诊断通过。
- 修复分支标签连线与轨道断开：Webview 的 `rowRefX` 与 `calcMaxLane` 原先仍依据旧的 `lane/lanes`，而提交点已改按 `inputSwimlanes` 计算，导致标签横线位置错列。新增 `rowMaxSwimlane`，统一以当前提交 `circleIndex`、输入和输出泳道最大索引决定标签起点和图宽。编译、格式检查与文件诊断通过。
- 修复 merge 起点及多轮分支首提交连接遗漏：撤回“复用已有父 hash”的去重，它会破坏 VS Code 对 merge 后续父的独立追加语义。现 `buildGraph` 与官方一致：当前提交命中输入泳道时第一父原位替换，后续父总追加独立输出泳道；未命中时所有父都追加。Webview 对后续父 hash 从 `outputSwimlanes` 尾部反向查找，连接最后追加的父轨，避免同 hash 已存在于旧轨道时接错。新分支首提交仍从第一父绘制连接。`npm run compile`、`git diff --check` 与相关诊断通过。
- 修复重复 hash 泳道造成的分叉/多轮 merge 断线：`buildGraph` 现在只将 `inputIndex` 对应的首个当前 lane 替换为第一父，其他同 hash lane 作为汇入轨移除；SVG 也仅在主 lane 有第一父时推进 `outputIndex`，额外同 hash 输入 lane 用贝塞尔曲线汇入当前提交点。根提交不会消耗输出槽位，后续旁路 lane 的索引保持对齐。`npm run compile`、`git diff --check` 与两文件诊断通过。
- 修复每行节点上下轨道的视觉断裂：`.commit-row` 原为 `min-height: 26px` 另加 1px border，SVG 固定为 26px；`drawSvg` 又将 `detailsBottom` 截到 `svgH - 1`，相邻行形成至少 1–2px 空隙。行改为 `box-sizing: border-box`，轨道终点改为 `svgH`，普通与展开行均能贴合下一行。`npm run compile`、`git diff --check` 与文件诊断通过。
- 收紧提交图横向间距：Webview 常量 `LANE_W` 从 20 改为 16，`REF_GAP` 从 21 改为 14；泳道、SVG 宽度和标签锚点均从常量派生，能够同步缩小。`npm run compile`、`git diff --check` 与文件诊断通过。
- 按用户指定继续收紧：`LANE_W` 由 16 调为 12，`REF_GAP` 由 14 调为 10。`npm run compile`、`git diff --check` 与 `gitkViewProvider.ts` 诊断通过。
- 汇合点和标签连线样式：将汇合判定从仅 `parents.length > 1` 扩展为多父 merge 或 `inputSwimlanes` 中同 hash 出现多次；所有汇合点均渲染彩色外环加菱形圆心，普通提交仍为实心圆，HEAD 外环保持。提交到首个分支标签的延长线终点从 `refX - 5` 改为 `refX`，直接贴合标签左边缘。`npm run compile`、`git diff --check` 与文件诊断通过。
- 按用户要求将汇合点由菱形圆心改回 VS Code 风格同心双圆：保留彩色外环与背景内芯，中心以彩色圆点替代菱形；汇合判定、普通节点与 HEAD 外环规则不变。`npm run compile`、`git diff --check` 与文件诊断通过。
- 根据截图强化汇合节点可见性：由“外环 + 实心内点”改为两个空心同心圆，外环半径为普通节点半径加 2，内环为普通半径减 1，均使用 1.5px 描边与编辑器背景填充，确保外圈和内圈在深色背景均清晰可见。
- 按最新期望图调整汇合节点：不再使用双空心圆，改为半径 `r+1` 的细彩色外环（背景填充）加半径 `r-3` 的实心彩色中心圆，视觉与期望截图一致；`npm run compile` 与文件诊断通过。
- 修复汇合点外环不可见：外环沿用了 `.dot`，其 CSS 将 `stroke` 强制覆盖为编辑器背景色，覆盖了 SVG 内联的提交色。外环改用 `.join-dot`，保留彩色描边；编译和诊断通过。
- 布局需求（水平滚动 + 软换行）经与用户确认取舍：二者互斥（软换行后内容不再横向溢出），最终方案为"软换行可切换、默认开启"，固定栏为"水平滚动时保持不动"。实现：工具栏新增 `#wrap` 按钮与 `softWrap`（默认 true）状态，`updateSoftWrap()` 切换 `#list.wrap` 类；软换行下 `.line`/`.line-code` 用 `width:100% + minmax(0,1fr) + pre-wrap + overflow-wrap:anywhere`、`.pane` 设 `overflow-x:hidden`、tail 宽度改 100%；关闭软换行回退 `max-content + pre`，并排两栏各自 `overflow-x:auto` 并复用既有 `syncPaneScroll`（`softWrap` 时直接 return 跳过同步）。行号/marker 栏改 `background-color` 不透明底 + `linear-gradient` 叠加暗化 + `z-index:2`，避免软换行折行时穿透显示代码；`.context-fold` 改 `position:sticky; left:0; z-index:3; max-width:100vw` 使展开/收起栏横向滚动不平移，`.file-header` 原有 sticky 保持。13 项样式/逻辑断言全过，`npm run compile` 与 lint 通过。
- 软换行改为按面板宽度自适配：`.pane` 与 `.unified .unified-pane` 加 `container-type: inline-size` 建立容器查询上下文；`.text-fill-to-end-tail` 由 `calc(100vw - 100% - 36px)` 改为 `max(0px, calc(100cqw - 100% - var(--line-number-width,52px) - 16px))`，内联模式另用 `122px`（18+52+52）扣除三列行号宽；`.context-fold` 由 `max-width:100vw` 改为 `width:100cqw`；`.unified .unified-pane` 补 `overflow-x:auto`（软换行时 hidden），此前内联模式缺滚动容器。软换行下并排/内联尾块均为 `width:100%`。13 项断言全过，`npm run compile` 与 lint 通过。
- 用户反馈看不到水平滚动条（软换行默认开启时被隐藏、且原本在每个 diff 内部），改为 customDiffPanel 底部常驻全局滚动条：新增 `#hscroll`（`position:fixed; bottom:0`）内含 `#hscrollLeft`/`#hscrollRight` 两个 `.hscroll-bar`（各含 `.hscroll-sizer` 撑宽），并排两条、内联时 `#hscroll.unified` 只留一条；`.pane` 与 `.unified-pane` 用 `scrollbar-width:none` + `::-webkit-scrollbar{height:0}` 隐藏自带滚动条；`#list:not(.wrap)` 加 `padding-bottom:24px` 预留空间。逻辑上以 `paneSelector/panesFor/applyScroll/measureSide` 让底部条驱动该侧全部 diff 代码栏，代码栏滚动反向同步回底部条（`syncingPaneScroll` 防抖），`updateHorizontalScrollbar()` 按 `scrollWidth-clientWidth` 最大值设置 sizer 宽度并在 `softWrap || scrollable<=0` 时隐藏；渲染完成、renderDiff（含展开/收起）、布局切换、软换行切换、window resize、loading 重置处均调用刷新。15 项断言全过，`npm run compile` 与 lint 通过。
- 修复行数不等 replacement 块的配对错误（用户 inkengine commit aebd0555 截图：左14 `kTextureSize` 应整行删除、左15/右14 `kBorderWidth` 3.0f→1.0f 才是修改行）：`appendChanged` 原按 slot 序号硬配（slot0 = 左14 配 右14），导致无关行被当修改行、真正的修改行落到无配对 slot。新增 `lineSimilarity()`（`splitDiffTokens` 去空白后按多重集合共享 token 数算 Dice 系数）与 `pairSegmentRows()`：等长段保持顺序配对；不等长时以较长侧为基准，对较短侧贪心选相似度最高且 >0.2 阈值的行配对，未被选中的行返回 `undefined` 配对（走 `wholeLineRanges` 整行标记）。`wholeLineRanges` 从 `!isReplacement` 分支提升到 `appendChanged` 作用域供两分支共用。复现验证：相似度 0.909(左15↔右14) vs 0.364(左14↔右14)，配对结果 `[[1,undefined],[2,1]]` 正确；7 项断言全过（相似度比较、正确配对、整行删除、slot 数、等长顺序、纯删除、纯新增），`npm run compile` 与 lint 通过。
- 并排两条底部滚动条改为双向同步 + 修软换行重叠：原先每条只驱动同侧 pane（`bindBar(bar, side)` 各自 `applyScroll(side)`），左右互不联动。新增 `syncAllSides(scrollLeft)`（并排时同时 `applyScroll('left')` 与 `applyScroll('right')`，内联只驱动 unified-pane）与 `syncBars(scrollLeft, exclude)`（两条 bar 互相同步），`bindBar` 去掉 side 参数，pane 滚动监听也改为调用两者。软换行重叠根因：`#list.wrap .line` 设 `height:auto` 折行，但行内 `.word-removed/.word-added/.word-modified`、`.word-anchor`、`.text-fill-to-end-tail` 仍固定 `height:var(--line-height)`，且并排左右对应行折行数不同导致错位。修复：软换行下词级高亮改 `display:inline; height:auto; vertical-align:baseline`，锚点改 `height:1em; vertical-align:text-bottom`，`text-fill-to-end-tail` 直接 `display:none`（折行下无意义）；新增 `alignWrappedRows()` 按行索引取左右 `offsetHeight` 较大值写 `minHeight` 对齐（非软换行/内联时清空内联样式），在渲染完成、renderDiff、布局切换、软换行切换、window resize 处调用。15 项断言全过，`npm run compile` 与 lint 通过。
- 修复长文本横向滚动时行号被顶走：根因是上一轮为宽度自适应给 `.pane` / `.unified .unified-pane`（即滚动容器本身）加了 `container-type: inline-size`，容器查询会建立新包含块并使内部 sticky 失去相对滚动容器的定位，导致行号随内容平移。修复：把容器上下文从滚动容器移到非滚动的 `.split`；由于 `.split` 是两列 grid，`cqw` 换算需调整——并排单栏用 `50cqw`（`.text-fill-to-end-tail`、`.context-fold`），内联占满全宽用 `100cqw`（`.unified .text-fill-to-end-tail`、`.unified .context-fold`）。同时 `.line-number, .line-marker` 由 `position:relative` 改 `sticky`，行号/marker z-index 统一提到 3（原 `.line-number` 被后置规则降回 1），`.context-fold` 提到 4 保证高于行号。15 项断言全过，`npm run compile` 与 lint 通过。
- 行号栏彻底不透明 + 左右宽度对齐：此前所有变更态 gutter 规则（`.removed/.added/.modified-*/.moved-*` 及 `.change-block` 下同类）用半透明 `background` 简写覆盖了基础规则的不透明 `background-color`，横向滚动时代码从底下透出。统一改法：保留基础 `background-color: var(--vscode-editorGutter-background,...)` 不透明底，变更色改用 `background-image: linear-gradient(色, 色)` 叠加；`.placeholder .line-number` 与 `.change-block .line-number/.line-marker` 从 `transparent` 改为显式不透明底（原 `transparent` 声明只保留给 `.line-code`）。宽度对齐：`renderDiff` 原先按每个 pane 各自算行号位数与 `scrollWidth`，导致左右 `scrollWidth` 不等、底部两条滚动条无法同步；改为 `digits` 取两栏最大位数后统一写 `--line-number-width`，并新增 `alignContentWidth(targets)` 对并排两栏取 `max(scrollWidth)` 统一写 `minWidth`（内联 `.unified-pane` 单独计算，避免被并排宽度互相拉扯）。12 项断言全过（含"gutter 规则中不存在半透明 background 简写"扫描），`npm run compile` 与 lint 通过。
- 继续修三项：1) 行号栏仍可能透：`--vscode-editorGutter-background` 在部分主题本身带透明度，8 处不透明底统一批量替换为 `var(--vscode-editor-background, #1e1e1e)`（带十六进制兜底）。2) 水平滚动到行尽头时行号被带走的真因是 `.line { width: max-content }` —— sticky 只能在父元素范围内固定，短行滚到自身尽头后行号被推出；改为 `.line { width: var(--content-width, max-content) }` + 列模板末列 `minmax(max-content, 1fr)`（unified 同步），JS 在 `alignContentWidth` 中先 `removeProperty('--content-width')` 再按该组 `max(scrollWidth)` 写回栏级 `--content-width`（`.change-block` 仍写内联 minWidth）；软换行下用 `#list.wrap .line { max-width:100% }` 与 `#list.wrap .change-block { min-width:0 !important }` 使其失效。3) 两条滚动条滑动距离不一致：`updateHorizontalScrollbar` 原先各自用本侧 `measureSide` 设置 sizer 宽度，改为先 `sides.reduce` 取两侧最大 overflow，再统一写入两条 bar。15 项断言全过，`npm run compile` 与 lint 通过。
- 行宽严格以两边最长为准：原 `alignContentWidth` 定义在 `renderDiff` 内且把 `.unified-pane` 单独成组（各自算宽），且布局/软换行切换后不重算，隐藏栏 `scrollWidth=0` 导致旧值残留。改为提取到外层 `alignContentWidth(scope)`：按 `.diff` 遍历，`.pane` 与 `.unified-pane` 纳入同一组，先清 `--content-width` 与 `.change-block` 内联 minWidth，`softWrap` 时直接 return，只统计 `clientWidth>0` 的可见栏取 `max(scrollWidth)` 后统一写回；行号宽度除写各 pane 外也写到 `holder`（`.diff`）供内联栏继承。调用点补齐至 6 处（renderDiff、渲染完成、布局切换、软换行切换、resize），且都在 `updateHorizontalScrollbar()` 之前。13 项断言全过，`npm run compile` 与 lint 通过。
- 标题栏与行号层级/固定修正：`.pane-title`（栏内路径栏）原先无 `position`，水平滚动时随内容漂移且层级低于行号（行号 z-index 3 会盖住它）。改为 `position:sticky; top:0; left:0; z-index:5`，宽度按容器单位贴合可视区（并排 `50cqw`、`.unified .pane-title` 用 `100cqw`、`#list.wrap .pane-title` 用 `100%`）；`.file-header` z-index 由 2 提到 6，保证行号始终在标题栏之下。两者背景都加 `background-color: var(--vscode-editor-background, #1e1e1e)` 不透明底，原 `background` 简写（会重置 background-color）改为 `background-image: linear-gradient(色, 色)` 叠加，避免主题变量带透明度时行号透出。13 项断言全过（含 z-index 数值比较：file-header 6 > pane-title 5 > line-number 3），`npm run compile` 与 lint 通过。
- 层级重排 + 滚动距离改回各侧独立：1) 行号被 `#toolbar`（Gitk Diff 工具栏，原 z-index 3 与行号相同）盖住问题 —— 行号/marker（含 unified 三列）统一降到 `z-index: 1`（仅高于代码内容），其余固定元素全部在其之上：`#toolbar` 3→8、`#hscroll` 4→7、`.file-header` 6、`.pane-title` 5、`.context-fold` 4。2) 按用户要求水平滚动条以本侧最长行为准而非两侧一致：`updateHorizontalScrollbar` 恢复各侧 `measureSide` 独立设置 sizer 宽度（`scrollable` 仅用于判断是否隐藏整条）；`alignContentWidth` 内的统一宽度改为**每栏按自身 `scrollWidth`** 写 `--content-width`（不能取消该统一，否则 `.line { width: max-content }` 会让短行滚到自身尽头时把行号带走）。行号栏 `--line-number-width` 仍取两栏最大位数保持 gutter 对齐。16 项断言全过（含全部层级数值比较），`npm run compile` 与 lint 通过。
- 用户确认最终宽度策略为“左右面板内容宽度取较宽一侧，行宽随面板自然适配”，撤销上一轮各侧独立宽度/距离的改动：`alignContentWidth` 对可见 `.pane/.unified-pane` 取 `max(scrollWidth)`，统一写入各 pane 的 `--content-width` 与 `.change-block` minWidth；`updateHorizontalScrollbar` 先取两侧 `measureSide` 最大 overflow，再给两条底部 bar 写同一 sizer 宽度。软换行时继续不强制宽度。7 项断言全过，`npm run compile` 与 lint 通过。
- 用户要求软换行保持 Diff 算法与高亮逻辑一致、修内联行号显示两行。确认 `calculateDiff`/`refineReplacement`/行配对/`innerChanges` 未修改：软换行只应改变 CSS 排版；并排与内联都继续直接消费同一份 `innerChanges`/`highlighted`。修渲染：`.unified .line-marker` 固定 `18px`，`.unified .line-number` 固定 `52px` + `white-space:nowrap` + 紧凑 padding，避免继承并排可变 `--line-number-width` 后出现两行；其 sticky 左偏移保持 0/18/70。软换行下仅对内联 gutter 加 `align-self:stretch`，使其覆盖折行后的整段视觉行高；词级高亮仍保留原 class/颜色，仅以 `display:inline;height:auto;vertical-align:baseline` 随文本折行，避免重叠。12 项断言全过（算法不读 softWrap、两种视图同 innerChanges、颜色不变、内联三列固定），`npm run compile` 与 lint 通过。
- 用户要求内联模式行号栏视觉效果与并排一致。差异根因：内联覆盖为固定 52px/padding 0 4px，且只 `.pane` 有 `+/-` 伪元素，导致动态 gutter、行号内边距和变更标记不一致。统一：`.unified .line-number` 改为 `width/min-width: var(--line-number-width,52px); padding:0 2px 0 20px; white-space:nowrap`，内联 grid（常规/软换行）同用该变量，第三个行号 sticky left 用 `calc(18px + var(...))`；给 `.unified` 补与 `.pane` 同样的 removed/added/moved `−/+` 伪元素；内联 tail 由硬编码 122px 改为扣 `18px + 2*动态 gutter`。10 项一致性断言全过，`npm run compile` 与 lint 通过。
- 并排模式软换行高亮需覆盖完整视觉行高：保持 Diff/range 算法不变，仅改 `#list.wrap` 渲染样式；`.word-removed/.word-added/.word-modified` 添加 `line-height:var(--line-height)` 与 `-webkit-box-decoration-break:clone; box-decoration-break:clone`，折行后每个 span 片段独立绘制覆盖整行高的背景；`.word-anchor` 同步改为完整 `--line-height`。5 项断言全过（含 calculateDiff 不读 softWrap），编译和 lint 通过。
- 按用户要求先回退并排模式到上一次提交 `HEAD`（`9c088d6`）并将现有对比职责迁移过去：采用受控迁移而非直接保留工作区 CSS。以 `HEAD:customDiffPanel.ts` 整体恢复视图结构（并排 CSS、滚动、软换行等布局回到提交基线），只从当前工作区逐函数迁入比较数据与高亮逻辑：`calculateDiff`（含 `appendChanged` 闭包）、`refineReplacement`、`projectRanges`、`computeAlignmentSegments`、`renderRows`、`codeHtml`；保留上次提交的 `renderDiff` 结构。验证显示 `renderDiff` 与 HEAD 逐字一致，且脚本不含本轮 `softWrap/hscroll/alignWrappedRows/alignContentWidth` 布局逻辑；同时保留行数不等 replacement 的 `lineSimilarity/pairSegmentRows` 配对、`wholeLineRanges` 整行增删投影、`innerChanges` 数据生成。编译、11 项迁移断言与 lint 全过。
- 继续落实“把对比职责迁移到算法/配对/innerChanges/高亮渲染函数”：将原嵌在 `calculateDiff.appendChanged()` 内的 `lineSimilarity` / `pairSegmentRows` 提升为脚本顶层独立比较算法函数，参数由闭包 left/right 改为显式 `originalIndexes, modifiedIndexes, originalLines, modifiedLines`；`appendChanged()` 仅通过 `pairSegmentRows(originalSegment, modifiedSegment, left, right)` 消费配对结果并投影行类型/innerChanges。`wholeLineRanges()` 也提升为独立高亮范围投影函数，移除 appendChanged 内部重复定义。复现 `kTextureSize` / `kBorderWidth`：配对 `[[1,undefined],[2,1]]`，前者整行删、后者行级修改；6 项职责边界/行为断言、`npm run compile` 与 lint 全过。
- 用户反馈迁移后仍未与 HEAD 对齐，要求继续。最终选择严格对齐而非继续混合迁移：恢复 `calculateDiff()` 为 `HEAD` 逐字版本，移除未被 HEAD 控制流使用的 `lineSimilarity` / `pairSegmentRows` / `wholeLineRanges` 辅助函数，并移除唯一残余空行差异。最终 `git diff --exit-code HEAD -- src/webview/customDiffPanel.ts` 返回 0（文件与 HEAD 完全一致）；`npm run compile` 与该文件诊断通过。
- 按用户最新要求重新迁移“刚刚逻辑”，但不再把策略内嵌进并排 slot 控制流：新增独立比较数据阶段 `lineSimilarity`（token Dice）、`pairSegmentRows`（不等长块贪心相似度配对）、`wholeLineRanges`（整行增删高亮范围）、`buildReplacementRows`（基于 `refineReplacement` + `computeAlignmentSegments` + `projectRanges` 输出 `{originalIndex, modifiedIndex, innerChanges}`）。`calculateDiff.appendChanged()` 不再定义配对/投影策略：replacement 仅执行 `refineReplacement` 后调用 `buildReplacementRows` 并消费行数据；纯增删复用 `wholeLineRanges`。复现 `kTextureSize`/`kBorderWidth`：前者整行删、后者正确配对修改，并带相应 innerChanges；6 项管线职责/行为断言、`npm run compile` 与 lint 全过。
- 恢复此前移除的布局逻辑，并和迁移后的比较管线完整集成：以暂存区版本作为布局来源恢复 `softWrap/updateSoftWrap`、`#wrap`、`#hscroll` 双底部条、`updateHorizontalScrollbar`、`alignWrappedRows`、`alignContentWidth`、sticky 行号栏及相关 CSS/DOM；保留当前独立 `lineSimilarity/pairSegmentRows/wholeLineRanges/buildReplacementRows` 管线，`calculateDiff` 只消费该管线输出且不内嵌策略。首次自动合并漏插入 buildReplacementRows 的三项依赖（暂存区内嵌函数被误识别为顶层），已补齐，集成验证 26 项全部通过（管线函数、无内嵌策略、innerChanges、软换行、滚动、行号、布局-算法隔离），`npm run compile` 与 lint 通过。
- 用户反馈恢复布局后仍影响高亮渲染。根因：`buildReplacementRows()` 对未配对行注入 `wholeLineRanges(...fillTextToEnd)`，且纯增删分支也注入整行 innerChanges，导致原有词级高亮被额外深色整行高亮覆盖；即便移除 wholeLineRanges，跨行 mapping 的 `projectRanges` 仍会给未配对行带入范围。修复：未配对 slot 的 `innerChanges.original/modified` 强制为空；纯增删 `append()` 回到不传 innerChanges（仅保留已有行级 type 背景）；已配对行继续使用 `projectRanges` 的原始词级范围。复现 `kTextureSize`/`kBorderWidth`：前者仍无配对删除但 innerChanges 空，后者词级高亮保留；5 项断言、`npm run compile` 与 lint 通过。
- 用户反馈上述比较数据阶段迁移导致问题更大，已立即执行 `git checkout -- src/webview/customDiffPanel.ts`，将工作区文件恢复到**暂存区**的既有实现（不是 HEAD）；本轮迁移的 `buildReplacementRows`/顶层配对逻辑与高亮范围改动均已撤回。`git diff -- src/webview/customDiffPanel.ts --exit-code` 返回 0，`npm run compile` 与该文件 lint 通过。未处理其他已有改动；仅删除了本轮生成的 `tmp-current-vs-head.diff`。
- 用户改为要求保持 HEAD 逻辑，仅重新引入软换行。使用 `git restore --source=HEAD --worktree src/webview/customDiffPanel.ts`（不改变暂存区）将工作区恢复为 HEAD 比较/配对/innerChanges/高亮基线，然后只新增软换行 UI：工具栏 `#wrap`、`softWrap=true`、`updateSoftWrap()` 和点击事件；CSS 仅加 `#list.wrap` 的 pane/line/line-code 折行规则（`pre-wrap` + `overflow-wrap:anywhere`），不写 `.word-*` 或 `.text-fill-to-end-tail` 覆盖。验证 `calculateDiff/refineReplacement/projectRanges/computeAlignmentSegments/renderRows/codeHtml` 均与 HEAD 逐字一致，算法函数均不读取 softWrap；12 项断言、`npm run compile` 与 lint 通过。
- 设计并落地扩展 logo：图形语义与几何直接取自项目绘图代码（`gitLogProvider.ts` 的 `LANE_COLORS[0]=#e06c75`、`[1]=#61afef`；`gitkViewProvider.ts` 的 `LANE_W=12`、`DOT_R=5`、`stroke-width:1.5`、join-dot 双环、`.dot` 用编辑器背景描边），按 128 设计栅格等比放大为 lane 线宽 3.5 / 节点 r=8 / HEAD 双环 r=11.75+4.5。产物：`media/gitk-logo.svg`（彩色主 logo，含三节点一直通两曲线汇入 + 三条 `#5c6370` 提交行占位条）、`media/gitk-icon.svg`（24px 单色 mask 图标，纯白不透明）、`media/gitk-logo.png` 256px 与 `media/gitk-logo-128.png`。`scripts/gen-logo.cjs` 为零依赖光栅化脚本（1024 超采样 + 预乘 alpha 降采样 + 手写 CRC32/PNG chunk），与 SVG 共用同一套坐标常量，重跑即可再生 PNG。`package.json` 三处接入：新增顶层 `"icon": "media/gitk-logo.png"`（Marketplace）、`viewsContainers.panel[0].icon` 由 png 改 `media/gitk-icon.svg`、`views` 项补 `icon` 消除清单 lint warning（仅剩既有 star-activation info）。用 ASCII 采样预览校验 PNG 图形正确，`tsc --noEmit` 通过。AI 草稿图已删除，空目录 `media/logo-candidates` 的删除命令被用户拒绝，需手动清理。
- 定位"首次初始化加载完又回到加载中"（仅分析，未改代码）：三条链路叠加。1) 双入口重复刷新：`extension.ts:24-25` 激活即 `initializeBackground()`→`refresh(true)`（此时 `this.view` 为空，进度无处投递）；用户打开面板后 `resolveWebviewView`→`initializeView`，`hasPreparedInitialData()`（要求 repositories/branches/commits 非空且 `isLoading===false`）在后台刷新未完成时为 false → 再 `await this.refresh()`，`refresh()` 先 `refreshAbortController.abort()` 掐掉进行中的首次加载并从"初始化环境..."重启。2) 自触发文件监听：`initializeGitWatchers()` 注册 `**/.git/index`、`**/.git/refs/**` 监听 → `queueRepositoryStateRefresh()` 300ms 去抖 → `refresh(false)`；而加载链自身 `loadBranchesWithVirtualCommits`→`getWorkingTreeChanges`→`readWorkingTreeChangesFromCli` 执行 `git status --porcelain -z --untracked-files=all` + `git diff`，git 刷新 stat 缓存会回写 `.git/index` → 监听命中 → 再次刷新；`queueRepositoryStateRefresh` 没有 `queueLifecycleRefresh` 里那句 `initializingViewGeneration === viewGeneration` 的初始化护栏。另外 `pollWorkingTreePresence` 每 3s 再跑一次 `git status`，持续提供触发源。3) 文案退化成通用"加载中..."：`setLoading(true)` 只置 `isLoading`、不设 `loadingMessage`（置 false 时才清空），webview `state.isLoading` 分支取 `state.loadingMessage || '加载中...'`；同时 `refreshSelectors` 无条件 post `branchesLoading`，分支下拉退回"加载分支..."spinner。修复方向（待确认）：初始化期间对 `queueRepositoryStateRefresh` 加护栏 / 用 HEAD+refs 内容哈希比对代替 index 写入触发 / 忽略自身 git 调用引起的 index 事件；并让 `setLoading(true)` 带上阶段文案。
- 已实施上述修复（仅改 `src/webview/gitkViewProvider.ts`，+84/-24）：1) `initializeView` 新增分支，`hasPreparedInitialData()` 为 false 但 `refreshAbortController` 存在（后台首次加载进行中）时只 `pushStateToWebview()` + 新增 `republishLoadingProgress()` 重播最后一次进度，不再 `await refresh()` 打断。2) 新增字段 `lastLoadingProgress` 与统一出口 `postLoadingProgress(phase, message, current, total)`（同时写入 `loadingMessage` 并缓存），全文 11 处 `postMessage({type:'loadingProgress'...})` 全部替换，现仅 `postLoadingProgress`/`republishLoadingProgress` 两处直接 postMessage。3) `setLoading` 签名改 `(value, message?)`，`value=true` 时写入阶段文案、`value=false` 时清空 `lastLoadingProgress`；三个调用点补文案：`refreshInternal`='正在刷新提交历史...'、`refreshCurrentHeadBranch`='切换当前分支...'、`refreshBranchCommits`='刷新分支...'。4) `queueRepositoryStateRefresh` 的定时回调改调新增的 `refreshOnRepositoryStateChanged()`：先判 `initializingViewGeneration === viewGeneration` 以及 `refreshAbortController && commits.length === 0`（覆盖后台加载期，此时 initializingViewGeneration 为 0）直接 return；再用新增 `readRepositoryStateSignature()`（复用已有 `getGitRepositoryState`，拼 head/branch/refs/status 以 \u0000 连接）与 `repositoryStateSignature` 比对，相同则忽略，从而屏蔽自身 `git status`/`git diff` 回写 `.git/index` stat 缓存触发的伪事件。5) `refreshInternal` 成功收尾调 `captureRepositoryStateSignature()` 建立基线，避免首个 watcher 事件因无基线必然刷新。

- 加载性能可选方案：优先将分支元数据、工作区变更和子模块发现拆分为独立的按需任务；可采用 VS Code Git 扩展 API 提供的已发现仓库以跳过自建根目录探测，子模块改为懒加载或显式展开。对 CLI 链路，分支首屏可用 `for-each-ref` 加轻量 HEAD 查询，工作区变更延后；子模块拓扑用 `.gitmodules`/仓库变更监听持久缓存，替代固定 10 秒全量扫描。

- 复核“同刷新快照复用”方案：`loadBranchesWithVirtualCommits` 内的 `getGitBranches`（refs/symbolic-ref/HEAD）与 `getWorkingTreeChanges`（status + 两条 diff）实际没有重复的 Git 命令；`captureRepositoryStateSignature` 的 HEAD/branch/refs/status 是提交加载完成后为 watcher 建立最新基线，不能直接复用较早快照，否则可能漏掉加载期间发生的变更。因此该方案收益低且会降低正确性，不应优先实施。`npm run compile`、`git diff --check`、文件诊断均通过。
