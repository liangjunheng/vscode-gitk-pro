# 2026-08-10

- 将 `changes` 与 `staged changes` 虚拟提交并入 `GitBranchOption.virtualCommits`：只挂载当前分支，分支加载与 Git 工作区状态刷新均更新该集合；Webview 的完整与局部渲染均直接读取该分支数据，不再以独立计数构造虚拟行。`npm run compile` 与 `gitkViewProvider.ts` lint 通过。
- 修复虚拟提交不显示：`getGitBranches()` 现并行读取 `getCurrentGitBranch()`，将匹配 `refs/heads/*` 的分支标记为 `description: 'current'`；此前所有本地分支都标记为“本地分支”，导致 `withCurrentBranchVirtualCommits()` 无法找到挂载目标。`npm run compile` 与 `gitLogProvider.ts` lint 通过。
- 修复 Multi-Diff 快速切换卡死：`DiffReader` 用内部请求代次使已停止或已过期的批量读取不再更新 Store；`CustomDiffPanel` 的 `loading/reset/begin` 递增渲染代次，逐帧渲染在代次不匹配时立即退出，仅最新 commit 可完成渲染。`npm run compile` 及两文件 lint 通过。
- 当前分支分组的实施方案：将 `GitBranchOption.description` 的“当前/本地/远程”复用拆为独立的分支类别/当前态字段及 commit hash；attached HEAD 建立当前分支视图，detached HEAD 合成 `HEAD (<short hash>)` 项；`virtualCommits` 仅附着当前分支视图；Webview 改为当前/本地/远程三分组，虚拟项作为提交选择而非 revision 过滤项。本地与远程 ref 仍独立可选，以相同 hash 表达其共享当前分支数据。
- 已实施当前分支分组：`GitBranchOption` 使用 `kind`（`current` / `local` / `remote`）与 `hash`；`getGitBranches()` 始终建立单独的当前分支视图，并以 `git rev-parse HEAD` 支持 detached HEAD。工作区虚拟提交只挂载当前视图，Webview 选择器分为当前、本地、远程三组。`npm run compile` 与相关 lint 通过。
- 修复 detached HEAD 加载失败：`getCurrentGitBranch()` 将 CLI `git symbolic-ref --quiet --short HEAD` 的预期非零结果转换为 `undefined`；`getCurrentGitHeadHash()` 继续提供 hash，当前分支视图可显示 `HEAD (<short hash>)`。`npm run compile` 和 `gitLogProvider.ts` lint 通过。
- 修复空 HEAD 名称导致的“未获得有效 Git 数据”：`getCurrentGitBranch()` 不再使用 `raceSources()`，因为 detached HEAD 下 API 与 CLI 都会合法返回 `undefined`，而该竞速助手要求至少一个有效值。改为优先读取 Git API，再回退 CLI；无具名分支时正常返回 `undefined`。`npm run compile` 和 `gitLogProvider.ts` lint 通过。
- 修复 Change Files 到 Multi-Diff 的选择同步和数据缺失：`selectFile` intent 立即写入 `selectedPath`，执行 effect 前同步文件列表高亮；`CustomDiffPanel` 以当前文件总数作为完成条件，并仅在收到等量 `DiffPayload` 后开始渲染；当 Diff 投影缩短时重置增量发送游标；`DiffReader.readGitObjects()` 严格校验 `git cat-file --batch` 的缺失对象和内容边界，避免解析错位。`npm run compile` 与四个相关文件 lint 通过。
- Webview 同步 `selectedBranches`，只有当前分支被选中时才渲染其 `Changes`、`Staged Changes` 虚拟提交。分支下拉不做去重；后续的去重需求应限定为分支图显示区域。`npm run compile` 与 `gitkViewProvider.ts` lint 通过。
- 恢复 `DiffReader.readGitObjects()` 对非 blob `git cat-file --batch` 头部的兼容跳过：子模块条目会产生如 `<hash> submodule` 的无 size 头部，严格解析会将其视为异常并中断整个 Diff 准备；恢复为无法解析 size 时跳过该对象。`npm run compile` 与 `diffReader.ts` lint 通过。
